# 根据具体进度的面试题积累
## Day 1 (2026-07-26) —— 集合进阶·Map系列 + Collections工具类 + 不可变集合

### 题目1：HashMap 与 Hashtable 的区别（结合线程安全与 null 值）
> 请从线程安全、是否允许 null 键/值、继承结构、迭代器特性等方面对比 HashMap 和 Hashtable。实际开发中推荐使用哪个？为什么？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 线程安全：
    - Hashtable：线程安全（方法用 synchronized 修饰），但性能差。
    - HashMap：线程不安全，但性能高。多线程下需使用 ConcurrentHashMap。
- null 键/值：
    - Hashtable：**不允许** null 键和 null 值，否则抛出 NullPointerException。
    - HashMap：**允许**一个 null 键和多个 null 值。
- 继承结构：
    - Hashtable：继承自 Dictionary（遗留类）。
    - HashMap：继承自 AbstractMap（现代集合框架核心成员）。
- 迭代器：
    - Hashtable：使用 Enumeration（早期迭代器），不快速失败。
    - HashMap：使用 Iterator，快速失败（并发修改时会抛 ConcurrentModificationException）。
- 实际开发：**HashMap** 是绝对首选，单线程直接用；多线程用 ConcurrentHashMap。Hashtable 已过时，不建议使用。
</details>

**我的初答**：
1. 线程安全方面HashMap不安全,Hashtable里加synchronized锁锁整张表,线程安全
- HashMap允许有一个null键,多个null值,而Hashtable不允许存在null键/值
- HashMap继承于AbstractMap而Hashtable继承于古老抽象类Dictionary
- 迭代器方面两者都不安全且Hashtable迭代速率是低于HashMap的
1. 实际开发中一般都使用HashMap,想要线程安全一般会使用ConcurrentHashMap,CAS + synchronized,效率比Hashtable高很多,而Hashtable除了一些远古的项目会使用,现在基本被淘汰

**错漏点**：
1. 需要修正与补充的细节
-   迭代器特性表述不准确
   HashMap 的 Iterator 是fail-fast（快速失败）；
-   Hashtable 有两套遍历：
   Enumeration：不支持 fail-fast，并发修改不会抛异常；
   Iterator：同样是 fail-fast。
-   你写的 “两者都不安全” 概念模糊：
   迭代器快速失败≠线程安全，只是并发遍历修改时 HashMap 会抛出ConcurrentModificationException；
   不能简单概括 “迭代都不安全”，建议区分 fail-fast 特性。
- HashMap多线程并发读写会出现数据错乱、死循环
- Hashtable put 时键或值为 null 直接抛空指针


### 题目2：Collections.synchronizedMap 与 ConcurrentHashMap 的区别（并发场景选型）
> 现有 `Map<String, String> map = Collections.synchronizedMap(new HashMap<>());` 和 `ConcurrentHashMap<String, String> chm = new ConcurrentHashMap<>();`，在多线程环境下，两者有什么区别？为什么更推荐用 ConcurrentHashMap？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- Collections.synchronizedMap：
    - 原理：通过一个互斥锁（synchronized）包装底层 HashMap，所有方法（get/put/size 等）都加锁，全局锁导致竞争激烈，并发吞吐量低。
    - 迭代器：返回的迭代器是 fail-fast 的，遍历时需手动同步整个 Map（否则可能抛 ConcurrentModificationException）。
- ConcurrentHashMap：
    - 原理：采用分段锁（JDK1.7）或 CAS + synchronized 桶锁（JDK1.8），锁粒度更细，读操作无锁，并发性能远优于 synchronizedMap。
    - 迭代器：弱一致性（fail-safe），遍历时不会抛异常，但可能不反映最新修改。
- 选型建议：高并发场景**必须用 ConcurrentHashMap**，它提供更高的吞吐量和更好的可伸缩性。synchronizedMap 仅适合并发极低或需要全表锁特定操作的场景。
</details>

**我的初答**：
1. 多线程环境下synchronizedMap的锁竞争会比ConcurrentHashMap更大
**需要学习**synchronizedMap与ConcurrentHashMap

**错漏点**：
1. Collections.synchronizedMap 并不是直接锁 HashMap 对象本身；
-   内部包装类 SynchronizedMap，所有方法（get/put/containsKey 等）共用同一把对象监视器锁（this）。
-   只要一个线程在读，其他线程读写全部阻塞；读写互斥
2. 不能直接说读不加锁：
-   get () 大部分情况无锁，依靠 volatile 可见性；
-   但是迭代、部分聚合操作、compute 系列方法依然会加锁。
-   准确说法：常规 get 查询大多无锁，不会阻塞写操作；读写不互斥
3. SynchronizedMap(HashMap)：允许 key=null，value=null
   ConcurrentHashMap：key、value 都不允许为 null，直接抛 NPE


### 题目3：如何创建不可变 Map？Collections.unmodifiableMap 与 Map.of 的区别（面试常客）
> Java 提供了几种方式创建不可变 Map？请分别说出 `Collections.unmodifiableMap` 和 `Map.of()`（Java 9+）创建的 Map 在“不可变性”上的本质区别（比如能否修改原 Map 来影响不可变 Map？能否添加/删除元素？）。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 两种常见方式：
    1. `Collections.unmodifiableMap(Map<K,V> m)`：返回原 Map 的只读视图。**原 Map 仍然可修改**，一旦原 Map 变化，只读视图也会随之变化（因为共享底层引用）。但通过只读视图直接调用 put/remove 会抛出 UnsupportedOperationException。
    2. `Map.of(K k1, V v1, ...)`（Java 9+）：返回**真正不可变**的 Map 实例，内部不持有原 Map 引用，完全独立。任何修改操作（put/remove）都会抛出 UnsupportedOperationException，且底层不可修改。
- 区别总结：
    - unmodifiableMap 是“不可修改的视图”，但原始 Map 可变，因此视图可能“被动变化”（非真正不可变）。
    - Map.of 是“真正不可变”，内容从创建后就固定，无法被任何途径改变。
- 使用建议：如果需要绝对不可变，优先用 Map.of（或 Map.copyOf）。若需包装现有 Map 并阻止外部修改，但自己仍可能修改原 Map，则用 unmodifiableMap。
</details>

**我的初答**：
1. Java提供了3种方法创建不可变的Map
2. 其中Collections.unmodifiableMap本质上只是创建了一个原Map的视图程序无法通过这个视图修改原本的Map集合,但程序可以直接修改原本的Map,如Map.put().视图会同步感知修改
3. Map.of()是直接创建一个无法修改的Map集合,程序修改运行时直接报错.

**错漏点**：
1. JDK 原生主流两种：
-   ① Collections.unmodifiableMap()（JDK1.2）
-   ② Map.of() / Map.ofEntries()（JDK9+）
-   你说 3 种，缺少说明第三种是什么（一般有人会把Map.ofEntries单独算，或者引入第三方 Guava ImmutableMap）。不加解释直接说 3 种属于错误表述。
-   建议表述：JDK 原生主要提供两种方式；拓展还有 Guava 的 ImmutableMap
2. “程序无法通过这个视图修改原本的 Map 集合” 
-    准确说法：调用视图的 put/remove/clear 等方法，会抛出 UnsupportedOperationException，不是单纯 “无法修改”，面试最好带上异常名称
3. Collections.unmodifiableMap()是浅层保护(致命缺陷): 只保护Map本身的键值对结构,不保护Value对象的内部状态.
如果Value对象内部是可变的(如List),外部仍可以调用list.add()修改内容,视图会感知到变化

---
## Day 2 (2026-07-27) —— Stream流

### 题目1：Stream 的惰性求值（中间操作 vs 终端操作，短路操作）
> 现有代码：list.stream().filter(x -> { System.out.println("过滤"); return x > 5; }).map(x -> { System.out.println("映射"); return x * 2; }).findFirst(); 控制台会打印几次“过滤”和“映射”？为什么？如果换成 forEach(System.out::println) 呢？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 打印结果：findFirst() 时，只打印 2次“过滤”和 1次“映射”（假设 list 前两个元素为 [3, 7, 10]）。原因是中间操作（filter、map）是惰性求值，只有在遇到终端操作（findFirst）时才会触发实际计算，且 findFirst 是短路操作，找到第一个满足条件的元素（7）后就停止，不再处理后续元素。
- 如果换成 forEach(System.out::println)（终端操作，无短路），则会遍历所有元素，每个元素都经过 filter 和 map，打印次数与 list 大小一致。
- 核心结论：惰性求值 + 短路操作可以显著提升性能，尤其在处理大数据量时。
</details>

**我的初答**：
1. 控制台不会打印"过滤"和"映射",因为Stream实际上是惰性求值,只会将过程记录到流水线上,等到终结操作执行时才执行
2. 如果换成forEach则不会执行,因为前面出现了return语句

**错漏点**：
1. **对惰性求值概念不够清晰**
2. Stream 是惰性求值没错，但 **findFirst() 是终结操作 **，会触发流水线执行,并且是短路型终结操作,达成目标就提前终止遍历
3. 如果 x<=5：filter 返回 false，直接舍弃该元素，不会走到 map；不打印映射,只打印过滤；
4. 一旦找到第一个满足 x>5 的元素：执行 map 打印映射，随后流水线立刻终止（短路终结）


### 题目2：map() 与 flatMap() 的核心区别（映射与扁平化）
> 现有 List<List<String>> nestedList = Arrays.asList(Arrays.asList("a", "b"), Arrays.asList("c", "d")); 请分别使用 map() 和 flatMap() 将嵌套集合转换为 ["a", "b", "c", "d"] 的 Stream，并说明两者的返回值类型有何不同，flatMap 的“扁平化”是什么意思？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- map()：将每个元素按规则转换为另一个值，返回 Stream 中每个元素仍然是 List（即 Stream<List<String>>），无法直接得到扁平化的字符串流。
  示例：nestedList.stream().map(list -> list.stream()) 得到 Stream<Stream<String>>。
- flatMap()：将每个元素（如 List）转换为一个新的 Stream，并将这些 Stream 合并为一个整体 Stream（即 扁平化），最终得到 Stream<String>。
  示例：nestedList.stream().flatMap(list -> list.stream()) 得到 Stream<String>，包含 "a","b","c","d"。
- 一句话区别：map 是一对一映射（返回 Stream 嵌套），flatMap 是一对多映射并将结果展平（返回单一 Stream）。
</details>

**我的初答**：
1. map(list -> list.stream())将每一个元素转化为一个Stream流,获得的仍然是内部存在嵌套的Stream流,不会转化为题目中所要求的形式
2. flatMap(list -> list.stream()) 会将每一个子Stream流打散消除嵌套,合并成一个Stream流

**错漏点**：
1. 缺少明确的返回值类型：题目明确要求说明两者返回值类型的差异，你只描述了 “嵌套的 Stream 流”，没有精准写出泛型类型。map 操作后的完整返回类型是 Stream<Stream< String >>（流中的每个元素都是一个子 Stream 流），这是核心得分点。
2. 表述可更严谨：map 的本质是一对一映射，输入 N 个元素就输出 N 个元素；这里每个 List 元素被映射为一个 Stream，因此最终是「流中套流」的双层结构，永远无法直接得到单层的元素流
3. 同样的flatMap返回的是Stream< String >没有提及
4. **关键** "扁平化"解释停留在现象,未触达本质: 只描述了"打散消除嵌套"的结果,但没有解释清楚扁平化的核心定义.
- `扁平化(flatten)`的本质是消除多层容器的嵌套结构,把内层容器中的元素逐个提取出来,拼接成一层线性的元素序列; 而flatMap = Map + flatten,即先执行映射把每个元素转成一个子流,再把所有子流展平合并成一个单一的流


### 题目3：Collectors.groupingBy() 的分组与下游收集器（多级分组）
> 现有 List<Student>（含 name、score、city），请写出一个 Stream 链，按城市分组，然后统计每个城市的平均分。请说明 Collectors.groupingBy() 和 Collectors.averagingDouble() 是如何协作的，以及分组结果的数据类型是什么（如 Map<String, Double>）。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 代码示例：
  Map<String, Double> avgScoreByCity = students.stream()
  .collect(Collectors.groupingBy(
  Student::getCity,                // 分组键（城市）
  Collectors.averagingDouble(Student::getScore)  // 下游收集器：计算平均分
  ));
- 执行流程：
    1. groupingBy 按 city 将 Student 分组，生成一个 Map<String, List<Student>>。
    2. 下游收集器 averagingDouble 对每个分组内的 List 执行聚合操作，计算 score 的平均值。
    3. 最终类型为 Map<String, Double>，key 为城市，value 为该城市学生平均分。
- 拓展：groupingBy 支持多级分组（嵌套第二个 groupingBy 作为下游收集器），如先按城市分组，再按年龄分组：Collectors.groupingBy(Student::getCity, Collectors.groupingBy(Student::getAge))。
</details>

**我的初答**：
**错漏点**：

---
## Day 3 (2026-07-28) —— 方法引用

### 题目1：方法引用的四种形式分别是什么？请各举一个简单的代码示例。
> 方法引用是Lambda的语法糖，它的四种形式分别对应哪些场景？请写出每种形式对应的Lambda等价写法，并说明在什么情况下方法引用会比Lambda更简洁。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 四种形式及示例（假设有 List<String> list）：
  1. 静态方法引用：类名::静态方法名
     示例：list.stream().map(Math::abs) 等价于 x -> Math.abs(x)
  2. 特定对象的实例方法引用：对象::实例方法名
     示例：list.forEach(System.out::println) 等价于 x -> System.out.println(x)
  3. 特定类型的任意对象的实例方法引用：类名::实例方法名
     示例：list.stream().map(String::toUpperCase) 等价于 x -> x.toUpperCase()
     （注意：这里是类名，不是对象，表示Stream中的每个元素作为调用者）
  4. 构造器引用：类名::new
     示例：Supplier<Student> sup = Student::new 等价于 () -> new Student()
- 简洁条件：当Lambda表达式的参数列表和方法调用的参数列表完全一致（或第一个参数作为调用者，剩余参数作为方法参数）时，可以用方法引用简化。
- 注意：方法引用本质上仍是Lambda，编译器会自动推导类型。
</details>

**我的初答**：
1. 方法引用的四种形式
- 一: 通过对象引用实例方法 对象::实例方法 如 System.out::println 当我们需要使用其他类中的对象来调用方法,这就能表达出这个对象做了什么.
  Lambda等价写法为s -> System.out.println(s);
- 二: 通过类名引用静态方法 类名::静态方法如 Integer::praseInt 当我们需要使用参数的本类或者其他类中的静态方法对参数进行操作时,使用类名::静态方法即可
  Lambda等价写法为 s -> Integer.parseInt(s);
- 三: 引用本类或其他类中的构造方法 类名::new 如 Student::new 当我们要将参数转化为其他类的类型时,可以使用该引用
  Lambda等价写法为 s -> new Student(s);
- 四: 通过类名引用实例方法 类名::实例方法 如 String:: toUpperCase 此时原本抽象类中的第一个参数被当作方法的调用者使用
  Lambda等价写法为 s -> s.toUpperCase();
2. 一般在我们需要在Lambda表达式中书写的内容比较复杂或者需要重复使用,我们可以将该内容抽取成方法并引用或者引用已经写好的方法,可以使得代码更加简洁,加快书写效率


**错漏点**：
1. 对于对象::实例方法 ⚠️场景描述优化：调用者固定为已有实例对象，流元素作为方法入参
2. 对于类名::new  ⚠️补充：支持无参、单参、多参构造，取决于函数式接口入参；不止是参数转换
3. 方法引用优先适用场景：lambda 内部只有【单纯调用一个方法】
   如果逻辑复杂（多行代码、判断、循环），根本不能写成方法引用！
- 正确描述: 当lambda表达式内部仅有一条语句,且仅仅是调用某个方法,此时可以替换成为方法引用,简洁方法引用不能承载复杂逻辑;复杂逻辑只能用完整lambda或抽取普通方法


### 题目2：方法引用与 Lambda 的转换等价性——为何 `System.out::println` 能替换 `x -> System.out.println(x)`？
> 方法引用 `System.out::println` 和 Lambda `x -> System.out.println(x)` 在底层是否完全等价？请从**参数传递**和**方法签名匹配**两个角度解释，并说明哪种写法在字节码层面更高效？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 参数传递分析：
  - Lambda `x -> System.out.println(x)`：将 Stream 中的元素 x 显式作为参数传递给 println 方法，签名是 (参数) -> void。
  - 方法引用 `System.out::println`：绑定到特定对象 System.out，当 Stream 传递元素时，自动作为 println 的参数，本质是 `(x) -> System.out.println(x)`，两者语义完全等价。
- 方法签名匹配：
  - 目标接口的方法签名（如 Consumer.accept(T)）必须与 println 的签名（println(T)）匹配，这里 T 为 String，完全吻合。
- 字节码层面：
  - 方法引用在大多数情况下比 Lambda 更高效（尤其是捕获外部变量时），因为方法引用直接指向一个已存在的方法，而 Lambda 在非捕获场景下会生成一个静态方法，但两者在 JVM 层面差异不大，更重要的是代码可读性。
- 核心结论：它们是语法等价，但方法引用更简洁且意图更清晰（“直接调用这个已存在的方法”），推荐优先使用。
</details>

**我的初答**：
**错漏点**：


### 题目3：构造器引用 `Student::new` 在 Stream 中的实战——如何将 `List<String>` 转换为 `List<Student>`？
> 现有 `List<String> names`，每个 String 代表学生姓名。使用 Stream 的 `map` 方法和构造器引用，将其转换为 `List<Student>`（假设 Student 类只有一个含参构造器 `Student(String name)`）。请写出完整代码链，并说明构造器引用在这里的优势。若 Student 有多个构造器，编译器如何选择？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 完整代码链：
  List<Student> students = names.stream()
  .map(Student::new)    // 构造器引用，等价于 name -> new Student(name)
  .collect(Collectors.toList());
- 优势：代码极简，无需显式写 `new Student(name)`，语义清晰（“将每个名字映射为一个学生”）。
- 多构造器选择规则：
  - 编译器根据 Stream 中的元素类型和目标接口的函数签名推断。
  - 例如 `Student::new` 在 `Function<String, Student>` 上下文中，会选择参数为 `String` 的构造器。
  - 若存在多个构造器（如 `Student(String name)` 和 `Student(String name, int age)`），编译器会报错（无法确定），需显式写 Lambda 或强制类型转换。
- 拓展：`Student[]::new` 是数组构造器引用，用于 `toArray(Student[]::new)`。
</details>

**我的初答**：
**错漏点**：

---
## Day 4 (2026-07-29) —— 异常

### 题目1：Java 异常体系结构（Error vs Exception，检查型 vs 非检查型）
> 请画出 Java 异常体系的继承结构，并说明 Error 和 Exception 的区别。Exception 又分为哪两大类？分别举例说明各自的典型子类。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 继承结构（顶层为 Throwable）：
  Throwable
  ├── Error（严重错误，程序无法恢复）
  │    ├── OutOfMemoryError
  │    ├── StackOverflowError
  │    └── NoClassDefFoundError
  └── Exception（程序可处理）
  ├── RuntimeException（非检查型异常，编译器不强制处理）
  │    ├── NullPointerException
  │    ├── ArrayIndexOutOfBoundsException
  │    ├── ArithmeticException
  │    └── ClassCastException
  └── 其他（检查型异常，编译器强制处理）
  ├── IOException
  ├── SQLException
  ├── ClassNotFoundException
  └── FileNotFoundException
- Error vs Exception：Error 是 JVM 层面的严重错误，程序无法自行恢复（如内存耗尽），不应捕获；Exception 是程序运行中可预见的异常情况，应捕获或抛出。
- 检查型 vs 非检查型：检查型异常必须在编译期处理（try-catch 或 throws），否则编译报错；非检查型异常（RuntimeException）编译器不强制处理，属于编程错误（如空指针、数组越界）。
</details>

**我的初答**：
1. Java的异常体系超类为Throwable,Error和Exception都直接继承与它,Exception又分为RuntimeException(非检查型)该异常不会在编译期检查,而是在运行时出现才抛出和编译期异常(检查型)编译期时编译器会自己检查是否出现异常
2. Error和Exception的主要区别是Error通常是非常严重的系统性错误,比如虚拟机出错,我们程序员一般不会去捕获
3. RuntimeException典型的例子就是IndexOutOfBoundsException,编译期异常呢如ClassNotFoundException

**错漏点**：
1. 概念表述不严谨
   “Exception 又分为 RuntimeException (非检查型)…… 和编译期异常 (检查型)”
 -  ✅ 准确定义：
   Exception 分为 RuntimeException（非检查异常） 和 除 RuntimeException 以外的 Exception 子类（检查异常）
 -  ❌ 你的误区：不能叫 “编译期异常 / 运行期异常”
   官方术语：受检异常（检查型）、非受检异常（非检查型）
2. 所有异常都是运行时抛出！
   区别不在于什么时候抛出，而在于：编译器是否强制要求处理（try-catch /throws）
   你原话容易产生误解：受检异常是编译期抛出，这是典型概念误区
3. 只说了 Error 特点，没有和 Exception 做对比：
-   Error：系统级致命错误，程序无法恢复,Error 本身也属于非检查异常；
-   Exception：应用层面异常，程序理论上可捕获、恢复。


### 题目2：throw 和 throws 的区别（作用位置 + 使用场景）
> 请从**作用位置**、**后面跟的内容**、**使用目的**三个维度对比 throw 和 throws 的区别，并各写一个简单的代码示例说明。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 对比表格：
  对比维度    throw    throws
  作用位置    方法体内部（作为语句）    方法签名末尾（作为声明）
  后面跟的内容    异常对象（new 出来的实例）    异常类型（Class 名，可多个用逗号隔开）
  使用目的    主动抛出异常（触发异常）    声明该方法可能抛出的异常（告知调用者处理）
- 代码示例（throw）：
  public void setAge(int age) {
  if (age < 0 || age > 150) {
  throw new IllegalArgumentException("年龄不合法：" + age);
  }
  this.age = age;
  }
- 代码示例（throws）：
  public void readFile(String path) throws IOException, FileNotFoundException {
  FileInputStream fis = new FileInputStream(path);
  // 可能抛出 IOException
  }
- 核心结论：throw 是“实际行动”（扔出异常），throws 是“提前声明”（告诉调用者可能会扔出什么异常）。
</details>

**我的初答**：
**错漏点**：


### 题目3：try-catch-finally 中 return 的执行顺序（经典坑题）
> 现有代码：
public static int test() {
try {
return 1;
} catch (Exception e) {
return 2;
} finally {
System.out.println("finally执行");
return 3;
}
}
调用 test() 返回什么？控制台输出什么？如果把 finally 中的 return 3 去掉，又返回什么？请说明 finally 中 return 对返回值的影响。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 完整代码有 finally 中的 return 3：返回值是 3，控制台输出 "finally执行"。
  原因：try 中的 return 1 先执行（准备返回），但 finally 块会覆盖返回值，最终返回 3。
- 去掉 finally 中的 return 3：返回值是 1，控制台输出 "finally执行"。
  原因：try 中 return 1 执行后，会先执行 finally 块（不影响返回值），然后返回之前准备的值 1。
- 核心结论：
    1. finally 块在 return 之前执行（但 return 表达式已计算完毕）。
    2. 如果 finally 中有 return，会**覆盖** try/catch 中的返回值。
    3. **绝对禁止在 finally 中使用 return**，因为会吞噬 try 中正常返回的值或异常，导致难以排查的 Bug。
- 如果 finally 中抛异常：try 中的 return 会被丢弃，最终抛出 finally 中的异常。
</details>

**我的初答**：
**错漏点**：

---
## Day 5 (2026-07-30) —— File

### 题目1：File 类中路径表示方法（相对路径 vs 绝对路径）
> 在 Java 中，File 类支持相对路径和绝对路径，请分别说明两者的区别。如果在代码中写 `new File("config.properties")`，JVM 默认从哪里找这个文件？如何获取文件在磁盘上的完整路径？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 绝对路径：从盘符（Windows）或根目录（Linux/Mac）开始的完整路径，如 `C:\\project\\config.properties` 或 `/home/user/config.properties`。具有唯一性，不受当前工作目录影响。
- 相对路径：相对于当前项目的工作目录（即启动 JVM 时的 `user.dir`），如 `new File("config.properties")` 表示当前工作目录下的文件。
- 默认查找位置：IDEA 中默认工作目录是**项目根目录**（即 `pom.xml` 或 `src` 的平级目录），因此 `config.properties` 应放在项目根目录下，否则会找不到文件（返回 false）。
- 获取完整路径：调用 `file.getAbsolutePath()` 或 `file.getCanonicalPath()` 可获取文件的绝对路径，用于调试或日志输出。
- 推荐实践：生产环境尽量使用绝对路径，或通过类加载器 `ClassLoader.getResource()` 读取类路径（`src/main/resources`）下的文件。
</details>

**我的初答**：
1. 相对路径是文件相对于当前项目的路径,该路径不稳定,打包jar,命令行运行时会失效
2. 绝对路径是基于所在盘符的路径,相对稳定,但在不同电脑上路径可能不同
3. new File("config.properties")JVM默认在c盘的config文件夹中找
4. 如果想要获取文件在磁盘上的完整路径可以右键查看属性

**错漏点**：
1. 不是「相对于项目」，而是相对于 JVM 进程的当前工作目录 (user.dir)。IDE 运行时工作目录默认是项目根目录，所以看起来像是项目路径；但是脱离 IDE（cmd、jar 启动）工作目录会变化。
- 不能直接说打包 jar 就失效：File 只能访问磁盘上的物理文件！jar 包内部资源不能用 File 读取，这是失效的真正原因，不是单纯相对路径问题。
- 优化表述：相对路径以进程当前工作目录为基准，基准目录随运行环境改变，可移植性差。
2.  new File ("config.properties") 查找位置
  原话：JVM 默认在 c 盘的 config 文件夹中找
  ❌ 严重错误
  完全主观臆断！没有默认 C 盘。
  规则：以JVM 当前工作目录 (user.dir) 去找。
-  IDE 启动：一般项目根目录
-  cmd 启动：你执行java命令时所在的文件夹
  可以通过 System.getProperty("user.dir") 打印查看。
3. 题目问的是代码中如何获取文件在磁盘上的完整路径,使用getAbsolutePath()或者file.getCanonicalPath()：规范化绝对路径（推荐，消除../ 这类符号）


### 题目2：如何遍历一个目录下的所有文件和子目录？（递归 vs listFiles()）
> 现有 `File dir = new File("D:/test");` 希望遍历该目录下的所有文件（包括子目录中的文件），请写出两种实现方式：一种使用 `listFiles()` + 递归，另一种使用 `Files.walk()`（NIO）。递归遍历时如何避免空指针异常？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 方式一：递归 + listFiles()
  public void listAllFiles(File dir) {
  if (dir == null || !dir.exists() || !dir.isDirectory()) return;
  File[] files = dir.listFiles();
  if (files == null) return;  // 防止空指针（某些系统权限问题返回 null）
  for (File file : files) {
  if (file.isDirectory()) {
  listAllFiles(file);  // 递归调用
  } else {
  System.out.println(file.getAbsolutePath());
  }
  }
  }
  注意：`dir.listFiles()` 可能返回 null（如没有访问权限），必须判空，否则会抛 NullPointerException。

- 方式二：Files.walk()（NIO，更简洁）
  Files.walk(Paths.get("D:/test"))
  .filter(Files::isRegularFile)   // 只过滤文件（不含目录）
  .forEach(System.out::println);
  优势：代码简洁，支持并行流，内置异常处理。
</details>

**我的初答**：
**错漏点**：


### 题目3：File 的 delete() 和 deleteOnExit() 的区别
> File 类中有 `delete()` 和 `deleteOnExit()` 两个方法，请分别说明它们的作用和使用场景。用 `delete()` 删除一个非空目录会发生什么？如何强制删除整个目录（含子文件）？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- delete()：立即尝试删除文件或空目录。如果文件不存在、目录非空或没有权限，返回 false。非空目录无法直接删除（需先递归删除子内容）。
- deleteOnExit()：不立即删除，而是标记该文件在 JVM 正常退出时（即程序运行结束）自动删除。适用于临时文件的清理，如 `File temp = File.createTempFile("tmp", ".txt"); temp.deleteOnExit();`，避免程序退出后残留临时文件。
- 删除非空目录：`delete()` 会返回 false，无法删除。必须先用递归或 NIO 的 `Files.walkFileTree()` 遍历删除所有子文件和子目录，最后再删除该目录。
- NIO 删除整个目录的快捷方式（Java 7+）：
  Files.walkFileTree(Paths.get("D:/test"), new SimpleFileVisitor<Path>() {
  @Override
  public FileVisitResult visitFile(Path file, BasicFileAttributes attrs) throws IOException {
  Files.delete(file);
  return FileVisitResult.CONTINUE;
  }
  @Override
  public FileVisitResult postVisitDirectory(Path dir, IOException exc) throws IOException {
  Files.delete(dir);
  return FileVisitResult.CONTINUE;
  }
  });
</details>

**我的初答**：
**错漏点**：

---
## Day 6 (2026-07-31) —— IO流 + 集合综合

### 题目1：try-with-resources 自动关闭流（结合异常处理）
> 在 IO 操作中，传统的 try-catch-finally 关闭流容易遗漏或代码臃肿。请使用 try-with-resources 语法改写以下代码，并说明 try-with-resources 如何保证流被正确关闭？如果 close() 也抛出异常，原始异常会被覆盖吗？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 传统写法（容易忘记 close）：
  FileInputStream fis = null;
  try {
  fis = new FileInputStream("test.txt");
  // 读取操作
  } catch (IOException e) {
  e.printStackTrace();
  } finally {
  if (fis != null) {
  try { fis.close(); } catch (IOException e) { e.printStackTrace(); }
  }
  }

- try-with-resources 改写（推荐）：
  try (FileInputStream fis = new FileInputStream("test.txt")) {
  // 读取操作（自动关闭）
  } catch (IOException e) {
  e.printStackTrace();
  }

- 原理：实现 AutoCloseable 接口的资源在 try 块结束时自动调用 close()。如果 close() 抛出异常，会被抑制（Suppressed），附加到原始异常上，可通过 `e.getSuppressed()` 获取，保证原始异常不被覆盖。
</details>

**我的初答**：
1. 传统写法: FileInputStream fis = null;
   try{
   fis = new FileInputStream("C:\\Users\\翟曙\\Desktop\\测试.txt");
   }catch (IOException e){
   e.printStackTrace();
   }finally {
   if(fis!=null){
   try {
   fis.close();
   } catch (IOException e) {
   e.printStackTrace();
   }
   }
   }
2. try-with-resources写法: try(FileInputStream fis = new FileInputStream("C:\\Users\\翟曙\\Desktop\\测试.txt")){
   }catch (IOException e){
   e.printStackTrace();
   }
3. 如果close()也抛出异常,原始异常不会被覆盖

**错漏点**：
1. 问题 1：完全没有回答try-with-resources 如何保证流被正确关闭？
标准原理:
-  1.  try-with-resources作用于实现AutoCloseable接口的对象
- 2. 无论try块正常执行还是抛出异常,JVM自动调用资源的close()方法
- 3. 自动关闭执行在catch和finally执行之前
- 4. 按照声明逆序关闭
2. 问题 2：close 抛异常，原始异常会不会覆盖（拓展完整原理）
- 当try块抛出原始异常,同时close()也抛出异常,原始的异常不会被覆盖.使用异常抑制机制,原始异常为主异常,close()抛出的异常被附加在主异常中,可以调用getSuppressed()获取


### 题目2：读取文件内容存入集合（IO + List/Map + 方法引用）
> 现有文件 `scores.txt`，每行格式为 `姓名,成绩`（如 `张三,85`）。要求读取该文件，将每行数据封装为 `Student` 对象（含 name 和 score），存入 `List<Student>`，并按成绩降序排序后输出。请写出完整代码链（可使用 Stream 或 Collections.sort），并说明如果用 `Files.lines()` 配合方法引用该如何简化。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 传统方式（BufferedReader + List + Collections.sort）：
  List<Student> list = new ArrayList<>();
  try (BufferedReader br = new BufferedReader(new FileReader("scores.txt"))) {
  String line;
  while ((line = br.readLine()) != null) {
  String[] parts = line.split(",");
  list.add(new Student(parts[0], Integer.parseInt(parts[1])));
  }
  } catch (IOException e) { e.printStackTrace(); }
  Collections.sort(list, (s1, s2) -> s2.getScore() - s1.getScore());  // 降序

- Stream + 方法引用简化（推荐）：
  List<Student> list = Files.lines(Paths.get("scores.txt"))
  .map(line -> line.split(","))
  .map(arr -> new Student(arr[0], Integer.parseInt(arr[1])))
  .sorted(Comparator.comparingInt(Student::getScore).reversed())
  .collect(Collectors.toList());

- 方法引用优势：`Student::getScore` 直接作为 Comparator 的 keyExtractor，比 Lambda 更简洁。
- 注意：Files.lines() 内部自动管理资源（无需手动 close），但需在 try-with-resources 中或确保流被关闭。
</details>

**我的初答**：
**错漏点**：


### 题目3：使用 FileOutputStream 追加写入 + 换行（结合异常处理和续写）
> 现有一个日志文件 `log.txt`，希望每次程序运行时往文件末尾追加一条日志，格式为 `时间戳 + 日志内容 + 换行`。请写出完整代码，要求：1）使用 FileOutputStream 的续写模式；2）正确处理异常；3）每次写入后自动换行（不依赖 `\n`，请使用跨平台换行符）。如果文件不存在，程序应自动创建文件。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 完整代码：
  public static void log(String msg) {
  try (FileOutputStream fos = new FileOutputStream("log.txt", true)) {
  String timestamp = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
  String line = timestamp + " - " + msg + System.lineSeparator();  // 跨平台换行
  fos.write(line.getBytes(StandardCharsets.UTF_8));
  } catch (FileNotFoundException e) {
  // 文件不存在会自动创建（FileOutputStream 会创建文件）
  System.out.println("创建新日志文件");
  } catch (IOException e) {
  e.printStackTrace();
  }
  }

- 关键点说明：
  1. `new FileOutputStream("log.txt", true)` 的第二个参数 `true` 开启**续写模式**，每次写入追加到文件末尾。
  2. `System.lineSeparator()` 返回当前系统的换行符（Windows: `\r\n`，Linux: `\n`），保证跨平台兼容。
  3. 文件不存在时，FileOutputStream 会自动创建（无需手动判断）。
  4. 使用 try-with-resources 自动关闭流，无需 finally。
</details>

**我的初答**：
**错漏点**：

---
## Day 7 (2026-08-01) —— 字符集与字符流

### 题目1：FileReader 读取 GBK 编码文件时为什么会出现乱码？如何解决？（编码与解码）
> 现有 GBK 编码的文本文件 `test.txt`，使用 `FileReader fr = new FileReader("test.txt")` 读取中文时控制台输出乱码。请说明乱码产生的根本原因，并写出两种解决方案（至少一种需要结合 `InputStreamReader` 指定字符集）。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 乱码原因：
  FileReader 默认使用**系统平台默认字符集**（如 Windows 下为 GBK，IDEA 运行环境为 UTF-8）。当文件编码（GBK）与读取时的解码字符集（UTF-8）不一致时，字节转字符的过程就会出错，导致中文乱码。
- 解决方案一（转换流 + 指定字符集）：
  try (InputStreamReader isr = new InputStreamReader(new FileInputStream("test.txt"), "GBK")) {
  // 后续读取操作
  }
  原理：用 FileInputStream 读取原始字节，通过 InputStreamReader 显式指定 GBK 解码为字符。
- 解决方案二（Files.newBufferedReader 指定 Charset，更推荐）：
  try (BufferedReader br = Files.newBufferedReader(Paths.get("test.txt"), Charset.forName("GBK"))) {
  // 读取操作
  }
- 核心结论：处理文本文件时必须保证**编码与解码字符集一致**，否则必然乱码。
</details>

**我的初答**：
1. 读取中文时控制台输出乱码的主要原因有两点:
- 读取字节时出现漏读. 该文本文件的编码使用的字符集规范与解码使用的字符集规范不同 
1. 解决方案: 通过字节流读取,并通过String的构造方法按照指定的方式进行解码
2. 通过InputStreamReader指定解码字符集

**错漏点**：
1. FileReader无法手动指定编码规范,会按照系统默认的解码规则进行解码,与GBK字符集编码的该文件不一致,导致出现乱码
2. 补充: InputStreamReader 作为字节流→字符流桥梁，可以显式指定解码字符集 GB
- Reader r = new InputStreamReader( new FileInputStream("test.txt"), "GBK");


### 题目2：FileWriter 写入数据后，为什么文件内容为空？（缓冲区与 flush 机制）
> 现有代码片段：
FileWriter fw = new FileWriter("test.txt");
fw.write("你好世界");
// 没有调用 close()
程序运行后 `test.txt` 文件内容为空，请解释原因。`close()` 和 `flush()` 在字符流中分别起什么作用？若想在写入过程中实时看到内容（不关闭流），应该怎么做？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 原因分析：
  字符流（如 FileWriter）底层存在**字符缓冲区**（内置了 Writer 的缓冲区），`write("你好世界")` 只是把数据写入了内存缓冲区，并未立即刷新到磁盘文件。若程序结束时未调用 `close()` 或 `flush()`，缓冲区数据丢失，导致文件为空。
- close() vs flush()：
  - flush()：强制将缓冲区中的字符刷新到磁盘（写入目标文件），但流仍可继续使用。
  - close()：先调用 flush() 刷新缓冲区，然后释放系统资源（关闭流），之后流不可再用。
- 实时写入（不关闭流）：
  在每次写入后调用 `fw.flush()`，或使用 `BufferedWriter` 并设置自动刷新（`new BufferedWriter(fw, 8192)` 但仍需手动 flush，或调用 `new PrintWriter(fw, true)` 启用自动刷新）。
- 最佳实践：使用 try-with-resources 自动关闭流，确保 flush 和 close 被执行。
</details>

**我的初答**：
1. 没有关闭对应的流,缓冲区中的内容就不会被写入文件
2. close()用于关闭程序与对应文件的链接,将文件描述符释放. flush()用于刷新内存,调用close()时内部会同时使用flush(),但手动书写更稳妥
3. 写入过程中实时看到内容不了解

**错漏点**：
1. 第一点表述不够严谨
- 完善: FileWriter()自带内置缓冲区,内容会被先读取到缓冲区中,缓冲区未满时,数据不会自动持久化到磁盘
.没有调用close()方法,缓冲区数据不会自动刷新落地,文件内容为空
2. close ()、flush () 解释存在缺陷
-   close()：①先自动执行 flush ()，把缓冲区残留数据写入磁盘；②释放 IO 资源（文件句柄）；③流关闭后无法继续读写。
-   flush ()：仅强制将缓冲区现有数据刷新到磁盘，不关闭流，资源不会释放，流依旧可以继续写入。
   你的误区：“手动书写更稳妥” 表述模糊；没有区分二者核心差别（是否关闭流、释放资源）。
3. 想要不关闭流、实时把内容写入文件：每次写入后手动调用 flush ()



### 题目3：FileWriter 与 FileOutputStream 的本质区别（字符流 vs 字节流 + 转换流继承关系）
> FileWriter 与 FileOutputStream 都能往文件写入数据，它们在继承体系和处理单位上有何不同？`FileWriter` 的父类是什么？为什么说 `FileWriter` 实际上依赖于 `FileOutputStream` 工作？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 处理单位不同：
  - FileOutputStream（字节流）：以 byte（字节）为单位写入，适合图片、视频等二进制文件。
  - FileWriter（字符流）：以 char（字符）为单位写入，专门处理文本文件，底层会自动将字符按默认字符集编码为字节序列。
- 继承体系差异：
  - FileOutputStream 继承自 OutputStream。
  - FileWriter 继承自 OutputStreamWriter（转换流），而 OutputStreamWriter 继承自 Writer。
- 为什么依赖 FileOutputStream：
  FileWriter 是 OutputStreamWriter 的便捷子类，其底层本质是 `new FileOutputStream(path)` 字节流，再套上编码转换功能。它等价于 `new OutputStreamWriter(new FileOutputStream(path), Charset.defaultCharset())`，只不过把指定字符集的能力固定为默认字符集。
- 结论：FileWriter 是“字符流的外表，字节流的内心”，一切文本写入最终都要转成字节落到磁盘。
</details>

**我的初答**：
1. FileWriter属于字符流,处理的单位是字符.FileOutputStream属于字节流,处理单位是单个字节
2. FileWriter继承于转换流OutputStreamWriter.FileWriter实际上是OutputStreamWriter的一种"快捷方式".而OutputStreamWriter,内部通过FileOutputStream来从磁盘读取内容

**错漏点**：
1. 父类错误：FileWriter直接父类是 OutputStreamWriter 这点是对的；但注意面试完整继承链：Writer → OutputStreamWriter → FileWriter。
2. 动词错误：OutputStreamWriter内部是写磁盘，不是 “从磁盘读取内容”。它拿到 FileOutputStream，把字符转成字节输出写入文件，不是读。
3. 缺少关键依据：没有说明「FileWriter 构造方法内部实例化 FileOutputStream 传给父类 OutputStreamWriter」，面试写出来才是完整得分。

---
## Day 8 (2026-08-02) —— 字符流原理与缓冲流

### 题目1：缓冲流（BufferedReader / BufferedWriter）为什么能大幅提升 I/O 性能？
> 使用 `BufferedReader` 包装 `FileReader` 后，读取速度为什么比直接用 `FileReader` 快很多？请从**磁盘 I/O 次数**和**内存操作**两个角度解释底层原理，并说出默认缓冲区大小是多少。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 底层原理（核心）：
  - 无缓冲流（FileReader）：每次调用 read() 都会直接触发一次操作系统级的磁盘 I/O 读取操作（按字节/字符），频繁的磁盘访问会导致大量上下文切换，速度极慢。
  - 缓冲流（BufferedReader）：内部维护了一个缓冲区（默认 8192 个字符，即 8KB）。第一次读取时，会一次性从磁盘读取 8KB 的数据填充到内存缓冲区中，后续的 read() 操作直接从内存缓冲区获取数据，只有当缓冲区数据耗尽时，才会再次触发磁盘 I/O。
- 性能提升本质：**用一次大块磁盘 I/O（耗时）替代了无数次小块磁盘 I/O（更耗时）**，将磁盘 I/O 次数从“数据长度次”降低为“数据长度 / 8192 次”，同时利用内存高速读写的优势。
- 装饰器模式体现：BufferedReader 是对 Reader 的增强装饰，在不改变底层 FileReader 的前提下，为其赋予了缓冲能力。
</details>

**我的初答**：
1. BufferedReader包装FileReader后,BufferedReader会通过内部的长度为8192的char数组来减少IO的次数,以此来提高性能

**错漏点**：
1. 没有区分磁盘 I/O 和 内存操作两个角度；
-   没有解释原生FileReader读取逻辑：无缓冲时，每次read()大概率发起一次系统调用读取磁盘；
-   没有说明缓冲机制流程：一次性批量读取一块数据进内存缓冲区，后续读取优先从内存数组拿，内存访问远快磁盘 IO；
-   没有点明：磁盘 IO 属于低速硬件操作，系统调用开销很高；内存读写属于 CPU 高速操作
2. 没有 BufferedReader：你每调用一次 read () → 就触发一次底层解码尝试；
-  有 BufferedReader：一次批量读出一大批字符，一次性执行大量解码，后续几十上百次 read () 直接拿缓存字符，不再触发解码

**注意**
- 标准解析中的: 无缓冲流（FileReader）：每次调用 read() 都会直接触发一次操作系统级的磁盘 I/O 读取操作是简化表述,并不严谨
- ✅ 真实情况：FileReader.read () 不一定每次都触发磁盘 IO。优先从 StreamDecoder 的 byte [] 缓冲区拿字节；缓冲区空了，才发起一次系统调用读取磁盘

### 题目2：BufferedReader 的 readLine() 方法底层是如何判断一行结束的？
> BufferedReader 提供了 readLine() 方便地按行读取文本。请问它内部是如何识别换行符的？它能够正确识别 Windows（\r\n）、Linux（\n）和旧 Mac（\r）的换行符吗？如果文件末尾没有换行符，它会怎么处理？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 识别逻辑：readLine() 内部会持续从缓冲流中读取字符，遇到换行符（\n）或回车符（\r）时停止，并返回当前行的字符串（不包含换行符）。
- 跨平台兼容性：Java 的 readLine() 能够自动识别并处理三种主流的换行符：
  1. \n（Linux/Unix/Mac OS X）
  2. \r\n（Windows）
  3. \r（旧 Mac OS）
     内部逻辑会智能判断，遇到 \r 时会再 peek 一下下一个字符是否是 \n，若是则一起跳过；若不是则只跳过 \r。
- 文件末尾处理：如果文件最后一行没有换行符，readLine() 依然会正常读取这一行的数据并返回（不会报错），直到读取完所有字符后返回 null（表示文件结束）。
- 注意：readLine() 返回的内容**不包含**行终止符，如果需要保留换行符，需要手动添加。
</details>

**我的初答**：
1. 通过对应的编码规则识别换行符,所有操作系统的换行符它都能正确识别
2. 如果文件末尾没有换行符,它会接着读取下一行的内容

**错漏点**：
1. 识别原理表述错误
- “通过对应的编码规则识别换行符” 的说法不对。BufferedReader 属于字符流，其处理的数据已经是解码完成的 char 字符，换行符识别是纯字符层面的逻辑判断，和编码规则没有关系。它是通过逐个比对字符是否为 \r 或 \n 来判定行结束，不依赖编码规则。
2. 没有说明处理细节：它不仅能识别三种换行符，还会将换行符本身从返回结果中剔除；且针对 Windows 的 \r\n 会作为一个完整换行处理，不会拆分成两个换行、产生多余空行
3. 文件末尾无换行的处理完全错误
-   “接着读取下一行的内容” 不符合实际逻辑。文件末尾已经没有后续数据，不存在 “下一行”。正确逻辑是：读到文件末尾时，如果缓冲区还有未读完的有效字符，readLine () 会将其作为最后一行正常返回；再次调用时才会返回 null 标识文件结束，不会丢失最后一行数据。

### 题目3：字符流缓冲区中的 flush() 和 close() 到底做了什么？（自动刷新机制）
> 在 BufferedWriter 中，调用 `write("数据")` 后，数据并没有立即写入文件。请说明 `flush()` 和 `close()` 各自的作用以及两者的关系。如果在 try-with-resources 中自动关闭流，还需要手动调用 flush() 吗？什么场景下必须调用 flush()？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- flush() 的作用：强制将当前缓冲区中积攒的字符数据，立即刷新（写入）到底层文件或输出设备中，但流对象本身不会被关闭，后续仍可继续写入。
- close() 的作用：先自动调用一次 flush()（确保缓冲区数据不丢失），然后释放流对象占用的系统资源（如文件句柄），关闭后流不可再用。
- try-with-resources 自动关闭：不需要手动调用 flush()。因为 try-with-resources 会在 try 块结束时自动调用 close()，而 close() 内部已经包含了 flush() 操作，所以数据一定会被写入。
- 必须手动 flush() 的场景：
  1. 实时日志：需要立即将日志写入文件，防止程序崩溃导致数据丢失。
  2. 交互式输入输出：如聊天程序，需要对方即时看到消息。
  3. 长时间运行的大文件写入：定期 flush 可以避免缓冲区积压过大，同时将已处理数据落盘。
- 注意：如果没有调用 close() 或 flush()，程序正常结束（main 退出）时，缓冲区数据可能会丢失（取决于 JVM 实现），因此**始终要确保关闭流或刷新缓冲区**。
</details>

**我的初答**：
**错漏点**：

---
## Day 9 (2026-08-03) —— 转换流与序列化流

### 题目1：转换流（InputStreamReader / OutputStreamWriter）的作用与使用场景
> FileReader 和 FileWriter 默认使用系统平台字符编码，当需要指定特定字符集（如 GBK）读写文本时，为什么必须使用转换流？请写出使用 InputStreamReader 读取 GBK 编码文件的代码片段，并说明转换流在“字节流 → 字符流”中间扮演了什么角色。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 为什么必须用转换流：
  FileReader / FileWriter 是便捷类，底层固定使用默认字符集（Charset.defaultCharset()），无法在构造时指定编码。当文件编码与系统默认编码不一致时（如 Windows 默认 GBK，但文件为 UTF-8），直接使用 FileReader 必然乱码。转换流（InputStreamReader / OutputStreamWriter）允许在构造时显式指定字符集，是唯一的标准解决方案。
- 代码示例（读取 GBK 文件）：
  try (InputStreamReader isr = new InputStreamReader(new FileInputStream("gbk.txt"), "GBK")) {
  int ch;
  while ((ch = isr.read()) != -1) {
  System.out.print((char) ch);
  }
  } catch (IOException e) {
  e.printStackTrace();
  }
- 角色定位：
  转换流是**字节流与字符流之间的桥梁**。InputStreamReader 将字节输入流（InputStream）按指定字符集解码为字符；OutputStreamWriter 将字符按指定字符集编码为字节输出到底层字节流。它们本身不产生数据，只负责编码/解码的转换。
- 最佳实践：处理文本文件时，优先使用 Files.newBufferedReader(Paths.get("path"), Charset.forName("UTF-8"))，它内部封装了转换流且更简洁。
</details>

**我的初答**：
1. 因为在JDK11以前,只有转换流可以指定字符集,而字符流无法指定字符集
2. InputStreamReader isr = new InputStreamReader(new FileInputStream("a.txt"), Charset.ofName("GBK"))
3. 转换流是字节流与字符流之间的桥梁,字符流继承与转换流,不需要手动new字节流

**错漏点**：
1. 换句话说：编码解码的核心能力、“字节流到字符流的桥梁” 角色，自始至终都是 InputStreamReader 在承担。FileReader 只是把 “手动创建字节流 + 手动传入转换流” 的样板代码藏起来了
2. 为什么教材 / 考试题依然强调 “必须用转换流”
- 考察基准版本是 JDK 8
  国内高校教材、校招笔试面试几乎全部以 JDK 8 为默认考察环境，JDK 8 及更早版本没有这个构造，在这个前提下 “必须用转换流” 是完全成立的。
-  考点是底层原理，不是版本 API
  这道题的核心考察点是字节流与字符流的转换机制、编码解码的桥梁作用，而不是 “哪个版本新增了什么语法糖”。
  如果答题只说 “JDK11 以后不用了”，等于完全没答到核心考点，依然会大量失分。
3. 四、补充：转换流永远不会被淘汰
-   不要误以为 JDK11 之后转换流就没用了。
   FileReader 只能封装文件字节流，而 InputStreamReader 可以接收任意 InputStream 子类（网络套接字流、字节数组流、压缩流、管道流等）。
   只要涉及「通用字节输入流 → 按指定编码转字符流」的场景，无论什么 JDK 版本，都必须使用转换流，这是它不可替代的核心价值。


### 题目2：序列化与反序列化的核心机制（Serializable 接口 + transient 关键字）
> 现有 User 类实现了 Serializable 接口，包含 name（String）、password（String）和 age（int）三个字段。要求：1）写出将 User 对象序列化到 user.txt 文件的代码；2）如果 password 字段不希望被序列化（存到磁盘），应该使用什么关键字修饰？3）静态变量（static）和瞬态变量（transient）在序列化时的行为有何不同？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 序列化代码示例（写入）：
  User user = new User("张三", "123456", 20);
  try (ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream("user.txt"))) {
  oos.writeObject(user);
  } catch (IOException e) {
  e.printStackTrace();
  }
- 反序列化代码示例（读取）：
  try (ObjectInputStream ois = new ObjectInputStream(new FileInputStream("user.txt"))) {
  User user = (User) ois.readObject();
  System.out.println(user);
  } catch (IOException | ClassNotFoundException e) {
  e.printStackTrace();
  }

- 防止字段序列化：
  使用 **transient** 关键字修饰 password 字段（private transient String password;）。序列化时会忽略该字段，反序列化时 password 会被赋予默认值（引用类型为 null，基本类型为 0）。
- static vs transient 的核心区别：
  - static（静态变量）：属于类级别，不属于对象。序列化的是**对象状态**，因此 static 变量**不会被序列化**（即使不加 transient）。
  - transient（瞬态变量）：属于对象级别，但主动标记为不序列化，用于保存敏感信息（如密码）或冗余缓存数据。
  - 结论：两者都不会被序列化，但 static 是因为“不属于对象”，transient 是因为“主动拒绝被序列化”。
</details>

**我的初答**：
**错漏点**：


### 题目3：serialVersionUID 的作用与不声明的后果（版本兼容性）
> 在序列化类中，建议显式声明 `private static final long serialVersionUID = 1L;`。如果不声明，JVM 会自动生成一个 UID，这有什么潜在风险？请结合“类结构变更”场景说明。如果反序列化时发现 serialVersionUID 不一致，JVM 会抛出什么异常？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- serialVersionUID 的作用：
  它是序列化版本号，用于在反序列化时验证序列化对象的字节流中的类定义与当前 JVM 中的类定义是否兼容（即是否是同一个版本的类）。
- 不声明的潜在风险（极严重）：
  如果未显式声明，JVM 会根据类的结构（字段、方法、访问修饰符等）通过哈希算法生成一个 UID。一旦对类做了任何改动（如新增字段、删除字段、修改字段类型、修改方法签名等），JVM 重新计算的 UID 就会改变。
  结果：旧版本序列化的二进制文件在新版本代码中反序列化时，由于 UID 不匹配，JVM 会直接抛出 **InvalidClassException**，导致反序列化彻底失败，所有存量数据无法读取。
- 解决方案：
  显式声明一个固定值（如 1L），并约定：后续对类的修改（如新增字段）应保持兼容性（可反序列化时赋予默认值），只要 UID 不变，JVM 就不会报错。
- 最佳实践：
  每个实现 Serializable 的类都强制显式添加：`private static final long serialVersionUID = 1L;`（或用 IDE 生成固定的 64 位哈希值）。
</details>

**我的初答**：
**错漏点**：

---
## Day 10 (2026-08-04) —— 打印流与压缩流

### 题目1：PrintStream 与 PrintWriter 的异同及内部错误处理机制
> PrintStream 与 PrintWriter 均提供便捷的 print/println 方法，且不抛出 IOException。请从底层字节/字符流差异、编码指定方式、以及内部错误处理机制（checkError()）三个维度说明两者的本质区别。追问：System.out 属于哪种类型？为何设计为不抛异常？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 核心差异：
  1. 操作单位：PrintStream 继承自 FilterOutputStream，操作字节（byte），并内置编码转换（将字符按指定字符集编码为字节）；PrintWriter 继承自 Writer，操作字符（char），直接处理字符流。
  2. 编码指定：PrintStream 构造时可用 Charset 参数（底层通过 OutputStreamWriter 转换），PrintWriter 亦可指定 Charset，但 PrintWriter 支持更多字符流特性（如写入 char[]）。
  3. 错误处理：两者均不抛出 IOException，而是通过内部 boolean 标志（trouble）记录异常，需调用 checkError() 返回 true 表示发生过异常。PrintStream 的 println 内部捕获异常后调用 setError()。
- System.out 定位：System.out 是 PrintStream 类型（字节流），确保控制台输出时直接处理字节，兼容各类原生控制台。
- 设计意图：不抛异常是为了让打印操作不中断主流程（如日志或调试输出），将错误延迟暴露给调用者主动检查。
</details>

**我的初答**：
1. PrintStream是基于字节流的包装流,每次打印都是基于底层的字节流读取数据,使用默认的编码指定方式
2. PrintWriter是基于字符流的包装流,只能打印纯文本内容,可以指定编码方式
3. 两者都通过内部捕获异常不让异常抛出
4. System.out属于PrintStream
5. 日志/调试不应因为异常而停止,所以Java设计内部捕获,可以通过checkError()查看

**错漏点**：
1. 表述错误：“基于底层的字节流读取数据” 有误，两者均为输出流，执行的是写入操作，而非读取
2. 表述不准确：“PrintStream 使用默认的编码指定方式” 容易造成误解 ——PrintStream 并非只能使用平台默认编码，其构造方法支持显式指定字符集（Charset / 编码名称），只是常用的 System.out 采用了平台默认编码。
3. 内部错误处理机制部分
-   描述过于笼统，未紧扣 checkError() 说明核心机制：
   两者内部都维护一个错误标志位（trouble 标志），捕获 IOException 时将该位置为 true；checkError() 的作用就是返回这个标志位。
-   关键特性：该标志位一旦置为 true 就无法重置，只要输出过程中出现过一次 IO 错误，后续调用 checkError() 永远返回 true。
-   未区分层面差异：PrintStream 在字节写入层面捕获异常，PrintWriter 在字符写入层面捕获异常
4. 不抛异常的原因表述正确但不够完整：除了 “日志 / 调试不应因异常停止”，更核心的设计考量是：print/println 定位为便捷打印方法，若抛出受检异常 IOException，会强制所有打印语句都包裹 try-catch，严重降低开发效率；“内部吞异常 + checkError 检测” 的设计，是在易用性和可靠性之间的权衡。


### 题目2：ZipOutputStream 写入条目的顺序约束与 closeEntry 的必要性
> 使用 ZipOutputStream 压缩文件时，必须严格遵循 putNextEntry -> write -> closeEntry 的顺序。请解释 ZipEntry 在 ZIP 文件格式中的存储结构（中央目录与本地文件头），为何 write 必须在 closeEntry 之前完成？若忘记调用 closeEntry，会导致什么后果？追问：能否在写入过程中随机跳跃修改已完成的条目？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- ZIP 格式结构：每个条目包含本地文件头（Local File Header）+ 压缩数据 + 数据描述符，整个文件末尾附加中央目录（Central Directory）。中央目录记录每个条目的偏移量、CRC32 校验和及压缩大小。
- 严格顺序原因：putNextEntry 写入本地文件头，write 追加压缩数据，closeEntry 计算并回填 CRC32 和大小信息（若数据描述符启用）并更新中央目录缓存。若不按此顺序，压缩数据流无法正确终止。
- 未调用 closeEntry 后果：当前条目的压缩数据虽写入，但 CRC32 未写入，中央目录不完整，导致解压时校验失败（ZipException）或部分文件损坏，且后续无法正常写入下一个条目。
- 禁止随机修改：ZIP 流是顺序写入的，已完成的条目无法回退修改，除非重新生成整个 ZIP 文件。
</details>

**我的初答**：
**错漏点**：


### 题目3：GZIPOutputStream 与 ZipOutputStream 的压缩粒度差异及 PrintWriter 包装陷阱
> GZIPOutputStream 和 ZipOutputStream 同属压缩流，但 GZIP 只能操作单个数据流，Zip 可管理多条目。请说明两者在压缩粒度上的本质区别。追问：若用 PrintWriter 包装 ZipOutputStream 写入文本，并尝试在循环中 putNextEntry 创建多个文件，哪一步可能导致数据丢失？应如何调整关闭顺序？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 压缩粒度：
  - GZIPOutputStream：将单一字节流整体压缩（无目录结构），解压时还原单一文件/流，无文件名记录。
  - ZipOutputStream：封装独立条目（Entry），每个条目拥有独立的文件名、压缩方法、CRC32 和时间戳，支持多文件归档（类似文件夹）。
- PrintWriter 包装陷阱及数据丢失：
  当 PrintWriter 包装 ZipOutputStream 时，PrintWriter 会维护内部字符缓冲区。若在循环中交替执行 putNextEntry -> PrintWriter.println -> closeEntry，PrintWriter 的缓冲区可能尚未 flush，导致数据滞留在 PrintWriter 的缓存中，未实际写入 ZipOutputStream，最终 closeEntry 时数据不完整或文件大小为 0。
- 修正策略：
  1. 在 closeEntry() 前显式调用 PrintWriter.flush()。
  2. 关闭顺序：先 flush/close PrintWriter，再 close ZipOutputStream，确保缓冲数据全部落入底层流后再封存 ZIP 结构。
- 推荐替代：使用 Files.newBufferedWriter 配合 ZipOutputStream 时，在调用 closeEntry 前强制 flush。
</details>

**我的初答**：
**错漏点**：

---
## Day 11 (2026-08-05) —— IO流综合复习

### 题目1：System.out.println() 的底层流类型与自动刷新机制
> System.out.println("hello") 中的 out 是 PrintStream 实例。请问：PrintStream 与 BufferedOutputStream 的缓冲机制有何不同？println() 是否自动调用 flush()？如果调用 System.setOut(new PrintStream(new FileOutputStream("log.txt"))) 重定向输出，当程序异常退出时，缓冲区数据可能丢失，如何避免？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- PrintStream 的缓冲：内部通过 BufferedWriter 或直接使用 OutputStream，但 println() 方法会在写入换行符后自动调用 flush()（可通过构造参数 autoFlush 控制，默认 System.out 的 autoFlush 为 true）。
- 与 BufferedOutputStream 的区别：BufferedOutputStream 的 flush() 需手动调用，而 PrintStream 的 println() 在 autoFlush=true 时会自动刷新。
- 重定向陷阱：若用 PrintStream 包装 FileOutputStream 且未开启 autoFlush，异常退出时缓冲区数据不落盘。解决方案：
  1. 构造时传入 true 开启自动刷新：new PrintStream(new FileOutputStream("log.txt"), true)
  2. 或定期手动调用 flush()，并在 JVM 关闭钩子中强制刷新：Runtime.getRuntime().addShutdownHook(new Thread(() -> out.flush()))
- 追问：System.out 的 autoFlush 默认 true，但 System.err 呢？为何设计为不同？
</details>

**我的初答**：
1. printStream内部不持有缓冲区.BufferedOutputStream内部持有缓冲区,默认长度为8192字节的字节数组,每次大量从OutputStream读取数据,增快效率,减少通过Write()解码和磁盘调用消耗
2. println在执行完毕后内部会自动调用flush().
3. 通过手动增加flush(),来实现显式刷出.

**错漏点**：
1. “PrintStream 内部不持有缓冲区” 是错误结论
- PrintStream 并非没有缓冲，它内部封装了BufferedWriter + OutputStreamWriter，自带字符级缓冲区，用于提升字符转字节的效率，同时负责编码转换、系统换行符适配。它和 BufferedOutputStream 的区别是缓冲层级不同，而非 “有无缓冲”。
2. 流的方向描述错误
- “每次大量从 OutputStream 读取数据” 表述完全颠倒：OutputStream是输出流，行为是写入数据到目标，不是读取。BufferedOutputStream 的逻辑是：把待写出的数据暂存到内部 byte 数组，缓冲区满 / 手动 flush 时，才一次性调用底层流的 write 方法，以此减少系统调用次数。
3. 编解码方向描述错误
- “减少通过 Write () 解码消耗” 有误：输出场景是编码（字符→字节），解码是输入流（字节→字符）的行为；且 BufferedOutputStream 只做纯字节缓冲，完全不涉及编解码。

- 自动调用flush()结论过于绝对，忽略了autoFlush开关这个核心前提：
1.  不是所有println()都会自动 flush，是否触发完全取决于 PrintStream 构造时的autoFlush参数。
2.  只有autoFlush = true时，println 输出换行符后才会触发底层流的 flush；如果使用默认构造new PrintStream(out)，autoFlush默认为 false，println 不会自动 flush。
3.  触发条件本质是遇到换行符\n，而非 “println 执行完毕”。比如print("hello\n")在 autoFlush=true 时也会触发 flush，和 println 效果一致。
4.  补充：我们日常用的System.out会自动 flush，是因为 JVM 初始化时显式传入了autoFlush = true，这是个例，不是 PrintStream 的通用行为。


### 题目2：Files.readAllLines() 与 BufferedReader.readLine() 的内存极限差异
> Files.readAllLines(Paths.get("big.txt")) 与 new BufferedReader(new FileReader("big.txt")).lines().forEach(...) 都能读取全部行。请问两者在内存占用上有何本质区别？如果文件大小为 5GB，哪一种是安全的？为什么？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- Files.readAllLines()：一次性将全部行读入 List<String>，所有行字符串常驻堆内存，若文件过大直接导致 OOM（即便 1GB 文本也可能耗尽堆空间）。
- BufferedReader.lines()：返回一个 Stream<String>，底层基于缓冲流按需读取（每次 readLine 触发一次磁盘 I/O），处理完一行即可被 GC，不要求全量加载到内存。
- 安全性结论：5GB 文件必须使用 BufferedReader.lines() 或自行实现分批读取（如按块读取），readAllLines() 仅适用于 MB 级别的小文件。
- 追问：Files.lines() 内部是否自动关闭底层文件句柄？若 Stream 未正确 close()，会造成文件句柄泄漏吗？
</details>

**我的初答**：
1. Files.readAllLines()是直接将文件中所有的内容读取出来.
2. FileReader("big.txt")).lines().forEach(...)则是一行一行地读取文件中的内容
3. 前者一次性读取,内存占用极大.后者每次只读取一行,内存占用比较小
4. 5GB的文件读取,使用readLine将会比较安全,因为系统能分配给虚拟机的内存大小可能会小于5GB,这样会导致内容漏读

**错漏点**：
1. 底层读取方式描述错误
- 你认为前者"一次性读取",后者"一行一行地读取文件",这是常见误区.实际上二者的底层IO读取逻辑完全一致:
- `Files.readAllLines()`的内部实现就是基于`BufferedReader`,同样靠内部缓冲区批量读取文件内容,再拆分成行.二者的读取效率、IO次数没有区别,差异只在[读出来的数据要不要全部存下来].
2.  没有触达最核心的本质区别：数据是否全量驻留内存
-  这是两者最根本的差异，你的回答只描述了表面现象，没有点明本质：
  Files.readAllLines()：每读取一行就存入List<String>集合，直到文件读完，所有行的内容全程驻留在堆内存中，内存占用随文件大小线性增长。
  BufferedReader.lines().forEach(...)：lines()返回的是懒加载的Stream<String>，内存中只保留当前正在处理的一行 + 内部缓冲区数据，已处理的行会失去引用、被 GC 回收，不会累积，内存占用和文件总大小无关。
3.  “后者每次只读取一行” 的表述不准确
-  BufferedReader内部有默认 8192 字符的字符缓冲区，底层是批量把文件数据读到缓冲区，再从缓冲区拆分出行交给业务代码，并不是每处理一行就发起一次磁盘 IO。你混淆了「业务层逐行处理」和「底层批量读取」的概念。
4.  内存量级认知有偏差
-  全量加载的内存占用不是 “等于文件大小”，而是远大于文件本身的字节体积。文件是字节存储（如 UTF-8 下英文占 1 字节、中文占 3 字节），而 Java 的String以char[]存储（每个 char 占 2 字节），再加上对象头、集合的内存开销，全量加载后的内存通常是原文件的 1.5~3 倍。


### 题目3：从压缩包读取文本并统计单词频率（ZipInputStream + BufferedReader + 集合综合）
> 现有一个 zip 文件，内含多个 .txt 文件（UTF-8 编码），要求遍历所有条目，读取文本内容，统计所有单词出现次数（忽略大小写，按空格和标点分割），并输出 Top 10 高频词。请写出核心代码框架，并指出 zip 流嵌套 buffered 流时，正确的关闭顺序是什么？若某个 txt 文件为空或读取异常，如何保证其他文件仍能正常处理？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 代码框架（核心流程）：
  Map<String, Long> wordCount = new HashMap<>();
  try (ZipInputStream zis = new ZipInputStream(new FileInputStream("data.zip"))) {
  ZipEntry entry;
  while ((entry = zis.getNextEntry()) != null) {
  if (!entry.isDirectory() && entry.getName().endsWith(".txt")) {
  try (BufferedReader br = new BufferedReader(new InputStreamReader(zis, StandardCharsets.UTF_8))) {
  br.lines()
  .flatMap(line -> Arrays.stream(line.split("[\\s\\p{Punct}]+")))
  .filter(w -> !w.isEmpty())
  .forEach(word -> wordCount.merge(word.toLowerCase(), 1L, Long::sum));
  } catch (IOException e) {
  System.err.println("跳过文件 " + entry.getName() + "，原因：" + e.getMessage());
  // 继续处理下一个 entry（因为 zis 仍在正确位置）
  }
  }
  zis.closeEntry();  // 确保当前条目被关闭，即使 reader 已关闭
  }
  } catch (IOException e) { e.printStackTrace(); }
  // Top 10 输出：wordCount.entrySet().stream().sorted(...).limit(10)...

- 关闭顺序与注意事项：
  1. 内层 BufferedReader 的 close() 不会关闭外层的 ZipInputStream（因为 InputStreamReader 包装的是 zis，但关闭 reader 不会影响 zis）。
  2. 必须保证 zis.closeEntry() 被执行，即使读取过程中抛异常，也要在 finally 或 try-with-resources 层级确保当前条目关闭，否则后续条目无法正常读取。
  3. 单个文件异常时应捕获并记录，继续循环处理下一个条目，不影响整体流程。
- 追问：如果 BufferedReader 关闭后，再调用 zis.closeEntry() 是否会失败？为什么？（提示：检查 ZipInputStream 内部状态）
</details>

**我的初答**：
1.

**错漏点**：

---
## Day 12 (2026-08-06) —— IO流进阶补全

### 题目1：RandomAccessFile 的读写指针与线程安全性
> RandomAccessFile 支持随机读写，通过 seek() 移动文件指针。请问它与 FileInputStream/FileOutputStream 在继承体系上有什么本质不同？如果两个线程同时操作同一个 RandomAccessFile 实例进行写操作，是否线程安全？为什么？如何实现线程安全的文件追加写入？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 继承体系差异：RandomAccessFile 直接继承 Object 并实现 DataInput/DataOutput 接口，不属于 InputStream/OutputStream 体系。它独立于字节/字符流层级，专用于支持 seek 操作的随机访问文件。
- 线程安全性：非线程安全。因为 read/write 操作均依赖文件指针（fd 中的 position），且未加任何同步锁。两个线程并发写入会互相覆盖对方的数据，导致数据错乱或指针错位。
- 线程安全追加方案：
  1. 加外部锁：synchronized (raf) { raf.seek(raf.length()); raf.write(data); }
  2. 使用 FileChannel 的 position() + 原子性写入（但 position 操作仍非原子，需配合锁）
  3. 使用 java.nio.channels.FileLock 尝试获取文件锁（跨进程，但性能低）
- 注意：如果仅追加写，最简单方案是使用 FileOutputStream 构造时传入 true（底层文件通道的 append 属性由操作系统保证原子性）。
</details>

**我的初答**：
**错漏点**：


### 题目2：InputStream.available() 的误用陷阱（网络流与本地文件的差异）
> 常见代码：byte[] data = new byte[in.available()]; in.read(data); 在读取本地文件时通常工作正常，但在 Socket 或管道流中却可能读取不全。请解释 available() 方法的真实含义（返回不阻塞情况下可读字节数），并说明为什么不能依赖它来确定文件总大小。追问：如果必须用 available() 读本地文件，你如何修正这段代码？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- available() 真实语义：返回当前输入流中**在不阻塞下一次调用**的情况下可以读取的字节数估计值，并不保证剩余全部数据。对于 Socket 流，它只返回当前操作系统接收缓冲区中的就绪字节，而非报文总长度。
- 为何本地文件可用：FileInputStream 的 available() 底层通过 `FileChannel.size() - position` 计算（因为磁盘文件的数据已经在文件系统中存在，无需网络阻塞），所以它确实返回剩余全部字节数。
- 误用后果：在 Socket 流中使用会导致每次只读取一部分，从而丢失后续数据（因为网络包分批到达）。
- 正确读取模式：必须循环调用 read(byte[] buffer) 直到返回 -1，不论用什么流。
- 修正代码（若仍想用 available 做批量优化）：
  int totalBytes = in.available(); // 仅用于预估缓冲大小
  ByteArrayOutputStream baos = new ByteArrayOutputStream();
  byte[] buffer = new byte[Math.min(8192, totalBytes)];
  int len;
  while ((len = in.read(buffer)) != -1) { baos.write(buffer, 0, len); }
</details>

**我的初答**：
**错漏点**：


### 题目3：编码转换中的“不可映射字符”与替代策略
> 使用 `new String(byte[], "GBK")` 将 GBK 字节转为 String 时，若字节序列包含非法 GBK 码位，JVM 默认如何处理？请指出 `CharsetDecoder` 中的 CodingErrorAction 枚举（REPLACE、REPORT、IGNORE）各自的作用。若希望在解码时将非法字节替换为“?”，应如何配置？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 默认行为：`new String(bytes, "GBK")` 底层使用 `Charset.defaultCharset().newDecoder()`，其默认的 `CodingErrorAction` 为 **REPLACE**，会将非法字节替换为 Charset 默认的替代字符（通常是 `\uFFFD`，即 `?`）。
- CodingErrorAction 三类：
  1. REPLACE：将非法序列替换为预定义的替代字符（可自定义）。
  2. REPORT：抛出 MalformedInputException 或 UnmappableCharacterException，需要显式 try-catch。
  3. IGNORE：直接跳过非法字节，不报错也不替换，导致结果字符串长度丢失。
- 配置示例（替换为自定义字符“?”）：
  CharsetDecoder decoder = Charset.forName("GBK").newDecoder();
  decoder.onMalformedInput(CodingErrorAction.REPLACE);
  decoder.onUnmappableCharacter(CodingErrorAction.REPLACE);
  decoder.replaceWith("?");
  String result = decoder.decode(ByteBuffer.wrap(bytes)).toString();
- 实际开发推荐：使用 Files.newBufferedReader(Paths.get(""), Charset.forName("GBK"))，内部默认采用 REPLACE，但可通过构造 `InputStreamReader` 时传入解码器来控制。
</details>

**我的初答**：
**错漏点**：

---
## Day 13 (2026-08-07) —— 多线程创建与启动

### 题目1：Thread、Runnable、Callable 三种创建方式的本质差异及使用场景
> 请从以下维度对比三种方式：1）资源复用性（是否建议将 Runnable 或 Callable 任务与 Thread 对象分离）；2）返回值与异常处理机制；3）启动方式。追问：若一个类已继承父类，应选用哪种方式？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 资源复用性：
  - Thread：继承方式，每个线程持有独立子类实例，任务代码耦合在线程子类中，无法复用任务对象。
  - Runnable / Callable：任务与线程分离，可将同一任务实例传给多个 Thread 对象（如线程池），实现资源复用。
- 返回值与异常：
  - Thread / Runnable：run() 方法无返回值，且无法抛出受检异常（必须内部 try-catch）。
  - Callable：call() 方法有返回值（通过 Future 获取），且允许抛出受检异常（如 IOException）。
- 启动方式：
  - Thread / Runnable：通过 Thread.start() 启动。
  - Callable：需包装为 FutureTask（实现了 Runnable），再传入 Thread 或提交给线程池（ExecutorService.submit() 返回 Future）。
- 已继承父类选型：必须实现 Runnable 或 Callable（因 Java 单继承限制）。
</details>

**我的初答**：
1. 应将Runnable或Callable的任务于Thread对象分离,以提高资源复用性
2. Thread对象无法抛出异常,只能进行捕获.Runnable与Callable类型对象可以抛出异常.Callable对象有返回值
3. 应选用Runnable或Callable

**错漏点**：
1. Thread本身是线程执行载体,它是一次性对象,执行完run()就结束,不能重复start;
而任务对象(Runnable/Callable)可以反复交给多个Thread/线程池去执行,**任务和线程分离,就是为了任务复用**.
2. 只有Callable能抛出异常有返回值.异常会被封装到FutureTask,等到get()时才抛出.Runnable不能抛出受检异常.
3. Thread对象无法抛出异常表述不准确: Thread只是执行载体,异常来源于任务的run/call,并不是Thread本身的异常.
4. 未提及三种启动方式.


### 题目2：Runnable 与 Callable 的互相转换及 FutureTask 的阻塞行为
> Runnable 可通过 `Executors.callable(Runnable task)` 转换为 Callable，此时调用 Future.get() 返回什么？若将该 Callable 提交给线程池，且任务执行过程中未调用 `set()` 方法，get() 会永远阻塞吗？请结合 FutureTask 源码说明“等待-通知”机制。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 转换返回值：`Executors.callable(Runnable task)` 返回的 Callable 在执行后返回 null（因其适配器内部固定返回 null）。
- 阻塞行为：FutureTask 内部维护一个 volatile int state（NEW -> COMPLETING -> NORMAL / EXCEPTIONAL / CANCELLED）。`get()` 方法会检查 state，若未完成则调用 `awaitDone()` 进入自旋或阻塞（通过 LockSupport.park()），直到任务执行完毕并调用 `finishCompletion()` 唤醒等待线程。任务执行过程中，无论是否显式调用 set()，FutureTask 的 run() 方法结束时会自动 set 结果或异常，因此不会永远阻塞（除非任务永远不执行完毕，如死循环）。
- 注意事项：若提交的任务是 Runnable 而非 Callable，`submit(Runnable)` 内部会用 `RunnableAdapter` 适配为 Callable，返回的 Future.get() 仍为 null，但任务执行仍会正常完成并唤醒阻塞线程。
</details>

**我的初答**：
**错漏点**：


### 题目3：调用 start() 与直接调用 run() 的底层行为差异（线程生命周期）
> 现有 `Thread t = new Thread(() -> System.out.println("run"))`。直接调用 `t.run()` 与 `t.start()` 有何本质区别？请从操作系统线程创建、JVM 栈分配、以及当前线程上下文三个角度解释。若重复调用 `start()` 两次，JVM 会如何处理？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 本质区别：
  - start()：JVM 调用底层 native 方法 `start0()`，向操作系统申请创建一个新线程，为该线程分配独立的栈空间（默认 1MB），将当前线程状态从 NEW 转为 RUNNABLE，并等待系统调度执行 run() 方法。
  - run()：当前线程（即调用者线程）直接执行 run() 方法体内的代码，不创建新线程，不分配新栈，相当于普通方法调用。
- 重复调用 start()：线程状态机规定，一个 Thread 对象只能被启动一次。第二次调用 start() 时，JVM 会检查 threadStatus 字段（非 0 表示已启动），直接抛出 IllegalThreadStateException。
- 追问：若 Thread 子类重写了 run() 方法，但误调用 start() 前先调用了 run()，后续 start() 会怎样？（提示：run() 执行后线程状态仍为 NEW，start() 依然会创建新线程并再次执行 run()，导致同一业务逻辑被执行两次）。
</details>

**我的初答**：
**错漏点**：

---
## Day 14 (2026-08-08) —— 线程状态、同步机制与死锁

### 题目1：线程的六种状态及其迁移路径（结合锁与阻塞 API）
> 请描述线程在 `synchronized` 阻塞、`Object.wait()`、`Thread.sleep(long)` 和 `Thread.join()` 状态下，分别对应 JVM 六种状态中的哪一种？`BLOCKED` 与 `WAITING` 在等待目标上有何本质区别？当线程被 `LockSupport.park()` 挂起时，其状态是 `WAITING` 还是 `TIMED_WAITING`？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 状态映射：
  - `synchronized` 等待锁：**BLOCKED**（处于 entry set 等待锁释放）。
  - `Object.wait()` / `Thread.join()` / `LockSupport.park()`：**WAITING**（等待另一个线程显式唤醒，无超时参数）。
  - `Thread.sleep(long)` / `wait(long timeout)` / `join(long millis)` / `LockSupport.parkNanos()`：**TIMED_WAITING**（有超时参数）。
- `BLOCKED` vs `WAITING`：
  - `BLOCKED`：等待**外部的互斥锁**（synchronized），锁释放后由 JVM 自动唤醒参与竞争，不依赖其他线程的 notify。
  - `WAITING`：等待**特定的信号**（notify / unpark / 线程终止），必须在代码层面显式调用对应唤醒操作。
- `LockSupport.park()` 状态：进入 **WAITING**（不带超时）或 `TIMED_WAITING`（带超时），且不会释放 `synchronized` 锁（与 `wait()` 不同）。
</details>

**我的初答**：
1. synchronized阻塞对应的是等待,Object.wait()对应的是就绪,Thread.sleep(long)对应的是超时等待,Thread.join()对应的也是就绪.
2. BLOCKED是拿不到锁就一直阻塞等待,WAITING则是拿不到锁自动放弃拿锁,执行其他代码.
3. LockSupport.park()不了解

**错漏点**：
1. 对于几种对应的线程状态搞混.正解请查看标准答案
2. 线程的6种状态分别为NEW,RUNNABLE,BLOCKED,WAITING,TIMED-WAITING,TERMINATED.
3. BLOCKED只针对synchronized监视锁,目标是获取锁,描述没有点名等待对象.
4. WAITING 理解错误：WAITING 状态不是拿不到锁、去执行其他代码，线程进入 WAITING 之后是不运行、不执行代码的；并不是放弃锁去跑别的逻辑，这个核心逻辑理解偏差很大.
5. LockSupport.park()无超时参数，线程状态为 WAITING；带时间的parkNanos/parkUntil才是TIMED_WAITING。


### 题目2：synchronized 与 ReentrantLock 的底层实现差异（锁升级 vs AQS 同步器）
> `synchronized`（基于监视器锁）和 `ReentrantLock`（基于 AbstractQueuedSynchronizer）在锁获取失败时的处理机制有何不同？请从以下维度对比：1）锁释放的自动性（是否必须在 finally 解锁）；2）等待可中断性；3）公平性实现；4）底层阻塞机制（JVM 内部锁升级 vs LockSupport.park/unpark）。追问：`synchronized` 在 JDK 1.6 之后的性能优势体现在哪里？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 锁释放：`synchronized` 自动释放（退出同步块或异常时由 JVM 指令 `monitorexit` 保证）；`ReentrantLock` 必须在 `finally` 中显式调用 `unlock()`，否则导致锁泄漏。
- 等待可中断性：`ReentrantLock.lockInterruptibly()` 支持在等待锁时响应中断；`synchronized` 等待锁时不可中断（阻塞状态不响应 `interrupt()`）。
- 公平性：`synchronized` 非公平；`ReentrantLock` 构造参数支持公平模式（但公平锁会降低吞吐量，因需检查队列头）。
- 底层阻塞：
  - `synchronized`：依赖 JVM 的 ObjectMonitor 机制，通过 CAS + 自旋 + 操作系统互斥量（`pthread_mutex`）实现锁升级（偏向 → 轻量 → 重量）。
  - `ReentrantLock`：依赖 AQS 的 volatile int state + FIFO 双端队列 + `LockSupport.park()` 挂起，挂起时调用 `Unsafe.park`（基于 `pthread_cond_wait`）。
- `synchronized` 性能优势：JDK 1.6 后引入偏向锁和轻量级锁，在低竞争场景下无需进入内核态，开销低于 `ReentrantLock`。高竞争场景两者相近，但 `ReentrantLock` 提供更灵活的 tryLock 超时和条件队列。
</details>

**我的初答**：
1. synchronized在执行完临界区的代码后会自动释放锁.ReentrantLock必须在finally中手动unlock()释放锁.
2. 基于监视器锁的等待无法被中断,而ReentrantLock等待是可以被中断的.
3. synchronized锁是非公平锁,ReentrantLock可以设置为公平锁或非公平锁
4. synchronized锁会随着竞争的激烈程度逐渐升级,ReentrantLock不了解
5. Java在JDK1.6的时候对synchronized进行了性能优化

**错漏点**：
1. synchronized 由编译器自动插入 monitorenter/monitorexit 指令，不仅正常退出同步块会释放锁，临界区抛出异常时也会自动释放
2. 等待可中断性
- 表述不准确，存在明显漏洞：
- synchronized：线程处于 BLOCKED 状态抢锁时，确实不可响应中断，只能阻塞直到抢到锁，中断标记会在抢到锁之后才被处理。
- ReentrantLock：不是所有等待场景都可中断。普通 lock() 方法和 synchronized 一样，等待过程不可中断；只有调用 lockInterruptibly() 时，等待过程中才能响应 InterruptedException 并主动放弃抢锁。你没有区分这两种 API，结论过于绝对。
3. ReentrantLock 默认非公平（性能更高），可通过构造参数 true 开启公平锁，严格按照队列的 FIFO 顺序分配锁。你答到了核心差异，方向正确。
4. 底层阻塞机制
-   只答对了 synchronized 的一半，ReentrantLock 部分完全缺失：
-   synchronized：JDK 1.6 后引入锁升级机制（无锁→偏向锁→轻量级锁→重量级锁）。轻量级锁阶段通过自旋等待，不阻塞线程；只有升级到重量级锁，才会通过操作系统内核态挂起线程。你只提到 “锁升级”，但没有说明阻塞机制的分层逻辑。
-   ReentrantLock：基于 AQS 同步队列，抢锁失败时将线程封装为节点加入队列尾部，直接调用 LockSupport.park() 挂起线程；释放锁时调用 LockSupport.unpark() 唤醒后继节点。全程不依赖监视器锁，也没有锁升级过程。
- 6.追问：JDK 1.6 后 synchronized 的性能优势
-  核心优势是低竞争场景下开销极低，通过一系列锁优化减少了重量级锁的内核态切换开销：
-  **锁升级机制**：大部分无竞争 / 轻竞争场景停留在偏向锁、轻量级锁阶段，避免内核态线程切换的高昂成本。
-  **适应性自旋**：轻量级锁阶段根据历史自旋成功率动态调整自旋次数，减少无效空转，兼顾性能与 CPU 开销。
-  **锁消除**：JIT 编译时消除不可能存在共享竞争的锁，减少无意义加锁。
-  **锁粗化**：将相邻的多次加锁解锁合并为一次，降低频繁加锁的开销。

### 题目3：死锁的代码触发、定位与预防（jstack 分析 + 循环等待破坏）
> 写出两个线程各持有对方所需锁的经典死锁代码。当程序卡死后，使用 `jstack <PID>` 的输出中，哪些关键字能明确提示死锁？死锁产生的四个必要条件中，`ReentrantLock` 的 `tryLock(long timeout, TimeUnit)` 破坏了哪个条件？`synchronized` 无法超时等待，应如何预防死锁？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 死锁代码模板：
  Object lockA = new Object(), lockB = new Object();
  new Thread(() -> { synchronized(lockA) { sleep(100); synchronized(lockB) {} } }).start();
  new Thread(() -> { synchronized(lockB) { sleep(100); synchronized(lockA) {} } }).start();
- jstack 定位：输出中明确包含 `"Found one Java-level deadlock"`，并列出两个线程各自持有的锁（`waiting to lock` vs `locked`）及其 ID。
- 破坏条件：`tryLock` 实现了**超时放弃等待**，破坏了“不可抢占”条件（即便持有锁，超时后主动释放已占资源）。
- `synchronized` 预防策略：强制所有线程以**固定全局顺序**获取锁（如按 System.identityHashCode 排序），破坏“循环等待”条件。
</details>

**我的初答**：
**错漏点**：

---
## Day 15 (2026-08-09) —— 等待唤醒机制与阻塞队列

### 题目1：wait/notify 的监视器锁契约与虚假唤醒（Spurious Wakeup）
> `Object.wait()` 被调用时，为什么必须持有该对象的监视器锁（即必须在 synchronized 块内）？`notify()` 调用后，被唤醒的线程是立即执行，还是需要重新竞争锁？请说明使用 `if (condition)` 而非 `while (condition)` 包裹 `wait()` 在高并发下的风险。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 锁契约根源：`wait()` 的内部操作顺序是“释放当前持有的监视器锁 → 线程进入等待集（WaitSet） → 挂起”。若未持有锁，则无法释放，直接抛出 `IllegalMonitorStateException`。这是 JVM 层面强制保证 wait/notify 必须基于共享资源锁进行互斥通信。
- 唤醒后的执行路径：`notify()` 仅将目标线程从 WaitSet 移入同步队列（EntrySet），线程不会立即执行。它必须重新竞争该监视器锁，成功获取锁后，才从 `wait()` 调用处恢复执行。
- `if` 的风险（虚假唤醒）：操作系统底层 `pthread_cond_wait` 可能在没有 `notify` 信号时返回（如信号中断），或 `notifyAll` 唤醒后条件状态已被其他线程改变。使用 `if` 会导致线程在条件不满足时直接执行后续逻辑，引发数据错乱。必须使用 `while (condition)` 在每次恢复后重新校验条件。
</details>

**我的初答**：
1. Object.wait()被调用时,Object必须持有对象的monitor监视器,才能将其放入对象内部维护的Wait Set.
2. notify()调用后,被唤醒的线程被Wait Set移动到Entry Set重新与其他线程竞争同一把锁.
3. if(condition)在高并发情况下可能出现Spurious Wakeup假唤醒的情况,执行了代码,而线程并没有被唤醒

**错漏点**：
1. 明显不足：只描述了 “要放入 Wait Set 所以要持有锁” 的现象，没有回答设计层面的本质原因。
   强制在 synchronized 内调用 wait ()，核心目的是保证 **「条件检查 + 进入等待」两个操作的原子性** ，避免竞态条件：如果不持有锁，可能在判断条件为真、还没调用 wait () 的间隙，其他线程修改了条件并发出 notify，导致这次唤醒信号永久丢失，线程会一直阻塞等待。
   同时你也没有补充：wait () 被调用后，会**主动释放当前持有的监视器锁**，让其他线程可以进入同步块修改条件。
2. 细节缺失：notify () 调用本身不会立即释放锁，必须等调用 notify 的线程退出同步块、释放监视器锁之后，被唤醒的线程才会正式参与锁竞争；只有成功抢到锁，线程才会从 wait () 方法返回，继续执行后续代码。
3. if包裹wait()的风险
- 存在核心概念错误: 你表述的"执行了代码,而线程并没有被唤醒"完全颠倒了虚假唤醒的定义.
- 虚假唤醒(Spurious Wakeup)指的是: **线程没有收到notify/notifyAll信号,却自动从wait()中返回(醒过来了)**, 此时等待的业务条件往往仍然不满足.
- 逻辑漏洞: 如果`if(condition)`包裹wait(),线程被虚假唤醒后不会进行二次校验条件,会直接向下执行业务逻辑,导致在条件不成立的情况下操作共享数据,引发并发错误.
- 场景遗漏: 除了虚假唤醒,`notifyAll()`批量唤醒多个线程时,第一个抢到锁的线程会修改条件;后续抢到锁的线程面对的已经时不满足的条件,if判断同样会导致错误执行,这也是必须用while的核心场景之一.


### 题目2：Thread.sleep() 与 Object.wait() 在锁释放与 CPU 占用上的底层差异
> 两者均可使线程暂停，但 `sleep()` 不释放锁，`wait()` 释放锁。请从操作系统线程状态（RUNNABLE vs WAITING）和 JVM 调度策略说明：为何 `sleep(1000)` 在竞争锁时会阻塞其他线程？`wait(1000)` 超时后是否需要重新竞争锁？若线程在 `wait` 等待期间被 `interrupt()`，锁的状态会发生什么变化？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 锁保持差异：
  - `sleep()`：监视器锁（Monitor）仍归属于当前线程，其他线程无法进入同步块，处于 `BLOCKED` 状态。线程本身在 JVM 层面进入 `TIMED_WAITING`，但底层 OS 线程可能仍保持 RUNNABLE（取决于 JVM 挂起实现）。
  - `wait()`：主动释放监视器锁，使其他线程能获取锁并修改共享条件。自身进入 `WAITING` 状态并让出 CPU。
- 超时唤醒机制：
  - `wait(1000)` 超时后，JVM 自动将其移出 WaitSet，但**不会自动获取锁**。线程必须进入 EntrySet 重新竞争锁，成功获取后才能返回。若锁被其他线程持有，即使超时也要阻塞等待锁释放。
- `interrupt()` 触发：若线程在 `wait()` 中因 `interrupt()` 被唤醒，会先恢复竞争锁，持有锁后再抛出 `InterruptedException`（即先 regain lock，再 throw）。
</details>

**我的初答**：
1. (巨大错误)sleep(1000)时sleep()不释放锁,线程一直处于RUNNABLE状态,占用CPU性能,其他线程无法拿到锁,只能进入EntryList处于WAITING状态阻塞等待.
2. wait(1000)等待时不消耗CPU性能,超时后重新进入EntryList与其他线程共同竞争锁
3. wait()等待期间被interrupt(),直接与其他线程竞争锁

**错漏点**：
1. sleep (1000) 阻塞其他线程的原因
-   存在三处核心错误：
- 线程状态错误：Thread.sleep(1000) 执行时，JVM 线程状态是 TIMED_WAITING，不是 RUNNABLE。RUNNABLE 代表线程可运行或正在运行，sleep 会主动让出 CPU 调度权，进入超时等待状态。
- CPU 占用理解错误：sleep () 会主动释放 CPU 时间片，操作系统将线程挂起，期间不占用 CPU 资源，并非 “一直占用 CPU 性能”。sleep 阻塞其他线程的根本原因是它持有监视器锁不释放，而非占着 CPU 不放。
- 其他线程状态错误：竞争 synchronized 锁失败的线程处于 BLOCKED 状态（对应 EntryList 队列），不是 WAITING。WAITING 是无超时的无限等待状态（如无参 wait、join），BLOCKED 才是锁竞争失败的专属状态。
2. 核心结论正确
3. 缺失的关键细节：只有成功获取到锁之后，线程才会从 wait () 方法处抛出 InterruptedException，同时清除中断标记；如果没抢到锁，会继续在锁队列中阻塞等待。
- 锁状态完整变化：调用 wait () 时锁已经被释放；中断唤醒瞬间线程不持有锁；抢到锁后重新持有锁，随后抛出中断异常。


### 题目3：BlockingQueue 的内部条件队列实现（Condition）及与 wait/notify 的等价关系
> `ArrayBlockingQueue` 内部使用 `ReentrantLock` 和 `Condition`（`notEmpty` 和 `notFull`）实现等待唤醒。相较于手写 `wait/notify`，使用 `Condition` 的优势是什么？请简述 `put(E e)` 在队列满时的处理流程，并指出若 `await()` 捕获到中断信号，队列内部状态如何保证一致性？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- `Condition` 优势：
  - 多条件队列分离：`notFull` 和 `notEmpty` 各自独立，唤醒时精准定位（如 `signal()` 仅唤醒 `notEmpty` 上的阻塞线程），避免了 `notifyAll` 唤醒无关线程导致的性能损耗和过度竞争。
  - 可中断与超时集成：提供 `awaitNanos` 和 `awaitUntil`，且支持 `lockInterruptibly`。
- `put(E e)` 核心流程（`ArrayBlockingQueue` 源码逻辑）：
  1. 获取可中断锁 `lock.lockInterruptibly()`。
  2. `while (count == items.length) notFull.await();`（防止虚假唤醒）。
  3. 执行 `enqueue(e)`（底层直接将元素放入数组索引）。
  4. 递增 `count`，调用 `notEmpty.signal()` 唤醒等待取值的消费者。
  5. 确保 `finally { lock.unlock(); }`。
- 中断一致性：若 `await()` 抛出 `InterruptedException`，JVM 会确保恢复中断状态并抛出异常，**但锁会在抛出异常前自动释放**。队列内部尚未入队，数据保持一致性，调用者需自行处理中断。
</details>

**我的初答**：
**错漏点**：

---
## Day 16 (2026-08-10) —— 线程池基础与创建

### 题目1：ThreadPoolExecutor 的核心参数与任务提交的完整执行流程（饥饿与拒绝策略）
> 请详细解释 `ThreadPoolExecutor` 的 7 个核心构造参数，并说明当 `execute()` 提交一个任务时，线程池内部的完整执行顺序（从核心线程到队列再到最大线程）。追问：当使用无界队列（如 `LinkedBlockingQueue`）时，`maximumPoolSize` 和 `keepAliveTime` 参数会失效吗？为什么？如果核心线程数设置为 0，会发生什么情况？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 核心参数：
  1. corePoolSize：核心线程数，即使空闲也会保留（除非设置 allowCoreThreadTimeOut）。
  2. maximumPoolSize：最大线程数，当队列满时允许创建的最大额外线程数。
  3. keepAliveTime：非核心线程（或超时核心线程）的空闲存活时间。
  4. unit：keepAliveTime 的时间单位。
  5. workQueue：阻塞队列，用于存放等待执行的任务。
  6. threadFactory：线程工厂，用于创建新线程（可自定义名称、优先级等）。
  7. handler：拒绝策略，当线程池和队列都满时触发。
- 任务提交完整流程（4步判断）：
  1. 当前线程数 < corePoolSize：直接创建新核心线程执行任务。
  2. 当前线程数 >= corePoolSize：尝试将任务放入 workQueue 阻塞队列。
  3. 如果队列已满（有界队列）：创建非核心线程执行任务（直到达到 maximumPoolSize）。
  4. 如果当前线程数 >= maximumPoolSize 且队列已满：触发拒绝策略（默认 AbortPolicy 抛异常）。
- 无界队列陷阱：`LinkedBlockingQueue` 默认容量为 `Integer.MAX_VALUE`，任务永远堆积在队列中，永远不会触发创建非核心线程，导致 `maximumPoolSize` 和 `keepAliveTime` 参数完全失效。此时若任务生产速度远大于消费速度，将导致内存溢出（OOM）。
- 核心线程数为 0 的行为：当 corePoolSize = 0 时，任务会直接进入队列，若队列为空且无核心线程，则创建非核心线程执行任务（类似 `newCachedThreadPool` 的部分行为）。
</details>

**我的初答**：
1. ThreadPoolExecutor的七个核心构造参数corePoolSize核心线程数,maximumPoolSize最大线程数,keepAliveTime临时线程在空闲多久后释放,TimeUnit设置空闲释放的时间单位,BlockingQueue阻塞队列,ThreadFactor线程工厂,拒绝策略.
2. execute()提交任务时会先查看核心线程满了没,如果满了,进入阻塞队列阻塞等待,如果阻塞队列也满了,创建临时线程执行,如果临时线程也已达上线,触发拒绝策略.
3. 使用无界队列时maximumPoolSize和keepAliveTime参数不会失效,因为无界队列也可指定大小.
4. 设置核心线程数为0,会抛出异常

**错漏点**：
1. 七个核心构造参数存在问题：

   拼写错误：正确名称是 ThreadFactory（线程工厂），不是 ThreadFactor。
   
   仅罗列名词，缺少核心作用解释，比如未说明线程工厂可自定义线程命名 / 优先级、阻塞队列用于缓存等待任务、拒绝策略是池和队列均满时的兜底处理逻辑。
2. 存在问题：
   
   表述不严谨：不是 “核心线程满了没”，判断依据是当前工作线程总数是否小于 corePoolSize；即使有空闲核心线程，只要总数未达到核心数，提交新任务仍会新建核心线程。
   
   细节缺失：新建的线程会直接执行当前提交的任务，而非先从队列取；核心线程默认永久存活，不会因空闲自动销毁（需手动开启 allowCoreThreadTimeOut）。
   
   术语不规范：“临时线程” 标准称呼为非核心线程 / 救急线程。
3. 无界队列下参数是否失效

   结论与原因均完全错误：
   
   概念混淆：指定了固定容量的是有界队列；无界队列（如无参构造的 LinkedBlockingQueue）容量为 Integer.MAX_VALUE，本质上永远不会被放满。
   
   maximumPoolSize 会失效：队列永远不会满，永远走不到 “队列满才创建非核心线程” 的逻辑，线程数最多维持在核心线程数，最大线程数形同虚设。
   
   keepAliveTime 会失效：该参数作用是回收超过核心数的空闲非核心线程，既然不会创建非核心线程，参数自然无法生效；未开启核心线程超时时，核心线程永不回收，参数完全无用。
4. 核心线程数设为 0 的结果
      
   结论完全错误：corePoolSize = 0 是 ThreadPoolExecutor 的合法配置，不会抛出异常。
- 正确行为是：
      
  提交任务时，当前线程数不小于核心数（0），会直接尝试将任务放入阻塞队列。
      
  若队列入队失败（如 SynchronousQueue 同步队列），会判断是否小于最大线程数，创建非核心线程执行 —— 这正是 Executors.newCachedThreadPool 的实现原理。
  
  若配合无界队列使用，队列永远不会满，则永远不会创建工作线程，任务会持续堆积在队列中无法执行，属于业务坑，但不会触发异常。

### 题目2：Executors 工厂方法的底层隐患（无界队列与最大线程数滥用）
> 使用 `Executors.newFixedThreadPool(10)` 和 `Executors.newCachedThreadPool()` 分别存在什么内存风险？请从底层构造参数（队列类型和 maximumPoolSize）分析为什么阿里巴巴开发规范禁止使用 `Executors` 创建线程池，并要求手动创建 `ThreadPoolExecutor`。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- newFixedThreadPool(10)：底层使用 `LinkedBlockingQueue`（无界队列），最大线程数等于核心线程数（即 maximumPoolSize = 10）。在高并发下，任务堆积在无界队列中，持续占用内存，可能耗尽堆空间并触发 OOM（OutOfMemoryError）。
- newCachedThreadPool()：底层使用 `SynchronousQueue`（不存储任务的队列），核心线程数为 0，最大线程数为 `Integer.MAX_VALUE`。每个提交的任务都会直接创建新线程，若任务执行速度慢于提交速度，系统将创建大量线程，导致操作系统资源耗尽（无法创建本地线程）或触发 `OutOfMemoryError: unable to create new native thread`。
- 规范依据：阿里巴巴 Java 开发手册明确禁止使用 `Executors` 创建线程池，因其参数设置不可控。强制要求使用 `new ThreadPoolExecutor` 并显式指定有界队列和合理的最大线程数。
</details>

**我的初答**：
**错漏点**：


### 题目3：shutdown() 与 shutdownNow() 的状态迁移及未执行任务的处理策略
> `ThreadPoolExecutor.shutdown()` 和 `shutdownNow()` 分别将线程池状态切换为哪个状态？调用后，对阻塞队列中尚未执行的任务分别如何处理？`awaitTermination(long timeout, TimeUnit unit)` 在优雅关闭中起什么作用？若在调用 `shutdown()` 后立即提交新任务，会发生什么？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 状态迁移：
  - `shutdown()`：将状态切换为 **SHUTDOWN**（不再接收新任务，但会执行完队列中已存在的任务）。
  - `shutdownNow()`：将状态切换为 **STOP**（不接收新任务，停止执行所有正在运行的任务，并中断工作线程，返回队列中尚未执行的任务列表）。
- 未执行任务处理：
  - `shutdown()`：队列中的任务会继续执行完毕（线程会从队列中正常 poll/take 任务）。
  - `shutdownNow()`：直接返回队列中未执行的任务（`List<Runnable>`），并中断所有工作线程（`Thread.interrupt()`），但不保证任务能立即停止（需任务支持中断）。
- `awaitTermination` 作用：阻塞当前线程，等待线程池状态变为 TERMINATED（所有任务已完成，线程已回收）。通常用于优雅关闭时等待任务完成，若超时则返回 false，需配合重试或强制结束策略。
- 提交新任务：若调用 `shutdown()` 后调用 `execute()`，线程池会立即执行拒绝策略（默认抛 `RejectedExecutionException`），因为状态检查在任务接收入口处强制校验。
</details>

**我的初答**：
**错漏点**：

---
## Day 17 (2026-08-11) —— 网络编程基础（UDP/TCP/IP地址）

### 题目1：Java 中 UDP 与 TCP 编程的核心差异（协议特性与 API 实现）
> 请从以下四个维度对比 Java 中 UDP（DatagramSocket）和 TCP（Socket/ServerSocket）的编程模型：1）数据传输的可靠性；2）是否面向连接；3）数据收发单位（数据报 vs 流）；4）对端不可达时的异常反馈。追问：在 TCP 编程中，`Socket.getOutputStream().write(data)` 将数据写入流后，是否意味着数据已成功到达接收端？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- UDP（无连接）：
  1. 可靠性：尽最大努力交付（best-effort），可能丢包、乱序，无 ACK 确认。
  2. 面向连接：无，发送前无需建立连接。
  3. 数据单位：DatagramPacket（数据报），以包为单位发送，有大小限制（64KB）。
  4. 异常反馈：对端不可达时，默认情况下发送端无任何反馈（需使用 connect() 方法绑定远端后，部分系统会抛出 PortUnreachableException）。
- TCP（面向连接）：
  1. 可靠性：可靠，通过三次握手建立连接，ACK 确认、超时重传、滑动窗口保障数据有序到达。
  2. 面向连接：是，必须建立连接后才能通信。
  3. 数据单位：流（Stream），无包边界，通过 InputStream/OutputStream 读写。
  4. 异常反馈：对端不可达或连接断开时，读写操作会抛出 IOException（如 SocketException: Connection reset）。
- 追问陷阱：`write(data)` 仅表示数据已写入本地 TCP 发送缓冲区，成功返回不代表对端已收到数据。若网络中断，数据可能在发送缓冲区中滞留，最终丢失。需依赖应用层 ACK 确认业务数据的完整到达。
</details>

**我的初答**：
1. UDP是面向无连接的,数据传输不可靠,数据收发单位为数据报,发出数据不管对端是否收到都无反馈
2. TCP是面向连接的,数据传输可靠,通过流进行数据收发,对端不可达时收到反馈会重新发送.
3. 不意味,需要等到接收端反馈后才能代表已经接收到了数据

**错漏点**：
- 1.UDP协议方面存在不足：
-   对端不可达的异常反馈描述不完整。UDP 是无连接协议，调用DatagramSocket.send()时，即使对端 IP 不存在、端口未监听，绝大多数情况下也不会立刻抛出异常 —— 底层不会同步返回可达性结果，应用层感知不到投递失败；仅在极少数场景下会收到 ICMP 不可达报文，Java 也不会在 send 时同步抛出，常表现为后续 receive 时间接触发异常。
-   未对应题目要求的 Java API 层面：UDP 编程依赖DatagramSocket作为套接字、DatagramPacket作为数据载体，每个数据包独立携带目标地址，你没有关联到具体 API 实现。
- 2.TCP 四个维度描述存在错误与不足：
 - 对端不可达的异常反馈理解有误。“收到反馈会重新发送” 的表述不对：TCP 的重传是超时未收到对端的 ACK 确认就自动重传，不是 “收到不可达反馈才重发”；如果对端真的持续不可达，TCP 会在多次重传、超时到期后，才向应用层抛出异常，并非立刻就能感知并反馈。
 - 异常场景未区分：连接建立阶段（Socket.connect()）对端不可达，会快速抛出ConnectException；但连接建立后网络断开 / 对端宕机，应用层无法立刻感知，要等重传超时后才会收到IOException。
 - 未对应 Java API：TCP 编程基于ServerSocket监听连接、Socket代表双向连接，通过InputStream/OutputStream字节流传输，数据没有明确的包边界。
 -  3.Socket.getOutputStream().write(data)只是把数据拷贝到操作系统内核的 TCP 发送缓冲区，并没有直接发送到网络；后续由操作系统内核负责分片、发送、维护 ACK 确认。即使内核收到了 TCP 的 ACK，也仅代表对端操作系统收到了数据，不代表对端应用程序已经读取并处理了数据。

### 题目2：ServerSocket.accept() 的阻塞特性与多客户端并发处理模型
> `ServerSocket.accept()` 方法的阻塞行为是什么？当该方法的调用线程被阻塞时，其他连接请求会如何处理（是否存在连接队列）？请写出一个支持多客户端并发访问的简单服务器伪代码，并说明为何通常将 `accept()` 获得的 Socket 交给新线程处理。如果服务端使用单线程循环处理，会出现什么弊端？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 阻塞特性：`accept()` 会阻塞当前线程，直到有一个新的客户端连接成功建立，返回一个 Socket 对象（代表服务端与客户端的连接通道）。
- 连接队列机制：当有多个客户端同时发起连接时，操作系统内核会维护一个半连接队列和全连接队列（已完成三次握手）。`accept()` 只是从全连接队列中弹出头部 Socket，未弹出的连接会在内核中排队，队列长度由 `ServerSocket` 构造函数的 `backlog` 参数控制（默认 50）。当队列满时，新连接请求将被拒绝。
- 多线程/线程池伪代码：
  try (ServerSocket server = new ServerSocket(8080)) {
  ExecutorService pool = Executors.newFixedThreadPool(10);
  while (true) {
  Socket socket = server.accept();
  pool.submit(() -> handle(socket)); // 独立线程处理读写，主线程继续接受新连接
  }
  }
- 单线程弊端：如果单线程循环，处理某个客户端的长时读写操作会阻塞主线程，导致其他客户端的连接请求无法被及时接受，严重影响并发性能。
</details>

**我的初答**：
1. accpet()会阻塞当前的线程,直到有一个新的客户端连接成功建立
2. 当有多个客户端同时发起连接请求时,操作系统内部会维护一个半连接队列和一个全连接队列.Socket只是从全连接队列弹出头部连接.连接队列的长度由ServerSocket()的构造函数backlog确定,默认长度为50.
   当连接请求数量超过队列长度,就会拒绝连接.
3.
ServerSocket server = new ServerSocket(10086);
ExecutorsService pool = Executors.newFixedThreadPool(10);
while(true){
Socket socket = server.accept();
pool.submit(() -> handle(socket));
}
- 如果服务器通过单线程循环处理,处理所消耗的时间会大大降低处理效率.

**错漏点**：
- 补充细节：accept()本身只从全连接队列取已完成三次握手的连接；队列无就绪连接，线程阻塞，不占用 CPU。
- 错漏点：
  
  1.backlog参数对应的是全连接队列的最大容量，半连接队列大小由操作系统内核参数控制，不受 backlog 影响。
 
  2.Java 中ServerSocket无参构造默认 backlog 不是 50，JDK 默认值是50这个是旧印象，无参构造实际默认值为 50，但不同 JDK 版本略有差异；带参构造才可以手动指定 backlog。
  
  3.队列满的行为：全连接队列满，新完成握手的连接会被丢弃；半连接队列满，客户端 SYN 包直接丢弃，不一定直接返回拒绝，客户端会重试。表述 “就会拒绝连接” 不够严谨。
  
  4.accept()做的动作：取出全连接队列队首已经完成三次握手的 Socket，不是从操作系统随便拿连接。

- 单线程模型：accept()→处理当前 socketIO 业务→再回到 accept ()。
  
  在处理某个客户端 IO 时，accept 无法执行，不能接收新客户端连接；其他客户端只能排队等待全连接队列，队列打满直接丢失连接。不是单纯 “慢”，是同一时刻只能服务一个客户端，其他客户端无法建立连接。

- 为什么 Socket 要交给新线程处理
-  accept()所在线程主要职责是接收新连接；
  客户端业务读写 IO 是阻塞操作。如果在 accept 线程直接处理 socket 读写，该线程就无法回到 accept 接收下一个客户端。
  把 socket 交给独立线程 / 线程池，主线程可以立刻回到accept()，持续接收新客户端连接，实现多客户端同时服务。


### 题目3：InetAddress 地址解析的缓存机制与 UnknownHostException
> `InetAddress.getByName(String host)` 和 `InetAddress.getLocalHost()` 分别用于获取 IP 地址。请解释 DNS 解析结果在 JVM 中的缓存行为（正面缓存与负面缓存），并说明在服务端网络编程中，若依赖域名连接外部服务，缓存可能导致什么问题？当主机名无法解析时，这两个方法分别抛出什么异常？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 缓存机制：
  - 正面缓存（成功解析）：缓存时间由 JVM 的 `networkaddress.cache.ttl` 属性控制（默认 30 秒）。
  - 负面缓存（解析失败）：缓存时间由 `networkaddress.cache.negative.ttl` 控制（默认 10 秒）。
- 对服务端的影响：若连接外部服务（如数据库）使用域名，JVM 缓存可能导致 IP 变更时无法感知，引发连接失败。解决方案：调整 `networkaddress.cache.ttl` 为 0（禁用缓存）或 使用 `InetAddress.getByName` 时刷新 DNS 缓存（需调用 `SecurityManager` 权限）。
- 异常抛出：
  - `getByName(host)`：主机名无法解析时抛出 `UnknownHostException`（受检异常）。
  - `getLocalHost()`：若本机 IP 地址无法解析（如 /etc/hosts 配置错误），抛出 `UnknownHostException`。
</details>

**我的初答**：
1. 完全不知道

**错漏点**：

---

## Day 18 (2026-08-12) —— 网络编程收尾（粘包、超时、UDP进阶）

### 题目1：TCP 粘包/拆包现象、根源与业界解决方案
> 在 Java 原生 Socket 编程中，使用 InputStream 读取 TCP 数据时，为什么经常出现“粘包”或“拆包”现象？请从 TCP 流协议特性与 Nagle 算法两个角度解释根本原因。在应用层解决该问题的三种常见方案（定长、分隔符、长度字段）中，哪一种最通用且安全？请结合 ByteBuffer 或自定义协议简述实现思路。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 现象：粘包（多个独立数据包被合并读）、拆包（一个数据包被分两次读）。
- 根源：
  1. TCP 是面向流的协议，无包边界，接收端仅看到字节流，不感知发送端的 write 次数。
  2. Nagle 算法（默认开启）：将多个小包合并发送，减少网络开销，加剧粘包。
  3. 接收端缓冲区大小与发送端窗口协商导致拆包。
- 业界方案对比：
  1. 定长法（固定长度）：简单，但浪费空间，且长度不合适时处理复杂。
  2. 分隔符法（如换行符）：适合文本协议（如 HTTP），但二进制数据需转义，效率低。
  3. 长度字段法（LengthField）：最通用安全，在包头固定位置写入包体长度，解析时先读长度，再读指定字节。
- 实现（长度字段法）：
  写入时：ByteBuffer.allocate(4 + data.length).putInt(data.length).put(data)
  读取时：先读 4 字节转 int len，再循环读 len 字节直至读完。
</details>

**我的初答**：
**错漏点**：


### 题目2：Socket 的连接超时与读取超时设置，以及 close() 与 shutdownOutput() 的区别
> 在服务端编程中，`Socket.setSoTimeout(int timeout)` 和 `Socket.connect(SocketAddress endpoint, int timeout)` 分别控制什么行为？若未设置，默认行为是什么？追问：调用 `socket.close()` 与 `socket.shutdownOutput()` 在 TCP 连接释放上有何本质区别？如果只调用 `shutdownOutput()`，对端 `read()` 会返回什么？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 连接超时（connect(timeout)）：控制三次握手的最大等待时间，超时抛出 `SocketTimeoutException`。默认 Infinite（无限等待，依赖 OS 超时）。
- 读取超时（setSoTimeout）：控制 `read()` 操作阻塞的最大时间，超时抛出 `SocketTimeoutException`（但连接未断开）。默认 0（无限阻塞）。
- close() 与 shutdownOutput() 区别：
  - close()：立即关闭整个连接（双向），释放本地端口，丢弃输出缓冲区的未发送数据。
  - shutdownOutput()：仅关闭输出方向，发送 FIN 包告诉对端“不再发送数据”，但输入方向保留，可继续读取对端数据。对端 `read()` 在读取完已有数据后返回 -1（EOF）。
- 典型用法：半关闭场景，如 HTTP/1.1 客户端发送请求后调用 `shutdownOutput()`，等待服务端响应数据，读完后关闭输入。
</details>

**我的初答**：
**错漏点**：


### 题目3：UDP 的 connect() 方法与 DatagramPacket 复用（端口不可达检测）
> UDP 是无连接协议，但 Java 的 `DatagramSocket.connect(InetAddress address, int port)` 提供了“连接”方法。请问该方法实际做了哪些事情？它是否进行了三次握手？调用后，若对端不存在或未开启端口，调用 `send(packet)` 会立即收到异常吗？如何复用同一个 `DatagramPacket` 对象发送不同数据到同一目标？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- `connect()` 实际行为：
  1. 锁定目标地址和端口（不发送任何网络包，无三次握手）。
  2. 过滤掉非目标地址的接收数据报（只接收符合 connected 地址的包）。
  3. 允许在调用 `send` 时省略地址（直接使用绑定的目标）。
  4. 某些系统上，如果对端不可达，后续 `send` 会抛出 `PortUnreachableException`（ICMP 端口不可达消息）。
- 是否握手：否，仅本地状态变更，无网络交互。
- 异常反馈：`send` 本身不会阻塞等待 ACK，UDP 无确认。但后续 `receive` 或再次 `send` 时，若收到 ICMP 错误（异步），会抛出 `PortUnreachableException`。
- 复用 DatagramPacket：
  每次发送前调用 `packet.setData(data, offset, length)` 重置数据，并调用 `packet.setSocketAddress(target)` 重置目标（若未 connect 或需变更目标）。注意：原 packet 内部引用持有的旧 byte[] 会被替换。
</details>

**我的初答**：
**错漏点**：

---

## Day 19 (2026-08-14) —— 反射核心机制与API

### 题目1：获取 Class 对象的三种方式及其底层触发差异（类加载 vs 实例创建）
> 在 Java 中获取 `Class` 对象的三种方式分别是什么（`Class.forName`、`类名.class`、`实例.getClass()`）？它们在类加载阶段的行为有何本质不同？追问：使用 `Class.forName("com.mysql.jdbc.Driver")` 时，为什么会导致该类的静态代码块自动执行，而 `Driver.class` 却不会？若类路径中不存在该类，前两种方式分别抛出什么异常？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 三种方式及其行为：
  1. `Class.forName(String className)`：显式触发目标类的**完整类加载过程**（加载、连接、初始化），其中初始化阶段会执行静态代码块和静态变量赋值。适用于需要确保类被初始化的场景（如 JDBC 驱动注册）。
  2. `类名.class`（如 `Student.class`）：仅触发**类加载阶段**，不触发初始化（静态代码块不会执行）。适用于仅需获取元数据而不希望触发静态副作用。
  3. `实例.getClass()`：必须在对象已经创建后调用，返回该对象运行时类的 Class 实例（包括子类类型）。适用于运行时获取实际类型（如多态场景）。
- 异常对比：
  - `Class.forName`：抛 `ClassNotFoundException`（受检异常，需显式 try-catch）。
  - `类名.class`：若类不存在，编译期直接报错（无法通过编译），不会抛运行时异常。
- 追问深挖：JDBC 驱动规范利用 `Class.forName` 的执行静态初始化块注册 DriverManager 的核心机制，而 `Driver.class` 不会触发注册，因此无法建立连接。
</details>

**我的初答**：
1. Class.forName()在类加载阶段会触发连接和静态代码块而类名.class只会触发连接,不会触发静态代码块.实例.getClass()只有存在实例时调用,此时类已初始过.
2. 不知道为什么Class.forName()会导致静态代码块执行
3. 第一种会抛出ClassNotFoundException.

**错漏点**：
1. 类加载的完整流程分为 `加载 → 链接（验证 / 准备 / 解析） → 初始化` 三个核心阶段，三种方式触发的阶段深度不同，这是它们的本质区别。
2. 类名.class(类字面常量)只会触发**加载和链接**,并不会触发初始化阶段
-  编译期就确定 Class 对象的引用，运行时只完成类的加载、验证与准备，不会执行静态代码块、静态变量显式赋值等初始化逻辑；只有后续真正访问类的静态成员、创建实例时，才会触发初始化。
3. 实例.getClass 不触发任何类加载阶段.
- 能调用该方法说明对象已经实例化完成，对应类早已走完加载、链接、初始化全流程。该方法仅返回对象运行时对应的 Class 对象，不会重复执行类加载。
4. Class.forName() 触发完整的类加载流程
- 运行期动态加载类，默认参数 initialize = true，会完整执行类的初始化逻辑，包括静态代码块、静态变量的显式赋值。
  它存在重载方法 Class.forName(name, initialize, classLoader)，可手动指定是否触发初始化。
- 为什么 Class.forName 会执行静态代码块，类名.class 不会？
     
  核心原因是是否触发类的初始化阶段：
  
  Class.forName("com.mysql.jdbc.Driver") 默认开启初始化，会执行类的 <clinit>() 方法，也就是静态代码块、静态成员的显式赋值逻辑。JDBC 旧版本正是利用这个特性，让 Driver 类的静态块自动执行 DriverManager.registerDriver(new Driver())，完成驱动注册。

  Driver.class 仅触发加载和链接阶段，不会进入初始化步骤，因此静态代码块不会被执行。
- 类名.class出现的异常：
-  编译期：如果类不存在，直接编译失败，无法生成字节码文件；
-  运行期：若编译时类存在、但运行时类路径丢失了该类，会抛出 NoClassDefFoundError，属于 Error 级别的系统错误，是类链接阶段找不到类定义导致的。



### 题目2：反射创建对象与访问私有成员（暴力破解的界限与性能代价）
> 使用反射创建私有构造函数的类的实例时，`getDeclaredConstructor` 与 `getConstructor` 的区别是什么？调用 `setAccessible(true)` 后，JVM 底层做了哪些安全检查的抑制？请说明反射调用比直接调用慢 2~3 个数量级的三点核心原因。若在循环中高频调用反射方法，应如何优化？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- `getConstructor` vs `getDeclaredConstructor`：
  - `getConstructor`：仅返回 `public` 级别的构造方法（包括继承的 public 构造），无法获取私有构造。
  - `getDeclaredConstructor`：返回该类中**所有访问级别**的构造方法（包括 private、protected、default），但不包含父类的构造方法。
- `setAccessible(true)` 底层机制：通过 `ReflectPermission` 权限检查，关闭 Java 语言访问控制（`Field`/`Method`/`Constructor` 的 `override` 标志设为 true），跳过 JVM 的 `checkMemberAccess` 校验。但它不会绕过 `SecurityManager` 的全局限制。
- 性能慢的核心原因：
  1. 方法查找的 O(n) 遍历（需按名称及参数类型匹配）。
  2. `setAccessible` 对访问权限的动态校验开销（即使设置为 true，每次调用仍会检查）。
  3. 参数装箱与拆箱的额外对象创建（基本类型需包装为 Object 数组）。
  4. JIT 编译器无法对反射调用进行内联优化（去虚化失败）。
- 优化方案：将 `Method` 或 `Constructor` 对象缓存为 `static final` 字段，避免重复 `getDeclaredMethod` 的遍历查找。高频调用建议迁移至 `MethodHandle`（性能更接近直接调用）。
</details>

**我的初答**：
**错漏点**：


### 题目3：getFields 与 getDeclaredFields 的继承范围差异及泛型擦除后的“漏网之鱼”
> `Class.getFields()` 与 `Class.getDeclaredFields()` 在返回字段范围上的本质区别是什么？若父类中有 `public String name`，子类未定义任何字段，在子类上调用 `getDeclaredFields()` 会返回父类的 `name` 吗？追问：如果字段类型为 `List<String>`，通过 `getField("list").getType()` 返回的是什么类型？若要获取实际的泛型参数 `String`，应调用哪个方法？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 范围区别：
  - `getFields()`：返回当前类及其所有父类中 **`public` 修饰**的字段（包括继承的 public 字段）。
  - `getDeclaredFields()`：仅返回当前类中**所有访问级别**的字段（private、protected、default、public），**不包含父类的任何字段**（无论是否 public）。
- 继承验证：子类调用 `getDeclaredFields()` 不会返回父类的 `name`，即使它是 `public` 的。若需要获取包括父类在内的所有 public 字段，必须循环向上遍历父类并调用 `getFields()`。
- 泛型擦除与类型获取：
  - `getType()`：返回原始类型 `List.class`（因泛型擦除，运行时字节码中字段签名仅保留 `List`）。
  - 获取泛型参数：需调用 `getGenericType()`，若返回类型为 `ParameterizedType`，则调用 `getActualTypeArguments()[0]` 获取 `String.class`（但仅限字段签名中明确写入 `List<String>` 的情况，局部变量无法获取）。
</details>

**我的初答**：
**错漏点**：

---

## Day 20 (2026-08-14) —— 动态代理

### 题目1：JDK 动态代理的底层字节码生成机制与接口强制约束
> `Proxy.newProxyInstance(ClassLoader loader, Class<?>[] interfaces, InvocationHandler h)` 生成的代理类是什么类型？为什么它必须基于接口？请从 Java 的单继承机制和 `Proxy` 类的继承结构解释。若目标类未实现任何接口，强行调用 `newProxyInstance` 会抛出什么异常？追问：生成的代理类字节码被缓存在何处？如何将生成的字节码输出到文件进行分析？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 代理类类型：运行时动态生成一个名为 `$Proxy0` 的类，该类继承自 `java.lang.reflect.Proxy` 并实现传入的所有接口。单继承限制导致无法继承目标类，因此必须通过接口暴露方法。
- 未实现接口的后果：调用 `newProxyInstance` 时，`interfaces` 数组为空或目标类无接口，会抛出 `IllegalArgumentException: interface array is empty`。
- 字节码缓存与输出：
  - 缓存：默认在 JVM 内存中由 `WeakCache` 维护（key 为 ClassLoader + 接口数组组合），防止重复生成。
  - 输出：设置系统属性 `sun.misc.ProxyGenerator.saveGeneratedFiles` 为 `true`（JDK 8）或 `jdk.proxy.ProxyGenerator.saveGeneratedFiles`（JDK 11+），字节码会写入当前目录下的 `com/sun/proxy/$Proxy0.class` 文件，可用反编译工具（如 Jad）分析内部 `invoke` 调用逻辑。
</details>

**我的初答**：
1. 生成的代理类是一个工具类,它重写了接口中所有的抽象方法.
2. 它必须基于接口的原因是Java的单继承,生成的动态代理类＄Proxy0已经继承了Proxy,如果要它去伪装目标类就必须通过接口实现,然后向上强转成接口的类型.
3. 不知道
4. 默认存在内存中.不知道

**错漏点**：
1. ❌ “工具类”表述不准确。
-   ✅ 准确说，JDK 动态代理生成的代理类是一个 final 类，它继承了 java.lang.reflect.Proxy，并实现了传入的接口，类名通常为 $Proxy0、$Proxy1 等。
-   ⚠️ 另外，“重写了接口中所有的抽象方法”不完整：代理类还会重写接口中的 default 方法（如果有），以及 Object 的 equals、hashCode、toString 三个方法，并把调用转发给 InvocationHandler.invoke
2. “向上强转成接口的类型”表述不够准确。
-   ✅ 更准确的说法是：代理类本身 implements 了那些接口，因此它可以赋值给接口类型的引用，而不是“强转成接口类型”。它通过实现接口来获得目标对象的接口类型。
3. 会抛出IllegalArgumentException.
4. 代理类字节码的缓存位置
- 实例缓存：Proxy类内部维护了一个WeakCache弱引用缓存，以「类加载器 + 接口数组」作为唯一 key，缓存已生成的代理类Class对象。同一个类加载器、相同的接口组合，只会生成一次代理类，后续调用直接复用，避免重复生成字节码。
- 内存存储：生成的字节码被类加载器加载后，类元数据在 JDK 7 及之前存放在永久代（PermGen），JDK 8 及之后存放在元空间（Metaspace），默认不会持久化到本地磁盘。
5. 输出字节码到文件的两种方式
   
   方式 1：系统属性自动输出（最简方式）
   
   在程序启动时设置对应系统属性，JDK 生成代理类时会自动将字节码写入本地文件：

JDK 8 及更早版本：
System.setProperty("sun.misc.ProxyGenerator.saveGeneratedFiles", "true");

JDK 9 及更高版本（模块化后包名变更）：
System.setProperty("jdk.proxy.ProxyGenerator.saveGeneratedFiles", "true");

开启后，生成的$ProxyN.class文件会自动输出到项目根目录下的com/sun/proxy子目录中

方式 2：手动生成并写入文件
直接调用字节码生成工具类，自定义类名与输出路径，以 JDK 8 为例：

// 手动生成代理类字节码
byte[] classBytes = sun.misc.ProxyGenerator.generateProxyClass(
"$ProxyCustom",
new Class[]{TargetInterface.class}
);
// 写入本地文件
Files.write(Paths.get("./$ProxyCustom.class"), classBytes);
这种方式灵活度更高，适合离线分析代理类的字节码结构


### 题目2：JDK 动态代理与 CGLIB 的底层实现差异（反射调用 vs 字节码直接调用）
> CGLIB 通过生成目标类的子类实现代理，其 `MethodInterceptor` 与 JDK 的 `InvocationHandler` 在方法调用性能上有何本质区别？请解释 CGLIB 中 `FastClass` 机制的作用（为什么它比纯反射调用更快）？追问：CGLIB 无法代理哪些类型或方法？Spring AOP 默认选择 JDK 还是 CGLIB 的依据是什么？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 性能差异根源：
  - JDK 动态代理：`invoke` 内部通过 `Method.invoke` 反射调用，涉及 JVM 访问检查、参数装箱/拆箱，且无法被 JIT 内联优化。
  - CGLIB：`MethodInterceptor` 同样使用反射，但 CGLIB 引入了 `FastClass` 机制，为被代理类生成一个索引映射（将方法签名映射为 int 索引），调用时通过 `invoke(index, args)` 直接跳转到目标方法，减少了反射的方法名称查找开销，速度约为纯反射的 1.5~2 倍。
- CGLIB 限制：
  - 无法代理 `final` 修饰的类（无法继承）。
  - 无法代理 `final` 或 `static` 修饰的方法（无法重写）。
- Spring AOP 选择策略：
  - 默认：若目标类实现了至少一个接口，使用 JDK 动态代理；若未实现任何接口，使用 CGLIB。
  - 强制 CGLIB：可通过 `@EnableAspectJAutoProxy(proxyTargetClass = true)` 或配置 `spring.aop.proxy-target-class=true` 强制使用 CGLIB（即使有接口）。
</details>

**我的初答**：
**错漏点**：


### 题目3：InvocationHandler 中 invoke 方法的异常处理与代理对象传递陷阱
> 在 `InvocationHandler.invoke(Object proxy, Method method, Object[] args)` 中，若在实现中调用 `method.invoke(target, args)` 并捕获异常，与直接抛出原始异常有何区别？为什么在 `invoke` 中不能将 `proxy` 作为参数传给目标方法（如 `method.invoke(proxy, args)`）？追问：若在代理方法中调用代理对象自身的另一个方法（如 `this.toString()`），会导致无限递归吗？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 异常处理规则：
  - 若 `invoke` 中捕获异常但未重新抛出，则代理方法将正常返回（返回 null 或默认值），完全吞噬异常，导致调用方无法感知错误。
  - 正确做法：应抛出 `InvocationTargetException` 或原始异常（需确保异常类型在接口方法声明范围内，否则需包装为 `UndeclaredThrowableException`）。
- 传递 `proxy` 的后果：`method.invoke(proxy, args)` 会尝试将 `proxy`（代理对象）作为目标实例执行方法，但代理对象本身不包含业务逻辑，且调用任何代理方法都会再次进入 `invoke`，导致无限递归（最终 `StackOverflowError`）。必须传入目标对象（target）而非代理对象。
- 内部自调用递归：
  若代理对象的方法内部调用 `this.toString()`，由于 `this` 指向代理对象本身，调用会重新进入 `invoke`，若处理不当会造成无限递归。解决方案：在 `invoke` 中判断方法名，对于 `toString`/`hashCode`/`equals` 等特殊方法，可直接委托给目标对象，避免递归。
</details>

**我的初答**：
**错漏点**：

---

## Day 21 (2026-08-15) —— 枚举（Enum）

### 题目1：枚举的本质与实例化时机（为什么枚举不能继承其他类？）
> 枚举类通过 `enum` 关键字定义，实际上隐式继承了 `java.lang.Enum`。请说明枚举常量（如 `SPRING, SUMMER`）在 JVM 中是什么类型的对象？它们何时被实例化？如果枚举类有抽象方法，每个常量必须做什么？追问：枚举能否实现接口？枚举能否被 `final` 修饰？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 枚举常量本质：每个枚举常量都是该枚举类的**静态 final 实例**，在类加载的初始化阶段（`<clinit>`）一次性创建所有常量对象，并按声明顺序存储于一个隐式的数组 `$VALUES` 中。
- 抽象方法约束：若枚举类定义了抽象方法，每个常量必须使用匿名内部类或特定常量类主体（Constant-Specific Class Body）实现该方法，否则编译报错。
- 继承限制：枚举隐式继承 `Enum`，Java 不支持多继承，因此无法再继承其他类（但可实现多个接口）。
- `final` 修饰枚举：枚举类本身被编译器隐式标记为 `final`（除非有常量特定类主体，此时类是非 final 的但子类只能由编译器生成），无法手动用 `final` 修饰（编译报错）。
</details>

**我的初答**：
1. 枚举常量在JVM中是public static final 类型的.
2. 它们在类加载阶段被实例化.
3. 如果枚举中存在抽象方法,因为抽象方法本身是不能被实例化的,所以需要在枚举常量定义是手动重写所有的抽象方法.
4. 枚举本质就是一个特殊的类,它当然可以实现接口.枚举本身就是final不可被继承的.

**错漏点**：
1. 核心不足：只回答了修饰符，没有完整回应 “是什么类型的对象”—— 枚举常量本质是当前枚举类本身的实例对象。例如定义 enum Season { SPRING, SUMMER }，SPRING 就是 Season 类型的静态常量实例，而非其他类型。
2. 表述不精准：“类加载阶段” 的说法过于宽泛。类加载包含加载、链接、初始化三个子阶段，枚举常量的实例化严格发生在类的初始化阶段，由编译器生成的 <clinit>() 静态初始化方法统一完成创建和赋值；只有触发类的主动引用时才会执行初始化，且全局只实例化一次，天生线程安全。
3. 原因表述不准确：不是 “抽象方法不能被实例化”，正确逻辑是: 枚举类中定义了抽象方法,编译器会将其标记为抽象类;但枚举常量必须必须是可实例化的对象,因此每个常量都必须以**匿名内部子类**的形式重写并实现所有抽象方法,否则编译报错.
4. 表述不严谨：
- “枚举本身就是 final” 不完全准确：普通无抽象方法的枚举,会被编译器隐式添加final修饰.但存在抽象方法的枚举不会被添加,但它依然不能被继承-----因为枚举构造器默认私有,且Java语法禁止显式继承Enum枚举类
- 没有说明是否能被final修饰: **不能用显式final修饰枚举类,枚举的final是编译器隐式添加好了的,手动添加final直接报错.

### 题目2：枚举的自定义字段、构造器与 `values()` 的缓存机制
> 设计一个枚举 `ErrorCode`，包含 `code`（int）和 `message`（String）两个字段，并提供根据 `code` 获取枚举实例的静态方法 `fromCode(int code)`。枚举的构造器必须使用什么访问修饰符？为什么不能使用 `public` 修饰构造器？追问：`values()` 方法每次调用都会创建新数组吗？在性能敏感场景中应如何优化？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 代码示例：
  public enum ErrorCode {
  SUCCESS(200, "成功"),
  NOT_FOUND(404, "资源不存在"),
  SERVER_ERROR(500, "服务器内部错误");
  private final int code;
  private final String message;
  private ErrorCode(int code, String message) { this.code = code; this.message = message; }
  public static ErrorCode fromCode(int code) {
  for (ErrorCode ec : ErrorCode.values()) {
  if (ec.code == code) return ec;
  }
  return null;
  }
  // getter...
  }
- 构造器访问限制：枚举构造器**必须是 `private` 或包私有（默认）**，不允许 `public`/`protected`，因为枚举常量由 JVM 在内部创建，外部无法 `new` 枚举实例。若不显式声明，编译器默认生成 `private` 构造器。
- `values()` 缓存机制：`values()` 方法由编译器生成，每次调用会**返回一个新数组的副本**（防御性拷贝），避免外部修改内部缓存的常量数组。在性能敏感场景（如高频循环），应将 `values()` 的结果缓存为 `static final` 数组，避免重复分配内存。
</details>

**我的初答**：
**错漏点**：


### 题目3：策略枚举模式（常量特定方法 vs switch 语句的优劣）
> 现有枚举 `Operation`，包含 `ADD`、`SUBTRACT`、`MULTIPLY`，要求实现 `apply(int x, int y)` 方法返回计算结果。请分别使用 **switch 语句**和**常量特定方法（抽象方法）**两种方式实现。追问：若未来新增 `DIVIDE` 操作，两种方式各自的维护成本和风险是什么？哪种方式符合开闭原则（OCP）？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- switch 方式（在枚举外部的工具类中判断）：
  public static int apply(Operation op, int x, int y) {
  switch (op) {
  case ADD: return x + y;
  case SUBTRACT: return x - y;
  case MULTIPLY: return x * y;
  default: throw new IllegalArgumentException();
  }
  }
  - 缺点：新增操作时需修改 switch 代码，违反开闭原则，且若遗漏 default 分支，编译器不会强制提醒。

- 常量特定方法（策略枚举）：
  public enum Operation {
  ADD { public int apply(int x, int y) { return x + y; } },
  SUBTRACT { public int apply(int x, int y) { return x - y; } },
  MULTIPLY { public int apply(int x, int y) { return x * y; } };
  public abstract int apply(int x, int y);
  }
  - 优点：新增操作时仅需添加一个新的常量及其实现，无需修改现有代码，完全符合开闭原则。
  - 注意：若所有操作共享逻辑，可定义默认方法（Java 8+）或模板方法，但策略枚举通常采用抽象方法强制每个常量实现。
</details>

**我的初答**：
**错漏点**：

---

## Day 22 (2026-08-16) —— 日志框架与双重检查锁（DCL）

### 题目1：双重检查锁（DCL）中 volatile 的禁止重排序语义与半初始化对象暴露
> 在标准的双重检查锁单例模式中，`instance` 变量必须使用 `volatile` 修饰。请说明在不加 `volatile` 时，`instance = new Singleton()` 底层（字节码层面）的三步操作如何被重排序？这种重排序如何导致其他线程获取到一个未初始化完成的对象（部分构造器未执行）？追问：若将 `volatile` 替换为 `final` 修饰实例变量，是否能达到同样的安全效果？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 底层三步操作（非原子）：
  1. 分配内存空间（给对象分配堆内存）。
  2. 初始化对象（执行构造器，初始化成员变量）。
  3. 将引用指向内存地址（赋值给 instance）。
- 重排序风险：JIT 编译器或 CPU 可能将步骤 2 和 3 交换顺序（即先赋值再初始化）。若线程 A 执行完步骤 3（引用已指向内存）但步骤 2 未执行完毕，此时线程 B 进入 `if (instance == null)` 检查，发现不为 null，直接返回 instance，但该对象尚未完成构造，导致线程 B 访问未初始化的成员变量（如 int 返回 0，String 返回 null）或抛出异常。
- `volatile` 作用：通过内存屏障（LoadStore/StoreStore）禁止步骤 2 和 3 的重排序，保证初始化在赋值之前完成。
- `final` 替代性：`volatile` 是禁止重排序，`final` 在构造器内保证初始化完成（JVM 保证构造器内 final 字段的写操作不会与构造器外的引用赋值重排序），但 DCL 中 `instance` 是静态变量，用 `final` 修饰静态字段会使其变为常量（无法延迟赋值），因此不可替换，必须用 `volatile`。
</details>

**我的初答**：

instance = new Singleton()的底层分为三步:1分配内存 2初始化对象 3将地址赋值给instance对象.

JVM可能会将指令重排序为1->3->2. 导致其他线程在外层检查发现不满足(instance == null),直接将半初始化的对象拿去使用,出现未知的错误.

若将volatile替换为final只能保持单例模式,但不能防止指令重排序,无法解决安全问题.

**错漏点**：
- 存在的不足与细节缺失：
- 缺少字节码层面的对应指令：instance = new Singleton()对应字节码可归纳为三步：new指令分配堆内存、对象赋默认零值；invokespecial <init>调用构造方法完成对象初始化；putstatic将对象地址赋值给静态变量instance。重排序就发生在「构造方法执行」和「引用赋值」之间，后者可能被提前到构造器执行完成之前。
- 没有结合 DCL 的代码结构说明风险成因：正是因为第一次 null 检查在 synchronized 同步块之外，不需要抢锁就能读取 instance 的值，才会读到已经赋值但未完成初始化的引用；如果所有读取都在同步块内，锁的内存语义会保证可见性和有序性，不会出现半初始化问题，但会损失并发性能。
- 未明确重排序的规则依据：在没有volatile时，Java 内存模型遵守 as-if-serial 规则 —— 只要单线程下执行结果不变，JIT 编译器、CPU 都可以对无数据依赖的指令重排序。对象初始化和引用赋值不存在数据依赖，因此重排序是被允许的。

- 核心结论 “无法解决安全问题” 是对的，但理由和表述存在多处偏差：
- 语法不成立：DCL 场景下 instance 无法用 final 修饰。final修饰的静态变量必须在声明时或静态代码块中完成赋值，不能延迟到方法调用时才初始化，这和 DCL 懒加载的设计目标直接冲突；如果强行用 final，就只能写成饿汉式单例，不存在双重检查的应用场景。
- 对 final 的内存语义理解偏差：final 的安全保证是对象内部的 final 字段—— 它禁止构造器内 final 字段初始化与对象引用发布的重排序，保证引用可见时 final 字段已初始化。但它无法禁止「整个对象的构造过程」和「静态引用赋值」之间的重排序，非 final 字段仍可能处于半初始化状态，不能彻底解决 DCL 的问题。
- “只能保持单例模式” 表述不准确：final 修饰引用仅保证引用地址不可变，和单例的正确性没有直接关联；DCL 不加 volatile 的核心问题不是破坏单例性，是会让其他线程拿到未初始化完成的对象，引发程序逻辑异常。

### 题目2：SLF4J 门面模式与占位符 { } 的性能优势（延迟字符串构建）
> 在 SLF4J 中，`logger.debug("user: " + user.getName())` 与 `logger.debug("user: {}", user.getName())` 在运行时的性能表现有何本质区别？若当前日志级别为 `INFO`，`debug` 日志不会被输出，两种写法对 CPU 和内存的消耗差异是什么？追问：`user.getName()` 返回 null 时，这两种写法分别会产生什么结果（是否触发 NPE）？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 字符串拼接写法：`"user: " + user.getName()` 会在调用 `debug` 方法**之前**完成字符串拼接操作（无论日志级别是否启用）。即使最终日志被丢弃，拼接操作和 `getName()` 调用依然执行，造成不必要的 CPU 开销和临时字符串对象分配（占用堆内存）。
- 占位符写法：`logger.debug("user: {}", user.getName())`，SLF4J 仅在**确认日志级别启用后**，才会进行占位符替换和参数拼接。若 `debug` 级别关闭，`getName()` 方法根本不会被调用，参数传递的 `user.getName()` 虽然会被求值（Java 方法传参先求值），但 SLF4J 内部若不输出则不会进行 `toString` 拼接（占位符替换在输出时执行，但参数值已传入）。注：严格来说，方法传参时 `user.getName()` 依然会被求值，但不会执行后续的字符串格式化开销，且若 `user` 为 null，占位符写法会在内部处理为 "null"，不会抛 NPE。
- NPE 风险：
  - 拼接写法：若 `user` 为 null，`"user: " + user.getName()` 会抛出 `NullPointerException`（即使日志级别不匹配，也会因为方法调用顺序导致异常，阻断主流程）。
  - 占位符写法：若 `user` 为 null，`user.getName()` 同样会先求值，依然抛出 NPE（因为传参先求值）。但若写成 `logger.debug("user: {}", user)`，则不会抛 NPE。推荐使用 `logger.debug("user: {}", user)` 并确保 `toString()` 处理 null。
</details>

**我的初答**：
**错漏点**：


### 题目3：日志 MDC（Mapped Diagnostic Context）原理与异步日志下的上下文丢失
> 在 Logback/Log4j 中，MDC 可以用于存储请求链路 TraceId，实现日志追踪。请说明 MDC 底层的数据结构（通常基于 ThreadLocal），以及为什么在业务线程池或异步日志（AsyncAppender）场景下，MDC 上下文可能会丢失？若使用异步日志，如何确保 MDC 信息正确传递（如 Logback 的 AsyncAppender 是否需要特殊配置）？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- MDC 底层：基于 `ThreadLocal<Map<String, String>>`，每个线程维护独立的上下文 Map。在当前线程中调用 `MDC.put("traceId", id)` 后，同一线程后续的日志事件会自动携带该键值对。
- 线程池丢失问题：当业务将任务提交给线程池时，工作线程与主线程不同，不会自动继承 MDC 上下文。需在提交任务前手动拷贝 MDC 内容到新线程（如 `Runnable` 包装器中捕获父线程 MDC）。
- 异步日志丢失风险：
  - 默认 AsyncAppender 在子线程中消费日志事件，若日志事件未快照 MDC，则子线程无法访问原线程的 ThreadLocal。
  - Logback 的 AsyncAppender 默认会**自动快照 MDC**（将 MDC 内容复制到 `ILoggingEvent` 对象中），因此默认情况下 MDC 不会丢失。但若使用自定义线程池且未包装，需确保日志事件在提交时已包含 MDC 快照。
- 解决方案：使用 `MDC.getCopyOfContextMap()` 在任务提交前获取快照，在任务执行前通过 `MDC.setContextMap(map)` 恢复，并在 finally 中清除。
</details>

**我的初答**：
**错漏点**：

---

## Day 23 (2026-08-17) —— XML 基础与解析

### 题目1：DOM 解析与 SAX 解析的本质差异及适用场景（内存 vs 事件驱动）
> Java 中解析 XML 的 DOM（Document Object Model）和 SAX（Simple API for XML）两种方式，在内存占用、读写能力、遍历方向上有何本质区别？若需要修改 XML 文档并重新输出，应选择哪种解析方式？若只需要读取大文件（如 500MB）中的少量字段，应选择哪种？追问：StAX（Streaming API for XML）与 SAX 有何不同？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- DOM 解析：
  1. 原理：将整个 XML 文档一次性读入内存，构建成树形结构（Document 对象），所有节点均可随机访问。
  2. 优点：支持 XPath 查询、支持增删改节点并重新输出。
  3. 缺点：内存占用巨大（文档大小 × 5~10 倍），不适合大文件。
  4. 适用：小文件、需要频繁修改或随机访问的场景。
- SAX 解析：
  1. 原理：基于事件驱动（回调函数），顺序读取 XML 流，遇到开始标签、结束标签、文本时触发对应事件。
  2. 优点：内存占用极小（仅读取当前节点），速度快，适合处理超大文件。
  3. 缺点：只能顺序读取，无法修改文档，不支持回退或随机访问。
  4. 适用：大文件、只需提取部分数据、流式处理的场景。
- StAX（XML 流式 API）：
  与 SAX 类似也是流式处理，但 StAX 是**拉模型（Pull Parser）**，开发者主动从解析器中拉取事件（迭代器风格），而非被动接受回调（SAX 是推模型 Push Parser）。StAX 更易控制解析流程，支持过滤和提前终止，且可以同时读写。
</details>

**我的初答**：
1. DOM解析是将整个XML文件的内容加载进入内存,并以树的形式存在.它的内存占用非常大,既能够读XML也能够进行修改,通过遍历树的方式遍历.
2. SAX解析则是通过流的方式一行一行加载XML文件,它的内存占用很小,但是只能读,不能写,无法进行遍历,因为上一行已经从内存中释放.
3. 若要修改XML文档并重新输出就选择DOM解析
4. 读取少量字段就使用SAX流式解析.
5. StAX不了解

**错漏点**：
1. 细节缺失：没有明确 DOM 的遍历能力特性。DOM 支持随机访问、双向遍历，可以通过节点关系任意访问子节点、父节点、兄弟节点，也可以反复跳转、回溯到任意节点，这是它和 SAX 最核心的差异之一。
2. 存在两处关键认知偏差:
- **解析粒度错误**: SAX不是一行一行加载,而是**事件驱动的流式解析**.解析器按照XML的标签结构触发事件(开始标签,结束标签,文本内容,属性等),和"行"没有关系,换行,缩进在XML文件中只是普通文本内容.
- **遍历描述错误**: 不是不能遍历,而是只能**单向顺序遍历**.解析器从头到尾依次解析,被解析过的节点在触发完事件后就被丢弃,不会保留在内存中,因此无法反向遍历以及随机访问.
- **StAX**: 同样是流式解析，核心差异在于事件的控制方向，以及读写能力:
- 1.驱动模式不同(核心区别)
- SAX是**推模式**: 解析器主动向应用程序推送事件,应用程序被动接收,必须跟着解析器的节奏走完整个文档.
- StAX是**拉模式**: 应用程序主动向解析器拉取事件,可以随意控制节奏,想停止就停止,想继续就继续,无需处理不关心的节点.
- 2.读写能力不同
- SAX只能读取XML,无法写出
- StAX即支持流式读取也支持流式写入,即可以解析XML,也可以输出XML
- 3.使用灵活性不同
- SAX必须从头解析到尾
- StAX支持随时停止解析,可以在处理完相关的任务逻辑在继续解析,也可以在解析到需要的内容后直接停止.

### 题目2：DTD 与 XML Schema（XSD）的约束能力对比（命名空间与数据类型）
> XML 的 DTD（文档类型定义）和 XSD（XML Schema 定义）在定义 XML 结构时，两者在语法（DTD 非 XML 语法 vs XSD 基于 XML）、数据类型支持（string vs int/date）、命名空间支持、以及扩展性上有何本质区别？在实际开发中，为何 XSD 已逐步取代 DTD？追问：若要在 XML 中引入命名空间以避免元素重名冲突，DTD 和 XSD 哪个支持？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 语法差异：
  - DTD：使用独立于 XML 的语法（如 `<!ELEMENT 元素名 (子元素)>`），不遵循 XML 规范，难以工具化校验和解析。
  - XSD：基于 XML 语法编写（`<xs:schema>`），本身就是有效的 XML 文件，可利用现有 XML 工具处理。
- 数据类型：
  - DTD：仅支持字符串（PCDATA/CDATA），无法约束整数、日期、枚举等类型。
  - XSD：支持丰富的内置类型（string、int、decimal、dateTime、boolean）以及自定义复杂类型，可精确约束数据格式（如正则表达式 `pattern`）。
- 命名空间：
  - DTD：不支持命名空间（无法区分同名元素）。
  - XSD：完全支持命名空间（`targetNamespace` 和 `xmlns`），适用于企业级多模块集成。
- 扩展性：
  - DTD：继承机制弱，难以复用和扩展。
  - XSD：支持 `import`、`include` 和 `redefine`，支持继承和派生。
- 取代原因：XSD 更强大、更规范、支持命名空间和强数据类型，已逐步取代 DTD（但仍需兼容遗留系统）。
</details>

**我的初答**：
**错漏点**：


### 题目3：XML 在 Java 生态中的典型应用场景（配置文件与数据交换）
> 在 Java 开发中，XML 常用于哪些场景？请列举至少三个具体框架或规范中的实际用途（如 Spring、MyBatis、Web 服务）。相较于 JSON，XML 在可读性、解析效率、元数据表达能力上有何优劣？在现代微服务架构中，为何 JSON 更受青睐？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 典型应用场景：
  1. Spring 框架早期配置文件（现仍支持 XML 定义 Bean，如 `applicationContext.xml`）。
  2. MyBatis 的 SQL 映射文件（`XXXMapper.xml`，定义 SQL 与 Java 方法的映射关系）。
  3. Web 服务（SOAP）的消息格式（基于 XML 的 SOAP 协议）。
  4. Maven 的 `pom.xml`（项目依赖和构建配置）。
  5. Java Web 应用的 `web.xml`（Servlet 映射和过滤器配置）。
- XML vs JSON 对比：
  - 可读性：JSON 更简洁、更易读（无闭合标签），XML 冗余（闭合标签占空间）。
  - 解析效率：JSON 解析速度更快（库更轻量），XML 解析（尤其是 DOM）较慢且内存占用高。
  - 元数据表达能力：XML 更强（支持属性、命名空间、Schema 校验、注释），JSON 更弱（无标准注释，Schema 支持较弱）。
- JSON 在微服务中更受青睐的原因：
  1. 轻量高效，网络传输开销小。
  2. 与 JavaScript/前端天然亲和（直接解析为对象）。
  3. 主流 RESTful API 标准格式，生态成熟。
  4. 对 NoSQL 数据库（如 MongoDB）支持良好。
</details>

**我的初答**：
**错漏点**：

---

## Day 24 (2026-08-18) —— 类加载器与单元测试

### 题目1：双亲委派模型（Parents Delegation Model）的源码实现与设计缺陷
> 请描述 JVM 的类加载器层次结构（Bootstrap、Platform/Extension、Application）及双亲委派模型的核心流程。在 `java.lang.ClassLoader.loadClass(String name)` 源码中，为什么先委托父加载器，而不是直接加载？追问：JDBC 驱动（如 `com.mysql.jdbc.Driver`）是如何利用线程上下文类加载器（Thread Context ClassLoader）打破双亲委派模型的？若未破坏，会出现什么问题？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 层次结构：
  - Bootstrap ClassLoader（原生，加载 `rt.jar` 核心类）。
  - Platform/Extension ClassLoader（加载 `lib/ext` 扩展类）。
  - Application ClassLoader（加载 classpath 下的类）。
- 双亲委派流程（源码 `loadClass`）：
  1. 检查该类是否已被当前加载器加载（`findLoadedClass(name)`）。
  2. 若未加载，调用父加载器的 `loadClass`（递归）。
  3. 若父加载器无法加载（抛出 `ClassNotFoundException`），才调用自身的 `findClass` 加载。
- 优势：避免核心类（如 `java.lang.String`）被篡改，保证 JVM 安全；避免重复加载。
- 破坏双亲委派（SPI 机制）：
  - JDBC 的 `DriverManager` 由 Bootstrap ClassLoader 加载，但它需要加载 classpath 下的 MySQL 驱动（Application ClassLoader 加载）。
  - 通过 `Thread.currentThread().getContextClassLoader()` 获取 Application ClassLoader，显式加载驱动，父加载器反向委托给子加载器。
  - 若未破坏，Bootstrap 找不到 MySQL 驱动，导致 `ClassNotFoundException`，数据库无法连接。
</details>

**我的初答**：
1. 类加载器分为启动类加载器,平台类加载器,应用程序类加载器.
2. 这就是双亲委派模型,先委托父类加载,而不是自己直接加载的原因第一个是: 保证加载不受外部污染,如果自己写了一个Java.lang.String不会被执行.第二点是: 保证一个类只会被一个加载器执行,不会出现加载多次的情况.
3. JDBC驱动不知道

**错漏点**：
1.两个注意点

Bootstrap ClassLoader（启动类加载器）：用 C++ 实现（HotSpot 内嵌），不是 Java 类，在 Java 中获取它返回 null。负责加载 JAVA_HOME/lib 下的核心类库（JDK 8 及之前是 rt.jar；JDK 9+ 模块化后是 java.base 等核心模块）。

Extension / Platform ClassLoader：JDK 8 叫 Extension ClassLoader（扩展类加载器），加载 jre/lib/ext；JDK 9 模块化后改名为 Platform ClassLoader（平台类加载器），加载平台模块（如 java.sql）。面试时最好按版本说明，不能笼统地说"平台类加载器"涵盖 JDK 8。

Application ClassLoader（应用程序/系统类加载器）：加载 classpath 下的用户类，是 ClassLoader.getSystemClassLoader() 的返回值，也是大多数用户类的默认加载器。

层级关系（注意：是**组合关系而非继承关系**，通过 parent 字段持有父加载器引用）：

Bootstrap ClassLoader (C++, 顶层)

↑ parent

Platform (JDK9+) / Extension (JDK8) ClassLoader

↑ parent

Application ClassLoader

↑ parent

自定义 ClassLoader（如 Tomcat 的 WebappClassLoader）

**双亲委派模型核心流程**：收到加载请求 → 查缓存（findLoadedClass）→ 委托父加载器（递归向上）→ 父加载器全部失败 → 自己调用 findClass() 加载。

**为什么先委托父加载器而不是直接加载?** 你回答的两点基本对,但建议表述得更准确:

1.**避免类重复加载(唯一性)**: 父类加载器加载过的类,子类就不会再加载.配合synchronized类加载锁和findLoadedClass缓存,保证同一个全限定名的类在JVM中**由同一个加载器只加载一次**.
类的唯一性 = **全限定类名** + **加载它类的加载器**共同决定.

2.**保证核心类库的安全(沙箱安全)**: 核心类总是由最顶层的Bootstrap加载.如果允许用户类加载器直接加载,用户伪造一个java.lang.String 就可能被加载进JVM,污染甚至替换核心API.

JDBC 与线程上下文类加载器（TCCL）如何打破双亲委派?

1.矛盾在哪
   
java.sql.DriverManager 位于 java.sql 模块（JDK 8 在 rt.jar），由 Bootstrap/Platform 类加载器加载。但 com.mysql.cj.jdbc.Driver 这类驱动实现类在应用的 classpath 下，只能由 Application ClassLoader 加载。
 
如果严格遵循双亲委派：DriverManager 想加载驱动实现类时，只能委托给自己的加载器（Bootstrap/Platform），而 Bootstrap 看不到 classpath 下的 MySQL 驱动 → 直接 ClassNotFoundException，SPI 机制根本无法工作。

2. JDBC 4 之前的"手动打破"
   Class.forName("com.mysql.jdbc.Driver");   // 用当前线程上下文加载器加载
   
   Connection conn = DriverManager.getConnection(url, user, pwd);

3. SPI + TCCL 的机制（JDBC 4 / JDK 6 之后）
   JDK 引入 SPI（Service Provider Interface）+ ServiceLoader 机制自动注册驱动
  
DriverManager 的静态代码块中执行：

static {
loadInitialDrivers();   // 内部调用 ServiceLoader.load(Driver.class)
...
}

ServiceLoader.load(Driver.class) 的源码核心是：

public static < S > ServiceLoader< S > load(Class< S > service) {
// 关键：使用当前线程的上下文类加载器，而不是 ServiceLoader 自己的加载器
ClassLoader cl = Thread.currentThread().getContextClassLoader();
return new ServiceLoader<>(Reflection.getCallerClass(), service, cl);
}

TCCL 默认就是 Application ClassLoader（线程创建时继承，主线程的 TCCL 即系统类加载器）。这样，由 Bootstrap 加载的 DriverManager，通过"借用"线程上下文类加载器，反向委托给下层的 Application ClassLoader 去扫描 META-INF/services/java.sql.Driver 文件并加载 MySQL 驱动实现类。



### 题目2：JUnit 5 的测试生命周期与扩展模型（@BeforeEach vs @BeforeAll 的执行顺序）
> 在 JUnit 5 中，`@BeforeAll`、`@BeforeEach`、`@Test`、`@AfterEach`、`@AfterAll` 的执行顺序是什么？若父类与子类均有这些注解，顺序如何交织？`@TestInstance(Lifecycle.PER_CLASS)` 的作用是什么？它如何改变 `@BeforeAll` 和 `@AfterAll` 的访问修饰符要求？追问：使用 `@ExtendWith(MockitoExtension.class)` 时，Mock 对象的初始化发生在哪个阶段（`@BeforeEach` 之前还是之后）？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 标准执行顺序（单类）：
  `@BeforeAll`（静态） → `@BeforeEach` → `@Test` → `@AfterEach` → `@AfterAll`（静态）。
- 继承体系交织（父类 + 子类）：
  - 父类 `@BeforeAll` → 子类 `@BeforeAll`
  - 父类 `@BeforeEach` → 子类 `@BeforeEach`
  - `@Test`（子类）
  - 子类 `@AfterEach` → 父类 `@AfterEach`
  - 子类 `@AfterAll` → 父类 `@AfterAll`
    （类似 `super()` 的调用链，但子类在父类之后执行，注意 `@AfterEach` 是子类先于父类执行）。
- `@TestInstance(Lifecycle.PER_CLASS)`：
  - 默认是 `PER_METHOD`（每个测试方法创建一个新实例）。
  - 切换为 `PER_CLASS` 后，测试类只实例化一次，`@BeforeAll` 和 `@AfterAll` 无需声明为 `static`，允许非静态方法访问实例变量。
- Mockito 初始化时机：
  - `MockitoExtension` 的 `beforeEach` 扩展执行早于 `@BeforeEach` 方法，因此 `@Mock` 对象在进入 `@BeforeEach` 之前已初始化完成，可直接使用。
</details>

**我的初答**：
1. 执行顺序为@BeforeAll(所有测试执行前执行一次)->@BeforeEach(每次测试执行前执行)->@Test(测试)->@AfterEach(每次测试执行完后执行)->@AfterAll(所有测试执行完后执行一次)
2. 若父类与子类均有这些注解不了解
3. 后面两个皆不了解.

**错漏点**：


### 题目3：类加载器与资源文件加载（getResource 与 getResourceAsStream 的路径解析规则）
> 在单元测试中，使用 `ClassLoader.getResourceAsStream("config.properties")` 与 `Class.getResourceAsStream("/config.properties")` 在路径解析上有何区别？前导斜杠 `/` 的含义是什么？若使用 `this.getClass().getResourceAsStream("config.properties")`（非静态上下文中），相对路径基于哪个包进行解析？追问：在 Maven 项目中，测试资源目录（`src/test/resources`）与主资源目录（`src/main/resources`）在运行时各自的类加载器访问优先级有何不同？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- `ClassLoader` 定位规则：
  - `ClassLoader.getResource(name)`：`name` 不能以 `/` 开头，路径相对于类路径（classpath）的根目录（即 `target/classes` 或 `target/test-classes`）。
- `Class` 定位规则：
  - `Class.getResource(name)`：
    - 若 `name` 以 `/` 开头，则从 classpath 根目录查找（等同于 `ClassLoader.getResource(name.substring(1))`）。
    - 若 `name` 不以 `/` 开头，则相对于当前类所在的包路径（即自动拼接 `packageName + "/" + name`）。
- 相对路径示例：
  `this.getClass().getResourceAsStream("config.properties")` 查找 `com/example/config.properties`（假设当前类在 `com.example` 包下）。
- Maven 资源优先级：
  - 测试执行时，`src/test/resources` 的内容优先于 `src/main/resources`（Maven 会将两者合并到 classpath，但测试资源先加载，可覆盖主资源）。
  - 若同名文件存在，测试资源会隐藏主资源（符合测试隔离原则）。
- 注意：`ClassLoader.getSystemResource` 类似但基于系统类加载器，不常用。
</details>

**我的初答**：
**错漏点**：

---

## Day 25 (2026-08-19) —— JavaSE 综合复习（基础语法 + 集合 + 并发 + 异常）

### 题目1：String 的不可变性与 intern() 的内存池机制（内存模型角度）
> 现有代码：`String s1 = "java"; String s2 = new String("java"); String s3 = s2.intern(); System.out.println(s1 == s2); System.out.println(s1 == s3);` 输出结果是什么？请从 JVM 的常量池与堆内存角度解释 intern() 的核心作用。追问：在 JDK 1.6 与 JDK 1.8 中，intern() 的实现差异（字符串常量池的位置变化）会导致上述结果产生什么不同？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 输出结果：`false` 和 `true`。
  - `s1` 指向常量池中的 "java"，`s2` 指向堆中新创建的对象，地址不同。
  - `s2.intern()` 将堆中的字符串引用注册到常量池（若常量池已存在相同字符串，则返回已有引用），返回常量池中的 "java" 引用，因此 `s1 == s3` 为 true。
- JDK 版本差异：
  - JDK 1.6：常量池在方法区（PermGen），`intern()` 会将堆字符串**复制**一份到方法区，返回方法区引用（若常量池已存在则直接返回已有引用）。`s1` 与 `s3` 指向方法区同一对象，结果为 true。
  - JDK 1.7+：常量池移至堆，`intern()` 不再复制，而是将堆中的**首次出现**的字符串引用直接存入常量池。`s1` 指向常量池引用，`s2` 指向堆对象，`s2.intern()` 返回常量池地址，故结果为 true（注：若先出现 `new String("java")` 再 `intern()`，此行为仍成立；但本例 `s1` 先于 `s2` 创建，常量池已有 "java"，`intern()` 返回常量池地址，结果同上）。
- 关键结论：`intern()` 能够显式将堆中字符串推入常量池，常用于大量重复字符串的存储优化。
</details>

**我的初答**：
1. 第一个输出false,第二个输出true.直接创建字符串会先查看字符串常量池是否存在该字符串,如果有,直接复用,如果没有则在常量池中创建一个
2. new String("java")会绕过常量池直接在堆内存中创建该字符串对象,所以与s1==比较是false,因为两者的地址值不同.s2.intern()会将堆内存中的该字符串引向常量池所以s3指向的实际上是常量池中的字符串,这种方式可以减少堆内存中与常量池中重复的字符串.
3. JDK1.6与JDK1.8 intern()的差异不了解

**错漏点**：

基本结论正确但解释有错误

第1点: 基本正确.字面量"java"编译期进入Class常量池,运行期加载到字符串常量池中,后续存在相同字面量直接复用常量池中引用.

第2点: new String("java")会绕过常量池直接在堆内存中创建该字符串对象**不准确**.

new String("java")实际上**涉及两个对象**

1.字面量`"java"`仍然会在字符串常量池中创建(如果常量池中不存在的话),这一步不会被绕过;

2.new String(...)**在堆中创建一个String对象**,其内部的char数组/value指向的是字符串常量池字面量的拷贝,但**这个对象本身不存在常量池中**.

所以 s1 == s2 为false的本质是 s1指向常量池对象,s2指向堆对象,是两个不同的对象实例.

第三点: intern()的语义(规范定义):

- 如果字符串常量池中已包含了一个**内容相等**(equals为true)的字符串,则**返回池中引用**;否则,将该字符串放入池中并返回池中引用.

- 本题中`s2.intern()`发现池中已有`"java"`所以直接返回池内引用,所以s1==s3结果为true.

| 版本             | 常量池位置                | 溢出异常                                |
| -------------- | -------------------- | ----------------------------------- |
| JDK 1.6        | **方法区（永久代 PermGen）** | `OutOfMemoryError: PermGen space`   |
| JDK 1.7+ / 1.8 | **堆（Heap）**          | `OutOfMemoryError: Java heap space` |
JDK 1.8 同时彻底移除了永久代（类元数据移到 Metaspace，使用本地内存），字符串常量池留在堆中。

#### intern()行为差异-----这是最容易被问倒的点
关间区别在**池中不存在该字符串时"的处理**:
- JDK1.6: 把堆中的字符串**复制一份到永久代的常量池**,返回的是**池中那份新副本**的引用.即使池中原本没有,intern()返回值与原堆对象**永远不相等**.
- JDK1.7/1.8: 不再复制,而是**在常量池中记录一个指向堆中已有对象的引用**,intern()返回的就是**原堆对象本身**.

五、面试加分点

常量池移到堆的好处：永久代大小固定（默认很小）易 OOM，移入堆后受 GC 统一管理，池内字符串可以被回收（无引用时）。

1.8 的"存引用不复制"避免了 double 内存占用，是配合位置迁移做的优化。

String s = new String("java") 创建了 2 个对象（池 1 个 + 堆 1 个）——经典陷阱题。


### 题目2：HashMap 的 put 流程、树化阈值与扩容死锁历史（JDK 1.7 vs 1.8）
> 描述 `HashMap.put(K key, V value)` 在 JDK 1.8 中的完整执行路径（哈希计算 → 寻址 → 插入链表/红黑树）。为什么链表长度超过 8 且数组长度 >= 64 时才转为红黑树？追问：JDK 1.7 的 HashMap 在多线程环境下进行 `resize()` 时为何会出现死锁？JDK 1.8 如何修复该问题？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- put 执行流程：
  1. 计算 `hash = (key == null) ? 0 : (h = key.hashCode()) ^ (h >>> 16)`（扰动函数，混合高 16 位）。
  2. 计算数组索引 `i = (n - 1) & hash`（n 为数组长度，必须为 2 的幂）。
  3. 若桶为空，直接创建 Node 插入。
  4. 若桶不为空，遍历链表/红黑树，通过 `==` 和 `equals` 判断 key 是否已存在，存在则覆盖 value。
  5. 若 key 不存在，尾插法追加新节点（JDK 1.8 尾插）。
  6. 若链表长度 > 8 且数组长度 >= 64，转为红黑树；若数组长度 < 64，优先扩容。
- 树化阈值设计：概率哈希冲突符合泊松分布，链表长度达到 8 的概率极低，因此 8 是时间与空间的平衡阈值。64 的数组容量保证树化前有足够空间，避免数组过小导致的频繁树化冲突。
- JDK 1.7 死锁根源：采用头插法，并发 `resize()` 时，多个线程同时转移链表元素会导致环形链表，在 `get` 操作中陷入死循环（CPU 100%）。
- JDK 1.8 修复：改为尾插法，保持元素原有顺序，避免了环形链表；同时引入 `ConcurrentHashMap` 作为高并发替代方案。
</details>

**我的初答**：
**错漏点**：


### 题目3：try-catch-finally 中 return 与异常吞噬（字节码层面的执行顺序）
> 现有代码：`public static int test() { try { int a = 1/0; return 1; } catch (Exception e) { return 2; } finally { return 3; } }` 返回值是多少？若将 `finally` 中的 `return 3` 去掉，返回多少？请从字节码的 `athrow` 和 `ireturn` 指令顺序解释为什么 `finally` 会覆盖返回值或吞噬异常。追问：若 `finally` 块中也抛出异常，原始异常会如何处理（抑制机制）？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 完整代码返回 `3`（`finally` 中的 `return` 覆盖 `catch` 的 `return 2`）。
- 去掉 `finally` 的 `return` 后返回 `2`（异常触发 catch 块，`finally` 执行但不影响返回值）。
- 字节码执行顺序：
  1. `try` 块中 `1/0` 抛出异常（指令 `athrow`）。
  2. 匹配 `catch` 块，执行 `return 2`（但仅存储返回值，不会立即返回）。
  3. 执行 `finally` 块，若 `finally` 包含 `return`，则直接覆盖之前的返回值并跳转返回；若无 `return`，则按原存储值返回。
- 异常吞噬：
  - 若 `finally` 块内也抛出异常，`finally` 的新异常会完全替代 `try/catch` 中的原始异常，导致原始异常信息丢失（异常吞噬）。
  - 在 try-with-resources 中，通过 `Throwable.addSuppressed()` 机制，将 `close()` 抛出的异常附加到原始异常上，避免吞噬。
- 最佳实践：**禁止**在 `finally` 中使用 `return` 或抛出未处理异常。
</details>

**我的初答**：
**错漏点**：

---

## Day 26 (2026-08-20) —— 注解（Annotation）与JavaSE综合

### 题目1：注解的保留策略（RetentionPolicy）与反射获取的边界
> 请说明 `@Retention(RetentionPolicy.SOURCE)`、`CLASS`、`RUNTIME` 三者的区别。若自定义注解仅用于编译期代码检查（如 `@Override`），应使用哪种策略？在运行时通过 `Class.getAnnotations()` 获取注解时，为什么无法获取 `CLASS` 策略的注解？追问：`getAnnotations()` 与 `getDeclaredAnnotations()` 在返回范围上有何差异（是否包含父类注解）？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 三种策略差异：
  - `SOURCE`：仅在源码中保留，编译时丢弃（如 `@Override`、`@SuppressWarnings`）。用于编译器校验或代码生成器（如 Lombok 的 `@Data` 实际虽多为 `SOURCE`，但 Lombok 在编译期处理）。
  - `CLASS`：保留到字节码（.class）文件，但 JVM 类加载时忽略，运行时不可读。常用于字节码框架（如 ASM）或 APT（注解处理器）生成代码。
  - `RUNTIME`：保留到运行时，可通过反射读取。常用于框架配置（如 Spring 的 `@Component`、`@Autowired`）。
- 运行时不可读原因：`ClassLoader` 在加载类时，会将 `RetentionPolicy.CLASS` 的注解从字节码中丢弃（不加载到元数据区），因此反射 API 无法访问。
- `getAnnotations()` vs `getDeclaredAnnotations()`：
  - `getAnnotations()`：返回当前类及**父类**上所有 `RUNTIME` 策略的注解（若父类注解未被子类覆盖且未被元注解 `@Inherited` 限制）。
  - `getDeclaredAnnotations()`：仅返回当前类直接声明的注解，不包括父类。
</details>

**我的初答**：
1. SOURCE仅保留在源码中,CLASS会被写入字节码文件,但不会被加载到内存中,RUNTIME会被加载到内存,且能被反射调用.
2. 如果注解仅用于编译期代码检查使用SOURCE即可
3. 运行时无法获取CLASS策略的注解是因为,该策略的注解不会被加载到内存中,运行期不存在,自然无法获取.
4. getAnnotations()返回该对象上的所有注解.getDeclaredAnnotations()不了解.

**错漏点**：
1. `.class`文件**是会被加载进内存的**(类加载器读的就是它),CLASS策略的注解确实**写在字节码里,会被加载进内存**.
2. 真正的原因是: JVM在运行期**不向反射API暴漏**CLASS策略的注解.
3. 字节码中注解以`RuntimeVisibleAnnotations`(RUNTIME)和`RuntimeInvisibleAnnotations`(CLASS)两种属性存储.名字已经说明了一切: **对运行时不可见**----数据在,但`Class.getAnnotations`只读取Visible那张表.
4. 所以标准的表述应该是: **CLASS策略的注解保留在字节码中,但运行期JVM不会将其提供给反射,因此无法获取**.
- 第 2 点补充：@Override、@SuppressWarnings 确实都是 SOURCE，编译器用完即弃，连字节码体积都不占。

@Inherited  // 关键前提

  @Retention(RetentionPolicy.RUNTIME)

  @interface MyAnno {}

@MyAnno

class Parent {}

class Child extends Parent {}

| 方法                         | 范围                       |
| -------------------------- | ------------------------ |
| `getAnnotations()`         | 本元素上的所有注解 **+ 从父类继承的注解** |
| `getDeclaredAnnotations()` | **仅本元素上直接声明的注解**，不含继承    |
- 三个必须强调的边界条件
- 1.**继承的前提是注解本身标注了**`@Inherited`,否则`getAnnotations`也拿不到父类注解;
- 2.`@Inherited`**只对类(class)生效**-----方法、字段上的注解**永远不会继承**.子类重写父类方法时,方法注解不会被带下来
(这是Spring 早期的一个经典坑：`@Transactional` 标在接口方法上对 CGLIB 代理不生效的相关问题)
- 3.两者都只返回RUNTIME策略的注解,SOURCE/CLASS永远拿不到.

### 题目2：自定义注解结合动态代理模拟框架功能（AOP 前置日志）
> 定义注解 `@Log`（`RUNTIME` + `METHOD`），用于标记 Service 层方法。请结合 JDK 动态代理，编写 `InvocationHandler` 的逻辑：在调用被 `@Log` 标记的方法时，先打印 `"执行方法：" + methodName`，再执行目标方法。若目标方法未标记，则直接执行。追问：若使用 CGLIB 代理，判断逻辑是否需要调整？Spring AOP 默认如何区分代理方式？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 核心实现（InvocationHandler 片段）：
  public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
  // 注意：method 是代理接口上的方法，若目标类实现接口且使用 JDK 代理
  if (method.isAnnotationPresent(Log.class)) {
  System.out.println("执行方法：" + method.getName());
  }
  return method.invoke(target, args);
  }
- CGLIB 适配：
  CGLIB 代理时，Method 直接来自目标类（非接口），同样使用 `method.isAnnotationPresent(Log.class)` 判断，逻辑无需调整（但需注意代理方法的参数匹配）。
- Spring AOP 区分策略：
  - 默认：目标类实现接口 → JDK 动态代理；未实现接口 → CGLIB。
  - 可通过 `@EnableAspectJAutoProxy(proxyTargetClass = true)` 强制 CGLIB。
- 注：若 JDK 代理且 `Log` 注解标注在实现类的方法上而非接口方法上，`method.isAnnotationPresent` 将返回 false（因接口方法未继承注解）。需通过 `target.getClass().getMethod(...).isAnnotationPresent(Log.class)` 获取实际实现类的注解。
</details>

**我的初答**：
**错漏点**：


### 题目3：@Inherited 元注解的类继承陷阱与方法覆盖行为
> 父类 `Base` 被 `@MyAnnotation`（`@Inherited` 修饰）标注。子类 `Child` 继承 `Base` 但不添加任何注解。在子类上调用 `Child.class.getAnnotation(MyAnnotation.class)` 能否获取到？若在父类 `Base` 中有一个 `@MyMethodAnnotation` 标记的方法 `print()`，子类重写 `print()` 但不添加任何注解，调用子类 `print` 方法的 `Method` 对象获取 `MyMethodAnnotation` 会怎样？请解释 `@Inherited` 的适用范围。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 类级别结论：**能获取到**。`@Inherited` 元注解的作用就是让子类自动继承父类的类级别注解（前提是父类标注了该注解且子类未覆盖）。
- 方法级别结论：**不能获取到**。`@Inherited` 仅适用于 **类/接口**（即 `ElementType.TYPE`）的注解继承，不适用于方法、字段等元素。子类重写方法后，该方法的注解信息是独立的，父类方法上的注解不会被继承（除非框架如 Spring 使用 `AnnotatedElementUtils` 合并父类方法注解）。
- 底层根源：反射的 `AnnotatedElement.getAnnotation` 对于类会向上遍历父类检查 `@Inherited` 标记；但对于方法，反射不会向上搜索父类方法，需调用者自行递归获取。
- 最佳实践：若需继承方法注解，需自定义逻辑（如 `Spring` 的 `AnnotatedElementUtils.findMethodAnnotation` 支持向上查找）。
</details>

**我的初答**：
**错漏点**：

---

## Day 27 (2026-08-21) —— JavaSE 综合收官（集合、并发、Optional）

### 题目1：Optional 的 orElse() 与 orElseGet() 的求值时机及性能陷阱
> 现有代码：
> String result = Optional.ofNullable(getUser())
>     .map(User::getAddress)
>     .map(Address::getCity)
>     .orElse(fallbackCity());
> 如果 `getUser()` 返回 null，`fallbackCity()` 是否会被执行？如果将 `orElse` 改为 `orElseGet(() -> fallbackCity())`，执行逻辑有何不同？追问：在 Spring 的 SpEL 表达式或方法引用中，`orElse` 的这种急加载特性可能导致什么意想不到的副作用（如数据库查询或远程调用）？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- `orElse(fallbackCity())`：`fallbackCity()` 会被 **立即执行**（无论 Optional 是否包含值）。因为 Java 方法传参采用**急加载（Eager Evaluation）**，参数在传入方法前必须完成求值。即使 Optional 非空，`fallbackCity()` 也会被执行（浪费 CPU 或产生不必要的副作用）。
- `orElseGet(() -> fallbackCity())`：`fallbackCity()` 仅在 Optional 为 **空** 时才会被调用（**惰性加载**），避免不必要的开销。
- 副作用风险：若 `fallbackCity()` 包含远程调用、数据库查询或复杂计算，且 Optional 非空，`orElse` 会造成额外的系统负担和潜在的异常（如网络超时）。实战中应优先使用 `orElseGet` 或 `orElseThrow`。
- 特别注意：若 `fallbackCity()` 返回 null，两种方式均会赋值 null；若想抛出异常，需使用 `orElseThrow()`。
</details>

**我的初答**：
1.

**错漏点**：


### 题目2：synchronized 与 ReentrantLock 在等待中断与条件变量上的本质差异
> 请从以下两个维度深入对比 `synchronized` 和 `ReentrantLock`：1）当线程竞争锁失败时，`synchronized` 的阻塞状态（BLOCKED）不可被中断，而 `ReentrantLock.lockInterruptibly()` 可被中断，这种差异在分布式服务或长时间挂起任务中可能导致什么实际问题？2）`synchronized` 的 `wait/notify` 与 `ReentrantLock` 的 `Condition.await/signal` 在支持**多个等待集**方面有何不同？这如何影响生产者-消费者模式的设计？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 中断响应差异：
  - `synchronized`：等待锁时处于 `BLOCKED` 状态，完全不响应 `Thread.interrupt()`。在极端情况下（如死锁或持锁线程执行极慢），等待线程无法被外部信号唤醒，可能导致服务超时堆积。
  - `ReentrantLock.lockInterruptibly()`：在等待锁时响应中断，抛出 `InterruptedException`。允许调用者设置超时降级或取消任务，避免资源耗尽。
- 条件变量差异：
  - `synchronized`：每个对象仅有一个等待集（WaitSet），`notify/notifyAll` 无法区分被唤醒的线程类型，易导致“过早唤醒”或“全部唤醒”的性能损耗。
  - `ReentrantLock`：通过 `newCondition()` 创建多个 `Condition`（如 `notEmpty`、`notFull`），可精准唤醒特定等待队列（如 `signal()` 仅唤醒等待 `notEmpty` 的消费者），减少无效锁竞争，提升并发效率。
- 实践指导：高并发且有复杂等待条件（如限流器、连接池）时，优先选择 `ReentrantLock` + `Condition`。
</details>

**我的初答**：
1. 这种差异在分布式服务或长时间挂起任务中不了解.
2. ReentrantLock支持多个等待Condition而synchronized只有一个WaitSet.
3. 在生产者-消费者模式下notify随机唤醒WaitSet中的一个线程可能会导致生产者唤醒生产者,白白浪费一次唤醒,而ReentrantLock将生产者和消费之放入两个不同的Condition队列,signal只唤醒其中一种队列的头一个线程.

**错漏点**：

**一、可中断性的本质差异与实际问题**

**机制层面**

synchronized：竞争锁失败的线程进入 BLOCKED 状态，挂在监视器锁的 EntryList 里排队，对外部中断完全无感知。调用 thread.interrupt() 只会设置中断标志位，线程依然傻等，没有任何方式让它放弃抢锁。

ReentrantLock：默认 lock() 同样不可中断（这点很多人会答错），但它额外提供：

lockInterruptibly()：排队期间响应中断，抛出 InterruptedException 主动退出抢锁；

tryLock(timeout)：超时自动放弃，返回 false。

也就是说，差异的本质不是"一个能中断一个不能"，而是 ReentrantLock 把"放弃抢锁"变成了一个可编程的选项，synchronized 没有这个选项。

**实际问题(面试要答出场景,而不是背定义)**
1. **无法优雅关闭(最常被考)**
- 线程池`shutdownNow()`或收到停机信号时,靠`interrupt`唤醒工作线程.如果线程正BLOCKED在synchronized上,中断无效,线程不退出,JVM关不掉、资源不释放.微服务滚动发布时就表现为,老实例迟迟摘不掉流量.
2. **线程堆积 → 服务雪崩**
- 典型链路: 持有锁➡调用下游RPC➡下游挂起无超时➡所有后续线程BLOCKED在锁上.这些线程**不可中断、不可超时**,只能等第一个线程永远返回.线程池耗尽、请求堆积,内存打满.整个服务被一把锁拖死.用`tryLock(超时)`枪锁失败就快速失败+熔断.可以把这个故障域限制住.
3. **死锁无法外部打破**
- synchronized死锁后线程互相BLOCKED,只能kill进程.ReentrantLock可以配合`tryLock`超时放弃,回退重试,或者用`lockInterruptibly`让看门狗线程中断某个参与者来破坏.
- 一句话总结：synchronized 的抢锁是"单向门"——进了排队就出不来；ReentrantLock 给了你**超时和中断两个出口**，这在长耗时、可能挂起的分布式调用里是生死差别。

**二、等待集（Wait Set）差异与生产者-消费者设计**

机制层面

synchronized：每个 monitor 只有一个 wait set。wait() 进去的线程不分条件，全混在一个队列里。notify() 唤醒哪一个由 JVM 实现决定（规范上是不确定的），notifyAll() 全唤醒。

ReentrantLock：一把锁可以 newCondition() 出任意多个 Condition，每个 Condition 有自己独立的等待队列。signal() 只唤醒指定 Condition 里**等待时间最长的线程**（公平模式下严格 FIFO；不是简单地"唤醒头一个"，还取决于公平策略，你初答里这点可以修正）。

**对生产者-消费者的影响（以有界缓冲区为例）**

用 synchronized 写，"缓冲区不满"和"缓冲区不空"是两个等待条件，但只能共用同一个 wait set：

你的第 3 点说对了一半：notify() 可能生产者唤醒生产者。但真正的危害不是"浪费一次唤醒"，而是信号丢失：生产者唤醒了另一个生产者，对方一看缓冲区还是满的，继续 wait，这次 notify 就凭空消失了。极端情况下消费者全在睡、没人再发出唤醒 → 全体线程永久挂起（类似死锁）。

所以 synchronized 版本必须用 notifyAll() + while 循环条件判断，代价是惊群效应：每次唤醒所有线程，大多数发现条件不满足又睡回去，CPU 白白消耗在上下文切换上。



### 题目3：ArrayList 与 LinkedList 在内存布局与 CPU 缓存命中率上的硬件级差异
> 除了众所周知的查询（O(1) vs O(n)）和插入删除时间复杂度外，从**CPU 缓存行（Cache Line）**和**内存碎片**角度分析：为何即便在中间位置插入操作较多时，`ArrayList` 的实际执行速度仍可能快于 `LinkedList`？请结合循环遍历（for-i vs 迭代器）在不同集合上的表现差异说明现代硬件对顺序内存访问的偏好。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 内存布局与缓存局部性：
  - `ArrayList`：底层连续内存块（数组），遍历时 CPU 会预加载连续数据到缓存行（Cache Line，通常 64 字节）。相邻元素的访问命中率高（空间局部性），迭代速度极快，且 `for-i` 与迭代器性能相近。
  - `LinkedList`：节点分散在堆内存各处（非连续），通过指针关联。遍历时每次 `next` 都需要通过指针跳转，无法利用 CPU 预取机制，且每次访问易引发 Cache Miss（缓存缺失），性能远低于 ArrayList。
- 插入操作隐藏开销：
  - `ArrayList` 中间插入需调用 `System.arraycopy` 移动元素，但该操作在连续内存上执行，JVM 底层高度优化（使用 CPU 向量指令），且仅复制引用（非对象本身），速度远高于预期。
  - `LinkedList` 中间插入虽仅改指针，但需先遍历到指定位置（O(n) 且含有随机指针访问，Cache Miss 严重），其总耗时通常劣于 `ArrayList`。实测数据量小于 10 万时，`ArrayList` 在绝大部分场景均优于 `LinkedList`。
- 结论：除头部频繁插入/删除的队列/双端队列场景外，始终优先使用 `ArrayList`。`LinkedList` 在 Java 生态中已被视为“效率极低的集合”。
</details>

**我的初答**：
**错漏点**：

---

## Day 28 (2026-08-22) —— JavaSE 高阶特性收官（枚举单例、反射泛型、动态代理对比）

### 题目1：枚举实现单例模式的原理与序列化安全保障（反序列化与反射攻击）
> 使用枚举（`enum`）实现单例被《Effective Java》推荐。请说明枚举单例如何保证实例唯一性：1）JVM 对枚举常量实例化的底层机制（静态初始化+`<clinit>`线程安全）；2）为何枚举能天然防止反射攻击（`newInstance()` 对枚举类的限制）；3）为何枚举单例在反序列化时不会产生新实例（`readEnum` 方法的行为）。追问：若枚举单例中有需要初始化的资源（如数据库连接），应在哪个位置初始化？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 实例化机制：枚举常量是 `static final` 字段，在类加载的初始化阶段（`<clinit>`）由 JVM 创建并赋值，由 `ClassLoader` 保证线程安全。
- 反射防御：`Constructor.newInstance()` 会检查 `Modifier.ENUM`，若为枚举类则直接抛出 `IllegalArgumentException`，无法通过反射创建新实例。
- 序列化安全：`ObjectInputStream.readEnum()` 返回枚举常量（通过 `Enum.valueOf`），不会调用构造器或 `readObject`，因此反序列化不会创建新对象。
- 资源初始化：应在枚举的构造器或 `static` 块中完成（如 `INSTANCE` 构造时），但注意枚举构造器在类加载时执行一次，适合初始化无状态资源。若有复杂依赖，可考虑在 `getInstance()` 方法中懒加载。
</details>

**我的初答**：
**错漏点**：


### 题目2：反射获取泛型类型参数（ParameterizedType 与 TypeVariable 的应用）
> 现有泛型类 `GenericDao<T>`，子类 `UserDao extends GenericDao<User>`。请通过反射获取 `UserDao` 父类 `GenericDao` 的泛型参数 `User.class`。请写出完整代码（使用 `getGenericSuperclass` 和 `ParameterizedType`）。追问：若 `UserDao` 本身也是泛型（`UserDao<T>`），则获取 `T` 对应的具体类型（如运行时传入 `User`）时，需要额外处理 `TypeVariable`，请简要说明如何实现（提示：需从子类继承链逐层解析）。

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 标准实现（获取直接父类的实际类型参数）：
  public Class<?> getSuperClassGenericType(Class<?> clazz) {
  Type type = clazz.getGenericSuperclass();  // 获取带泛型参数的父类
  if (type instanceof ParameterizedType) {
  ParameterizedType pType = (ParameterizedType) type;
  Type[] actualTypes = pType.getActualTypeArguments();
  if (actualTypes.length > 0 && actualTypes[0] instanceof Class) {
  return (Class<?>) actualTypes[0];
  }
  }
  return null;
  }
  // 使用：UserDao 中调用 getSuperClassGenericType(UserDao.class) 返回 User.class
- 多层泛型解析（若子类仍为泛型）：
  当 `UserDao<T>` 继承 `GenericDao<T>` 且具体使用时为 `new UserDao<User>()`，此时 `UserDao` 本身未确定 `T`，需进一步获取子类实例的 `Type`。利用 `getClass().getGenericSuperclass()` 只能获取到 `GenericDao<T>`（`T` 为 `TypeVariable`），需借助子类的具体化信息（如 `new TypeReference<User>()` 或通过 `getActualTypeArguments` 后递归判断）。
</details>

**我的初答**：
**错漏点**：


### 题目3：JDK 动态代理与 CGLIB 的底层实现对比（字节码生成策略与性能取舍）
> 请从以下维度对比两种代理机制：1）生成代理类的字节码方式（JDK 通过 `ProxyGenerator` 生成 `.class` 字节码，CGLIB 通过 ASM 框架直接操作字节码）；2）代理类与被代理类的关系（接口实现 vs 子类继承）；3）对 `final` 方法和 `static` 方法的处理差异；4）在 JDK 1.8 及之后，两者在方法调用性能上的差距是否还显著？追问：Spring AOP 中，为何默认采用 JDK 动态代理？在什么情况下必须强制使用 CGLIB？

<details>
<summary><strong>点击展开标准解析</strong></summary>

- 字节码生成：
  - JDK：使用 `sun.misc.ProxyGenerator` 生成代理类的字节码数组，通过 ClassLoader 定义类。
  - CGLIB：使用 ASM 库直接生成目标类子类的字节码，更灵活但依赖第三方库。
- 代理关系：
  - JDK：必须实现接口，代理类实现了所有接口，通过 `InvocationHandler` 分发调用。
  - CGLIB：通过继承目标类，重写非 `final` 方法实现增强，无法代理 `final` 类和 `final` 方法。
- `static` 方法：两者均无法代理（静态方法属于类，不属于实例，调用不经过代理对象）。
- 性能差异（JDK 1.8+）：
  - 两者方法调用性能已趋于接近（JDK 对反射做了优化，如 `MethodHandle`），但 CGLIB 创建代理对象更慢，JDK 创建更轻量。
  - 在方法调用次数极高（> 百万级）时，CGLIB 可能稍快，但差距已不再显著（约 10%~20%）。
- Spring AOP 默认策略：
  - 若目标类实现至少一个接口，默认使用 JDK 动态代理（更轻量，符合面向接口编程）。
  - 若目标类未实现接口，则自动回退到 CGLIB。
  - 强制使用 CGLIB：通过 `@EnableAspectJAutoProxy(proxyTargetClass = true)` 或配置 `spring.aop.proxy-target-class=true`，适用于需要代理所有方法（包括非接口方法）或无法修改接口的场景。
</details>

**我的初答**：
**错漏点**：

---

