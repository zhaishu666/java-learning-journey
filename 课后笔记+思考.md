## Day 01 (2026-7-22)
### 今天学习了TreeMap以及HashMap的源码
> TreeMap能干什么?怎么用?
1. TreeMap是基于红黑树的Map的实现类,它的特点主要是Key有序,增删改的时间复杂度为O(log n),且不允许null key
> TreeMap是怎么实现的?
1. 底层数据结构与TreeSet相同,都是红黑树
2. 每个节点都是entry结构
>为什么这样设计,解决了什么问题?
1. 设置底层为红黑树的集合,可以对添加的对象进行排序,我们可以实现Comparable接口或者在创建集合时传递Comparator对象,指定比较规则
2. 对于只需要添加和删除的情况下,我们可以使用HashMap,对于需要key有序或者范围查询时TreeMap更合适

## Day 02 (2026-7-23)
### 今天学习了Collections工具类
> Collections能干什么?怎么使用?
1. Collections是集合的工具类,提供工具方法帮助我们修改集合,想要使用Collections直接类名加.调用内部方法就行
> Collections是怎么实现的?
1. 排序和查找:sort(list) — 底层是 TimSort（归并排序优化）
   O(n log n)，稳定排序，对已部分有序的数据特别快
   binarySearch(list, key) — 二分查找 O(log n)
   前提：list 必须已排序
   reverse / shuffle / swap — O(n) 操作
   frequency / disjoint — 统计与判断
> 为什么这样设计,而不是设计成接口?
1. 工具类更强调怎么操作集合,JDK8以前接口中只允许有抽象方法,如果是接口就需要实现类实现里面所有的方法

##  Day 04 (2026-7-25)
### 入侵性
> 入侵性的概念是什么?
1. 入侵性简单来说就是达成某个行为,是否需要修改源代码
2. 细致的来说,就是框架/库迫使你的代码依赖它的程度
> 入侵性怎么降低?
1. (中等入侵) 实现接口分离,比如implement Comparable的接口
2. (低入侵) 配置文件/外部装配 比如框架通过 XML / 配置文件从外部注入依赖,业务代码完全不知道框架的存在
3. 注解 + 约定(低入侵但更现代)
> 为什么这样设计?
1. 为什么 Spring 要花大力气追求低入侵性？
- Spring 出现的年代（2003），主流是 EJB——高入侵性的代表。EJB 的问题：你的业务代码必须 import 一堆 javax.ejb.*，实现 SessionBean 接口，代码只能在 EJB 容器里跑。\
- 本地测试要启动容器，迁移到别的框架等于重写。Spring 的核心卖点就是"让你的业务代码保持纯净 POJO，框架通过 IoC 容器从外部管理依赖"。这样代码可测试（单元测试直接 new 对象）、可迁移（换框架不影响核心逻辑）、可复用（业务逻辑和框架解耦）

### 创建不可变集合
> 为什么要创建不可变集合?
1. 当我们不想别人去修改某个数据,把它防御性地拷贝到不可变集合中是个不错的选择
2. 当我们创建了一个集合,但不希望后面其他调用者去修改这个集合里的内容

> 怎么去创建一个不可变集合?
1. 直接使用List,Set,Map接口中的of方法就可以实现,其中Set和Map的of方法创建的对象是不允许有重复Key的,否则会抛出IllegalArgumentException
2. 其中Map的of方法最多只能传10个键值对,可以使用ofEntries传多个键值对对象,或者使用copyOf方法

>为什么要这样设计?直接用数组不行?
1. 数组缺点：
-   元素可修改
   只要拿到数组引用，任何人都能修改数组内部元素，无法对外暴露一份 “只读视图”。
-   没有统一集合接口
   数组不能直接传入接收 Collection/List 的方法，需要手动转换，无法配合集合流、工具类。
-   无法禁止 add/remove（本身就没有这些方法），但语义模糊
   别人看到数组，无法区分设计者意图：是允许修改元素，还是只是固定长度？
-   不能轻易返回安全副本
   如果你把内部数组直接 return 出去，外部篡改会直接影响类内部数据（破坏封装）。
2. 不可变集合外部无法修改,不可变对象天然安全,多线程情况下不需要加锁
3. 不可变 List/Set/Map 实现了 Collection 接口，可以直接用于 Stream、Collections 工具、各种接收集合的框架方法

## Day 05 (2026-7-26)
### Stream流
> Stream流是什么?Stream流有什么作用?
1. Stream流是Java设计的一种用于处理单列集合的一种方式,Stream流可以帮助我们过滤集合中不符合条件的元素,以及对stream流中元素进行处理,我们可以通过Stream流来获得一个新的符合条件的集合

**修正版**
-  "单列集合"不准确
- 漏了最核心的特征——声明式
-   Stream 是 Java 8 引入的用于处理元素序列的 API。它的核心特征是**声明式**——你描述"我要什么结果"，而不是"怎么一步步得到结果"。Stream 本身不存储数据，它只是对数据源的一个"流水线视图"。
> 什么样去使用Stream流?
2. 我们可以直接通过集合.调用Collection中的stream()方法直接获得stream的对象,然后对这个对象进行些处理
- 比如使用中间方法filter,limit,skip,distinct,map等,达到条件后使用终结方法forEach循环输出打印,count统计集合中的元素个数,或者collect将流中对象存储到集合中

**修正版**
1. 通过 Collection.stream() 或 Arrays.stream() 创建 Stream。
- 分类对了，但漏了一个关键概念——惰性求值
- 操作分两类：中间操作（filter/map/sorted/distinct/limit/skip）是**惰性的**——调了不执行，只是记录到流水线；终结操作（forEach/collect/count/reduce）才触发整条流水线执行。
- Stream 是一次性的，终结操作后不能复用

> 为什么要设计Stream流?Stream流中的很多功能也能直接通过遍历循环再进行判断实现啊?
1. Stream流处理更加便捷高效,并且不会修改原来集合中的内容

**修正版**
1. 设计Stream的核心原因是引入**声明式编程**-----用filter+map+collect来描述"我要什么",替代for+if的"怎么做"
- 好处有三: 
- 一是可组合,操作链式拼接,复杂的数据处理一行搞定
- 二是可并行,parallelStream()自动利用多核CPU,for循环要自己写线程
- 三是函数式风格,配合Lambda可以把行为当参数传
2. 注意Stream不一定比for高效,Stream的优势在代码可读性和开发效率

## Day 06 (2026-7-27)
### 方法引用
> 什么是方法引用?方法引用有什么作用?
1. 方法引用就是通过,引用本类或者其他类中的方法来来达成我们想要的目的,以减少重复代码的书写,加快开发效率,使代码更加简洁的方式

**修正版**
- 漏了一句关键的话: 方法引用是Lambda的进一步简化
- 记住一句话: 当Lambda只是调用一个现成的方法时,用方法引用代替Lambda,更短更清晰


> 怎么样去使用方法引用?
1. 只有当方法的参数形式是函数式接口时才可以使用,对于其他类,构造方法,静态方法,以及实例方法有不同的形式.
- 但所引用的方法的参数类型,返回值都必须与抽象方法一致,且引用的方法必须符合抽象类的目的
- 但对于类名直接引用,如 String::toUpperCase toUpperCase参数列表中的第一个形参可以为空,抽象类第一个形参隐式被方法调用者的对象

**修正版**
1. 参数类型必须完全相同,但返回值有个例外: 当抽象类中返回值为void时,引用的方法可以有返回值,只是被忽略
- 问题2："对于类名直接引用，如 String::toUpperCase，第一个形参可以为空"——表述不够清楚。
- 这个是方法引用中最难理解的一种形式，叫**"类名::实例方法"**。规则是：
- 函数式接口的第一个参数，变成了实例方法的调用者（receiver）
- // String::toUpperCase 是"类名引用实例方法"
- // 等价于 Lambda: (String s) -> s.toUpperCase()
- Function<String, String> f = String::toUpperCase;
- // 第一个参数 "hello" 变成了 toUpperCase() 的调用者
- String result = f.apply("hello");  // "HELLO"
-  这里不是"第一个形参为空"，而是函数式接口的第一个参数被"借"去当了方法调用者


> 为什么要设计方法引用? Lambda表达式其实已经够简洁了,难道也是为了引入声明式? 方法引用底层到底是怎么样执行的呢?
1. 如果我们有多个Lambda表达式执行的是同一个操作,而且这个操作需比较多的代码,我们将这些操作抽取成方法,再使用方法引用的格式,将会减少不少代码的书写,也体现了Java语言面向对象的特征
2. 底层是调用了对应的方法,通过方法的return实现的吗?

**修正版**
1. 问题1："体现了Java语言面向对象的特征"——说反了
- 方法引用和Lambda表达式一样,是Java向函数式编程靠拢的特性,不是面向对象的体现.面向对象的特征是封装,继承,多态.
- 方法引用的本质是把方法当作值来传递----这恰恰是函数式编程的核心思想.Java8引入这些特性,是为了让Java也能支持函数式编程风格
2. 底层不是简单的"调用方法+return"。方法引用在编译后和 Lambda 一样，生成 invokedynamic 指令，运行时由 JVM 动态生成函数式接口的实现类。这个实现类的方法体就是"转发调用"到被引用的方法
3. **总的来说** 设计方法引用的核心原因是：当一个 Lambda 的函数体仅仅是调用一个已存在的方法时，用方法名替代 Lambda 体，代码更短、意图更清晰、能直接复用已有逻辑。
底层和 Lambda 一样，编译为 invokedynamic 指令，运行时 JVM 动态生成函数式接口的实现对象，方法体转发调用到被引用的方法。方法引用是 Java 引入函数式编程风格的一部分，不是面向对象特性的体现

## Day 07 (2026-7-28)
### 异常
> 什么是异常? 抛出异常有什么作用?
1. 异常就是程序运行时出现的错误,分为严重的Error以及Exception,其中Error通常是系统性的问题,不需要我们程序员去捕获.
2. Exception又分为编译期异常以及运行期异常,在执行代码时抛出异常可以帮助程序员去更好的理解运行时出现的问题的原因

**错漏点**
1. 问题1：异常不只是"运行时出现的错误" 编译期异常在编译时就被检查，还没运行就要求你处理，它不一定是"运行时错误"。更准确的说法：
- 异常是程序执行过程中出现的**非正常情况**,JVM会创建一个异常对象来描述它.异常机制让程序有机会优雅地处理这些问题,而不是直接崩溃
2. 缺少异常体系的全貌 Throwable: 所有异常的根,RuntimeException运行时异常(非受检),编译期异常(受捡)


> 怎么样抛出异常,以及我们怎么捕获异常,具体怎么实现?
1. 我们可以对方法内部可能出现异常内容的代码进行判断,如果不符合条件,就通过throw手动抛出异常.
2. 对于编译期异常必须在方法名后加throws来声明可能抛出的异常,而运行期异常可以忽略不写
3. 我们捕获异常的目的是为了让代码可以继续执行下去.我们捕获异常使用try-catch语句,判断在try中出现的异常能否与catch()中的异常类型相匹配
如果匹配,就不会执行try中的后续的语句,直接执行catch中的语句,如果try中可能存在多种异常,我们就需要使用多个catch语句或者JDK7一个catch中写 | 判断符合哪一个异常
值得注意的是一个try语句中最多只能抛出一个异常,因为try中后续的代码即使有异常也不会执行

**错漏点**
* 你写的内容基本对，漏了一个重要的东西——finally. 
finally {
   // 无论有没有异常，都会执行
   // 用来关闭资源（文件、数据库连接等）
   }
- finally的核心用途: 资源清理. 无论 try 里有没有异常、catch 有没有匹配到，finally 都会执行。JDK 7 之后有了 try-with-resources，自动关闭实现了 AutoCloseable 的资源，更简洁:
- // JDK 7+，自动关闭，不需要 finally
-  try (FileReader fr = new FileReader("a.txt")) {
-  // 读文件
-  } catch (IOException e) {
-  // 异常处理
-  }
-  // fr 自动关闭了，不用手动 close

2. 你说"捕获异常的目的是让代码继续执行下去"——这只是一种情况。捕获异常的目的可能是：

- 恢复执行（你的情况）
- 记录日志后重新抛出（让上层处理）
- 包装成更有意义的异常再抛（异常转换）
- 清理资源后退出

> 为什么要设置异常这个概念?对于语句符不符合我们可以通过if语句判断,filter对流中的数据进行过滤都是可行的啊?异常的底层代码到底是如何执行的?
1. 不太了解

**错漏点**
所以异常解决的四个核心问题：
异常解决的四个核心问题

| 问题 | if-else 的困境 | 异常怎么解决 |
|------|----------------|--------------|
| 传播 | 每层都要手动检查返回值并传递 | 异常自动沿调用栈向上传播，中间层可以完全不管 |
| 不可忽略 | 返回值 -1 你可以假装没看见 | 受检异常编译器强制你处理或声明，想忽略都忽略不了 |
| 类型安全 | 错误码是 int，没有类型区分 | 每种异常是一个类，catch 可以精确匹配不同类型 |
| 信息携带 | 只能返回一个数字 | 异常对象携带消息、调用栈、cause 链，精确到哪一行出的错 |


2. 底层怎么执行的？
你问的"底层代码如何执行"：

//1. throw new FileNotFoundException("xxx")
   → JVM 在堆上创建一个异常对象（和 new 普通对象一样）

//2. JVM 从当前方法开始，沿调用栈往上找匹配的 catch
   start() → processData() → readFile()  ← 抛出点
   JVM 从 readFile 往上找：
   readFile 没有 try-catch → 继续
   processData 没有 try-catch → 继续  
   start() 有 try-catch → 匹配 FileNotFoundException → 执行 catch

//3. 如果一路找到 main() 都没有匹配的 catch
   → 交给 JVM 默认异常处理器
   → 打印堆栈轨迹（就是你在控制台看到的红色报错）
   → 终止当前线程

- 关键点：异常处理不是"跳转"，是"栈展开"（Stack Unwinding）。 JVM 一层一层退栈，每退一层检查有没有 catch，找到就跳进去执行，找不到就继续退。这个过程是 JVM 层面支持的，不是 Java 语法层面模拟的。

## Day 08 (2026-7-29)
### File类

> 什么是File类,file类对我们有什么作用呢?
1. File类是Java提供的帮助我们操作文件及文件夹的javabean类.
2. 我们可以通过new File类型的对象来让我们操作指定路径的文件

**错漏点**
- 问题1："javabean类"用错了。
JavaBean 是一个有严格定义的概念——私有字段 + getter/setter + 无参构造 + 实现 Serializable。
File 类不符合这个规范。准确说法：File类是Java提供的用于表示文件和目录路径的类
- 问题2：漏了 File 最本质的特征——它只是路径的抽象，不代表文件真实存在
  这个特征是理解 File 类的关键：File 对象 ≠ 文件本身。它只是一个路径的"名片"，名片上写着地址，但地址那头的房子可能还没盖。

> 我们该怎么样使用File类?
1. 创建File类的对象,调用里面对应的方法就可以了,如getPath(),length(),以及创建文件文件夹的createNewFile(),mkdirs(),还有获取遍历常用的listFile()

**错漏点**
- 两个容易踩的坑：
- 坑1：listFiles() 对不存在的目录返回 null，不是空数组
- 坑2：delete() 只能删空目录。 目录里有内容就删不掉，得先递归删里面所有文件再删目录。这也是很多面试题的考点


> 为什么要设计File类,目前好像并没有学到啥要操作外部文件的内容?当我们使用File对象时JVM底层是如何实现它的呢?
1. 没有头绪

**错漏点**
1. File 类是 IO 流的前置基础，你马上就要用到了
- 设计File类的原因1: 跨平台抽象
- 不同操作系统的路径规则完全不同,如果没有 File 类，你写 C:\Users\xxx 到 Linux 上跑就废了。File 类把这些差异封装掉了
- 原因2: 路经管理和内容读写分离
- 这是最重要的设计思想：File 类不负责读写文件内容，你不能用 File 对象去读文件里写了什么——那是 IO 流的活
2. 底层怎么执行?
- File 类内部就存了一个 String 类型的 path 字段。真正干活的方法都是 native 方法，调用的是操作系统的文件系统 API：
- Java 代码: f.exists()
  ↓
  JVM 调用 native 方法 (C++ 实现)
  ↓
  Windows: 调用 Win32 API GetFileAttributesW()
  Linux:   调用 POSIX access() 函数
  ↓
  操作系统检查文件系统，返回结果
- 所以 File 类本质上是 Java 对操作系统文件系统 API 的一层封装，让你不用关心底层是 Windows 还是 Linux，用同一套 Java 代码操作文件。

## Day 09 (2026-7-30)
### IO流第一天(学习了IO流的基本概念,FileOutputStream,FileInputStream以及如何write()时换行和续写)
> 什么是IO流?IO流对于我们程序员开发有什么作用?

**我的理解**
1. IO流是Java提供的存储和读取数据的解决方案,在昨天所学的File类中,我们虽然可以文件整体进行一些操作,但无法修改里面的内容,IO流就是解决这些的方案
2. 目前学习进度有限,不过我认为IO流对程序的主要作用应该是能够帮我们存储和读取进度,如游戏进程.或对某些数据进行修改

**错漏点**
1. "存储和读取数据的解决方案"——太笼统，没说到"流"这个核心概念。
- "流"不是比喻，是实际机制：数据像水流一样，一个字节一个字节地从一端流向另一端，不是一次性全部搬运。这就是为什么叫"流"而不是叫"文件操作工具"。
2. "修改数据"不准确,IO流只能进行读出来,写进去的操作,要想修改里面的内容就需要把文件完整的读出来,然后在内存中改,最后重新写回去
3. 漏了IO流的分类体系,IO流分为字节流和字符流. 字节流:OutputStream,InputStream. 字符流: Write和Reader

> IO流是怎么实现的呢?对于前面的Stream流也一样,虽然流,流水线之类的名称很贴切,但没看到具体的代码实现让人对自己的理解底气有点不足
1. 我的理解: Java底层创建了与对应文件的链接,让我们可以直接操作文件,我们操作完成后需要释放资源,防止内存溢出

**错漏点**
1. "创建了与对应文件的链接"方向对,但不够准确,流对象内部持有的是一个操作系统级别的`文件描述符`,操作系统打开对应的文件表项找到文件
2. "防止内存溢出"不完全对,释放的是文件描述符,操作系统对每个进程同时打开的文件数有上限有上限,你不close(),它就一直占着,超过了就直接Too many open Files,程序直接崩溃

> 为什么要设计IO流?而不是其他的,比如专门操作Input和Output的工具类
1. 我的理解: 设计IO流是为了让我们更好的操作Input和Output,通过创建不同的流的对象,通过对应的方法来进行操作

**错漏点**
1. 你的回答是循环论证： "设计 IO 流是为了更好地操作 Input 和 Output"——这等于说"设计汽车是为了更好地开车"。没有回答到点上。
2. **真正的理由有三个**：
- 理由1: 流式处理——再大的文件也不会撑爆内存: 如果没有流,你读取一个10GB的文件需要10GB的内存,而流可以让我们控制每次读多少,逐部份读入
- 理由2: 统一抽象——同一套API适配所有数据源: 你学会了 InputStream，就同时学会了文件读取、网络读取、键盘输入——因为 API 完全一样，都是 read()。 如果每种数据源都设计一套工具类，你要学 N 套 API。
- 理由3: 装饰器模式——流可以套娃增强功能: 一层套一层,每层加一个功能,互不干扰.这就是设计"流"这个抽象的原因,而不是工具类,因为只有对象才能被包装,静态工具方法无法套娃

## Day 10 (2026-7-31)
### IO流第二天(学习了字符集,字符流的基本概念,FileReader,FileWriter)
> 什么是字符集,字符流?字符流与前面学习的字节流有什么区别? 

我的回答:
1. 字符集是计算机编码和解码的"模板",计算机通过解码后对应的字符集的码数寻找对应的字符,以及将字符编码成对应的码数,再转化为二进制存储
2. 字符流主要操作对应的字符,它本质上就是字节流+字符集,底层操作依靠的就是字节流.
3. FileReader和FileWriter是操作文件的输入和输出的实现类,我们可以通过创建对应的对象来操作文件
4. 字符流可以自动识别对应的字符,比如再使用GBK字符集时,字符流识别到中文字节时自动读取两个字节

**错漏点**
1. "码数"应该是"码点（Code Point）"。 术语要准确，面试时用错术语会扣分
2. 漏了 ASCII 和 Unicode 的关系,Unicode怎么诞生的要搞清楚

> 字符流是怎么实现英文一次读一个字符,中文一次读多个字符的?

我的回答:
1. 根据二进制数开头是1还是0判断是否为中文,1开头的是中文,再按照对应的字符集决定一次读取多少字节

**错漏点**
1. 你说"根据二进制开头是1还是0判断"——方向对，但只对了一半。
这个机制取决于用什么字符集编码，不同字符集的判断方式不同：
- GBK 编码规则：
-  英文：1字节，最高位是0，范围 0x00-0x7F
-  中文：2字节，第一字节最高位是1（范围 0x81-0xFE）
- UTF-8 编码规则（变长）：
-  英文：0xxxxxxx          → 1字节，0开头
-  中文：1110xxxx 10xxxxxx 10xxxxxx → 3字节，1110开头
-  Emoji：11110xxx ...     → 4字节，11110开头
- 核心：字符流不是"猜"有几个字节，是根据编码规范的前缀位精确判断的

> 字符流能帮助我们更好地读取和输出中文,那还会用到字节流吗?

我的回答:
1. 我们应该在开发时很少用到字节流了,可能在只操作英文时会使用到字节流

**错漏点**
1. 实际情况完全相反：实际开发中字节流用得比字符流多得多

|数据类型| 	该用什么流|	例子|
|:---|:------|:---|
|纯文本文件| 	字符流  |	.txt / .csv / .java / .xml / .json|
|非文本文件| 	`字节流`  |	图片、视频、音频、压缩包、Excel、PDF|
|网络传输| 	`字节流`  |	HTTP请求体、Socket通信、文件上传下载|
|序列化| 	`字节流`  |	对象序列化 ObjectOutputStream|

核心原则记住一句话：

文本数据用字符流，二进制数据用字节流。判断标准是"文件能不能用记事本打开还能看懂"——能看懂的是文本，看不懂的是二进制。

- 一句话总结第三层：字符流不是替代字节流的，是字节流在"文本处理"场景的增强版。底层还是字节流，只是包了一层自动编解码的逻辑。非文本数据永远用字节流。

## Day 11 (2026-8-1)
### IO流第三天(字符流的原理,缓冲流,以及一些练习)
> 字符流的缓冲区是什么?缓冲流是什么?

**我的回答**
1. 在读取数据时,字符流会创建一个默认长度为8192字节的字节数组,这就是缓冲区.
2. 缓冲流是Java提供的用来包装基本流的方式,它内部包装了更大的缓冲区,可以提高字符流的读写效率

**错漏点**
1. 问题1：把两层缓冲区混为一谈了
-字符流里有两个完全不同的缓冲区，你把它们当成了一个：

|缓冲区| 在哪里| 类型    | 大小    |作用|
|:----|:-----|:------|:------|:---|
|缓冲区①| InputStreamReader（FileReader 的父类）内部| byte[]| 8192 字节|从 FileInputStream 读一批字节，用于解码成字符|
|缓冲区②| BufferedReader 内部 | char[]| 8192 字符|从下层 Reader 读一批字符，减少 I/O 调用次数|

- 你说的"8192字节的字节数组"指的是缓冲区①——但这是 FileReader 自带的，不是缓冲流加的。缓冲流（BufferedReader）加的是缓冲区②，而且它是 char 数组不是 byte 数组。
- FileReader自带的缓冲区主要用于解码,如果频繁调用read()(一次读取一个字符),每次都会向上通知解码器尝试获取字符,会频繁触发解码逻辑,频繁调用底层读取
即使底层有byte缓冲,大量微小粒度的read()依然开销巨大
- BufferedReader 的 char [] 缓冲区作用：一次性批量读取字符存入char数组,后续就算多次read(),readLine()都直接从char数组中读取,减少频繁调用StreamDecoder读取和解码的次数

问题2：`"更大的缓冲区"是错的`。 BufferedReader 默认 char[8192]，跟 InputStreamReader 的 byte[8192] 差不多大。它不是靠"更大"提速，而是靠减少底层调用次数提速——下面第二层详细说。

> 字符流如何利用缓冲区实现数据读写的?缓冲流内部是怎么来包装的?

**我的回答**
1. 字符流的输入流会先将磁盘中的数据读到缓冲区中,然后通过read()方法将数据读取出来,再通过write()方法写入输出流的缓冲区中,然后通过缓冲区持久化到硬盘中
只有当缓冲区中的数据全部被读取后,缓冲区才会重新从磁盘中读取数据
2. 缓冲流内部把基本流中的字节数组替换为了一个更大的字节数组,以此提高效率

**错漏点**
1. 问题1：读写流程混在一起了，而且"持久化到硬盘"术语不对。
应该是 flush（刷出），不是"持久化"。更关键的是——你漏了一个能让你程序出 bug 的概念：
缓冲输出流必须 flush 或 close，否则数据可能还留在缓冲区里，没写到文件！
2. 问题2："替换为更大的字节数组"是错的——BufferedReader 不是"替换"，是"包装"。
BufferedReader 内部持有的是一个 Reader 对象（就是你传给它的 FileReader），它不会去改 FileReader 内部的东西。
它只是在自己的 read() 方法里，一次性从 FileReader 读一批字符存到自己的 char[] 里，然后你每次调 read() 都从它的 char[] 里取。
- 这叫装饰器模式——包一层，加功能，不改动被包装的对象

> 为什么缓冲区是这样设计的以及为什么要设置缓冲区?那这跟我们在读取是read()形参中创建的数组有何区别?

**我的回答**
1. 设置缓冲区是为了提高内存与磁盘之间交换数据的速度,数组长度设置为8192字节可能是比较常用
2. read()形参创建的数组是为了提高内存内部的数据传输速率

**错漏点**
1. 问题1："8192可能是比较常用"——你应该追问为什么。
8192 字节 = 8KB = 2 个操作系统内存页（每页 4KB）。磁盘 I/O 以页为单位，一次读 8KB 刚好对齐两个页，不会浪费也不会多跑一趟。这是经过工程验证的最优值，不是随便选的
2. 问题2：核心原因不是"提高速度"，是"减少次数"。
- 磁盘慢不是因为单次读取慢，是因为每次找数据要寻道。读 1 个字节和读 8192 个字节，寻道时间几乎一样。所以：
- 不用缓冲区：读 8192 个字符 → 调 8192 次 read() → 8192 次磁盘寻道 → 慢死
- 用缓冲区：  读 8192 个字符 → 调 1 次底层 read → 1 次磁盘寻道 → 快 8192 倍
- "减少 I/O 调用次数"才是缓冲区的核心价值，不是"提高传输速度"。
3. "提高内存内部传输速率"方向对但不精确

## Day 12 (2026-8-2)
### IO流第四天(学习了转换流,序列化流与反序列化流)
> 什么是转换流,什么是序列化与序列化流?

**我的回答**
1. 转换流是字节流与字符流之间的桥梁,可以通过转换流包装字节流,再用字符流包装,达到字节流使用字符流方法的作用.FileReader继承于转换流InputStreamReader
2. 序列化就是把数据转化成二进制流后输出,通常用于数据保存.对象想要实现序列化必须实现Serializable接口,且序列化后的数据不允许改变
,否则反序列化会出错.如果想要修改Javabean类而反序列化不出错,就需要给Javabean类添加serialVersionUID版本号
3. 如果想要Javabean类中的某个成员变量不被序列化,可以使用transient修饰符修饰

**错漏点**
1. 问题1："序列化后的数据不允许改变"——理解偏差。

- 不是"不允许改变"，而是：
- 序列化文件你当然可以改——用文本编辑器打开就是一堆乱码
- 你改了也白改，因为反序列化时按二进制协议解析，你手动改了字节，协议解析不出来就会报错
- 真正的问题是你改了 JavaBean 类本身（比如加了个字段），再去反序列化以前存的文件——类的结构变了，JVM 认为"这不是同一个类了"，就抛 InvalidClassException
- serialVersionUID 的作用：告诉 JVM "只要这个版本号没变，就算类加了字段，也是同一个类，别报错"。JVM 会把文件里有的字段读出来，类里新增的字段用默认值

> 转换流是怎么实现流的转换的?序列化流与反序列化流是怎么工作的来实现序列化与反序列化?

**我的回答**
1. 不了解

**错漏点**
1. 转换流怎么实现"字节→字符"的？
- 核心：InputStreamReader 内部持有一个 InputStream（字节流）和一个 Charset（字符集）。每次调 read()，它从字节流读字节，用字符集解码成字符，返回给你
- 字符集是 InputStreamReader 的属性，不是 FileReader 加上去的。FileReader 只是 InputStreamReader 的"预设快捷版"，帮你选好了 FileInputStream 和默认字符集，自己什么都不干
2. 序列化:反射遍历字段,按照类型写字节 
- transient 字段和 static 字段不会被序列化  static 属于类，不属于对象 → 序列化的是对象状态，static 不在其中
- 遗漏: 序列化的引用问题——循环引用怎么处理？
- ObjectOutputStream 给每个对象分配一个序号，序列化过的不再重复序列化，只写一个引用标记。这叫"序列化图"而非"序列化树"。你应该追问这个机制


> 字符流是字节流通过转换流加字符集转换过来的吗?设置序列化流的原因?序列化是怎么将数据变成乱码的?

**我的回答**
1. 不了解
2. 数据序列化存储到本地文件中,是乱码的形态,用户无法轻易修改数据
3. 不知道

**错漏补充**
- 字符流就是字节流通过转换流加字符集转换过来的
1. 问题1："乱码"不是设计目的，是副作用。

- 序列化产出二进制不是"为了防止用户修改"，而是因为：

- 二进制比文本高效——一个 int 存成 4 字节，存成文本 "12345" 要 5 字节还得解析
- 二进制类型安全——读回来直接是 int/String/对象，不用自己解析文本格式
- 二进制支持引用——对象 A 引用对象 B，序列化只存一次 B，反序列化时恢复引用关系。文本做不到
- "乱码"是因为二进制不对应任何字符集的编码表，用记事本打开当然看不懂。这是二进制格式的天然特征，不是故意设计来防篡改的。
2. 问题3：为什么要设计序列化流？

|场景|	不用序列化|	用序列化|
|---|---|---|
|对象存到文件|	自己写规则：哪个字段存、什么格式、怎么读回来|	一个 writeObject 搞定|
|对象通过网络传输|	自己写文本协议，接收方自己解析|	直接.writeObject + readObject|
|深拷贝对象|	手动递归复制所有字段|	序列化到内存再反序列化回来|

- 核心价值：把"对象状态 → 字节流"和"字节流 → 对象状态"这两个方向标准化，不用每次自己发明协议。

## Day 13 (2026-8-3)
### IO流第5天(学习了打印流,压缩流与解压缩流)
> 什么是打印流,压缩流与解压缩流?它们分别都有什么作用?

**我的回答**
1. 打印流是Java提供的一种包装流,基于装饰器模式,能够输出任意的数据类型,不需要手动转换类型.是最常用的流之一,如System.out.println
2. 压缩流与解压缩流都是Java提供的包装流,用于对于指定的文件或文件夹进行压缩或对压缩包进行解压缩,两者都可以指定字符集编码规则,每一个压缩包文件都是一个ZipEntry对象.
- 对于压缩流要注意的是putNextEntry(ZipEntry entry)创建压缩条目,必须要等到上一个条目closeEntry()后才能开启.
- 对于解压缩流,专有方法getNextEntry()读取ZipEntry对象,再通过字节流读取里面的每一个数据

**错漏点**
1. 打印流有一个关键特性你完全没提：不会抛出 IOException。print()/println() 方法内部自动 catch 了所有 IO 异常，这是它和其他流最大的行为差异。
-   打印流只有输出没有输入（不像其他流有 in/out 配对），它是"终端型"流

> 打印流是怎么实现什么数据都能打印的?压缩流与解压缩流又是怎么实现文件的压缩与解压的呢?

**我的回答**
1. 不了解

**错漏点**
1. 打印流怎么实现"什么都能打印"？
- 核心机制是:方法重载+统一转String. PrintStream 内部重载了所有基本类型 + Object
- 所有 print() 最终都走到 write(String s)，再有 println() = print() + newLine()。
2. 那为什么不会抛IOException?
- print内部捕获了所有异常,可通过checkError()查看
3. 压缩流怎么实现压缩？
- ZipOutputStream 的底层是 Deflater 对象，使用 DEFLATE 算法（LZ77 + Huffman）：

|步骤|	方法| 	底层在做什么                             |
|---|---|-------------------------------------|
|创建条目|	putNextEntry(entry)	|写入 Local File Header（文件名、时间戳、压缩方法标记） |
|写数据|	write(byte[])|	数据送入 Deflater 压缩，输出到内部 OutputStream|
|关闭条目|	closeEntry()|	调用 Deflater.finish() 完成当前条目压缩，记录 CRC32 校验值|
|收尾	|finish()/close()	|写入 Central Directory（所有条目索引）和 End of Central Directory 记录|
- 一个关键细节：closeEntry() 不是可选的。你不关上一个条目就开下一个，ZIP 文件结构就损坏了——这就是你笔记里说的"必须 closeEntry() 后才能开启下一个"的底层原因。

4. 解压缩流怎么实现解压？
- ZipInputStream 内部用 Inflater 对象：

|步骤|	方法|	底层在做什么|
|---|---|---|
|读条目	|getNextEntry()	|解析 Local File Header，初始化 Inflater，返回 ZipEntry 对象|
|读数据	|read(byte[])|	从底层 InputStream 读压缩数据 → Inflater 解压 → 返回解压后字节|
|读到-1	|—	|当前条目读完，自动定位到下一个条目的 Local File Header|
和压缩流一样是流式处理——不需要把整个 ZIP 文件加载到内存，读一个条目解压一个条目

> 为什么要设计打印流呢?之前那么多流难道不够输出时用吗? 压缩流与解压缩流为什么要设计?直接使用其他的解压软件就能实现这个功能? 这两个流在整个IO体系中处于什么位置呢?

**我的回答**
1. 打印流默认打印在控制台,可以让我们程序员更好的查看代码的状态,可以输出任意类型的数据,让我们不用手动转换类型
2. 压缩流与解压缩流可以让我们的软件具有这个功能比如Windows自带的压缩与解压缩
3. 两者都是不继承于其他流的高级流

**错漏点**
1. 为什么设计打印流

|  你的回答|	判定|	更深层原因|
|---|---|---|
|方便程序员查看状态|	✅ 对|	调试基础设施：没有打印流，每次调试都要 Integer.toString(i).getBytes() 然后 write()，没人受得了|
|不用手动转换类型	|✅ 对	|降低认知负担：print 关注的是"人类可读"，write 关注的是"机器可读"|
|  —	|❌ 遗漏|	容错设计哲学（fire and forget）：日志/调试输出不应该因为 IO 异常中断主业务逻辑。对比：如果用普通 FileOutputStream 写日志，磁盘满了直接抛异常崩程序——打印流不会|

2. 为什么设置压缩流与解压缩流

| 你的回答 | 	判定   | 更深层原因|
|---|-------|------|
| 让软件自带压缩功能 | 	✅部分对 | 不只如此——JAR 文件就是 ZIP 格式。Java 的类加载器、打包发布、Maven 依赖，全部建立在 ZIP 之上。没有压缩流，Java 生态本身就不成立|
| — | ❌ 遗漏  | 	流式处理 vs 全量加载：不用压缩流，你想读一个 ZIP 里某个文件，就得把整个 ZIP 读到内存。压缩流让你边读边解压，100MB 的 ZIP 只用几 KB 缓冲区 |
| — | ❌ 遗漏	 | 链式组合：new ZipInputStream(new BufferedInputStream(new FileInputStream("a.zip")))——可以和其他 IO 流无缝嵌套，这就是装饰器模式的威力 |
3. 它们都继承自 FilterOutputStream/FilterInputStream，和之前学的 BufferedInputStream、BufferedOutputStream、DataInputStream 是同一家族。装饰器模式是贯穿整个 IO 体系的设计主线，打印流和压缩流不是特例而是遵循同一套规则

## Day 14 (2026-8-4)
### 无
> 正则表达式中中文返回可以用[\u4e00-\u9fff]表示

## Day 15 (2026-8-5)
### IO流练习反思
> 对于字节流FileInputStream()的read()方法知识有所遗忘,跟BufferedReader的readLine()有搞混.
1. read()方法返回的是int类型的,如果read()是空参实现的,在FileInputStream中返回的就是字节对应的ASCII码值.如果是字符流中,那返回的就是对应字符集中的码值.
2. 如果我们手动书写了buffer数组,那么read()返回的int类型的数据就是实际读取到的`字节或者字符数`
3. 这时我们write()时,内部填写的就是(buffer,0,len),代表写入数组中实际的元素
4. 而BufferedReader的readLine()则是将一整行数据转换成String类型返回,通过回车符判断一整行是否结束(不同操作系统都可识别)\n,\r\n,\r.且不返回回车符.

> 对于带权重的算法进行了初步了解.
1. 如果我们想要通过权重来分配每个对象所被选到的概率,可以给每个对象设定权重,然后向加获得总的权重,再通过随机一个这个总权重范围内的数字.再通过遍历相加每个对象的权重,看看数字落到哪个区间.

> 学习了String类中的trim()方法,该方法的作用是去除字符串两端的ASCII码表中的空格等

1. 实际上去除的是ASCII码小于普通空格的所有元素,包含了制表符,换行符等
2. 底层是通过双指针实现去除字符串两边的空格的,而中间的空白不会动

## Day 16 (2026-8-6)
### 多线程第一天(学习了properties配置文件,多线程的基本概念,以及创建线程的前3种方法)

> 什么是properties配置文件,它是干什么的?进程,线程的基本概念?创建线程的前三种方法是哪三种?多线程到底是什么?

**我的回答**
1. properties是一种文件类型,常用来进行程序的一些数据的配置,将这些配置持久化保留.比如游戏设置的内容
2. 进程: 系统分配资源的最小单位,一个正在进行种的程序就是一个进程.每个进程分别独立,不相互影响
3. 线程: CPU的最小调度单元,一个进程里面可以有多个线程,一个进程里的所有线程共享一个堆内存但线程本身是私有的,元空间,栈都是线程私有的
4. 创建线程的前三个方法:
- 1.创建一个类继承Thread类,并重写里面的run()方法. 缺点: 类只能单继承,会占用仅有的继承位.
- 2.创建一个类实现Runnable接口,并重写里面的run()方法,也是最常用的创建线程的方法. 好处: 一个类可以实现多个接口,完全不影响继承.
- 3.FutureTask + Callable.创建一个类实现Callable<T>,再通过包装成FutureTask类型交给Thread. 好处:解决了前面两个方法没有返回值,无法抛出异常的问题
5. 多线程是一种为了提高进程效率的方法,在进程等待时执行其他的代码

**错漏点**
1. properties既是一个Java类(继承了Hashtable),拥有Map集合的全部特征,又是一种文件格式(key=value).你说的只是文件格式这一半
2. (概念错误)元空间（Metaspace）是共享的！ 存放类信息、常量池，所有线程都能访问
3. 记忆口诀: 堆和元空间共享,PC(程序计数器),虚拟机栈,本地方法栈私有
4. 1.Thread调用 start() 启动线程，不是调用 run() 2.Runnable对象还是要包装成 new Thread(runnable).start()，Runnable本身不是线程 3.需补充：futureTask.get() 会阻塞当前线程直到结果返回
5. 这只描述了"阻塞等待时切换"这一个场景。多线程还有另一个核心场景：多核CPU上真正并行执行（4核CPU可以同时跑4个线程）

> 怎么通过properties配置文件进行配置的?具体该怎么使用这些创建的线程?怎么样通过这些线程实现多线程?难道是创建多个Thread对象,调用start()就是多线程了?

**我的回答**
1. 通过每次执行程序时读取properties配置文件的内容实现设置.
2. 第一种通过继承Thread类,创建Thread的子类对象,来使用Thread的方法.第二种通过调用重写的run()方法调用里面的内容.第三种将包装后的FutureTask类型的返回值交给Thread后进行处理.
3. 不清楚,目前猜想是多个Thread类型对象同时调用.

**错漏点**
1. properties过程: load() 读文件 → getProperty() 取值 → setProperty() 改值 → store() 写回文件。本质就是对Map做持久化。
2. 基本正确
3. "猜想是多个Thread类型对象同时调用"——方向对了，但关键细节没抓住。

|调用方式|	发生了什么|	是不是多线程？|
|---|---|---|
|thread.start()|	JVM向OS申请创建新线程，新线程自动调用run()|	✅ 是，有新线程|
|thread.run()|	就是普通的方法调用，在当前线程里执行run()的代码|	❌ 不是，没有新线程|

> 为什么要专门使用properties类型的文件配置?线程以及多线程跟我们之前学习的代码右键run执行之间有什么区别?

**我的回答**
不清楚

**错漏点**
1. 为什么用 Properties 配置文件
-   如果数据库密码写死在代码里，每次换环境都要改代码重新编译.且非程序员可改,运维人员改配置文件就行，不需要懂Java代码
2. 之前你写的所有代码都是单线程的：
-   多线程：main方法 → 启动线程A → 启动线程B → main继续执行
   ↓           ↓            ↓
   线程A干活    线程B干活     main也在干活
   （三件事同时进行）
3. 具体区别

|维度|	单线程（之前）|	多线程（现在）|
|---|---|---|
|执行方式|	一件事做完再做下一件|	多件事交替/同时进行|
|阻塞影响|	一处阻塞全程序卡死|	一个线程阻塞，其他线程继续跑|
|CPU利用|	单核也只能用一部分|	多核CPU可以真正并行|
|复杂度	|简单，不用考虑线程安全|	复杂，要考虑共享数据竞争、死锁|

## Day 17 (2026-8-7)
### 多线程第二天(学习了Thread类中的常用方法,同步代码块,同步方法,Lock锁,死锁)

> 列举出Thread类中的常用方法.说明同步代码块,同步方法是什么,两者之间有什么区别?说明Lock锁,死锁的概念.

**我的回答**
1. Thread类中的常用静态方法:sleep() 特点:抱着锁睡觉,到时间自然醒.常用的非静态方法如setName(),getName(),setProportiey(),getProportiey(),setDeamon()等
2. 同步代码块Java为了解决多线程下的并发修改问题所设计的一种代码块.该代码块格式为synchronized(锁对象){} 组成只有当线程持有锁对象时才能进入{}也就是临界区执行内部代码
,每次只有一个线程持有这个锁的对象.当线程执行完临界区的代码后自动释放所持有的锁.这里有几个需要注意的点: 任何非null的对象都可以作为锁对象,锁的对象必须唯一,一般调用对象静态情况下为该类类名.class,
实例对象时用this.
3. 同步方法则是在整个方法上加锁,锁的位置在修饰符的后面,只有当对象持有该锁时,才能使用该方法.不过这种锁直接锁整个方法,锁粒度比较大,并发性能下要低于同步代码块.
4. Lock锁是Java为了解决synchronized锁的不足而推出的新的锁.因为synchronized的锁是JVM层面的,隐式加锁,我们无法知道什么时候有锁,什么时候释放.
于是Lock在JDK5被推出,Lock本身是个接口,我们一般通过它的实现类Reentrantable()创建Lock对象.它最大的优点就是灵活,我们可以通过lock()和unlock()方法自主选择锁的位置.
以及它的一些特有方法如: tryLock(),尝试获得锁,未获得就自动释放手中的锁. unlock一定要写在finally块中.
5. 死锁就是多个线程互相持有对方的锁,但本身又不会自主释放自身的锁的情况.它的形成有4个原因.

**错漏点**
1. 拼写错误: set/getProportiey()应为set/getPriority().Reentrantable()应为ReentrantLock(),setDeamon()应为setDaemon().
2. Thread方法补充: currentThread()（获取当前线程对象）、join()（等待该线程执行完毕）、yield()（让出CPU时间片）、interrupt()（中断线程）
3. 补充一个关键细节：你说"执行完临界区代码后自动释放锁"——正确，但补一句：如果临界区代码抛异常，synchronized也会自动释放锁（JVM层面保证）。这和Lock不一样，Lock抛异常不会自动释放，所以你必须写finally。
4. 对于tryLock()的描述有误: tryLock()是尝试获取锁，成功返回true，失败返回false。它不会"自动释放你已有的锁"——你手里的锁你得自己unlock().
对比 lock()：拿不到就一直等（阻塞），而 tryLock() 拿不到就立刻走（不阻塞）。这就是它最大的价值。
5. 应将死锁的4个条件以及解决方法背下来.

> synchronized锁和lock锁的底层是怎么让不持有该锁的对象无法执行临界区的?两者一个是关键字,一个是接口,有什么区别吗?

**我的回答**
1. 底层通过判断线程是否持有锁,来决定是否让对象执行临界区的代码.
2. synchronized关键字太过固定,接口通过实现类创建它的对象,我们可以灵活地使用它的方法

**错漏点**
1. 循环论证,没有答到点上.
- 怎么实现,两个层面
- ① 字节码层面——synchronized代码块编译后会生成两条指令：

synchronized(obj) {

// 临界区代码

}

↓ 编译后

monitorenter       // 进入：尝试获取obj的monitor

// 临界区代码

monitorexit        // 退出：释放obj的monitor
- 每一个Java对象都有一个Monitor(管程/监视器).monitorenter就是去获取这个monitor
- 如果 Monitor 没被占用 → 获取成功，进入临界区
- 如果 Monitor 已被其他线程占用 → 阻塞等待
- ② 对象头层面——锁信息存在对象头的 Mark Word 中：

对象头

├── Mark Word（存锁状态、hashcode、GC年龄）

└── 类型指针（指向类元数据）

Mark Word 中的锁状态：

无锁 → 偏向锁 → 轻量级锁（自旋） → 重量级锁（monitor）

- 这就是锁升级-------synchronized不是一开始就上重量级锁,而是通过竞争的激烈程度逐渐上升
2. Lock 底层怎么实现？
   ReentrantLock 内部基于 AQS（AbstractQueuedSynchronizer）：

ReentrantLock

└── 内部类 Sync extends AQS

├── state（int变量，0=无人加锁，>0=已加锁，CAS修改）

├── exclusiveOwnerThread（记录持锁线程）

└── CLH队列（双向链表，存等待锁的线程）
- 加锁流程：

lock() → CAS尝试把state从0改成1

成功 → 记录当前线程为持锁线程

失败 → 包装成Node加入CLH队列尾部，阻塞等待

前一个线程unlock() → 唤醒队列头部的下一个线程




> 为什么要设计锁呢?有没有其他的方法可以解决多线程的问题?

**我的回答**
1. 为了解决多线程情况下并发修改可能会出现问题的情况
2. 不知

**错漏点**
为什么要设计锁？
1. 你说"为了解决并发修改问题"——方向对，但要更精确：
线程安全三要素，锁是为了同时保证这三点：

| 特性   | 	含义   | 	不加锁会出什么问题   |
|:-----|:---|:---|
| 原子性  | 操作不可分割，要么全做要么不做  | 	i++在多线程下不是原子操作，两个线程同时读到i=0，各加1，写回还是1而不是2 |
| 可见性  | 	一个线程修改了共享变量，其他线程立刻能看到 | 	线程A把i从0改成1，线程B的CPU缓存里i还是0，继续用旧值算 |
 | 有序性  | 	编译器/CPU不会把代码重排到导致结果错误	| 初始化对象的步骤被重排，别的线程拿到一个半初始化的对象  |

2. 有没有不加锁解决多线程问题?有,而且面试官很爱问这个

|方案|	原理|	适用场景|	解决了什么|
|---|---|---|---|
|volatile|	禁止CPU缓存+禁止指令重排|	状态标志位|	只保证可见性和有序性，不保证原子性（i++仍然不安全）|
|Atomic类|（AtomicInteger等）|	CAS（Compare-And-Swap）无锁原子操作|	计数器、累加器|	保证原子性，不加锁，性能更好|
|ThreadLocal|	每个线程各有一份副本，不共享|	数据库连接、SimpleDateFormat|	根本不共享→不需要锁|
|并发集合|	CopyOnWrite写时复制 / ConcurrentHashMap分段锁|	高并发读写集合|	集合层面自带的线程安全|
|不可变对象|	对象创建后不可修改（String、final字段）|	配置对象、常量|	不可变→没有修改→没有竞争|

- 一句话总结：锁是"大家排队用"的方案，这些替代方案是"根本不用排队"的方案——要么不共享（ThreadLocal），要么不改（不可变），要么用硬件指令保证原子（CAS）。

## Day 18 (2026-8-8)
### 多线程第三天(学习了等待唤醒机制以及通过阻塞队列实现等待唤醒机制)

> 什么是等待唤醒机制?什么是阻塞队列?

**我的回答**
1. 等待唤醒机制是基于synchronized为了实现两个线程之间交流的一种机制.它常见的Object方法为wait(),notify(),notifyAll().
最经典的场景是生产者与消费者,两者相互竞争同一把锁,不满足条件就wait()等待,通过另一条线程来notify()唤醒.
这里值得注意的点是wait()等待时是释放锁的,与sleep不同,且sleep()不需要锁对象.还有一点就是wait()应写在while循环中,而不是单纯的if判断,if判断的话可能会出现假唤醒等问题.
2. 阻塞队列waitQueue继承于Queue,它的常用的实现类有ArrayBlockedQueue,LinkBlockedQueue.ArrayBlockedQueue必须初始化大小,消费者可以通过get()方法获得队列中内容

**错漏点**
1. 两者相互竞争同一把锁不准确: 不是"竞争"，是"协作"——生产者放东西后通知消费者来取，消费者取完通知生产者继续放
2. 遗漏点：wait()/notify()必须在synchronized块内调用，否则抛 IllegalMonitorStateException。原因是：wait()要释放monitor，如果你没持有monitor，释放什么？
3. waitQueue拼写错误,应为BlockingQueue,继承自Queue接口,两个队列也应该为ArrayBlockingQueue,LinkBlockingQueue
4. 补充：LinkedBlockingQueue可以不指定大小（默认Integer.MAX_VALUE）
5. BlockingQueue的核心方法是 put() 和 take()，不是get()。get()是普通Queue的方法
6. 阻塞队列的核心特性你没说：put()和take()都是阻塞等待. 阻塞队列的价值就是：put和take自带阻塞，你不用自己写wait/notify了
7. 

> 等待唤醒机制是怎么实现唤醒wait()的线程的呢?如果是lock.notify(),此时线程在等待时已经释放锁了,怎么具体知道它在等待呢?
> 阻塞队列又是怎么让我们不用手动加锁就能实现等待唤醒机制的呢?

**我的回答**
1. 线程wait()后进入等待队列,notify()可以唤醒对应的线程,具体什么实现不了解
2. 阻塞队列内部封装了锁,它会给队列中的内容加锁

**错漏点**
1. 你问的问题很好——"线程等待时已经释放锁了，怎么知道它在等待？
- 答案：每个Java对象内部维护两个集合：

对象（锁对象）
├── Entry Set（入口集）—— 等待获取锁的线程队列
└── Wait Set（等待集）—— 调用了wait()的线程队列

|操作|	线程去了哪|
|---|---|
|线程要进synchronized但锁被占|	进入该对象的 Entry Set，阻塞|
|线程在synchronized内调用wait()|	释放锁，进入该对象的 Wait Set，阻塞|
|调用notify()|	从Wait Set中随机取一个线程，移到 Entry Set，让它重新竞争锁|
|调用notifyAll()|	把Wait Set中所有线程移到 Entry Set，全部重新竞争锁|
所以notify()不是"直接唤醒线程让它继续执行"，而是"把线程从Wait Set移到Entry Set，线程还要重新抢锁，抢到才能继续执行".

- Lock体系的等待/唤醒方法: Condition.await() / signal() / signalAll()
- Lock体系的优势：一个Lock可以创建多个Condition，比如阻塞队列里用两个——notEmpty和notFull，分别管理"队列空了等数据"和"队列满了等空位"两组等待线程，互不干扰。synchronized只有一个Wait Set，notifyAll会唤醒所有等待的线程，包括不该唤醒的。
- 你用的put()/take()内部就是 ReentrantLock + 两个Condition 的等待唤醒。阻塞队列把这套逻辑封装好了，你直接调put/take就行。

- 知识链梳理：AQS / CLH / Lock / Condition / BlockingQueue 的关系
  你说"AQS, CLU的，需要梳理"——应该是 CLH（不是CLU）。它们的关系是：

AQS（AbstractQueuedSynchronizer）
—— Doug Lea写的框架，核心是 state变量 + CLH变体队列（双向链表存等待线程）
│
├── ReentrantLock（AQS的子类实现）
│     └── lock.newCondition() → ConditionObject（AQS内部类）
│           —— 每个Condition有自己的条件队列（单向链表）
│
├── ReentrantReadWriteLock（读写锁，也基于AQS）
├── CountDownLatch（也基于AQS）
├── Semaphore（也基于AQS）
│
└── ArrayBlockingQueue
└── 内部持有 ReentrantLock + 两个Condition
—— 所以阻塞队列底层 = ReentrantLock + Condition = AQS
- 一句话：AQS是地基，ReentrantLock是房子，Condition是房子里的房间，BlockingQueue是精装修——你住进去不用自己建墙（不用自己加锁）。


> 等待唤醒机制具体是怎么设计的呢?锁好像已经能够解决多线程并发修改的问题了.
> 阻塞队列底层是怎么实现的呢?之前没学多线程时根本不了解这些队列,什么AQS,CLU的,需要梳理这些知识.

**我的回答**
1. 无

**错漏点**
1. 锁解决的是"互斥"问题，等待唤醒解决的是"协作"问题
- 等待唤醒是线程之间按条件协调执行顺序,如果没有这个机制,会出现忙等待,需要while(true)一直检查,浪费CPU性能
2. 阻塞队列为什么要设计？

|原因|	说明|
|---|---|
|封装等待唤醒|	生产者-消费者模式太常用了，每次手写wait/notify容易写错（忘记while、忘记notify）。BlockingQueue帮你封装好，put/take直接用|
|解耦|	生产者不需要知道消费者是谁，只管往队列里put；消费者不需要知道生产者是谁，只管从队列take。两者通过队列通信，互不依赖|
|线程安全内置|	不用你自己加锁，队列内部已经用ReentrantLock保证线程安全|

## Day 19 (2026-8-9)
### 多线程第四天(学习了线程池的基本概念以及如何创建线程池)

> 线程池是什么?它有什么要注意的点?它与之前我们创建多线程有什么关系?

**我的回答**
1. 线程池是Java为了解决手动创建线程繁琐而创建的一个容器,它可以帮我们对线程进行统一管理.
2. 它的一些基本知识保括如何创建线程池,我们可以通过new它的实现类ThreadPoolExecutor.几个关键的参数分别为核心线程数,总线程数,临时线程空闲多长时间释放,释放时长单位,阻塞队列,线程工厂,拒绝机制.
也可以通过Executors工具类创建,但存在内存泄露的风险,不符合阿里巴巴编码规范,线上禁止使用.
3. 它的一些核心方法包括submit(),executor(),shutdown(),以及shutdownNow().阻塞队列有ArrayBlockingQueue,LinkBlockingQueue,SynChronousQueue,DelayQueue.
4. 值得注意的是线程池的机制是核心线程满后,线程进入阻塞队列,阻塞队列满后才创建临时线程.都满后就会触发拒绝访问机制.
5. 它就是为了让我们更好实现多线程的.

**错漏点**
1. Executors工具类不是"内存泄露"，是OOM（内存溢出）。FixedThreadPool用无界队列 LinkedBlockingQueue→任务堆积撑爆内存；CachedThreadPool最大线程数Integer.MAX_VALUE→线程无限创建撑爆内存
2. executor()拼写错误,应为execute()submit()有返回值(Future)，execute()没有返回值.
3. 应为 LinkedBlockingQueue（少了d）、SynchronousQueue（S小写c，不是大写C）
4. 机制：核心线程满→阻塞队列→临时线程→拒绝机制	⚠️ 方向对	不是"线程进入阻塞队列"，是任务进入阻塞队列。线程是执行者，任务是Runnable
5. 4种拒绝策略没有题,需要背下来.
6. submit() vs execute() 的区别（你没提）：

|方法|	返回值|	能否捕获异常|	参数|
|---|---|---|---|
|execute(Runnable)|	void|	不能，异常直接抛出|	只接受Runnable|
|submit(Callable/Runnable)|	Future|	能，通过Future.get()获取|	接受Runnable和Callable|



> 线程池底层是如何实现装载并管理这些线程的呢?它有什么精妙之处?

**我的回答**
1. 底层通过创建HashSet来存储这些线程.其他不了解

**错漏点**
1. 线程池内部结构

ThreadPoolExecutor
   
   ├── HashSet<Worker> workers     ← 你说的HashSet，存的是Worker不是Thread

   ├── BlockingQueue<Runnable> workQueue  ← 存等待执行的任务

   ├── AtomicInteger ctl           ← 一个变量同时记录线程池状态和线程数量

   │     ├── 高3位：线程池状态（RUNNING/SHUTDOWN/STOP/TIDYING/TERMINATED）

   │     └── 低29位：线程数量

   └── Worker（内部类）

   ├── Thread thread         ← 真正的线程

   ├── Runnable firstTask    ← 第一个任务

   └── extends AQS           ← Worker继承了AQS！（你自己实现了不可重入锁）

execute() 的完整流程（这是线程池最核心的源码逻辑）

提交任务 task

↓

① 当前线程数 < corePoolSize？

→ 是：创建核心线程，执行task，结束

→ 否：继续

↓

② 阻塞队列没满？

→ 是：task放入阻塞队列，结束

→ 否：继续

↓

③ 当前线程数 < maximumPoolSize？

→ 是：创建非核心线程，执行task，结束

→ 否：继续

↓

④ 触发拒绝策略


- 你说的机制顺序是对的，但注意：核心线程和临时（非核心）线程在`代码层面没有区别`，都是Worker对象。区别只在于——超过corePoolSize后创建的线程，在空闲keepAliveTime后会被回收；核心线程默认不回收（除非设置allowCoreThreadTimeOut(true)）。

- Worker 怎么不断取任务执行？

- 核心线程用 workQueue.take()（阻塞，一直等），非核心线程用 workQueue.poll(keepAliveTime)（等一段时间没任务就退出，被回收）。这就是为什么非核心线程空闲超过keepAliveTime会自动销毁。


> 为什么要这样设计线程池?为什么是这些参数呢?实际开发中还需要我们手动创建线程吗?

**我的回答**
1. 不了解

**错漏点**
- 为什么要设计线程池？手动创建线程有什么问题？

|手动创建线程的问题|	线程池怎么解决|
|---|---|
|每次new Thread()都要创建和销毁线程，OS层面有开销（用户态→内核态切换）|	线程复用，创建后不销毁，反复执行不同任务|
|无限制创建线程会导致OOM（每个线程默认占1MB栈空间）|	线程池有maximumPoolSize上限，可控|
|线程无法统一管理（无法监控数量、无法优雅关闭）|	线程池提供shutdown()、shutdownNow()、监控方法|
|任务多时响应慢（每个任务都要等线程创建）|	核心线程常驻，任务来了直接执行，不用等创建|

- 为什么是这7个参数？每个参数解决什么问题？

|参数|	解决的问题|	如果没有会怎样|
|---|---|---|
|corePoolSize|	日常并发量下需要多少线程常驻|	每次任务来了都要创建线程，慢|
|maximumPoolSize|	峰值时最多允许多少线程|	无限创建→OOM|
|keepAliveTime|	非核心线程空闲多久后回收|	空闲线程一直占内存，浪费|
|unit|	keepAliveTime的时间单位|	—|
|workQueue|	任务排队的地方|	任务多时没地方放，直接拒绝|
|threadFactory|	统一创建线程的方式（命名、优先级等）|	线程没有统一命名，出问题无法排查|
|handler|	所有资源都满了怎么办	|没有兜底策略，任务直接丢失或异常|

- 实际开发中还需要手动创建线程吗？
  几乎不需要。 实际开发中创建线程的方式：

|场景|	方式|
|---|---|
|Spring项目|	@Async注解 或 注入 ThreadPoolTaskExecutor|
|需要自定义线程池|	new ThreadPoolExecutor(7个参数)|
|简单异步任务	|CompletableFuture.runAsync()（内部用ForkJoinPool）|
|定时任务	|ScheduledThreadPoolExecutor|

## Day 20 (2026-8-10)
### 网络编程第一天(多线程收尾,学习了网络编程的几个基本概念,ip地址,端口,UDP协议,TCP协议目前没了解)

> 创建线程池多大合适?网络编程是什么,它有什么基本概念?UDP协议的基本知识点?

**我的回答**
1. 创建线程池,对于CPU集中型: 核心线程数 = CPU线程数 + 1;这里+1是防止有线程阻塞而影响CPU利用率.
对于IO集中型: 核心线程数 = CPU线程数 * ( 1 + 平均IO等待时长/计算耗时);
2. 网络编程是不同计算机之间,通过网络实现计算机之间通信的Java程序.
3. 网络编程的三大基本概念分别为ip,端口,协议.
ip地址分为ipv4地址和ipv6地址,每一台设备都会分配一个ip地址,在Java中通过InetAddress实现,不过该类未提供构造方法,只能通过静态方法getByName()获得地址对象.
如果要定位具体是哪一个程序或网站,就需要使用到端口,范围为0~65535,前1023不可使用.
其中协议就包括了UDP协议,该协议是传输是不可靠的,且存在容量限制Java中有关的工具类:DatagremDocker()以及DatagremPackeger().通过前者创建连接通道,后者打包或者接收打包
有关主要方法有send(),对于解包有getBytes(),getLength(),getAddress(),getPort(),执行完毕后需调用close()释放资源.

**错漏点**
1. 应将集中该为`密集`

补充一个实操细节：实际开发中很少用公式精确计算，更常用的做法是先设一个经验值（CPU密集型=N+1，IO密集型=2N），然后通过压测调整。面试时能说出公式+经验值就足够了。
- ip地址补充：还有getLocalHost()获取本机IP；InetAddress没有公开构造方法，是静态工厂方法模式

- 0~1023是知名端口（Well-known Ports），保留给系统服务（HTTP=80, HTTPS=443, MySQL=3306），不是"不可使用"而是"不建议自定义程序使用"。自定义程序一般用1024以上的端口
- 应为 DatagramSocket（是Socket不是Docker）. 应为 DatagramPacket（是Packet不是Packeger）
- 遗漏的UDP关键特性: 不需要先建立连接，直接发送（对比TCP需要三次握手)

> 网络编程是如何让我们编写几行代码就实现的呢?创建这些对象时它们源码是怎么实现的?

**我的回答**
1. 不了解,源码看不懂

**错漏点**
1. 网络编程底层是怎么工作的？
   你写几行代码就能通信，不是因为Java有什么魔法，而是因为操作系统提供了Socket API，Java只是封装了它：
   
   你的Java代码
   ↓ Java封装
   DatagramSocket.send(packet)
   ↓ 调用
   操作系统Socket API (C语言层面的socket/sendto/recvfrom)
   ↓ 系统调用
   网卡驱动 → 物理网络 → 对方网卡 → 对方操作系统 → 对方的Java程序
-  关键认知：Socket不是Java发明的，它是操作系统的网络通信接口。C、Python、Go都有Socket API，Java的DatagramSocket/Socket只是对这个操作系统接口的Java封装。


> 为什么要专门设计工具类来实现网络编程?它跟多线程一样,学习起来都给人一种不知道如何运用的感觉.

**我的回答**
1. 帮助我们在服务器与客户端之间传输数据.

**错漏点**
- 为什么要专门设计网络编程工具类？

 |原因|	说明|
|---|---|
 |跨平台抽象|	Windows/Linux/Mac的Socket API细节不同，Java封装后你在任何系统写同样的代码都能通信|
 |面向对象封装|	操作系统的Socket是C语言接口（int fd + 一堆函数），Java把它封装成DatagramSocket对象，有方法有属性，更好用|
 |与IO体系统一|	Socket的输入输出流可以和Java IO体系无缝衔接——socket.getInputStream()返回InputStream，你之前学的BufferedReader/PrintStream全都能用|

- UDP为什么要设计成"不可靠"？
-  这听起来像缺陷，实际是刻意的选择：
- 为什么直播用UDP不用TCP？ 因为直播要求实时性，晚到的数据包没用了（你已经看到下一帧了）。TCP会重传丢失的包，导致画面卡顿——可靠性反而成了负担。UDP丢了就丢了，继续发新的，画面流畅。

- 关键认知：你学网络编程不是为了以后手写Socket通信，而是为了理解上层框架的底层原理。面试官问"Spring Boot怎么接收HTTP请求"，你回答时能说到"Tomcat底层用Socket监听端口，收到TCP连接后解析HTTP报文"，这就比只说"用@RequestMapping"的人高了一个档次。

## Day 21 (2026-8-11)
### 网络编程第二天(学习了TCP协议以及部分练习)

> TCP协议是什么协议?说出它的一些基本概念以及跟UDP协议的区别.

**我的回答**
1. TCP协议是面向连接协议,相比于UDP协议数据报,它更加安全可靠,通过流来进行数据传输.在Java中对应的工具类为Socket,ServerSocket.
2. 常用的方法有getInputStream(),getOutputStream(),accept(),close()等.
3. 客户端与服务器创建连接需要经过三次握手,第一次客户端生成随机初始序列号,向服务器发送SYN,进入SYN_SENT状态.第二次服务器向客户端发送SYN+ACK发送的初始序列号+1,表示连接可以建立,第三次客户端向服务器发送ACK两者同时进入ESTABLISHED状态.
为什么是三次而不是两次?为了防止服务器与过期的SYN产生连接,浪费服务器资源.服务器端会判断SYN是否过期来决定是否连接
4. 客户端与服务器断开连接则需要4次挥手,因为TCP是双向连接的,两边都需要断开,以客户端向服务器端断开连接来举例,第一次客户端先向服务器发送FIN,第二次服务器端向客户端发送ACK,第三次数据发送完后服务器向客户端发送FIN,第四次客户端向服务器发送ACK,客户端进入TIME_WAIT状态
为什么要有TIME_WAIT? 客户端在发送完ACK后等待2倍的MSL时间,方式服务器端没有收到ACK向客户端重发FIN,此时客户端可以重发ACK,确保完全断开连接.
5. 与UDP协议差别主要还是体现在可靠性上,TCP协议通常用在文件传输,打字输入等需要数据完全传输的场景.UDP协议通常在直播,视频播放时使用,数据丢包直接传输新的,方式重传卡顿.

**错漏点**
1. 补充一个你没提到的关键点——为什么挥手要4次而握手只要3次？
- 服务器收到FIN后，自己可能还有数据没发完，所以先回ACK（"我知道你要走了"），等数据发完再发FIN（"我也走了"）

- 遗漏点

|遗漏|	说明|
|---|---|
|TCP可靠传输的实现机制|	你说了TCP"可靠"，但没说怎么做到可靠的——序号、确认、超时重传、滑动窗口、拥塞控制。这是第二层要回答的|
|Socket和ServerSocket的关系|	ServerSocket负责"等连接"（accept），Socket负责"读写数据"。一个ServerSocket.accept()返回一个Socket，之后双方通过这个Socket的InputStream/OutputStream通信|



> Java也是通过封装操作系统提供的Socket API实现TCP协议的吗?还有什么其他要注意的点呢?

**我的回答**
1. Java就是通过封装Socket API来进行实现的.

**错漏点**
1. Socket 到底封装了什么？

   你的Java代码
   ↓
   Socket类 / ServerSocket类（Java API）
   ↓ native方法
   操作系统的 Socket API（C语言的 socket()、bind()、listen()、accept()、connect()、send()、recv()）
   ↓
   网卡驱动 → 网线 → 互联网

- Socket 的本质：操作系统提供的一个"网络通信端点"抽象。
- TCP 可靠传输怎么实现的？（你没回答的核心问题）
  你说TCP"安全可靠"，但可靠不是天生的，靠的是5个机制：

|机制|	作用|	通俗理解|
|---|---|---|
|序号（Sequence Number）|	每个字节都有编号，接收方知道哪些收到了、哪些没收到|	快递单号，每个包裹都有唯一编号|
|确认（ACK）|	接收方收到数据后回确认，告诉发送方"我收到了到第N个"|	签收快递|
|超时重传|	发送方等一段时间没收到ACK，就重发	|快递超时没签收，重新派送|
|滑动窗口|	接收方告诉发送方"我还能收多少"，发送方按窗口大小发|	快递柜容量有限，别一次塞太多|
|拥塞控制|	发送方根据网络拥堵情况动态调整发送速度，避免网络瘫痪|	路上堵车就开慢点|

关键：ServerSocket.accept() 不是"收到第一次握手就返回"，而是"等三次握手全部完成后才返回一个新的Socket对象"。这个新Socket代表了这条已建立的连接。


> 现阶段是只需要了解网络编程的知识点,这些内容在后面学习到web的时候才会用到吗?以后还需要手动new Socket吗?

**我的回答**
1. 不了解

**错漏点**

① 以后几乎不手动new Socket

|阶段|	你做什么|	底层谁在管Socket|
|---|---|---|
|现在（JavaSE）|	手写Socket/ServerSocket练手|	你自己|
|Spring Boot阶段|	写Controller处理HTTP请求|	Tomcat（内嵌服务器）|
|实际工作|	写业务逻辑，接收请求返回响应|	Tomcat + Spring MVC|

Tomcat的底层就是ServerSocket + 线程池：

你在Spring Boot写的代码
↓
Spring MVC（DispatcherServlet分发请求）
↓
Tomcat（HTTP服务器）
↓ 底层
ServerSocket.accept() 等连接 → 每来一个连接，用线程池分配一个线程处理
↓
Socket.getInputStream() 读HTTP请求
Socket.getOutputStream() 写HTTP响应

所以你这几天学的东西不是白学的——你手写的Socket通信，就是Tomcat内部在做的事。只不过Tomcat帮你封装了几百层，你只需要写 @GetMapping("/hello") 就能处理网络请求了。

## Day 22 (2026-8-12)
### 反射第一天(进行了网络编程收尾,学习了反射的基本概念)

> 反射是什么?反射有那些基本的概念?它是用来做什么的?它有那些弊端?

**我的回答**
1. 反射是Java提供的一种动态处理方式,它允许我们在运行阶段动态无视修饰符获取任何一种类的成员方法,构造方法,成员变量.
2. 获得Class对象的三种方式:
-   Class.forName("全类名"),全类名: 包名+类名. 这种方式使用次数最多,会触发静态代码块,当我们需要对获取的Class对象进行处理时适用.
-   类名.class  这种方式最安全,编译期时就能确定.不过这种方式获得的Class并不会触发静态代码块,一般用于当参数时,如作为锁对象.
-   对象.getClass()  通过该类的对象获得该类的Class对象,主要在要对对象属性进行处理时使用

- Java反射的三种API
- 1. Constructor获得构造方法,常用方法为getConstructors()获得所有public构造器,并放入数组返回,getDeclaredConstructors()获得所有构造器,包括private构造.
以及通过参数如(String.class)获得指定构造.可以通过newInstance()创建该构造器对象.如果是Declared获得的需要setAccessible(true)暂时绕过权限修饰符检查.
- 2. Field获得成员变量,常用方法与第一个类似,getFields()获得所有的public的成员变量.getDeclaredFields()获得所有包括private的成员变量.
也可以同过参数指定要获取的成员变量,如("name").可以通过获得的该name.get(对象)获取指定对象的该name值,也可以通过name.set(对象,Name)来设置指定对象该值.
如果要对private修饰的成员变量进行操作,必须要name.setAccessible()暴力反射绕过权限检查.
- 3. Method获得成员方法,与上面两个一致.

- 反射拥有极高的灵活性,可以让我们随时随地的查看和修改对象中的内容.
- 它的主要缺点体现在是运行期动态实现,效率比普通调用低非常多.而且安全性维护性不高,问题全在运行时出现.破坏封装,违背了Java面向对象的理念.

**错漏点**
1. 补充一个更完整的定义：反射是在运行期间动态获取类的信息（字段、方法、构造器、注解等）并操作它们的能力。关键词是运行期间——普通代码在编译期就确定了调用关系，反射在运行期才知道操作的是什么。
2. 补充 Class.forName("全类名")：不仅触发静态代码块，而是触发**完整的类加载过程**（加载→链接→初始化）。这是forName和.class的关键区别
3. 补充 类名.class：不触发初始化但会触发**加载和链接**（只是不执行静态代码块）。你说的"做锁对象"是个好例子
4. 补充：getClass()定义在Object类中，所有对象都有这个方法
5. 补充：newInstance()可以传参数 constructor.newInstance("张三", 20)；JDK9+推荐用 clazz.getDeclaredConstructor().newInstance() 代替过时的 clazz.newInstance()

6. Method的核心方法你没写：invoke(对象, 参数...) 调用方法。这是反射最常用的操作
- Method method = clazz.getDeclaredMethod("setName", String.class);
  method.setAccessible(true);
  method.invoke(obj, "张三");  // 等同于 obj.setName("张三")

- 反射速度补充：JDK有优化（反射方法调用15次后会生成字节码，接近普通调用速度）


> 反射底层是怎么实现的呢?它与普通的调用有什么不同?运行期动态实现,怎么个实现法?它的源码有什么神奇之处?

**我的回答**
1. 底层通过Class文件获得里面封装的内容.
2. 普通调用就直接编译期确定,而反射是通过编译好的Class文件调用.
3. 后面不知

**错漏点**
1. 反射能工作的前提是JVM已经把类加载到内存中了。整个链条：

编译期：
Student.java → 编译 → Student.class（字节码文件）

运行期：
JVM启动 → 类加载器把 Student.class 加载到内存
→ 方法区（元空间）中生成一个 Class<Student> 对象
→ 这个Class对象包含了Student类的所有信息：
├── 字段信息（名字、类型、修饰符）
├── 方法信息（名字、参数、返回值、修饰符）
├── 构造器信息
├── 父类信息
└── 注解信息
→ 反射就是通过这个Class对象，获取并操作这些信息

所以反射**不是**"读Class文件"——Class文件在类加载后已经被解析成内存中的Class对象了。反射操作的是这个内存中的Class对象，不需要再去读文件。

普通调用 vs 反射调用的区别

|维度|	普通调用|	反射调用|
|---|---|---|
|何时确定调用目标|	编译期|	运行期|
|类型检查|	编译期检查（s是Student类型，编译器知道有setName方法）|	运行期检查（method.invoke时才知道方法存不存在）|
|性能|	直接调用，快|	需要安全检查、参数类型匹配、方法查找，慢|
|灵活性|	只能调用已知的类和方法|	可以调用运行时才知道的类和方法|

反射为什么慢？性能开销在哪？

|开销来源|	说明|
|---|---|
|方法查找|	运行时遍历方法表找匹配的Method对象|
|安全检查|	每次invoke都要检查访问权限（除非setAccessible(true)）|
|参数装箱|	基本类型参数要自动装箱成Object|
|JIT优化受限|	反射调用难以被JIT内联优化|
但JDK有优化：Method.invoke()在调用超过15次后，JVM会生成一个"MethodAccessor"字节码，之后的调用直接走生成的字节码，性能接近普通调用。所以"反射非常慢"这个说法在现代JDK中已经被大幅改善。

setAccessible(true) 底层怎么绕过权限检查的？

正常调用：
Method.invoke() → 检查AccessibleObject.override标志
→ override=false → 检查调用者是否有权限访问private方法
→ 没权限 → 抛IllegalAccessException

setAccessible(true)：
→ 把override标志设为true
→ Method.invoke()看到override=true → 跳过权限检查 → 直接调用

注意：setAccessible(true)绕过的是Java语言层面的访问控制，不是JVM安全机制。如果有SecurityManager，setAccessible本身也可能被拒绝（不过JDK17之后SecurityManager已废弃）。


> 为什么要设计反射?如果是框架要使用,那是不是可以通过另外的内部权限之类的方法实现获得封装的内容,而不是选择反射.

**我的回答**
1.后面学习的很多内容都需要反射,如Spring ioc,现在的idea生成提示都是通过反射实现的.

**错漏点**
- 反射解决的核心问题是什么？
- 反射解决的是"**编译期不知道要操作什么类，运行期才知道**"的问题。

没有反射的世界：

java

// 没有反射：你要创建什么对象，编译时就得写死

Student s = new Student();

Teacher t = new Teacher();

// 如果类名是从配置文件读出来的，你怎么办？

// String className = config.getProperty("className");

// new ???  ← 编译器不允许你写变量类型来new

有反射：

java

// 有反射：类名从配置文件读取，运行时动态创建

String className = config.getProperty("className");  // "com.example.Student"

Class<?> clazz = Class.forName(className);

Object obj = clazz.getDeclaredConstructor().newInstance();

// 配置文件改成"com.example.Teacher"，代码一行不用改！

没有反射，Spring根本无法存在。 配置文件写的是字符串类名，Spring需要根据字符串动态创建对象——这件事不用反射做不到。

- 能不能用"内部权限"替代反射？答案: 不能.

|原因|	说明|
|---|---|
|Java没有"内部权限"这种东西|	Java的访问控制是public/protected/默认/private，没有"框架可以访问但用户不能访问"这种级别。Spring是第三方库，和你的代码不在同一个包，它只能看到public的东西|
|类名是运行期才知道的|	就算Spring能访问private，它也不知道要访问哪个类。类名是从配置文件/注解中读出来的字符串，必须用Class.forName()加载，这就是反射|
|编译期无法确定|	Spring在编译时不知道你要把哪些类注册为Bean，它只能在运行时扫描。不用反射，Java没有其他机制能在运行时动态操作任意类|

反射在设计层面的意义

|维度|	说明|
|---|---|
|配置驱动|	代码写逻辑，配置写数据，反射把两者连起来。Spring用XML/注解配置Bean，运行时反射创建|
|解耦|	框架代码不需要知道你的业务类，只通过反射操作。Spring不知道你有UserServiceImpl，但通过反射能创建和注入它|
|注解的基石|	注解本身不产生任何行为，必须通过反射读取注解才有意义。@Autowired、@GetMapping全是靠反射扫描和执行的|
|动态代理的基石|	JDK动态代理（Proxy.newProxyInstance）底层用的就是反射，这是Spring AOP的实现方式|
**一句话总结**：反射是框架的灵魂。没有反射，Java就没有Spring、MyBatis、Hibernate等任何框架。你之前学的IO流是基础设施，多线程是并发能力，网络编程是通信能力，而反射是让框架成为可能的能力。

## Day 23 (2026-8-13)
### 今天学习了动态代理的知识

> 动态代理是什么?它有什么知识点需要我们掌握?

**我的回答**
1. 动态代理是Java提供的一种无入侵性的一种实现类的方案.它允许我们在不修改源代码的情况下对类中的方法进行实现,并对其进行一些增强的操作.
2. 常用的代理方案有两种,第一中是JDK原生提供的代理方案,该方案要创建代理类原类必须实现接口.不然无法创建.第二种CGLIB第三方方案,不需要实现接口,这种方案之后会学,现在暂时了解存在.
3. 第一种动态代理方案具体怎么实现的?
- 前提条件: 存在对应的类,该类必须实现接口.
- 通过Proxy类中的静态方法newProxyInstance()来创建代理类的对象,其中一共有三个参数:
- 第一个参数表示通过哪一个类来加载.第二参数表示一个Class类的数组,该数组中传入对应实现的接口的class对象
- 第三个参数表示类实现器,一般通过匿名内部类的方式重写里面的invoke方法,该方法的三个参数(Object Proxy, Method method, Sting[] args),分别表示对象本身,一般不使用这个参数;方法的对象,我们可以对这些方法进行增强处理;方法的参数.
- 通过返回方法对象的invoke(原本类对象,args);来返回对应的方法,外层再返回代理类的对象.
- 外界代理类对象调用时会先触发newProxyInstance()中的invoke(),再通过内部invoke()实现原本类中对应的方法.

**错漏点**
1. invoke第三参数是 Object[] args，你写成了 Sting[] args。另外三个参数标准名是：Object proxy（代理对象本身）、Method method（被调方法）、Object[] args（方法实参）
2. 遗漏点：

|遗漏|	说明|
|---|---|
|第三个参数的名字|	你叫它"类实现器"，标准名字是 InvocationHandler（调用处理器） 接口。重写的那个方法叫 invoke()|
|触发机制|	你说"外界代理类对象调用时会先触发newProxyInstance()中的invoke()"——表述不准。准确说法：代理对象调用任意接口方法时，都会自动转发到 InvocationHandler.invoke()。不是"触发newProxyInstance()里的invoke"，newProxyInstance是创建代理的那一步，invoke是之后每次调用的入口|
其他细节区别看自己笔记

> 动态代理内部到底是怎么实现的?这些参数又各起了什么作用?光从自己写的代码很难理解内部做了什么.

**我的回答**
内部应该是通过反射获取创建代理类的.

**错漏点**
1. JDK 动态代理的底层：运行时生成字节码

Proxy.newProxyInstance(loader, interfaces, handler)
↓
运行时动态生成一个代理类（名为 $Proxy0 的字节码，默认存在内存里）
↓
$Proxy0 的结构：
class $Proxy0 extends Proxy implements 目标接口 {
// 继承了Proxy，构造时保存handler
// 实现了目标接口的每个方法，方法体都一样：
public String getName() {
return (String) handler.invoke(this, getName方法对象, 实参);
//     ↑ 方法体只有一行：把调用转发给handler
}
}

- 关键点：**代理类的每个方法都只做一件事——转发**。 
调用方调 proxy.getName() → 实际上执行的是 $Proxy0.getName() → 方法体里调用 handler.invoke() → 你的增强逻辑在这里执行 → 内部再 method.invoke(目标对象, args) 调真实方法。

- 完整调用链（面试必讲）

调用方
↓ 调 proxy.method()
$Proxy0.method()（代理类，字节码生成）
↓ 转发
InvocationHandler.invoke(proxy, method, args)   ← 你的增强逻辑在这里
↓ 内部调
method.invoke(目标对象, args)                    ← 反射调真实方法
↓
目标对象.method()                                ← 真实业务逻辑

为什么必须实现接口？（这是核心设计原因）

**因为Java单继承**.代理类$Proxy0已经继承Proxy类了,就不能再继承目标类了.那怎么让调用方感觉代理对象"就是"目标对象？只能通过实现相同的接口来伪装成同一种类型。

$Proxy0

├── 继承 Proxy（必须，Java规定）

└── 实现 UserService 接口（这样代理对象能被当成UserService用）

小结：JDK动态代理 = 反射（method.invoke）+ 字节码生成（$Proxy0）+ 多态（接口伪装）。


> 为什么要设计动态代理?还有没有非动态代理的低入侵方案?它在具体业务中有那些体现?

**我的回答**
1. 动态代理可以让我们在低入侵的情况下实现更多增强逻辑,它相比于修改源码更加灵活,而且不容易出错.

**错漏点**
1. 你只提到了动态代理的好处,但不是"为什么设计".

**设计的出发点：静态代理的痛点**

|静态代理的痛点|	动态代理怎么解决|
|---|---|
|每个类都要手写一个代理类 → 类爆炸|	一个 InvocationHandler 可以给任意类生成代理|
|接口加一个方法，代理类也要跟着改 → 维护成本高|	接口方法变化，$Proxy0 自动重新生成，代码不用动|
|增强逻辑散落在每个代理类里，重复|	增强逻辑只写在 invoke() 一处|

一句话：静态代理把"增强"写死在类里，动态代理把"增强"抽象成一段逻辑，运行时动态套到任何类上。

实际业务场景（回答你问的"具体业务中有哪些体现"）

|场景|	怎么用动态代理|
|---|---|
|Spring AOP 事务|	你在Service方法上标@Transactional，Spring运行时给Service生成代理，invoke()里"开启事务→调真实方法→提交/回滚事务"|
|Spring AOP 日志/权限|	同样机制，invoke()里先做权限校验、记日志，再调真实方法|
|MyBatis Mapper	|你只定义了接口 UserMapper 没有实现类，MyBatis用动态代理自动生成实现，invoke()里解析方法名拼SQL执行|
|RPC 远程调用|	你调 userService.save() 感觉是本地方法，代理对象把调用序列化成网络请求发到远程服务器（Dubbo、gRPC底层）|
|监控埋点|	给所有方法统一加耗时统计，不改业务代码|

有没有"非动态代理"的低入侵方案？

|方案|	是否低入侵|	问题|
|---|---|---|
|继承重写|	中等|	和目标类紧耦合，增强逻辑写死在子类，每个类写一个子类 → 类爆炸|
|装饰器模式|	中等|	需要修改调用方代码（把 new 目标对象 换成 new 装饰器）→ 还是入侵了调用方|
|动态代理|	✅ 最优|	调用方代码一行不用改，代理在运行时自动套上|

结论：动态代理就是"低入侵增强"的最优解，没有更好的替代。它把"改代码"变成了"套壳"，这是开闭原则（对扩展开放、对修改关闭）的经典体现。


## Day 24 (2026-8-14)
### 学习了枚举的知识,初步了解数据结构

> 什么是枚举?它有什么基本概念需要我们学习?它有什么特点?

**我的回答**
1. 枚举是Java提供的一种特殊的类,它的内部封装了大量的API,通过enum修饰符来表现.它内部存在一个或多个枚举类对象,中间用,隔开
2. 枚举类是一种特殊的类,所以它内部也可以定义成员变量,构造方法,成员方法.
还有常用的API如name()返回该枚举类对象的对象名,ordinal()返回按照定义顺序自动生成的排序,values()返回所有枚举类对象的数组,valueOf().
3. 对于枚举中的成员变量,一般都定义为private static final 常量,可以通过构造方法将这些常量传递给枚举类对象,给这些枚举类对象设定特定的参数.
比如代码中不能依靠ordinal()生成的排序序号,这种序号可能会发生改变.我们就可以给每一个对象设置一个指定的code.
对于枚举类中的构造方法,默认为private,写不写private都没有影响.它的作用主要就是初始化枚举类对象.
对于枚举类中的成员方法,一般只设置get参数的方法,以及其他的一些获取对应属性的方法.
4. 枚举也可以重写抽象方法,实现接口.不过它已经继承Enum类,无法再继承其他类.
5. 枚举的内部属性无法通过反射进行获取更改.

**错漏点**

**错误1** 你说"枚举中的成员变量一般定义为 private static final 常量"——这里 static 是错的

关键区别：

static → 所有实例共享一个变量，SPRING 和 SUMMER 的 code 相同 → 毫无意义

final（非 static）→ 每个实例有自己的 code 值，这才是给每个枚举对象设置独立参数

**错误2**: "枚举无法通过反射获取",获取可以,但修改有限制.

获取: 反射可以正常get()枚举的属性值,没问题.

修改: 限制很大-----不能通过Constructor.newInstance()创建枚举实例,直接报IllegalArgumentException,也不能修改final字段,这是JVM为了保护枚举单例语义的限制.

**2个注意点**: enum是关键字不是修饰符,构造方法默认为private且只能为private,写其他修饰符类型的直接报错.




> 枚举类内部到底是怎么实现的呢?枚举类对象如SUMMER它与普通的类对象有什么区别?

**我的回答**
1. 枚举类内部封装了大量的API.

**错漏点**

**枚举的底层实现: 编译器帮你生成了一个final class**

你写的：

java
public enum Season {
SPRING, SUMMER, FALL, WINTER;
}

编译后等价于：

java
public final class Season extends Enum<Season> {
public static final Season SPRING = new Season("SPRING", 0);
public static final Season SUMMER = new Season("SUMMER", 1);
public static final Season FALL   = new Season("FALL", 2);
public static final Season WINTER = new Season("WINTER", 3);

private Season(String name, int ordinal) {
        super(name, ordinal);  // 调用 Enum 父类构造
    }

// 编译器自动生成
public static Season[] values() { ... }
public static Season valueOf(String name) { ... }
}

SUMMER 和普通类对象的核心区别：

|  |枚举实例 SUMMER|	普通类对象 new Season()|
|---|---|---|
|创建方式	|类加载时 JVM 自动创建	|每次 new 创建一个|
|实例数量	|全局唯一一个（单例）	|想要多少有多少|
|线程安全	|天然安全（类加载保证）	|需要自己加锁|
|能否 new	|不能（构造方法私有）	|能|
|能否被继承	|不能（final class）|	看是否有 final|
一句话总结：**枚举实例本质是 public static final 的单例对象，在类加载的 <clinit> 阶段被创建，JVM 保证全局唯一**。

> 为什么要设计枚举类呢?枚举类它有什么应用场景?

**我的回答**
1. 没有枚举类的话,我们在需要使用一些固定的值时,只能通过static final定义,这种方法传递任何相同类型的参数都不会报错.并且打印后只显示值是多少,可读性差.
2. 我们通过枚举类创建的枚举类对象,可以直接将它表示什么设置成名字,简单易懂,且枚举类将API封装.不需要我们大量写重复的代码.

**错漏点**

你说的"魔法数字问题、可读性差"方向对了，但太浅。枚举真正的价值在以下几个场景：

① 替代魔法数字（你提到了）

// 烂：传参时 int 类型不区分，传1进去编译器不知道是季节还是月份
void setSeason(int season) { ... }

// 好：类型安全，传错类型编译直接报错
void setSeason(Season season) { ... }

② 状态机定义（面试最常考的应用）

public enum OrderStatus {
PENDING, PAID, SHIPPED, COMPLETED, CANCELLED
}

订单状态、支付状态、审批流程——凡是有"固定取值集合"的地方，枚举是首选。

③ 单例模式的最佳实现（Effective Java 作者推荐）

public enum Singleton {
INSTANCE;
// 天然线程安全、防反射攻击、防反序列化漏洞
}

为什么推荐枚举单例？因为它天生防反射（上面第二层讲的，newInstance 会抛异常）+ 天生防反序列化（序列化时 JVM 保证返回同一个实例）。这是双重检查锁单例做不到的。

④ 策略模式

public enum Calculator {
ADD { public int calc(int a, int b) { return a + b; } },
SUB { public int calc(int a, int b) { return a - b; } };
public abstract int calc(int a, int b);
}

每个枚举实例可以重写方法，自带不同行为——这就是策略模式的简化版。


## Day 25 (2026-8-15)
### 今天了解了Java log日志,双重检查锁,做了个简单的控制台版的通讯录管理系统

> log日志是什么?双重检查锁又是什么?两者的基本概念?有哪些内容需要我们掌握?

**我的回答**

1.log日志是为了让程序员更方便地查看程序的运行状态而产生的,它不同于直接System.out.println打印在控制台,我们可以将日志直接输入到本地文件中,在文件中查看.
并且在需要查看的内容改变时,不需要修改原代码,直接对对应的配置修改一下就行.最常用的是logback.

2.双重检查锁DCL是一种懒加载的保证单例模式的一种方法,它的目的是在保证单例对象的情况下,尽可能地减少锁的开销.

它主要由外层检查: 判断对象是否创建,防止后续线程再进来抢锁.中间锁: 保证线程安全.内部检查: 防止出现多个线程在对象未创建时进入阻塞等待状态绕过了外层检查,在创建对象后又重复创建.
其中对象必须通过volatile关键字修饰以保证其创建过程不被指令重排序,导致未完全初始化的对象被使用.

**错漏点**
1. log漏了两个最重要的概念：
- ①日志级别(最重要,必须记)
- TRACE < DEBUG < INFO < WARN < ERROR
- ② 门面 + 实现的分离
- 你说"最常用的是 logback"——准确说应该是 SLF4J（门面）+ Logback（实现）。这和你学过的 JDBC 思路一模一样：
- 代码里写 LoggerFactory.getLogger()（SLF4J 的 API），底层换成 Logback 还是 Log4j2 都不用改代码。你说的"侵入性低"其实就是在说这个，但没点破。
- 一个小纠正：你说"防止出现多个线程在对象未创建时进入阻塞等待状态绕过了外层检查"——更准确的说法是：
多个线程同时通过了外层检查（此时对象还没创建），然后排队抢锁，第一个线程创建完对象释放锁后，第二个线程拿到锁进来，如果不做内层检查就会重复创建。 不是"绕过"外层检查，是外层检查时对象确实还没创建，所以放行了。

> log日志是怎么实现它的功能的呢?双重检查锁线程是怎么执行的?

**我的回答**
1. log日志不了解
2. 双重检查锁线程的执行应该是: 通过外层检查->竞争锁->内部判断是否是第一个拿到锁的->创建对象

**错漏点**

日志的底层实现链路：

你的代码调用 logger.info("用户{}登录", name)
↓
SLF4J 门面转发给绑定的实现（Logback）
↓
Logback 的 Logger 对象做级别判断（当前配置 >= INFO？）
↓ 通过
格式化消息（替换占位符 {}）
↓
Appender 决定输出到哪里（控制台 ConsoleAppender / 文件 FileAppender）
↓
Layout 决定输出格式（时间戳 + 线程名 + 级别 + 类名 + 消息）

关键组件就三个：Logger（记录器）→ Appender（输出目的地）→ Layout（输出格式）。XML 配置文件配的就是这三个。你说的"通过XML配置"就是在配 Appender 往哪写、Layout 写成什么格式、Logger 用什么级别过滤。

**为什么需要内层检查?**

假设线程 A 和 B 同时到达 ①，都发现 instance == null（对象还没创建），都通过外层检查。然后 A 先抢到锁进入 ②③④，创建完对象，释放锁。B 接着拿到锁进入 ③——如果没有内层检查，B 会再 new 一次，单例就被破坏了。

**volatile关键字到底防什么?**

instance = new Singleton() 这一行实际分三步： 1.分配内存. 2.初始化对象. 3.把内存地址赋值给instance对象

JVM指令重排序可能会出现1->3->2,导致instance指向了一个未初始化的内存,别的线程在外层检查时看不到instance == null,直接将半初始化对象拿走.volatile通过在指令两端生成内存屏障,禁止两端指令重排序.

> 为什么要这么设计log日志和双重检查锁?

**我的回答**
log日志通过xml文件来配置.这样做侵入性很低,流程也不复杂.

双重检查锁前面已经很明确了

**错漏点**

log日志——你只说了"XML配置侵入性低"，太浅

为什么要设计"门面 + 实现"分离？

项目可能从 Log4j 迁移到 Logback，业务代码一行不用改
类比 JDBC：MySQL 换成 PostgreSQL，DAO 代码不用改
这就是"面向接口编程"在日志领域的应用
为什么要分级别？

生产环境不可能把所有日志都打出来（量太大），但也不能只看 ERROR（排查问题需要上下文）
级别让你按需过滤，开发时看 DEBUG，上线后看 INFO/WARN

三层不是问你"怎么做的"，是问你为什么这样做：

|问题|	答案|
|---|---|
|为什么不用 synchronized 修饰整个方法？|	每次调用都加锁，性能太差。99% 的时候对象已创建，不需要锁|
|为什么不只用外层检查（不加锁）？|	多线程下两个线程同时通过检查，重复创建，线程不安全|
|为什么不只加锁不做外层检查？|	每次都竞争锁，和直接 synchronized 方法一样慢|
|为什么不直接用饿汉式（static final）？|	饿汉式类加载就创建，如果不常用就浪费内存。DCL 是懒加载|
|为什么不直接用枚举单例？|	枚举单例更好——天生线程安全、防反射、防反序列化。DCL 是"不用枚举时的妥协方案"|
最后一条很重要：**Effective Java 明确推荐用枚举实现单例，而不是 DCL**。 你今天同时学了枚举和 DCL，应该把它们联系起来——枚举单例是更优解，DCL 是面试必考但不一定是最优实践。

> 做通讯录管理系统的反思

第一点: 将加载文件的方法写到了while循环内部,而每次只有在输入6关闭时才会save,这样会导致集合中出现多次加载文件中的内容

第二点: 在使用Collections.replaceAll()时竟然忘记重写equals和hashcode方法了,我应该再每次创建Javabean类时都顺手重写,防止出现这种致命的情况.

反思: 通讯录管理系统是否能通过枚举实现呢? 若出现多个线程同时修改内容,应该怎么改代码?有没有线程安全的单例集合能替代ArrayList?

**回答**

每次创建 JavaBean 类时都顺手重写 equals 和 hashCode"这个习惯意识是对的——但有个前提：只有这个类会被放进需要 equals 判断的容器里时才需要重写（HashMap 的 key、contains 判断、remove 判断）。如果只是个纯数据载体，不重写也没事。别变成无脑加。

Q1：通讯录能否通过枚举实现？

不能。枚举是"固定的有限常量集合"——四季、星期、订单状态。通讯录的联系人数量是动态的、运行时才确定的，你要 new 多少个 Contact 在编译时根本不知道。枚举实例在类加载时就全创建好了，没法动态添加。枚举适合"状态定义"，不适合"动态数据存储"。

Q2：多个线程同时修改内容怎么改？

三种方案，从简单到复杂：

// 方案1：用 Collections.synchronizedList 包装（最简单）  //没有想到
private List<Contact> contactList = Collections.synchronizedList(new ArrayList<>());

// 方案2：用 CopyOnWriteArrayList（读多写少时用）
private List<Contact> contactList = new CopyOnWriteArrayList<>();

// 方案3：手动加锁（最灵活，但代码复杂）
public void addContact(Contact c) {
synchronized (contactList) {
contactList.add(c);
}
}

## Day 26 (2026-8-16)
### 今天了解了XML文件以及dtd,Schema约束文件,LeetCode写了道罗马数转现代数的题目

> 什么是XML,dtd,Schema文件?它们有什么知识点需要我们掌握?

**我的回答**
1. XML配置文件不同与properties只能通过键值对的形式存放数据,它能通过标签灵活地创建多种文本参数,更加灵活.
关于XML我们要注意的主要是它的语法,对于<,>,&这些符号必须通过转义表示或者通过<![CDATA[...]]>来保留这些特殊符号,且XML第一行必须是声明内容,只能存在一个根标签.
2. dtd是XML的约束文件,它可以约束XML的格式.Schema则不仅能约束XML格式,还能约束XML文本的数据类型.

**错漏点**

你说了“Schema 能约束数据类型”，但它还有两个重要优势：

|对比项| DTD|	Schema|
|---|---|---|
|数据类型|	❌ 只能约束结构	|✅ 可约束类型（string/int/boolean…）|
|自身语法|	自己的一套奇怪语法|	本身就是 XML（学一种语法就够了）|
|命名空间|	❌ 不支持|	✅ 支持|
“Schema 本身也是 XML”这点很重要——意味着约束文件本身也能被 XML 工具解析和校验。

“XML 第一行必须是声明内容”——准确说法是：声明如果存在，必须位于第一行。声明本身可以省略（但不建议）。另外你漏了几个基础规则：标签区分大小写、属性值必须用引号、所有标签必须闭合。

> XML是怎么通过SAXReader就能读取的?dtd,Schema文件又是怎么约束XML文件的呢?

**我的回答**
通过DOM或者SAX模式将XML文件加载到内存中,然后获取其内容.dtd,Schema应该是通过反射来获取XML文件的格式,格式不符合,报错.

**错漏点**

反射获取完全是主观意象

DOM 和 SAX 是两种不同的解析策略，你把它们混为一谈了

|   |DOM| 	SAX    |
|---|---|----------------|
|原理|	一次性把整个 XML 加载进内存，构建成一棵树| 	流式逐行读，触发事件（开始标签/文本/结束标签） |
|内存|	占用大（整棵树都在内存）|	占用小（只保留当前正在处理的一段）|
|访问方式|	可随机访问任意节点，可回头|	只能从头到尾过一遍，不能回头|
|适合场景|	配置文件（小）|	大文件解析（几百 MB 的日志）|
SAXReader 的名字有点迷惑——它是 dom4j 库的类，虽然名字带 SAX，但实际做的事是：用 SAX 的方式逐行解析，同时在内存里构建出一棵 DOM 树。所以你能拿到 Document 对象然后 getRootElement() 遍历——这个树就是 DOM 模型。

② DTD/Schema 约束的真实原理——校验（Validation），和反射零关系

流程是这样的：

解析器（Parser）读到 XML 头部声明的约束引用
↓  例如 <!DOCTYPE root SYSTEM "my.dtd">
↓  或  <root xmlns:xsi="..." xsi:noNamespaceSchemaLocation="my.xsd">
解析器加载 DTD/Schema 文件，解析成一套"规则表"
↓
开始逐节点解析 XML：每遇到一个标签，
对照规则表检查——标签名合法吗？顺序对吗？出现次数超标没？数据类型匹配吗？
↓
任何一处不符合 → 立即抛出解析异常（SAXException等）

本质就是**解析器**边读边对照检查，跟你用 JSON 解析器校验 JSON 格式是一个思路。没有任何反射参与。


> 为什么要设计XML,dtd,以及Schema文件?

**我的回答**
1. XML是为了弥补txt文件可读性不高,不容易解析,properties文件又只能存储键值对形式,大量数据下难以定位的缺点,而设计出来的配置文件.
2. XML文件如果不被约束,可能会出现语义不统一,标签Text内部写入了不合适的内容的情况.这两者就是为了解决这些问题,让XML文件的内容更加规范化.

**错漏点**

① XML 的核心设计思想是 **“自描述”**

<user>
    <name>张三</name>
    <age>20</age>
</user>


标签本身就是含义（name、age），数据无需外部说明就能被理解。对比 txt 里的 张三,20——不看文档你根本不知道 20 是年龄还是体重。**自描述 + 层级结构 + 纯文本（平台无关）**，这三点让 XML 在上世纪成为跨系统数据交换的标准。

② **DTD/Schema 的本质是“契约”**

类比：

- DTD/Schema 之于 XML ≈ Java 接口之于实现类——规定了“必须有哪些东西、什么类型、什么顺序”
- 没有约束的 XML，写配置的人可能漏写必填项、写错标签名、顺序颠倒——解析的人（或程序）直接崩
- 有了约束文件，写的人和解析的人有了共同遵守的契约，XML 文件在交付时就能被机器校验合法性
  ③ 为什么 2026 年还要学它——历史存量

趋势上，注解 + YAML/JSON 正在取代 XML（Spring Boot 用 application.yml + 注解配置）。但：

- Maven 的 pom.xml 至今是 XML，你天天要用
- 大量老项目和公司存量系统还是 XML 配置
- 所以计划里说“会读会看结构即可”，你现在的掌握程度已经接近够用了


> 反思
1. 关于XML,dtd,Schema文件的语法掌握不牢,需要注意.\
2. 重新通过AI复习了Lock锁的Condition队列.与synchronized只有一个WaitSet不同,一个Lock锁可以创建多个Condition等待队列,通过signal()可以唤醒指定队列的第一个任务,重新进入AQS双向阻塞队列竞争锁.
3. Lock锁在面对任务时,什么时候创建新Condition?什么时候将任务放入旧Condition中?
4. 罗马数字对于ⅠⅤ这类需要减去左边的数字. 

**回答**
- 按“等待的原因”分组——不同条件用不同 Condition。
- 经典例子：生产者-消费者（阻塞队列）：

private final ReentrantLock lock = new ReentrantLock();
private final Condition notFull  = lock.newCondition();  // 队列满 → 生产者在这等
private final Condition notEmpty = lock.newCondition();  // 队列空 → 消费者在这等

如果只用一个 Condition 会怎样？ signal 唤醒的是“队列里第一个线程”，但它可能是生产者也可能是消费者——生产者想唤醒消费者，结果唤醒了另一个生产者，白白浪费一次唤醒甚至造成低效。两个 Condition = 精准唤醒对方阵营。
JDK 的 ArrayBlockingQueue 源码里就是这两个 Condition（notEmpty / notFull），有兴趣可以去看，代码很短。


## Day 27 (2026-8-17)
### 学习了类加载器,双亲委派模型以及单元测试,Junit

> 什么是类加载器,双亲委派模型,单元测试,Junit?它们有那些知识需要我们掌握?

**我的回答**
1. 类加载器负责将class字节码文件加载入内存中,并对其进行一些操作,其中过程分为5个阶段:
- **加载**: 在元空间中创建class字节码文件的class对象.
- **检验**: 检验字节码的合法性.
- **分配**: 为**静态变量**分配内存空间,并将其赋为默认初始化值,对于编译期常量(static final)此时赋的是实际值.
- **解析**: 将原文件中的解析引用转变为直接引用
- **初始化**: 将静态变量赋为实际值,并触发静态代码块.
2. 类加载器总共分为4层
- 初始化类加载器: 对JDK的核心文件进行初始化,是类加载器中的顶级父类
- 平台类加载器: 主要用于加载远端jar.父类是初始化类加载器
- 应用程序类加载器: 用于加载我们书写的类.父类为平台类加载器
- 自己写的类加载器: 自己书写
3. 双亲委派模型就是类加载器的典型模型:
- 指的是一个类加载器受到类的加载请求,不会自己首先加载,而是交给自己的父类加载,若父类不能加载才尝试自己加载.
- 这种模型有两大好处: 第一,保证原加载不被污染,自己写的java.lang.String不会被加载. 第二忘了.
- 打破双亲委派模型的例子: 忘了.
4. 单元测试是Java提供的一种测试代码的方法,它提供了统一的规范.Junit就是其中的最典型框架.
5. 现在最常用的是junit4和5,其中5常用的注解就是@Test,@beforeEach(在每个测试前加载一次),@beforeAll(在所有测试开始前执行一次)以及对应的@afterEach,@afterAll,
@displayName("设置测试名字"),@skip(跳过)
- 最常用的**断言**: assertEquals(),assertBoolean(),assertTure(),assertThrow().assertAll()
- 对于@Test的方法必须是public的无返回值无参数的方法.

**错漏点**
1. 错误 1：“解析：将原文件中的解析引用转变为直接引用”——术语错了，是** **符号引用**（Symbolic Reference）**转直接引用。符号引用就是 class 文件里存的“字符串形式的描述”（比如 java/lang/Object），直接引用是内存中的真实地址/指针。这是面试高频词，别记错。
2. 细节 2：“加载：在元空间中创建 Class 对象”——不完全准确。类的元数据（方法字节码、常量池等）进元空间，但 java.lang.Class 对象是放在堆里的。面试问到这点会加分。

② 类加载器分层——两个名字错、一个职责错

|你写的|	正确的|	职责|
|---|---|---|
|初始化类加载器|	启动类加载器（Bootstrap）|	加载 JDK 核心模块（java.base 等）|
|平台类加载器|	名字对，但职责错了|	加载 JDK 非核心模块（java.sql、java.xml 等），不是加载远端 jar|
|应用程序类加载器|	✅|	加载 classpath 上的类（你写的 + 第三方 jar）|
|自定义类加载器	✅|	继承 ClassLoader 自己写|
“加载远端 jar”这个职责在标准三层里不存在——那是 URLClassLoader 或网络类加载器（已废弃的 Applet 体系）干的事。

③ 双亲委派——机制对，忘掉的两点补上

好处2: **避免类的重复加载**,**保证类的唯一性**.同一个类只会被一个类加载器加载,JVM判断两个类是否相同是
通过"全类名 + 加载器"判断.没有委派,一个类可能会被多个类加载器加载,类型系统就乱了.

打破双亲委派的经典例子（面试必考，记两个就够）：

JDBC/SPI 机制：Driver 由厂商实现，Bootstrap 加载的 DriverManager 需要加载厂商的 Driver 类，但 Bootstrap 看不到 classpath——用线程上下文类加载器（Thread Context ClassLoader）反向委托给 App 加载器

Tomcat：每个 Web 应用有自己的 WebappClassLoader，优先自己加载（不先委派父加载器），这样两个应用可以用同一个 jar 的不同版本互不干扰

④ JUnit——三个小错

单元测试是 Java 提供的”——不准确。JUnit 是第三方框架，不在 JDK 里，是 Maven 依赖引入的。单元测试本身是一种测试方法论，也不是 Java 发明的。

@skip → 正确注解是 @Disabled

assertBoolean() 不存在——是 assertTrue() / assertFalse()；assertTure 拼写错了（assertTrue）；assertThrow 少个 s（assertThrows）

“@Test 方法必须是 public”——这是 JUnit 4 的规则。JUnit 5 里测试类和方法用包级私有（不写修饰符）就行，不需要 public。你学的是 JUnit 5 的注解却记了 JUnit 4 的规则，混了。


> 类加载器是怎么实现的?Junit是怎么实现不通过main方法也能运行的?

**我的回答**
1. 只知道语言描述,内部源码细节需要我们掌握吗?
2. 不明白

**错漏点**

先回答你的问题：“内部源码细节需要我们掌握吗？”

不需要。 类加载器源码是 C++/Java 混合的 JVM 内部实现，面试考的是概念模型（五阶段、双亲委派、打破场景），不是源码。之前说过了：这块属于 JVM 面试专题，现在掌握到概念层就够，第七阶段再系统背。你今天的“不知道”是合理的，不用慌。

JUnit 为什么没有 main 也能跑？——正好接上你学过的反射

你点 IDEA 的"运行测试" / Maven 执行 mvn test

↓

IDE/Maven 自己调用了 JUnit 的 Launcher（入口在 JUnit 框架里，不在你的代码里）

↓

JUnit 扫描你指定的测试类 → Class 对象

↓

用反射遍历所有方法：method.isAnnotationPresent(Test.class)

↓

对每个 @Test 方法：method.invoke(测试类实例)

↓

收集结果（哪个过了、哪个挂了、异常是什么），生成报告

看到了吗——JUnit 就是你 Day 22/23 学的反射 + 注解的直接应用：getDeclaredMethods() 找方法、判断注解、invoke() 执行。这就是“框架”的本质：入口和调度逻辑在框架里，你的代码只负责被反射调用。这也是为什么你学反射时说“不知道有什么用”——现在它就是答案之一。


> 为什么要设计类加载器,并将其分为这么多步骤?这样设计的原因?为什么设计单元测试?它相比于直接运行测试有什么好处?

**我的回答**
1. 不知道
2. 设计单元测试是为了解决直接运行测试的缺点,一点就是单元测试的某个@Test报错,其他的依旧会执行.第二点就是提供了统一的注解以及测试格式.

**错漏点**

① 为什么类加载要分五个阶段？

|设计|	原因|
|---|---|
|为什么懒加载（用到才加载）|	程序可能引用几百个类但只用到一个分支，全部预加载浪费内存和启动时间|
|为什么单独有“验证”阶段|	class 文件可以被任何工具生成/篡改，验证是安全防线——防止恶意字节码击穿 JVM（比如越界访问内存）|
|为什么“分配”和“初始化”分开|	分配阶段先给默认值（int=0、引用=null），保证初始化代码执行时变量至少有确定的初始状态，不会读到随机内存|
|为什么符号引用→直接引用	|class 文件是静态的，编译时不知道目标类在内存的哪个地址；解析阶段才把这些“符号”绑定到真实内存位置——这就是 Java 动态链接的特性|

② 为什么双亲委派？（两大好处你找回一个）

安全（核心类不被污染）+ 唯一性（类不重复加载）。再往深说一句：委派的方向是“向上”的，因为越上层加载的东西越基础、越可信——核心类让最顶级的 Bootstrap 加载，自定义类加载器管自己那一摊，各层各司其职。

③ 单元测试相比 main 方法测试的好处——你答了两条，再补三条

✅ 一个测试挂了不影响其他（隔离性）

✅ 统一注解和格式

➕ 断言 vs 肉眼：assertEquals(expected, actual) 自动判对错；main 方法里 System.out.println 你得用眼睛一条条核对结果对不对，100 个测试用例肉眼根本看不过来

➕ 回归测试自动化：改了代码后一键跑全部测试，立刻知道有没有改坏旧功能——这是 CI/CD 的基础

➕ 测试即文档：看测试方法名就知道这个方法预期行为是什么


## Day 28 (2026-8-18)
### 今天刷了几道关于二分查找的算法题,复习了关于动态代理的知识,以及优化通讯录查找系统.

> 反思

1.关于二分查找边界处理问题需要注意,二分查找书写时最好画相关的图来帮助理解,改查找的时间复杂度为O(logN).

2.关于动态代理: 动态代理是Java提供的一种运行期动态方案.JDK原生动态代理通过Proxy.newProxyInstance(类加载器,new class[]{需要实现的所有接口的class对象}, InvocationHandler调用处理器)实现,会返回一个代理类对象.

-InvocationHandler中invoke方法的三个参数(代理类本身, 要实现的方法, 方法实参),我们可以在invoke方法中写增强逻辑,执行完这些逻辑后可以通过反射调用该方法的类执行该方法.

-InvocationHandler运行时会生成动态代理类$ Proxy0,该类重写了接口中所有的方法,每次调用时都会将方法转发给invoke.

-为什么必须通过接口实现?因为InvocationHandler生成的动态代理类 $ Proxy0 已经继承了Proxy,如果想要伪装成目标类调用方法,就需要向上强转成对应的接口.

-为什么要设计动态代理?可以在不修改原代码的情况下执行增强逻辑(低侵入性),相比于静态代理一个动态代理可以同时代理多个目标类,代码复用性高.可以将每次书写静态代理的零散的增强逻辑封装在一起.

3.对于获取类中集合的方法,应该return原集合的副本(防御性拷贝),防止其他内容直接修改该集合.

> 纠正/补漏

①动态代理复习

错误 1：表述含糊——invoke 里到底调用谁的 方法？

你写的“通过反射调用该方法的类执行该方法”不通。准确说法：

public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {

// 增强逻辑（前置）

Object result = method.invoke(target, args);  // ← 调用的是【目标对象】的该方法

// 增强逻辑（后置）

return result;

}

坑（重要）：invoke 里千万别写 method.invoke(proxy, args)——proxy 是代理对象自己，等于代理调自己 → 又进 invoke → 无限递归 StackOverflowError。这是新手写动态代理的第一大坑，记住。

补充：invoke 第一个参数 proxy 的用途——它就是 newProxyInstance 的返回值本身，一般在 invoke 里用不上，但要在方法里返回代理对象或做类型判断时可以用它。

② 二分查找反思

你写的三条都对（边界处理、画图、O(logN)），但太泛了，属于“正确的废话”。刷题日的反思要写具体：

1.防溢出的中点写法（面试常考）

int mid = left + (right - left) / 2;   // ✓ 推荐写法

// int mid = (left + right) / 2;       // **left+right 可能溢出 int**

2.两种区间写法，循环条件必须配套（边界问题的根源）

| 区间定义                |	循环条件	|更新|
|---------------------|---|---|
| 左闭右闭 [left, right]	 |while (left <= right)|right = mid - 1 / left = mid + 1|
| 左闭右开 [left, right)	 |while (left < right)|right = mid / left = mid + 1|

比如今天应该写：“做了 LC704 二分查找。第一次提交错在 while 写成 < 但用了闭区间的 right = mid - 1，导致漏查最后一个元素。画图后发现……”。**具体到题、具体到错、具体到怎么发现的**——这才是以后能回看的东西

## Day 29 (2026-8-19)
### 今天了解了注解的具体知识,复习了关于intern()方法的内容,leetCode主要刷了关于快慢指针的内容.

> 对于注解的三层提问
> 什么是注解?它有那些知识需要我们了解的?

**我的初答**

注解是java提供的一种代码标记,它本质上是继承了Annotation的特殊接口.

它的主要用途: 第一点,编译期校验,@Override让编译器校验该方法是否合法.第二点,运行期功能配置,通过反射获取注解,执行相应的逻辑,Junit典型列子.

注解的生命周期分为SOURCE(仅留在源码中),CLASS(会被加载进class文件,但不会被加载进内存),RUNTIME(类加载时存入元空间,可以被反射调用),自己写的注解通过@Reetantion()指定生命周期,一般为RUNTIME.

限制注解在何处声明,通过@target()指定,取值来自ElementType的枚举: 常用TYPE(类,接口,枚举),METHOD(成员方法),FIELD(成员变量,字段)

关于自定义注解,注解属性默认为public类型通过default设置默认参数,若不设置默认参数,创建时必须传递参数值.特殊注解属性value,属性中若只存在一个value,创建注解时参数("这是一个注解")不需要写"value =".

两个常用API: isAnnotationPresent(..)判断是否存在指定注解.getAnnotation(..)获取指定注解.

**错漏点**

|你的表述| 	纠正                                                                                                                                                                                         |
|---|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|“本质上是继承了 Annotation 的特殊接口”| 	🔴 不准确。注解不能显式继承 Annotation——你自己写 @interface A extends Annotation 会直接编译错。真相是：编译器在编译时隐式让所有 @interface 类型实现 Annotation 接口（反编译 class 文件能看到 interface A extends Annotation + ACC_ANNOTATION 标志） |
|@Reetantion()| 	拼写：@Retention（笔误，但手写代码会编译错，注意）                                                                                                                                                             |
|“属性默认为 public 类型”| 	补充：注解“属性”本质是无参抽象方法（隐式 public abstract），返回值类型只能取：基本类型 / String / Class / 枚举 / 注解 / 它们的数组——超出这个范围编译不过                                                                                        |
|（未提及）| 	补充：元注解一共 5 个，你列了 @Retention @Target，还有 @Documented（进 javadoc）、@Inherited（子类可继承）、@Repeatable（JDK8 可重复注解）                                                                                    |
一个你没写但很关键的事实：@Retention 的默认值是 CLASS。这就是为什么自定义注解要被反射读到，必须显式写 @Retention(RetentionPolicy.RUNTIME)——不写就默认 CLASS，反射读不到，程序 silently 不工作。


> 注解内部是怎么实现这些功能的呢?

**我的初答**

对于编译期实现不了解

对于运行期功能配置就是通过寻找存在对应的注解的属性进行反射调用处理.

**错漏点**

@Override — javac 内置校验 编译器遍历父类/接口的方法表，找不到 同签名方法就报编译错（编译器硬编码）

@Getter — 注解处理器（Lombok） 编译期插入式注解处理轮次里直接改 AST， 生成方法 —— 源码没有、class 文件里有

@Test — 运行期反射（你已掌握的部分） 注解值存于 class 文件的 RuntimeVisibleAnnotations 属性 → 类加载后 Class 对象持有 → JUnit 扫描 isAnnotationPresent(@Test) → 反射 method.invoke() 执行

编译期就两条路：javac 内置检查（@Override，编译器硬编码的逻辑）和注解处理器（JSR 269，Lombok 在编译期改 AST 生成 getter——这就是为什么你用 Lombok 时源码里没有 getter、但能调用）。

> 为什么要设计注解?它有什么好处?

**我的初答**

既可以给程序员提醒,又能被程序识别.低侵入性.

**错漏点**

“低侵入”你答到了核心。补一个对比帮你答得更完整：

|对比对象|	注解的优势|
|---|---|
|vs 注释|	注释给人看，注解给编译器/程序看，能驱动行为|
|vs 标记接口（如 Serializable）|	注解能带参数，且不占用继承位置（一个类只能继承一个父类，但可以打任意多个注解）|
|vs 配置文件|	注解和代码在同一处，类型安全、编译期能查错、IDE 能跳转|


> 关于intern()方法复习

- 定义: 对于调用了该方法的字符串,如果常量池中存在对应字符串字面量,就返回池中字符串.如果常量池中不存在,则在常量池中创建该字符串并返回.
- 关键: 对于JDK1.6和1.8常量池中不存在字符串时intern()的表现差异
- 1.6下当str.intern()发现永久代中的字符串常量池不存在该字符串,则将该字符串复制到常量池,并返回池中的对象,两者不相等.
- 1.8下str.intern()发现堆内存中的字符串常量池不存在该字符串,不进行复制,而是将该字符串的引用放到常量池中,后续若创建相同字符串,则指向的是该对象.

**补充**
- 分界线是 JDK 7，不是 1.8。字符串常量池从永久代搬到堆、intern() 从“复制”改为“记录引用”，都是 JDK 7 做的，1.7 和 1.8 行为相同。

> 对于leetCode 283题移动零

开始时将nums[left == 0]作为交换两个指针的值的条件,否则两个指针都向右移动,这会导致出现数据的漏移,导致出错.

## Day 30 (2026-8-20)
### 今天主要复习了synchronized锁的内容,leetCode主要刷了几道滑动窗口的题目

> 关于synchronized的内容

**我的理解**
1. synchronized锁是Java为了解决并发修改问题而提出的,线程在竞争成功持有锁对象的monitor时,才能进入临界区执行内部的代码,其他线程未竞争成功就进入EntryList阻塞等待.
可以通过Object.wait()使该持有锁的线程释放锁,进入WaitSet()等待唤醒,使用notify()会随机唤醒该队列中的一个线程,被唤醒的线程重新进入EntryList与其他线程竞争锁.
临界区的代码无论是否执行完毕,或是抛出异常,都会自动释放锁.

**补充**

最大遗漏：锁升级（必补）

这是 synchronized 面试第一考点，你一个字没提。JDK 6 之后 synchronized 不再是单纯的"重量级锁"，它有完整的升级路径：

无锁 → 偏向锁 → 轻量级锁 → 重量级锁   （只升不降）

|锁状态|	触发场景|	原理|
|---|---|---|
|偏向锁	|只有一个线程访问|	在对象头里记下线程 ID，下次该线程进来直接进（CAS 都不用）|
|轻量级锁|	两个线程交替访问，但基本不冲突|	用 CAS 自旋，不阻塞|
|重量级锁|	真正的竞争/长持有|	才用你说的 monitor + EntryList 阻塞|
你笔记里写的 monitor/EntryList 那套机制，**只在重量级锁阶段才发生**。面试官问"synchronized 优化"指的就是这套升级。复习这一块时务必把这个表背下来——它就是第七阶段八股文清单里"锁升级"的具体内容。

两个补充：

1.synchronized 三种用法，锁对象不一样

public synchronized void m() {}        // 锁 this

public static synchronized void m() {} // 锁 当前类的 Class 对象

synchronized(obj) {}                   // 锁 obj

2. 可重入（你没提，但很重要）

同一个线程拿到锁后，再次进入另一个 synchronized 方法（同一个锁对象），不会死锁——JVM 计数器 +1，退出 -1，到 0 才释放。这叫"可重入锁"。ReentrantLock 名字里的 Reentrant 也是这个意思。

> 滑动窗口

**我的理解**
- 在解209题长度最小的子数组时,将缩进的判断条件写成了if()导致每次前面前移而后面只能缩进一位,而不是尽可能的缩进.
- 在解findMaxAverage时每次滑动窗口都for遍历窗口中每一个元素并相加,导致时间复杂读为O(n * k)导致超时,应该在刚来就确定总值,每次滑动sum += nums[r] - nums[r - k];  // 进入 nums[r]，离开 nums[r-k],使每次滑动都是廉价的


## Day 31 (2026-8-21)
### 今天复习了解了线程池的一些底层概念以及复习了之前学习过的内容,手写MyLinkedList练习,leetCode刷了关于螺旋矩阵的59,54题.

> 线程池概念.

**我的回答**
1. 线程池是Java为了避免频繁创建线程,实现线程复用,进行线程统一管理的方案.我们可以通过Executors工具类创建,不过该工具类提供的线程池具有OOM的风险,实际开发中禁止使用.
2. 通过new ThreadPoolExecutor()创建线程池的7个参数: ①corePoolSize核心线程数 ②maximumPoolSize最大线程数 ③keepAliveTime临时线程空闲后最大存活时间 ④unit存活时间的单位 ⑤workQueue阻塞队列 ⑥ThreadFactory线程工厂一般使用Executors.defaultThreadFactory ⑦拒绝策略
3. 生产者方向分析任务进入线程池会进行什么:
- execute(Task) -> int c = ctl(无锁AtomicInteger) -> workerCount是否大于corePoolSize:
- 小于: addWorker(Task,TURE)内部线程thread通过runWorker()直接执行任务.
- 大于: 判断workQueue是否已满,如果未满,任务进入队列workQueue.offer(Task)阻塞等待worker.getTask().如果workQueue满了,判断workCount是否大于maximumPoolSize,如果小于:addWorker(Task,FALSE),如果大于,直接触发拒绝策略.
4. 通过消费者方向分析runWorker()内部内容:
- 内部循环获取任务并执行:获得任务-> w.lock()state(0->1)表示繁忙-> Task.run()-> w.unlock()state(1->0)表示可被shutdown()->getTask()获取任务.
- 注意在取任务的时候核心worker没有拿到任务会一直等待,临时worker会在keepAliveTime后释放.
5. 线程池中的各种锁:
- ①Worker继承AQS:该锁主要用于标记Worker对象的状态,state(-1)表示创建但未使用,state(0)表示空闲,此时才能被shutdown(),相当于只停止空闲的线程,state(1)表示繁忙,无法被shutdown().
- ②mainLock = new ReentrantLock();该锁主要保证存放Worker的HashSet<Worker>在并发情况下的安全,保证addWorker等操作的安全.
- ③workQueue自带的锁,以linkedBlockingQueue()为例,内部维护notFull和notEmpty.
6. 为什么要将Thread封装在Worker中? 为了统一管理Thread放入HashSet中,不让Thread散落在线程池中,且可以通过继承AQS来标记状态.

**补充**

三处纠正/补充：

1.ctl 不只是 workerCount——一个变量存了两件事

你说“int c = ctl(无锁AtomicInteger)”，但没说它妙在哪：

private final AtomicInteger ctl = new AtomicInteger(ctlOf(RUNNING, 0));

// 高 3 位：runState（线程池状态：RUNNING/SHUTDOWN/STOP/TIDYING/TERMINATED）

// 低 29 位：workerCount（工作线程数）

一次 CAS 就能同时校验“池子还活着吗 + 线程数够不够”，避免用两个变量还要加锁保持一致。用一个 int 打包两个状态是 JDK 源码里很经典的设计，面试聊到这个会加分。

2.入队后有 recheck（二次校验），你漏了这一步

workQueue.offer(task);       // 入队成功后不是直接结束——

// recheck：

// ① 池子是否已经 SHUTDOWN（是 → 移除任务，执行拒绝策略）

// ② 是否还存在 worker（没有 → addWorker(null, false) 补一个线程来跑队列里的任务）

因为从 offer 到下一步之间，池子可能被 shutdown 了、或所有线程都死了。这一步是并发代码“入队后状态可能已变，必须复查”思想的体现。

3.拒绝策略要把四个名字背下来（你只写了“拒绝策略”四个字）：

|策略|	行为|
|---|---|
|AbortPolicy（默认）|	抛 RejectedExecutionException|
|CallerRunsPolicy|	谁提交谁自己跑（降速反压）|
|DiscardPolicy|	静默丢弃|
|DiscardOldestPolicy|	丢掉队列最老的任务，腾位置给新任务|


> 对于单向链表的一些理解.

1. head只用于存放头节点的地址,addFirst时一定要用newNode.next = head让新节点指向head后再head = newNode.
2. addLast()一定要区分head = null和不等于null的两种情况,否则Node cur = head; cur.next != null可能会因为cur本身是null,它的next直接NullPointerException
3. remove()时只需移动到要删除的结点的前一个节点(index -1)将该节点的cur.next = cur.next.next;指向后一个结点就行.
4. reverse()一定要注意三个指针指向结点移动的顺序.

**补充**

补一个能消灭你第 2 条问题的技术：虚拟头节点（dummy node）

你 addLast 要分两种情况，根源是“头节点可能为 null，没有统一的前驱”。加一个不存数据的假头节点，这个问题永远消失：

public class MyLinkedList {
private Node dummy = new Node(0);  // 永远存在的假头

    public void addLast(int val) {
        Node cur = dummy;              // 从假头开始，真正的头是 dummy.next
        while (cur.next != null) {     // head 为 null？没关系，cur 是 dummy，不会 NPE
            cur = cur.next;
        }
        cur.next = new Node(val);
    }
}

删除头节点也不再特殊：dummy.next = dummy.next.next，和其他节点删除完全同构

LeetCode 上大量链表题的标配起手式就是 ListNode dummy = new ListNode(0, head)——LC203 删除链表元素、LC21 合并两个有序链表，不用 dummy 会多写一堆 if，用了就是无分支统一逻辑

> 关于螺旋矩阵.

最好用的方法是四面收缩法,不需要考虑边界问题,只需要再下界收缩和左界收缩的时候判断是top<= bottom或left<=right,防止反复执行逻辑.

最开始使用模拟方法,这种方法一定要注意边界一致,左闭右开就全用这个.

## Day 32 (2026-8-22)
### 今天复习了HashMap相关的知识,MySQL初步了解DDL的几个基本语法,leetCode刷了两道三数之和的题目.

> HashMap的相关知识.

**我的理解**
1. HashMap是一种双列集合,每一个结点分别存储key和value.在JDK1.7及之前使用数组+链表的组合,并在每次添加结点时使用头插法,在JDK1.8时优化为数组+链表+红黑树的组合,并改用尾插法.
2. HahMap添加元素时会通过该元素的扰动后的hash&(数组长度 - 1)(相位与运算,等价取模)来确定该元素在数组中该存在的下标,如果该下标处存在链表或红黑树,则会遍历链表或红黑树,通过equals逐个比较,若存在相同,则覆盖原来的旧value并返回,若不相同,则插入链表尾部或加入红黑树中.
3. 这也就说明了为什么我们在重写equals的时候必须要重写hashCode,如果equals相同也就是逻辑上是同一个但不同地址的两个引用hashCode不同,可能会被放进不同的桶,这就违反了hashCode与equals的契约
4. HashMap的扩容机制: 当实际元素数量 > 数组容量 * 负载因子(默认0.75)时创建新数组并且容量为原来的两倍.JDK1.8进行了优化,因为HashMap每次扩容都为2的次幂,对于原结点在新数组中插入的位置,只需要计算最高位按位与计算是否为1,若为1则位置为老位置+老容量,若为0则依旧放在原来位置.
5. 进行元素迁移时HashMap会把原链表和红黑树分为分为两个链表和两个红黑树,这两个子链表或红黑树分别存放位置为老位置+老容量和老位置的结点,并整体插入,对于两个子红黑树如果长度小于6会自动转化为链表.
6. HashMap中链表转红黑树只有在链表长度>=6且数组长度>=64时才会发生.
7. JDK1.8HashMap改为尾插法的原因是防止HashMap在并发情况下出现循环链表(虽然HashMap本身在多线程下就不安全,但Java修补了这个问题).

**错漏补充**

一个硬伤：树化阈值记错了

你写“链表转红黑树在链表长度 >= 6 且数组长度 >= 64 时发生”——是 **>= 8，不是 6。**

红黑树结点数6时才退为链表是放止在链表与红黑树间频繁转换

|阈值|	值|	含义|
|---|---|---|
|TREEIFY_THRESHOLD|	8|	链表长度 ≥ 8 时才考虑转树|
|MIN_TREEIFY_CAPACITY|	64	|数组长度 < 64 时不转树，优先扩容（扩容能直接把链表拆散）|
|UNTREEIFY_THRESHOLD|	6|	扩容拆分后，树节点数 ≤ 6 时退化回链表|

- 三个补充（面试加分项）：

1. 扰动函数的具体公式（你说“扰动后”但没展开）：

static final int hash(Object key) {

int h;

return (h = key.hashCode()) ^ (h >>> 16);  // 高16位 异或 低16位

}

为什么扰动？hashCode 的低位在 (n-1) 的与运算里才参与运算，高位全浪费。异或一次把高位信息“揉”进低位，让分布更均匀、冲突更少。

2. **为什么树化阈值偏偏是 8？** 源码注释里给了答案：理想 hash 下桶内节点数服从泊松分布，链表长到 8 的概率约 0.00000006——树化是极端兜底，不是常态。转树代价大（节点内存翻倍），所以阈值选在一个“几乎不会发生，发生了说明 hash 已经烂透了”的位置。

3. 1.8 的 HashMap 并发下依然不安全——死循环修了，但 put 并发时数据覆盖丢失还在。所以才有 ConcurrentHashMap，它是第五阶段前的重点复习对象，这里先挂个钩子。

> DDL的几个基本语法.

**我的回答**
1. 创建数据库: create database [if not exists] 数据库名 [default charset 字符集];
2. 展示所有的数据库: show databases;
3. 切换数据库: use 数据库名;
4. 删除数据库: drop database 数据库名
5. 数据库的每个语句是通过分号结尾的,书写时必须加上分号.对于我们中文数据库,字符集应该使用utf8mb4支持4个字节,防止出现bug.

**补充**

两个补充：

1. 你学的还是“库级”操作，DDL 的重头在“表级”：

create table student (

id      bigint primary key auto_increment,

name    varchar(50) not null,

age     int,

created datetime

);

alter table student add column email varchar(100);   -- 加列

alter table student modify name varchar(100);        -- 改列类型

drop table student;                                  -- 删表

truncate table student;                              -- 清空表（保留结构）

接下来学的重点就是**建表 + 数据类型 + 约束**，这是 DDL 的核心。

2. **drop 是删库级操作，练手时加 if exists 保险**：

drop database if exists test_db;   -- 不存在也不报错

顺便建立个肌肉记忆：**drop 永远先 show 看一眼再执行**。库里没数据时随手 drop 无所谓，养成好习惯是为了将来有数据的时候不翻车。

> 关于三数之和的算法题.

三数之和的关键在于让一个数a固定,在通过b,c两个指针去逐个计算,并注意跳过重复的数字,在此之前对数组进行排序.

**犯的错误**: 使用
int j0 = j;
while (j < k && nums[j0] == nums[j0 + 1]) {
continue;   // j0 没有任何变化，条件恒为真时永远循环
}
没有给内部j0进行变化,导致出现死循环.

死循环的通用判定公式：while 的循环条件里出现的变量，循环体内必须有变化的机会。 你的条件取决于 j0，但循环体内谁都没动 j0——只要第一次进循环，就永远出不来。下次写任何 while 前默念一遍：**这循环里的变量谁在动？**

## Day 33 (2026-8-23)
### 复习了synchronized锁与ReentrantLock的interrupt的区别以及在现实情况下可能出现的问题,MySQL学习了创建table修改,删除及各种数据类型.LeetCode刷了203移除链表元素的题.

> 关于synchronized锁与ReentrantLock的可断与不可中断问题.

**我的理解**
1. 对于synchronized锁,当线程没有抢到锁对象进入BLOCKED状态,进入EntryList中排队等待,此时队列中的所有线程感知不到外部的终止信号,无法被打断,无法终止.
2. ReentrantLock的Lock锁同样不能被打断,但它提供了lockInterruptibly(),等待中的线程可以相应中断,抛出InterruptException退出抢锁.还提供了tryLock(timeout),超时就自动退出枪锁执行其他任务.
3. synchronized的问题反应在具体业务中:
- 线程池shutdownNow或者收到终止信号,线程池通过Interrupt()唤醒线程,线程BLOCKED不终止,JVM不关闭,内存不释放.
- 经典场景: 对象持有锁->调用下游RPC->下游挂起不超时->后续线程全部BLOCKED,这种情况堆积可能会导致线程池耗尽,内存雪崩,而tryLock(timeout)可以避免出现这种情况.
- 出现死锁的话多个线程相互BLOCKED,只能kill进程.ReentrantLock可以通过tryLock()超时放弃,或者使用lockInterruptibly()通过看门狗线程打断某一个线程.

**三个纠正/补充：**
1. 拼写：InterruptedException（你写成 InterruptException，少个 ed）。面试白板写这个词的概率不小。
2. **关键补充——synchronized 不是完全不可中断，要看阻塞在哪**：

|状态|	场景|	可中断？|
|---|---|---|
|BLOCKED|	等锁（进 EntryList 排队）|	❌ interrupt 无效|
|WAITING|	调用了 wait() 进 WaitSet|	✅ 抛 InterruptedException|
同一个synchronized线程,**卡在"等锁"不可中断,卡在"wait等通知"可中断.面试官很可能追问"那wait的线程能被打断吗".

3. **用词: tryLock(timeout)本身也是可中断的**-----它等待期间收到interrupt会抛出interruptedException,而不是傻等满超时.
所以它兼具"退出超时 + 响应中断"双保险,这也是它在死锁恢复里比无参tryLock()更常用的原因.


> MySQL学习的语句.

**我记得的**
展示该库中所有的表:
SHOW TABLES;

创建table:
create table 表名(
   字段1 字段1类型[COMMENT 备注],
   ...
   字段n 字段n类型[COMMENT 备注]
)COMMENT 表备注;

查询表的结构: DESC 表名;

查询指定表的建表语句: SHOW CREATE TABLE 表明;

**修改表**:

添加字段: ALTER TABLE 表名 ADD 字段名 长度[COMMENT 注释][约束];

修改数据类型: ALTER TABLE 表名 modify 字段名 新数据类型(长度);

修改字段名和字段类型: alter table 表名 change 旧字段 新字段 新字段类型(长度)[COMMENT 注释][约束];

删除字段: alter table 表名 drop 字段;

修改表名: alter table 表名 rename to 新表名;

删除表: drop table [if exists] 表名;

删除指定表,并重新创建该表: truncate table 表名;
 
字段类型有字符串类型,数值类型,时间类型.

**补充**

对照着记：**MODIFY 只改类型不改名，CHANGE 名字类型一起改（所以叫 change）**。这是最容易混的两条。

数据类型太笼统了，“字符串/数值/时间”三类谁都知道，要记的是具体选型：

|类型|	用途|	关键点|
|---|---|---|
|tinyint / int / bigint|	整数|	主键 ID 用 bigint（int 21亿不够用是真实事故）|
|decimal(10,2)|	精确小数	|金额必须用它，float/double 有精度丢失|
|varchar(N)|	变长字符串|	N 是字符数不是字节数；姓名 50、手机号 11 存 varchar(11)|
|datetime|	时间|	8 字节，范围 1000~9999 年|
|timestamp|	时间戳|	4 字节，只能到 2038 年，但自带时区转换|
这张表是建表选型的实际依据，比语法更值得背。尤其是 *8bigint 主键 + decimal 金额这两条**，做项目时天天用


> 关于移除链表元素的反思.

该题通常使用虚拟头结点或者递归的方式实现.

最开始解答时在修改链表的同时也移动指针,没有区分移除时指针不移动和不移除指针移动的场景.

对于创建虚拟头结点开始时的理解偏差:

    ListNode dummy = new ListNode(0,head);
    ListNode cur = dummy;
       
    while(cur.next != null){
            if(cur.next.val == val){
                cur.next = cur.next.next;
            }else{
                cur = cur.next;
            }
        }
        return dummy.next;

这里误以为改变cur.next仅会改变自己,不会影响dummy.next.
但开始时cur与dummy其实指向的是同一个对象,此时cur.next改变dummy.next会同时感应,但在cur = cur.next后,cur的会改后续的结点了,此时与dummy无关

## Day 34 (2026-8-24)
### 今天主要了解了char()与varchar()字节占用,尾部空格处理,性能的差异.了解MySQL图形化界面.

> char(20)和varchar(20)的区别.

**我的初答**
1. 对于char(20),table中实际占用的字符数就是20,未满20字符则会在尾部补空格,在取出时自动去除尾部的空格,常用于存储定长字符的内容.
2. 对于varchar(20),它会根据输入的字段实际占用的字符数调整大小,以实际字段大小存储,取出时不会去除尾部的空格,常用于存储用户名等不定长的内容.
3. 对于两者的性能差异,因为InnoDB本身是变长格式,所以两者性能几乎无差异,选型主要在于存储效率.
4. mysql8的默认模式下尾部的空格是参与比较的.

**错漏点**
1. 第 4 条说反了，必须改：
2. 你写"mysql8 默认模式下尾部空格参与比较"——**恰恰相反**：
- select 'abc' = 'abc ';        -- 结果：1（真！尾部空格被忽略）
- select 'abc' like 'abc ';     -- 结果：0（假！LIKE 不忽略）

|操作|	尾部空格|
|---|---|
|= 比较|	忽略（MySQL 默认 collation 是 PAD SPACE 的）|
|LIKE|	不忽略（逐字符比较）|
|唯一索引|	忽略——插入 'a' 和 'a ' 会冲突报错|
最容易踩的坑就是那个唯一索引：你插 'admin' 成功，再插 'admin '（带个空格）直接报 duplicate key——因为比较时尾部空格被抹掉了。这是真实项目里坑过很多人的点。

补一句应用场景：频繁**更新**的短字段（如性别、状态码 'Y'/'N'）用 char 略好——定长意味着原地更新，varchar 长度变化可能引起行内移动。

> 关于DDL语句的底层实现需要我们了解吗?

**我的初答**
1. 不了解

**回答**

直接答案：现在不用。 

DDL 的底层（比如 MySQL 8.0 的 instant add column 为什么能秒加列、online DDL 加索引怎么做到不锁表、copy 算法 vs inplace 算法）是 MySQL 深入阶段的内容，属于第七阶段八股范畴。

> 关于MySQL的SQL语句学习与JavaSE有那些不同,MySQL似乎不太需要我们知道底层原理?
1. 不了解

**回答**
- 答案是：**MySQL 阶段"多敲"远大于"多问为什么"，但你不能从此就不学原理——只是时机不同。**
- MySQL 知识分三层，每层对应不同阶段：

|层次|	内容|	什么时候学|	学习方式|
|---|---|---|---|
|语法层（现在）|	DDL/DML/查询/连接/分组聚合/子查询|	第二阶段|	多敲多练，像练打字|
|优化层|	索引、EXPLAIN 执行计划、慢查询|	第四阶段 SSM 前后|	结合项目里的实际 SQL|
|原理层|	B+ 树、MVCC、事务隔离级别、redo/undo log、行锁/表锁|	第七阶段八股|	集中背 + 画图理解|


## Day 35 (2026-8-25)
### 今天复习了关于concurrentHashMap的内容,学习了DML语句,DQL语句学习了条件查询和聚合函数,写了LC21题两个链表合并.

> 关于concurrentHashMap.

**我的初答**
1. concurrentHashMap在JDK1.7及以前采用数组+链表,通过Segment分段锁来保证多线程下的安全,多线程下仍有竞争.在JDK1.8HashMap优化后与HashMap保持一致,使用数组+链表加红黑树的形式,对于多线程安全下也进行了优化.
2. JDK1.8时concurrentHashMap采用了CAS + synchronized的方式,在添加元素时若该数组索引位置为空 ,则直接无锁CAS插入,若存在元素,则直接synchronized锁头结点保证多线程下安全.
3. table,baseCount等核心字段均采用volatile关键字修饰,保证可见性与防止指令重排序.
4. size统计通过无锁累加.

**错漏点**

**补充4个点,把你的知识补全成面试完整版:
1. **get()全程无锁(你漏了,面试必问)**
- 读操作不加任何锁----table引用,节点的val/next都是volatile修饰的,读的时候保证看到最新值.**读多写少场景下CHM的读性能和HashMap几乎一样**,这就是答案.
2. **size()的无锁累加具体怎么做的------CounterCell分数计数**
- 无锁累加展开讲是: baseCount + CounterCell[]两级结构----
- 没竞争时: CAS直接累加baseCount
- CAS失败(说明有竞争): 把增量记到CounterCell数组的某个格子里
- sum()时: baseCount + 所有格子求和
- **思想：把一个热点变量拆成多个格子**，竞争就分散了——和把堵车的单车道改成多车道一个道理。这就是 LongAdder 的原理，也是"为什么 CHM 不用 AtomicLong 统计"的答案（AtomicLong 所有线程 CAS 同一个值，高并发下疯狂失败重试）。
3. 锁粒度的演变（1.7 → 1.8 的本质）

|   |1.7|	1.8|
|---|---|---|
|锁的粒度|	Segment（一段，默认 16 段）|	单个桶的头节点|
|最大并发度|	固定 16（Segment 数）|	数组长度（越大越高）|

锁的单元从"一段"细化到"一个桶"，两个线程只要不写同一个桶就完全不互相阻塞。

4. **为什么用 synchronized 而不是 ReentrantLock**？——锁升级优化后 synchronized 性能已不输，而且它不需要 AQS 那套对象（每个桶一个 ReentrantLock 内存开销太大）。锁头节点时甚至不需要额外分配锁对象。

进阶一点的存在性知识（知道即可）：put 时如果发现桶里是 ForwardingNode（hash=MOVED），说明别的线程正在扩容，当前线程会帮助一起扩容（helpTransfer）——多线程协助扩容是 1.8 CHM 的精髓设计。

> 关于DML语句和DQL语句的理解.

**我的初答**
1. DML主要用于操作表里的数据,三个基本操作INSERT插入,UPDATE更改,DELETE删除.
2. insert into 表名(类型1.....)values(字段1...)  表名可以省略,也可以插入多行字段,现实开发中建议进行多行插入,速度块,减少网络往返和事务开销. insert ignore 可以在重复静默跳过,replace 冲突时先删旧数据再插入新数据,on duplicate key update 冲突时更新指定字段.
3. update 修改数据: update 表名 set 字段1=值1,...where 条件; 可以修改多个字段的值,必须加where,不然会该变表中所有字段1的值.
4. delete from 表名 where 条件; 能删除指定的行也能删除多行,如果没加where则会删除整张表的内容.
5. DQL语句主要用于查询表中的内容.
6. SELECT 字段列表 FROM 表名 WHERE 条件列表;
7. 需要注意的一些条件运算符: <> 和!= 表示不等于, BETWEEN...AND... 在某个范围之内,IN(...) 在IN之后列表中的值,多选1. LIKE占位符 _模糊匹配,%匹配最后一个字符.逻辑运算符AND &&, OR ||,NOT !.
8. 聚合函数: 将一列数据作为一个整体,进行纵向运算.常见聚合函数: count 统计数量 max,min,avg,sum求和.

**错漏点**

硬伤 1：LIKE 通配符说错了

你写“LIKE占位符 _模糊匹配，%匹配最后一个字符”——% 不是“匹配最后一个字符”：

|通配符|	含义|	例子|
|---|---|---|
|_|	匹配任意单个字符|	`like '_a%'`：第二个字符是 a 的，如 "cat"、"park"|
|%|	匹配任意长度字符串（可以是 0 个）|	`like 'a%'`：以 a 开头的；`like '%a%'`：包含 a 的|
% 可以出现在任何位置：开头（以…结尾的）、中间（包含…的）、组合使用。它和“最后一个字符”没有关系

表名永远不能省。可以省的是字段列表:

    INSERT INTO student VALUES (1, '张三', 20);
    -- 省略字段列表 = 必须按建表顺序给【所有】字段赋值，少一个就报错

所以实际开发反而建议写**全字段列表**——省略写法在表结构一变（加了一列）时就全崩了。另外你有个术语笔误：表名后括号里是字段列表，VALUES 后括号里是值列表，你写成了“类型1”“字段1”。

- 必须补的坑：count 与 NULL（高频面试题）

      count(*)      -- 统计所有行（包括 NULL 行）
      count(字段)    -- 统计该字段不为 NULL 的行数
      count(1)      -- 同 count(*)，扫全表给每行数个 1
所有聚合函数（max/min/avg/sum）都会**自动忽略 NULL**——avg(score) 如果有 3 行 score 是 NULL，这 3 行根本不参与平均。这是"为什么 avg 结果和我想的不一样”的头号原因。

顺带补一条：**判断 NULL 只能用 IS NULL / IS NOT NULL**，写 where score = NULL 永远查不出任何数据（和 NULL 比较结果都是 NULL，不是真）。


> LC21链表合并理解.

**我的回答**
1. 对于两个链表合并为新链表的题目,新链表可以使用虚拟头结点的方法,统一处理所有结点.
2. 对于两个链表的情况要根据实际情况分类讨论,是否为null,如果有一个链表为null,直接链接另一个链表,后续所有结点都本身已经连上了这点要注意.

一个**知识连接（记住它，价值翻倍）**：这道题的“每次取两边较小的”就是**归并排序的 merge 步骤**——归并排序 = 拆成两半 + merge。以后做 LC148 排序链表（中等）时，核心就是“快慢指针找中点拆两半 + 用你今天写的 merge 合并”。今天的题是那道的中场零件。

## Day 36 (2026-8-26)
### 了解了INSERT INTO...on duplicate key update和replace into的差异,学习了DQL的分组查询,排序查询,分页查询,写了LC707设计链表.
> on duplicate key update和replace into的差异.

**我的回答**
1. on duplicate key update 底层行为为update,在发生主键或唯一键冲突时只会更新对应的字段内容,其他内容不会更改.
- 不会触发自增主键into_increment.
- 会触发update触发器.
- 不会触发外键.
- 对于存在多个多个唯一键的情况下,会自动匹配触发的第一行,不稳定.
2. replace into 的底层行为为delete+insert,在发生主键或唯一键冲突时会删除旧整个字段列表,再插入新内容,未被更新的字段为默认值.
- 就算给主键显式赋值也有可能会触发自增主键.
- 会触发delete和insert触发器.
- 会触发外键,可能将子表连结删除.
- 存在多个唯一键的情况下可能将匹配的表全删除,非常不安全.

**错漏点**

你的四维对比几乎全对,详细请看**MySQLDay31,题目1**

|维度|	ON DUPLICATE KEY UPDATE|	REPLACE INTO|
|---|---|---|
|底层|	UPDATE（保留原行）|	DELETE + INSERT（删旧插新）|
|未提供的字段|	保持原值	|变默认值（整行重插）|
|自增主键|	不产生新行，ID 不变|	可能跳号（新行拿新 ID）|
|触发器|	触发 UPDATE 触发器|	触发 DELETE + INSERT 触发器|
|外键|	基本不触发级联|	可能级联删除子表|
|多唯一键|	只匹配一行，不稳定|	可能删除多行，很危险|
**补两个点：**

生产环境结论一句话记住：**写“存在就更新，不存在就插入”（upsert）时，永远用 ON DUPLICATE KEY UPDATE，别用 replace。** replace 的“删旧插新”会让没写的字段全丢成默认值，还可能误伤外键子表——这就是它“非常不安全”的根本原因。

一个高频使用场景（面试聊得出来会加分）：

     -- 用户签到/库存扣减：user_id 是唯一键
     INSERT INTO user_score (user_id, score)
    VALUES (1001, 10)
    ON DUPLICATE KEY UPDATE score = score + VALUES(score);

一句话完成“新用户插入、老用户累加”，不用先 SELECT 再判断。库存、签到、计数器都是这个套路。

小提醒：你写“不会触发自增主键 `into_increment`”——拼写是 auto_increment。另外严格说，在 innodb_autoinc_lock_mode 的某些模式下，即使冲突 InnoDB 也可能“预分配”过自增值导致跳号——这个属于极细的坑，知道存在即可，不用现在深挖。


> DQL分组查询,排序查询,分页查询的理解.

**我的初答**
1. 分组查询: select 字段列表 from 表名 group by 分组列表;
- group by把表分成多个小表,聚合函数再将小表中的内容整合成一行输出.
2. 排序查询: select 字段列表 from 表名 order by 排序列表;
- asc(默认)升序排序,desc降序排序.where不可以用别名,但order by可以用别名排序.
3. 分页查询: select 字段列表 from 表名 limit 起始索引,每页的记录数;
- 起始索引 = (要查询的页数-1) * 每页记录数.
- 对于存在大量数据要分页的情况,通过计算起始索引的效率太低,可以通过记住上一页最后的id,where id > ...时再limit 10;
- 没有分组的列表分页limit不稳定,可能会出现重复或遗漏,limit必须配合group by 使用.

**错漏点**

一个概念错误：**limit 配的是 ORDER BY，不是 GROUP BY**

你写“limit 必须配合 group by 使用”——错了，是 ORDER BY。

为什么？**没有排序的 LIMIT 是不稳定的**：

    SELECT * FROM user LIMIT 10, 10;   -- 第 2 页
    SELECT * FROM user LIMIT 20, 10;   -- 第 3 页
InnoDB 不保证没指定 ORDER BY 时的返回顺序。两次查询可能拿到不同的行，翻页时就出现“这页漏了几条、那页重复了几条”。所以分页必须：
    
    SELECT * FROM user ORDER BY id LIMIT 20, 10;

- 补一个底层逻辑，帮你永久记住“where 为什么不能用别名”——**SQL 执行顺序**：

      FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT
- WHERE 在 SELECT **之前**执行，别名（AS xxx）是 SELECT 阶段才起的，所以 WHERE 里压根还没有这个别名 → 用不了

深分页优化你答得很好 ✅：“记住上一页最后 id，where id > ... 再 limit”——这叫**游标分页（seek method**），是百万级数据翻页的标准优化，你能自己悟出来很了不起。补充一句它好在哪里：LIMIT 100000, 10 要扫掉前面 10 万行，WHERE id > xxx LIMIT 10 直接走索引定位，只扫 10 行。

> LC707设计链表的反思,实际开发中创建链表也可以使用虚拟头结点吗?

**我的回答**
1. 在最开始没有使用虚拟头结点的时候误认为

        for(int i = 0; i < index; i++){
            cur = cur.next;
         }
停留在index的前一个结点处,实际上停留的是在index的结点处,边界处理漏洞百出,这点必须加强.
2. 可以使用虚拟头结点的方式,将addToIndex()变为普通的结点添加,不需要考虑头结点,对于添加头结点和添加尾结点可以直接复用该方法.

**错漏点**
1. **生产代码里几乎不手写链表**。 Java 的 LinkedList 已经实现好了，你日常写业务用的就是它。LC707 是练数据结构的，不是让你以后自己造轮子。

2. **但“哨兵节点”的思想在生产代码里真实存在**。 JDK 的 LinkedList 内部就是**双向链表 + 首尾哨兵**——它常驻两个哨兵节点（first/last），让“头插、尾插、空表”全变成无分支的统一逻辑。所以 dummy node 不是刷题专用技巧，它和 JDK 源码是同一个设计思路。****

## Day 37 (2026-8-27)
### 今天学习了DCL语句以及函数中的字符串函数,数值函数,日期函数,leetCode写了206反转链表和19删除链表倒数第N个结点.
> 关于DCL语句的理解.

**我的初答**
1. DCL语句主要是用于操作和管理数据库权限,主要有GRANT(授权)和REVOKE(收权),今天学习的语法:
- 用户管理:
- 创建用户: create user '用户名'@'主机名' identified by '密码';
- 修改用户密码: alter user '用户名'@'主机名' identified with mysql_native_password '新密码';
- 删除用户: drop user '用户名'@'主机名';
- 这类操作主要由DBS进行,主机名可以使用'%'通配符,表示任意主机上都能登录.

- 权限控制:
- 查询权限: show grants for '用户名'@'主机名';
- 给予权限: grant 权限列表 on 数据库名.表名 to '用户名'@'主机名';
- 收回权限: revoke 权限列表 on 数据库名.表名 from '用户名'@'主机名';
- 多个权限之间使用逗号隔开,授权时数据库名和表名都能通过*通配符表示.

**错漏点**
1. 语法基本全对 ✅
2. **补一个你会踩的坑（比语法重要）**：MySQL 8.0 默认认证插件其实是` caching_sha2_password`，不是课程里教的 `mysql_native_password`。为什么课程让你用 native 那个？因为老的 JDBC 驱动连不上新插件，
会报 `Authentication plugin 'caching_sha2_password' cannot be loaded`——你以后用 Java 连 MySQL 八成会撞这个错。解决办法就两种：改用户认证方式为 native（课程教的），或升级 mysql-connector-j 到 8.x。到时候撞上别慌，回来看这条。

顺带记一句：直接改 `mysql.user` 表后要执行 `flush privileges`; 才生效；用 grant/revoke 不用刷。


> SQL中函数的理解

**我的初答**

1. 函数是可以直接被另一端程序调用的程序或代码,这些函数主要是为我们操作列表中的内容进行服务的,目前只了解了一些常用的函数

字符串函数: concat(s1,s2..)字符串拼接. lower(str)/upper(str)转化为小写/大写. lpad(str,n,pad)/rpad(str,n,pad)左/右填充,向str中填充pad,填充完后的长度为n.
trim(str)去掉字符串收尾空格. substring(str,start,len)从str中的start索引截取长度为len的字符串,该索引从1开始.

数值函数: ceil()向上取整. floor()向下取整. mod(x,y)返回x/y的模. rand()生成0~1之间的随机数. round(x,y)求x四舍五入的值,保留y位小数.

日期函数: curdate()返回当前日期,curtime()返回当前时间,now()返回当前日期和时间,year/month/day(date)返回date的年/月/日,
date_add(date,interval expr type)返回一个时间值加上一个时间间隔expr后的时间值,dateDiff(date1,date2)返回date1和date2之间的时间间隔天数.

**错漏点**

补三个易踩的坑：

concat **遇 NULL 整体变 NULL**：`concat('a', NULL, 'b')` 结果是 NULL，不是 'ab'。要么用 ifnull(col,'') 兜底，要么用 concat_ws。

substring 从 1 开始，Java 从 0 开始——你特意记了“索引从 1 开始”，很好，但这是 SQL 和 Java 思维最容易打架的地方，写 SQL 时别顺手减 1。

DATEDIFF(date1, date2) 是 **date1 - date2** 的天数，可正可负。写反了符号就全错，面试/业务里查“订单创建至今多少天”用 datediff(now(), created)。

> 对于206反转链表和19删除链表倒数第N个结点的反思.

**我的回答**
1. 对于反转列表,只需要将每个链表指向方向改变一下就行,
我们可以通过双指针pre和cur分别记录当前结点和前一个结点,临时指针temp指向后一个结点,每次改变方向后再按照

       pre = cur;
       cur = temp;
       temp = cur.next;
移动就行,对于头结点的处理依旧可以使用虚拟头结点
2. 对于删除链表倒数第N个结点,我们也可以通过双指针的形式加虚拟头结点形式完成,创建fast和slow两个指针,
让fast指针先移动n+1次,然后让slow和fast一起移动,当fast == null时,slow正好停在要删除的结点的前一个结点,直接删除即可.

**错漏点**

LC206 有两个问题：

问题 1：移动顺序写乱了

你写的顺序：

    pre = cur;
    cur = temp;
    temp = cur.next;   // ← 放最后，错了
temp（存后继）必须在**反转 cur.next** 之前保存，而且是每轮循环的第一步。放到最后时 cur 已经移动过了，cur.next 已经不是原来要存的那个后继了。正确四步顺序：

     while (cur != null) {
     ListNode temp = cur.next;  // ① 先存后继（不断链就全靠它）
     cur.next = pre;            // ② 反转
     pre = cur;                 // ③ pre 前移
     cur = temp;                // ④ cur 前移
     }
     return pre;   // pre 就是新头
口诀：**先存、再转、最后挪**。①必须最先，因为它要在 cur.next 被改写前把“后面的路”记下来。

问题 2：反转链表不需要 dummy（重要）

一句话：**只要“头节点可能被改掉（删/插）”，就上 dummy；只是重新连接或只读，就不用。**

## Day 38 (2026-8-28)
### 今天写了一道DML+DQL的题目,学习了流程函数及约束的知识,leetCode写了242:有效的字母异位词开始哈希表部分的刷题
> 一. 关于MySQL题库中Day32题目1的几个错误反思.

**我的初答**
1. 比较日期时未加引号,这会导致mysql将日期当作int计算来进行,如2020-1-1 得到2018再与对应日期进行比较,不会报错但答案不正确,对于日期和字符串都需要加引号.
2. 使用group by分组后,前面的select应为分组条件或聚合函数,其他内容无意义.
3. 'order by desc' 未加按照什么进行排序,排序规则应加上.
4. 对于行级属性的分类应交予where进行处理,分组后的数据过滤才交予having处理.
5. SQL语句的执行顺序: from->where->group by->having->select->order by-> limit

**错漏点**

**补充一个关联坑**：日期还可以直接和 `now()`、`curdate()` 比较（函数不用加引号），如 where entrydate > curdate() - interval 30 day。别矫枉过正给函数也套引号。

第 2、4、5 条放一起记成一个规则链：

执行顺序：FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT

↑行级过滤        ↑组级过滤

WHERE 在分组前（还是行），HAVING 在分组后（已是组）——所以“没分组的行筛选给 WHERE，分组后的组筛选给 HAVING”。你第 4 条总结的就是这个，对。


> 二. 关于流程函数及约束

**我的初答**
1. IF(value,t,f) 当value为true时返回t,否则返回f,类似三元运算.
2. IFNULL(value1,value2) 如果value1不为空,返回value1,否则返回value2.
3. case when [val1] then [res1]... else [default] end; 如果val1为true,返回res1,...否则返回default.
4. case [expr] when [val1] then [res1]... else [default] end; 如果expr等于val1,返回res1,...返回default.
- 关于约束
1. 目的是为了保证数据库中数据的正确,有效性和完整性.
2. NOT NULL 该字段不能为NULL
3. unique 字段唯一
4. primary key 主键,主键是唯一且不能为空的,MySQL中可以设置为auto_increment.
5. default 保存数据时,若没有赋值,则为default值.
6. check  8.0.16版本之后,用于检查是否符合check()中的内容.
7. foreign key 外键约束,让两个表间建立联系.
- 关于外键
1. 创建和删除外键:

1创建表时传递: [constraint] [外键名称] foreign key [外键字段名] references 表名(字段列名)

2通过: alter table 表名 add [constraint] [外键名称] foreign key [外键字段名] references 表名(字段列名);

删除: alter table 表名 drop foreign key 外键名称;

- 控制外键的更新/删除行为
- NO ACTION/RESTRICT 存在外键时不允许修改外键.
- CASCADE 修改父表中的外键时,同步修改子表中的外键,如果删除可能会把子表中的内容也删除.
- SET NULL 修改父表中的外键时,会将子表中对应外键内容设置为null.
- 语法: 在创建表语句后加 update cascade delete cascade

**错漏点**

补充三个点：

1. 外键的更新/删除行为是分开配的，你写的“update cascade delete cascade”实际语法是两个子句：

       foreign key (dept_id) references dept(id)
       on update cascade      -- 父表主键变，子表跟着变
       on delete restrict     -- 有子记录就不许删父
最常用的生产组合：on update cascade on delete restrict（改可以跟，删有孩子就拦）。

2. 重要实践观点（面试聊得到）：**阿里开发规范等大厂规范都明确禁止用物理外键**。原因：外键会影响插入性能、高并发下容易死锁、分库分表时外键根本没法跨库。
数据一致性靠**应用层代码**保证（插入前先查父表）。你学的外键语法是理解“参照完整性”的模型，实际项目里更多是逻辑外键（只建普通索引，不建 constraint）。这不是说白学了——面试必问外键行为，且小系统/课程设计里物理外键照样用。

3. **外键关联的必须是父表的唯一列（通常主键）**——关联一个普通列会直接报错。这也是“外键 = 指向唯一身份”的语义。

> 对于LC242题的反思,哈希表有那些特点?有所遗忘了.

**我的回答**
1. 解答这道题可以使用字符串的toCharArray()转化为数组,再用数组工具类Arrays.sort()排序后对比即可.
2. 也可以通过哈希表的方式记录每个字符,如果t字符串存在重复字符,就--,看是否存在小于0的值,存在则直接返回false

**补充**

**哈希表的核心特点一句话：用 O(1) 的查找，换 O(n) 的遍历。**

|操作|	数组（无序）|	哈希表|
|---|---|---|
|查“x 在不在”|	O(n) 遍历|	O(1) 近似|
|插入|	O(1)|	O(1)|
|代价|	—|	额外内存、无序、有哈希冲突|
所以**“需要快速判断某个元素是否出现过/出现过几次”的场景 → 上哈希**。LC242 的本质就是“统计字符出现次数”——哈希计数是它的本命解法。

## Day 39 (2026-8-29)
### 今天学习了多表查询的知识,写了Day33题目3与Day34题目1,LeetCode写了349两个数组的交集,202快乐数.
> 多表查询是什么?它有那些知识需要我们掌握的?

**我的初答**
1. 在我们实际开发当中,由于业务间的关联,表之间也会存在联系,这时就可能需要我们查询多个表中的信息.通常分为多个场景:
- 一对多: 我们需要在多的一方建立外键,指向一的一方的主键.
- 多对多: 建立第三张表,并包含多个表的外键.
- 一对一: 常用于数据拆分,一张表存放大致信息,另一张表存放详细信息,在详细信息表中创建外键关联另一张表的主键.
2. 多表查询的分类:
- 隐式内连接: select 字段列表 from 表A,表B where 条件...;
- 显式内连接: select 字段列表 from 表A [INNER] JOIN 表B ON 条件...;
- 内连接相当于查询两个表的交集.
- 左外连接: select 字段列表 表A LEFT [OUTER] JOIN 表B ON 条件...;
- 右外连接: 将左外的LEFT改为RIGHT即可.
- 外连接相当于查询本表的内容,包括了两表的交集
- 自连接: select 字段列表 表A 别名A JOIN/, 表A 别名B ON/where 条件...;
- 既可以内连接也可以外连接,自连接**必须使用别名**.
3. 联合查询
- select 字段列表 from 表A where 条件列表 union/union all select 字段列表 from 表B where 条件列表;
- 将两个查询后的表合并,合并的表字段列表必须相等,union all不会去重.
4. 子查询
- 分为标量子查询,列子查询,行子查询,表子查询

**错漏点**

一个概念补全：左外连接到底返回什么

你说“外连接相当于查询本表的内容，包括了两表的交集”——方向对，但补上关键的半句：**左表中没有匹配的行也会保留，右表字段填 NULL**：

    左外连接 = 左表的所有行 + 匹配上的交集；左表有、右表没有的行，右边补 NULL
这就是“为什么左外能查‘没有员工的部门’”——部门全保留，没匹配上的员工列是 NULL，用 `where e.id is null` 一筛就出来了（这个技巧叫反连接，很常用）。

**补一个选型直觉**（第三层该有的答案）：**以谁为主表，谁就得全保留，就让它当左表用左外**。“查所有学生及其成绩（没成绩的也要）”→ 学生 LEFT JOIN 成绩。内连接会悄悄丢掉不匹配的行——数据少了都不报错，这也是隐蔽 bug 的常见来源。


> 多表查询是如何实现的?它怎么连接两个表?MySQL的这些语法底层是靠Java这类计算机语言实现的吗?

**我的回答**
1. 不了解

**错漏点**
1. **MySQL 是 C/C++ 写的，不是 Java。**
2. JOIN 的底层实现，本质是“**两层循环找匹配**”——最朴素的形式叫嵌套循环连接（Nested-Loop Join）：

       // 把 SQL 的 join 翻译成代码，大致就是这个意思
       for (Row r : 表A) {              // 外层驱动表
         for (Row s : 表B) {          // 内层被驱动表
       if (r.id == s.a_id) {    // ON 条件
       输出(r, s);
       }
       }
       }
你现在完全能看懂这段伪代码了。真实 MySQL 做的优化：

- 被驱动表的连接列有索引 → 内层循环变成一次索引查找（B+ 树 O(log n)），性能天差地别。**这就是“JOIN 字段要建索引”的底层理由**
- 没索引时用 join buffer 把外层行批量缓存，减少内层扫描次数（Block NLJ）
- 优化器还会挑**小表当驱动表**（外表越小，循环次数越少）


> 为什么要设计多表查询以及它的这各种查询方式?

**我的回答**
1. 实际业务中可能存在多个表之间的联系,这时我们就需要进行多表查询来完成.
2. 这些查询方式满足了不同要求下的查询方式.

**错漏点**
无

> 对于写的题目的反思.

**我的回答**
1. Day33题目3中HAVING执行的顺序是在select前,但MySQL允许其使用别名是因为做了额外扩展,其他数据库中会报错:存在未知的字段.
2. Day34题目1中库表之间应用.而不是@,@只用于主机与端口之间.with grant option 的问题主要体现在权限失控扩散,审计困难,违背最小权限原则,级联回收风险.
3. 对于两个数组的交集可以通过一个HashSet进行去重记录一个数组,另一个记录交集内容,通过判断集合中是否存在另一个数组中的内容来判断是否加入,并不必担心重复add.
学习了`set1.retainALL(set2)` 这个方法: 去除set1中所有与set2不相交的内容.
4. 快乐数只会出现1或进行循环,可以通过HashSet判断是否循环,也可以通过快慢指针判定是否循环.

**补充**

顺带一个数字：**MySQL 权限表在 mysql 库里**（user/db/tables_priv），with grant option 让被授权者能“复制”权限给别人，DBA 撤销时还追不干净——这就是你四条风险的总根源。

**判断标准记住：凡是“过程无限重复、要么终止要么循环”的问题（判环、判重复状态），都能套 HashSet 或快慢指针。**


## Day 40 (2026-8-30)
### 今天主要进行了些多表查询的练习
> 对于今天练习的反思.

**我的回答**

对于对于GROUP BY 与 SELECT 之间规则了解有所缺失
- 我写的: select d.*,count(e.dept_id) from dept d left join emp e on d.id = e.dept_id group by d.id;
- group by d.id 而却 select d.* 

