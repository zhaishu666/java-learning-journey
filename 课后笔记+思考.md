## Day 01 (2026-7-22)
### 今天学习了TreeMap以及HashMap的源码
> TreeMap能干什么?怎么用?
1. TreeMap是基于红黑树的Map的实现类,它的特点主要是Key有序,增删改的时间复杂度为O(log n),且不允许null key
> TreeMap是怎么实现的?
1. 底层数据结构与TreeSet相同,都是红黑树
2. 每个节点都是entry结构
>为什么这样设计,解决了什么问题?
1. 设置底层为红黑树的集合,可以对添加的对象进行排序,我们可以实现Comparable接口或者在创建集合时传递Comparator对象,指定比较规则
2. 对于只需要添加和删除的情况下,我们可以使用HashMap,对于需要key有序或者范围查询时TreeMap更合适

## Day 02 (2026-7-23)
### 今天学习了Collections工具类
> Collections能干什么?怎么使用?
1. Collections是集合的工具类,提供工具方法帮助我们修改集合,想要使用Collections直接类名加.调用内部方法就行
> Collections是怎么实现的?
1. 排序和查找:sort(list) — 底层是 TimSort（归并排序优化）
   O(n log n)，稳定排序，对已部分有序的数据特别快
   binarySearch(list, key) — 二分查找 O(log n)
   前提：list 必须已排序
   reverse / shuffle / swap — O(n) 操作
   frequency / disjoint — 统计与判断
> 为什么这样设计,而不是设计成接口?
1. 工具类更强调怎么操作集合,JDK8以前接口中只允许有抽象方法,如果是接口就需要实现类实现里面所有的方法

##  Day 04 (2026-7-25)
### 入侵性
> 入侵性的概念是什么?
1. 入侵性简单来说就是达成某个行为,是否需要修改源代码
2. 细致的来说,就是框架/库迫使你的代码依赖它的程度
> 入侵性怎么降低?
1. (中等入侵) 实现接口分离,比如implement Comparable的接口
2. (低入侵) 配置文件/外部装配 比如框架通过 XML / 配置文件从外部注入依赖,业务代码完全不知道框架的存在
3. 注解 + 约定(低入侵但更现代)
> 为什么这样设计?
1. 为什么 Spring 要花大力气追求低入侵性？
- Spring 出现的年代（2003），主流是 EJB——高入侵性的代表。EJB 的问题：你的业务代码必须 import 一堆 javax.ejb.*，实现 SessionBean 接口，代码只能在 EJB 容器里跑。\
- 本地测试要启动容器，迁移到别的框架等于重写。Spring 的核心卖点就是"让你的业务代码保持纯净 POJO，框架通过 IoC 容器从外部管理依赖"。这样代码可测试（单元测试直接 new 对象）、可迁移（换框架不影响核心逻辑）、可复用（业务逻辑和框架解耦）

### 创建不可变集合
> 为什么要创建不可变集合?
1. 当我们不想别人去修改某个数据,把它防御性地拷贝到不可变集合中是个不错的选择
2. 当我们创建了一个集合,但不希望后面其他调用者去修改这个集合里的内容

> 怎么去创建一个不可变集合?
1. 直接使用List,Set,Map接口中的of方法就可以实现,其中Set和Map的of方法创建的对象是不允许有重复Key的,否则会抛出IllegalArgumentException
2. 其中Map的of方法最多只能传10个键值对,可以使用ofEntries传多个键值对对象,或者使用copyOf方法

>为什么要这样设计?直接用数组不行?
1. 数组缺点：
-   元素可修改
   只要拿到数组引用，任何人都能修改数组内部元素，无法对外暴露一份 “只读视图”。
-   没有统一集合接口
   数组不能直接传入接收 Collection/List 的方法，需要手动转换，无法配合集合流、工具类。
-   无法禁止 add/remove（本身就没有这些方法），但语义模糊
   别人看到数组，无法区分设计者意图：是允许修改元素，还是只是固定长度？
-   不能轻易返回安全副本
   如果你把内部数组直接 return 出去，外部篡改会直接影响类内部数据（破坏封装）。
2. 不可变集合外部无法修改,不可变对象天然安全,多线程情况下不需要加锁
3. 不可变 List/Set/Map 实现了 Collection 接口，可以直接用于 Stream、Collections 工具、各种接收集合的框架方法

## Day 05 (2026-7-26)
### Stream流
> Stream流是什么?Stream流有什么作用?
1. Stream流是Java设计的一种用于处理单列集合的一种方式,Stream流可以帮助我们过滤集合中不符合条件的元素,以及对stream流中元素进行处理,我们可以通过Stream流来获得一个新的符合条件的集合

**修正版**
-  "单列集合"不准确
- 漏了最核心的特征——声明式
-   Stream 是 Java 8 引入的用于处理元素序列的 API。它的核心特征是**声明式**——你描述"我要什么结果"，而不是"怎么一步步得到结果"。Stream 本身不存储数据，它只是对数据源的一个"流水线视图"。
> 什么样去使用Stream流?
2. 我们可以直接通过集合.调用Collection中的stream()方法直接获得stream的对象,然后对这个对象进行些处理
- 比如使用中间方法filter,limit,skip,distinct,map等,达到条件后使用终结方法forEach循环输出打印,count统计集合中的元素个数,或者collect将流中对象存储到集合中

**修正版**
1. 通过 Collection.stream() 或 Arrays.stream() 创建 Stream。
- 分类对了，但漏了一个关键概念——惰性求值
- 操作分两类：中间操作（filter/map/sorted/distinct/limit/skip）是**惰性的**——调了不执行，只是记录到流水线；终结操作（forEach/collect/count/reduce）才触发整条流水线执行。
- Stream 是一次性的，终结操作后不能复用

> 为什么要设计Stream流?Stream流中的很多功能也能直接通过遍历循环再进行判断实现啊?
1. Stream流处理更加便捷高效,并且不会修改原来集合中的内容

**修正版**
1. 设计Stream的核心原因是引入**声明式编程**-----用filter+map+collect来描述"我要什么",替代for+if的"怎么做"
- 好处有三: 
- 一是可组合,操作链式拼接,复杂的数据处理一行搞定
- 二是可并行,parallelStream()自动利用多核CPU,for循环要自己写线程
- 三是函数式风格,配合Lambda可以把行为当参数传
2. 注意Stream不一定比for高效,Stream的优势在代码可读性和开发效率

## Day 06 (2026-7-27)
### 方法引用
> 什么是方法引用?方法引用有什么作用?
1. 方法引用就是通过,引用本类或者其他类中的方法来来达成我们想要的目的,以减少重复代码的书写,加快开发效率,使代码更加简洁的方式

**修正版**
- 漏了一句关键的话: 方法引用是Lambda的进一步简化
- 记住一句话: 当Lambda只是调用一个现成的方法时,用方法引用代替Lambda,更短更清晰


> 怎么样去使用方法引用?
1. 只有当方法的参数形式是函数式接口时才可以使用,对于其他类,构造方法,静态方法,以及实例方法有不同的形式.
- 但所引用的方法的参数类型,返回值都必须与抽象方法一致,且引用的方法必须符合抽象类的目的
- 但对于类名直接引用,如 String::toUpperCase toUpperCase参数列表中的第一个形参可以为空,抽象类第一个形参隐式被方法调用者的对象

**修正版**
1. 参数类型必须完全相同,但返回值有个例外: 当抽象类中返回值为void时,引用的方法可以有返回值,只是被忽略
- 问题2："对于类名直接引用，如 String::toUpperCase，第一个形参可以为空"——表述不够清楚。
- 这个是方法引用中最难理解的一种形式，叫**"类名::实例方法"**。规则是：
- 函数式接口的第一个参数，变成了实例方法的调用者（receiver）
- // String::toUpperCase 是"类名引用实例方法"
- // 等价于 Lambda: (String s) -> s.toUpperCase()
- Function<String, String> f = String::toUpperCase;
- // 第一个参数 "hello" 变成了 toUpperCase() 的调用者
- String result = f.apply("hello");  // "HELLO"
-  这里不是"第一个形参为空"，而是函数式接口的第一个参数被"借"去当了方法调用者


> 为什么要设计方法引用? Lambda表达式其实已经够简洁了,难道也是为了引入声明式? 方法引用底层到底是怎么样执行的呢?
1. 如果我们有多个Lambda表达式执行的是同一个操作,而且这个操作需比较多的代码,我们将这些操作抽取成方法,再使用方法引用的格式,将会减少不少代码的书写,也体现了Java语言面向对象的特征
2. 底层是调用了对应的方法,通过方法的return实现的吗?

**修正版**
1. 问题1："体现了Java语言面向对象的特征"——说反了
- 方法引用和Lambda表达式一样,是Java向函数式编程靠拢的特性,不是面向对象的体现.面向对象的特征是封装,继承,多态.
- 方法引用的本质是把方法当作值来传递----这恰恰是函数式编程的核心思想.Java8引入这些特性,是为了让Java也能支持函数式编程风格
2. 底层不是简单的"调用方法+return"。方法引用在编译后和 Lambda 一样，生成 invokedynamic 指令，运行时由 JVM 动态生成函数式接口的实现类。这个实现类的方法体就是"转发调用"到被引用的方法
3. **总的来说** 设计方法引用的核心原因是：当一个 Lambda 的函数体仅仅是调用一个已存在的方法时，用方法名替代 Lambda 体，代码更短、意图更清晰、能直接复用已有逻辑。
底层和 Lambda 一样，编译为 invokedynamic 指令，运行时 JVM 动态生成函数式接口的实现对象，方法体转发调用到被引用的方法。方法引用是 Java 引入函数式编程风格的一部分，不是面向对象特性的体现

## Day 07 (2026-7-28)
### 异常
> 什么是异常? 抛出异常有什么作用?
1. 异常就是程序运行时出现的错误,分为严重的Error以及Exception,其中Error通常是系统性的问题,不需要我们程序员去捕获.
2. Exception又分为编译期异常以及运行期异常,在执行代码时抛出异常可以帮助程序员去更好的理解运行时出现的问题的原因

**错漏点**
1. 问题1：异常不只是"运行时出现的错误" 编译期异常在编译时就被检查，还没运行就要求你处理，它不一定是"运行时错误"。更准确的说法：
- 异常是程序执行过程中出现的**非正常情况**,JVM会创建一个异常对象来描述它.异常机制让程序有机会优雅地处理这些问题,而不是直接崩溃
2. 缺少异常体系的全貌 Throwable: 所有异常的根,RuntimeException运行时异常(非受检),编译期异常(受捡)


> 怎么样抛出异常,以及我们怎么捕获异常,具体怎么实现?
1. 我们可以对方法内部可能出现异常内容的代码进行判断,如果不符合条件,就通过throw手动抛出异常.
2. 对于编译期异常必须在方法名后加throws来声明可能抛出的异常,而运行期异常可以忽略不写
3. 我们捕获异常的目的是为了让代码可以继续执行下去.我们捕获异常使用try-catch语句,判断在try中出现的异常能否与catch()中的异常类型相匹配
如果匹配,就不会执行try中的后续的语句,直接执行catch中的语句,如果try中可能存在多种异常,我们就需要使用多个catch语句或者JDK7一个catch中写 | 判断符合哪一个异常
值得注意的是一个try语句中最多只能抛出一个异常,因为try中后续的代码即使有异常也不会执行

**错漏点**
* 你写的内容基本对，漏了一个重要的东西——finally. 
finally {
   // 无论有没有异常，都会执行
   // 用来关闭资源（文件、数据库连接等）
   }
- finally的核心用途: 资源清理. 无论 try 里有没有异常、catch 有没有匹配到，finally 都会执行。JDK 7 之后有了 try-with-resources，自动关闭实现了 AutoCloseable 的资源，更简洁:
- // JDK 7+，自动关闭，不需要 finally
-  try (FileReader fr = new FileReader("a.txt")) {
-  // 读文件
-  } catch (IOException e) {
-  // 异常处理
-  }
-  // fr 自动关闭了，不用手动 close

2. 你说"捕获异常的目的是让代码继续执行下去"——这只是一种情况。捕获异常的目的可能是：

- 恢复执行（你的情况）
- 记录日志后重新抛出（让上层处理）
- 包装成更有意义的异常再抛（异常转换）
- 清理资源后退出

> 为什么要设置异常这个概念?对于语句符不符合我们可以通过if语句判断,filter对流中的数据进行过滤都是可行的啊?异常的底层代码到底是如何执行的?
1. 不太了解

**错漏点**
所以异常解决的四个核心问题：
异常解决的四个核心问题

| 问题 | if-else 的困境 | 异常怎么解决 |
|------|----------------|--------------|
| 传播 | 每层都要手动检查返回值并传递 | 异常自动沿调用栈向上传播，中间层可以完全不管 |
| 不可忽略 | 返回值 -1 你可以假装没看见 | 受检异常编译器强制你处理或声明，想忽略都忽略不了 |
| 类型安全 | 错误码是 int，没有类型区分 | 每种异常是一个类，catch 可以精确匹配不同类型 |
| 信息携带 | 只能返回一个数字 | 异常对象携带消息、调用栈、cause 链，精确到哪一行出的错 |


2. 底层怎么执行的？
你问的"底层代码如何执行"：

//1. throw new FileNotFoundException("xxx")
   → JVM 在堆上创建一个异常对象（和 new 普通对象一样）

//2. JVM 从当前方法开始，沿调用栈往上找匹配的 catch
   start() → processData() → readFile()  ← 抛出点
   JVM 从 readFile 往上找：
   readFile 没有 try-catch → 继续
   processData 没有 try-catch → 继续  
   start() 有 try-catch → 匹配 FileNotFoundException → 执行 catch

//3. 如果一路找到 main() 都没有匹配的 catch
   → 交给 JVM 默认异常处理器
   → 打印堆栈轨迹（就是你在控制台看到的红色报错）
   → 终止当前线程

- 关键点：异常处理不是"跳转"，是"栈展开"（Stack Unwinding）。 JVM 一层一层退栈，每退一层检查有没有 catch，找到就跳进去执行，找不到就继续退。这个过程是 JVM 层面支持的，不是 Java 语法层面模拟的。

## Day 08 (2026-7-29)
### File类

> 什么是File类,file类对我们有什么作用呢?
1. File类是Java提供的帮助我们操作文件及文件夹的javabean类.
2. 我们可以通过new File类型的对象来让我们操作指定路径的文件

**错漏点**
- 问题1："javabean类"用错了。
JavaBean 是一个有严格定义的概念——私有字段 + getter/setter + 无参构造 + 实现 Serializable。
File 类不符合这个规范。准确说法：File类是Java提供的用于表示文件和目录路径的类
- 问题2：漏了 File 最本质的特征——它只是路径的抽象，不代表文件真实存在
  这个特征是理解 File 类的关键：File 对象 ≠ 文件本身。它只是一个路径的"名片"，名片上写着地址，但地址那头的房子可能还没盖。

> 我们该怎么样使用File类?
1. 创建File类的对象,调用里面对应的方法就可以了,如getPath(),length(),以及创建文件文件夹的createNewFile(),mkdirs(),还有获取遍历常用的listFile()

**错漏点**
- 两个容易踩的坑：
- 坑1：listFiles() 对不存在的目录返回 null，不是空数组
- 坑2：delete() 只能删空目录。 目录里有内容就删不掉，得先递归删里面所有文件再删目录。这也是很多面试题的考点


> 为什么要设计File类,目前好像并没有学到啥要操作外部文件的内容?当我们使用File对象时JVM底层是如何实现它的呢?
1. 没有头绪

**错漏点**
1. File 类是 IO 流的前置基础，你马上就要用到了
- 设计File类的原因1: 跨平台抽象
- 不同操作系统的路径规则完全不同,如果没有 File 类，你写 C:\Users\xxx 到 Linux 上跑就废了。File 类把这些差异封装掉了
- 原因2: 路经管理和内容读写分离
- 这是最重要的设计思想：File 类不负责读写文件内容，你不能用 File 对象去读文件里写了什么——那是 IO 流的活
2. 底层怎么执行?
- File 类内部就存了一个 String 类型的 path 字段。真正干活的方法都是 native 方法，调用的是操作系统的文件系统 API：
- Java 代码: f.exists()
  ↓
  JVM 调用 native 方法 (C++ 实现)
  ↓
  Windows: 调用 Win32 API GetFileAttributesW()
  Linux:   调用 POSIX access() 函数
  ↓
  操作系统检查文件系统，返回结果
- 所以 File 类本质上是 Java 对操作系统文件系统 API 的一层封装，让你不用关心底层是 Windows 还是 Linux，用同一套 Java 代码操作文件。

## Day 09 (2026-7-30)
### IO流第一天(学习了IO流的基本概念,FileOutputStream,FileInputStream以及如何write()时换行和续写)
> 什么是IO流?IO流对于我们程序员开发有什么作用?

**我的理解**
1. IO流是Java提供的存储和读取数据的解决方案,在昨天所学的File类中,我们虽然可以文件整体进行一些操作,但无法修改里面的内容,IO流就是解决这些的方案
2. 目前学习进度有限,不过我认为IO流对程序的主要作用应该是能够帮我们存储和读取进度,如游戏进程.或对某些数据进行修改

**错漏点**
1. "存储和读取数据的解决方案"——太笼统，没说到"流"这个核心概念。
- "流"不是比喻，是实际机制：数据像水流一样，一个字节一个字节地从一端流向另一端，不是一次性全部搬运。这就是为什么叫"流"而不是叫"文件操作工具"。
2. "修改数据"不准确,IO流只能进行读出来,写进去的操作,要想修改里面的内容就需要把文件完整的读出来,然后在内存中改,最后重新写回去
3. 漏了IO流的分类体系,IO流分为字节流和字符流. 字节流:OutputStream,InputStream. 字符流: Write和Reader

> IO流是怎么实现的呢?对于前面的Stream流也一样,虽然流,流水线之类的名称很贴切,但没看到具体的代码实现让人对自己的理解底气有点不足
1. 我的理解: Java底层创建了与对应文件的链接,让我们可以直接操作文件,我们操作完成后需要释放资源,防止内存溢出

**错漏点**
1. "创建了与对应文件的链接"方向对,但不够准确,流对象内部持有的是一个操作系统级别的`文件描述符`,操作系统打开对应的文件表项找到文件
2. "防止内存溢出"不完全对,释放的是文件描述符,操作系统对每个进程同时打开的文件数有上限有上限,你不close(),它就一直占着,超过了就直接Too many open Files,程序直接崩溃

> 为什么要设计IO流?而不是其他的,比如专门操作Input和Output的工具类
1. 我的理解: 设计IO流是为了让我们更好的操作Input和Output,通过创建不同的流的对象,通过对应的方法来进行操作

**错漏点**
1. 你的回答是循环论证： "设计 IO 流是为了更好地操作 Input 和 Output"——这等于说"设计汽车是为了更好地开车"。没有回答到点上。
2. **真正的理由有三个**：
- 理由1: 流式处理——再大的文件也不会撑爆内存: 如果没有流,你读取一个10GB的文件需要10GB的内存,而流可以让我们控制每次读多少,逐部份读入
- 理由2: 统一抽象——同一套API适配所有数据源: 你学会了 InputStream，就同时学会了文件读取、网络读取、键盘输入——因为 API 完全一样，都是 read()。 如果每种数据源都设计一套工具类，你要学 N 套 API。
- 理由3: 装饰器模式——流可以套娃增强功能: 一层套一层,每层加一个功能,互不干扰.这就是设计"流"这个抽象的原因,而不是工具类,因为只有对象才能被包装,静态工具方法无法套娃

## Day 10 (2026-7-31)
### IO流第二天(学习了字符集,字符流的基本概念,FileReader,FileWriter)
> 什么是字符集,字符流?字符流与前面学习的字节流有什么区别? 

我的回答:
1. 字符集是计算机编码和解码的"模板",计算机通过解码后对应的字符集的码数寻找对应的字符,以及将字符编码成对应的码数,再转化为二进制存储
2. 字符流主要操作对应的字符,它本质上就是字节流+字符集,底层操作依靠的就是字节流.
3. FileReader和FileWriter是操作文件的输入和输出的实现类,我们可以通过创建对应的对象来操作文件
4. 字符流可以自动识别对应的字符,比如再使用GBK字符集时,字符流识别到中文字节时自动读取两个字节

**错漏点**
1. "码数"应该是"码点（Code Point）"。 术语要准确，面试时用错术语会扣分
2. 漏了 ASCII 和 Unicode 的关系,Unicode怎么诞生的要搞清楚

> 字符流是怎么实现英文一次读一个字符,中文一次读多个字符的?

我的回答:
1. 根据二进制数开头是1还是0判断是否为中文,1开头的是中文,再按照对应的字符集决定一次读取多少字节

**错漏点**
1. 你说"根据二进制开头是1还是0判断"——方向对，但只对了一半。
这个机制取决于用什么字符集编码，不同字符集的判断方式不同：
- GBK 编码规则：
-  英文：1字节，最高位是0，范围 0x00-0x7F
-  中文：2字节，第一字节最高位是1（范围 0x81-0xFE）
- UTF-8 编码规则（变长）：
-  英文：0xxxxxxx          → 1字节，0开头
-  中文：1110xxxx 10xxxxxx 10xxxxxx → 3字节，1110开头
-  Emoji：11110xxx ...     → 4字节，11110开头
- 核心：字符流不是"猜"有几个字节，是根据编码规范的前缀位精确判断的

> 字符流能帮助我们更好地读取和输出中文,那还会用到字节流吗?

我的回答:
1. 我们应该在开发时很少用到字节流了,可能在只操作英文时会使用到字节流

**错漏点**
1. 实际情况完全相反：实际开发中字节流用得比字符流多得多

|数据类型| 	该用什么流|	例子|
|:---|:------|:---|
|纯文本文件| 	字符流  |	.txt / .csv / .java / .xml / .json|
|非文本文件| 	`字节流`  |	图片、视频、音频、压缩包、Excel、PDF|
|网络传输| 	`字节流`  |	HTTP请求体、Socket通信、文件上传下载|
|序列化| 	`字节流`  |	对象序列化 ObjectOutputStream|

核心原则记住一句话：

文本数据用字符流，二进制数据用字节流。判断标准是"文件能不能用记事本打开还能看懂"——能看懂的是文本，看不懂的是二进制。

- 一句话总结第三层：字符流不是替代字节流的，是字节流在"文本处理"场景的增强版。底层还是字节流，只是包了一层自动编解码的逻辑。非文本数据永远用字节流。

## Day 11 (2026-8-1)
### IO流第三天(字符流的原理,缓冲流,以及一些练习)
> 字符流的缓冲区是什么?缓冲流是什么?

**我的回答**
1. 在读取数据时,字符流会创建一个默认长度为8192字节的字节数组,这就是缓冲区.
2. 缓冲流是Java提供的用来包装基本流的方式,它内部包装了更大的缓冲区,可以提高字符流的读写效率

**错漏点**
1. 问题1：把两层缓冲区混为一谈了
-字符流里有两个完全不同的缓冲区，你把它们当成了一个：

|缓冲区| 在哪里| 类型    | 大小    |作用|
|:----|:-----|:------|:------|:---|
|缓冲区①| InputStreamReader（FileReader 的父类）内部| byte[]| 8192 字节|从 FileInputStream 读一批字节，用于解码成字符|
|缓冲区②| BufferedReader 内部 | char[]| 8192 字符|从下层 Reader 读一批字符，减少 I/O 调用次数|

- 你说的"8192字节的字节数组"指的是缓冲区①——但这是 FileReader 自带的，不是缓冲流加的。缓冲流（BufferedReader）加的是缓冲区②，而且它是 char 数组不是 byte 数组。
- FileReader自带的缓冲区主要用于解码,如果频繁调用read()(一次读取一个字符),每次都会向上通知解码器尝试获取字符,会频繁触发解码逻辑,频繁调用底层读取
即使底层有byte缓冲,大量微小粒度的read()依然开销巨大
- BufferedReader 的 char [] 缓冲区作用：一次性批量读取字符存入char数组,后续就算多次read(),readLine()都直接从char数组中读取,减少频繁调用StreamDecoder读取和解码的次数

问题2：`"更大的缓冲区"是错的`。 BufferedReader 默认 char[8192]，跟 InputStreamReader 的 byte[8192] 差不多大。它不是靠"更大"提速，而是靠减少底层调用次数提速——下面第二层详细说。

> 字符流如何利用缓冲区实现数据读写的?缓冲流内部是怎么来包装的?

**我的回答**
1. 字符流的输入流会先将磁盘中的数据读到缓冲区中,然后通过read()方法将数据读取出来,再通过write()方法写入输出流的缓冲区中,然后通过缓冲区持久化到硬盘中
只有当缓冲区中的数据全部被读取后,缓冲区才会重新从磁盘中读取数据
2. 缓冲流内部把基本流中的字节数组替换为了一个更大的字节数组,以此提高效率

**错漏点**
1. 问题1：读写流程混在一起了，而且"持久化到硬盘"术语不对。
应该是 flush（刷出），不是"持久化"。更关键的是——你漏了一个能让你程序出 bug 的概念：
缓冲输出流必须 flush 或 close，否则数据可能还留在缓冲区里，没写到文件！
2. 问题2："替换为更大的字节数组"是错的——BufferedReader 不是"替换"，是"包装"。
BufferedReader 内部持有的是一个 Reader 对象（就是你传给它的 FileReader），它不会去改 FileReader 内部的东西。
它只是在自己的 read() 方法里，一次性从 FileReader 读一批字符存到自己的 char[] 里，然后你每次调 read() 都从它的 char[] 里取。
- 这叫装饰器模式——包一层，加功能，不改动被包装的对象

> 为什么缓冲区是这样设计的以及为什么要设置缓冲区?那这跟我们在读取是read()形参中创建的数组有何区别?

**我的回答**
1. 设置缓冲区是为了提高内存与磁盘之间交换数据的速度,数组长度设置为8192字节可能是比较常用
2. read()形参创建的数组是为了提高内存内部的数据传输速率

**错漏点**
1. 问题1："8192可能是比较常用"——你应该追问为什么。
8192 字节 = 8KB = 2 个操作系统内存页（每页 4KB）。磁盘 I/O 以页为单位，一次读 8KB 刚好对齐两个页，不会浪费也不会多跑一趟。这是经过工程验证的最优值，不是随便选的
2. 问题2：核心原因不是"提高速度"，是"减少次数"。
- 磁盘慢不是因为单次读取慢，是因为每次找数据要寻道。读 1 个字节和读 8192 个字节，寻道时间几乎一样。所以：
- 不用缓冲区：读 8192 个字符 → 调 8192 次 read() → 8192 次磁盘寻道 → 慢死
- 用缓冲区：  读 8192 个字符 → 调 1 次底层 read → 1 次磁盘寻道 → 快 8192 倍
- "减少 I/O 调用次数"才是缓冲区的核心价值，不是"提高传输速度"。
3. "提高内存内部传输速率"方向对但不精确

## Day 12 (2026-8-2)
### IO流第四天(学习了转换流,序列化流与反序列化流)
> 什么是转换流,什么是序列化与序列化流?

**我的回答**
1. 转换流是字节流与字符流之间的桥梁,可以通过转换流包装字节流,再用字符流包装,达到字节流使用字符流方法的作用.FileReader继承于转换流InputStreamReader
2. 序列化就是把数据转化成二进制流后输出,通常用于数据保存.对象想要实现序列化必须实现Serializable接口,且序列化后的数据不允许改变
,否则反序列化会出错.如果想要修改Javabean类而反序列化不出错,就需要给Javabean类添加serialVersionUID版本号
3. 如果想要Javabean类中的某个成员变量不被序列化,可以使用transient修饰符修饰

**错漏点**
1. 问题1："序列化后的数据不允许改变"——理解偏差。

- 不是"不允许改变"，而是：
- 序列化文件你当然可以改——用文本编辑器打开就是一堆乱码
- 你改了也白改，因为反序列化时按二进制协议解析，你手动改了字节，协议解析不出来就会报错
- 真正的问题是你改了 JavaBean 类本身（比如加了个字段），再去反序列化以前存的文件——类的结构变了，JVM 认为"这不是同一个类了"，就抛 InvalidClassException
- serialVersionUID 的作用：告诉 JVM "只要这个版本号没变，就算类加了字段，也是同一个类，别报错"。JVM 会把文件里有的字段读出来，类里新增的字段用默认值

> 转换流是怎么实现流的转换的?序列化流与反序列化流是怎么工作的来实现序列化与反序列化?

**我的回答**
1. 不了解

**错漏点**
1. 转换流怎么实现"字节→字符"的？
- 核心：InputStreamReader 内部持有一个 InputStream（字节流）和一个 Charset（字符集）。每次调 read()，它从字节流读字节，用字符集解码成字符，返回给你
- 字符集是 InputStreamReader 的属性，不是 FileReader 加上去的。FileReader 只是 InputStreamReader 的"预设快捷版"，帮你选好了 FileInputStream 和默认字符集，自己什么都不干
2. 序列化:反射遍历字段,按照类型写字节 
- transient 字段和 static 字段不会被序列化  static 属于类，不属于对象 → 序列化的是对象状态，static 不在其中
- 遗漏: 序列化的引用问题——循环引用怎么处理？
- ObjectOutputStream 给每个对象分配一个序号，序列化过的不再重复序列化，只写一个引用标记。这叫"序列化图"而非"序列化树"。你应该追问这个机制


> 字符流是字节流通过转换流加字符集转换过来的吗?设置序列化流的原因?序列化是怎么将数据变成乱码的?

**我的回答**
1. 不了解
2. 数据序列化存储到本地文件中,是乱码的形态,用户无法轻易修改数据
3. 不知道

**错漏补充**
- 字符流就是字节流通过转换流加字符集转换过来的
1. 问题1："乱码"不是设计目的，是副作用。

- 序列化产出二进制不是"为了防止用户修改"，而是因为：

- 二进制比文本高效——一个 int 存成 4 字节，存成文本 "12345" 要 5 字节还得解析
- 二进制类型安全——读回来直接是 int/String/对象，不用自己解析文本格式
- 二进制支持引用——对象 A 引用对象 B，序列化只存一次 B，反序列化时恢复引用关系。文本做不到
- "乱码"是因为二进制不对应任何字符集的编码表，用记事本打开当然看不懂。这是二进制格式的天然特征，不是故意设计来防篡改的。
2. 问题3：为什么要设计序列化流？

|场景|	不用序列化|	用序列化|
|---|---|---|
|对象存到文件|	自己写规则：哪个字段存、什么格式、怎么读回来|	一个 writeObject 搞定|
|对象通过网络传输|	自己写文本协议，接收方自己解析|	直接.writeObject + readObject|
|深拷贝对象|	手动递归复制所有字段|	序列化到内存再反序列化回来|

- 核心价值：把"对象状态 → 字节流"和"字节流 → 对象状态"这两个方向标准化，不用每次自己发明协议。

## Day 13 (2026-8-3)
### IO流第5天(学习了打印流,压缩流与解压缩流)
> 什么是打印流,压缩流与解压缩流?它们分别都有什么作用?

**我的回答**
1. 打印流是Java提供的一种包装流,基于装饰器模式,能够输出任意的数据类型,不需要手动转换类型.是最常用的流之一,如System.out.println
2. 压缩流与解压缩流都是Java提供的包装流,用于对于指定的文件或文件夹进行压缩或对压缩包进行解压缩,两者都可以指定字符集编码规则,每一个压缩包文件都是一个ZipEntry对象.
- 对于压缩流要注意的是putNextEntry(ZipEntry entry)创建压缩条目,必须要等到上一个条目closeEntry()后才能开启.
- 对于解压缩流,专有方法getNextEntry()读取ZipEntry对象,再通过字节流读取里面的每一个数据

**错漏点**
1. 打印流有一个关键特性你完全没提：不会抛出 IOException。print()/println() 方法内部自动 catch 了所有 IO 异常，这是它和其他流最大的行为差异。
-   打印流只有输出没有输入（不像其他流有 in/out 配对），它是"终端型"流

> 打印流是怎么实现什么数据都能打印的?压缩流与解压缩流又是怎么实现文件的压缩与解压的呢?

**我的回答**
1. 不了解

**错漏点**
1. 打印流怎么实现"什么都能打印"？
- 核心机制是:方法重载+统一转String. PrintStream 内部重载了所有基本类型 + Object
- 所有 print() 最终都走到 write(String s)，再有 println() = print() + newLine()。
2. 那为什么不会抛IOException?
- print内部捕获了所有异常,可通过checkError()查看
3. 压缩流怎么实现压缩？
- ZipOutputStream 的底层是 Deflater 对象，使用 DEFLATE 算法（LZ77 + Huffman）：

|步骤|	方法| 	底层在做什么                             |
|---|---|-------------------------------------|
|创建条目|	putNextEntry(entry)	|写入 Local File Header（文件名、时间戳、压缩方法标记） |
|写数据|	write(byte[])|	数据送入 Deflater 压缩，输出到内部 OutputStream|
|关闭条目|	closeEntry()|	调用 Deflater.finish() 完成当前条目压缩，记录 CRC32 校验值|
|收尾	|finish()/close()	|写入 Central Directory（所有条目索引）和 End of Central Directory 记录|
- 一个关键细节：closeEntry() 不是可选的。你不关上一个条目就开下一个，ZIP 文件结构就损坏了——这就是你笔记里说的"必须 closeEntry() 后才能开启下一个"的底层原因。

4. 解压缩流怎么实现解压？
- ZipInputStream 内部用 Inflater 对象：

|步骤|	方法|	底层在做什么|
|---|---|---|
|读条目	|getNextEntry()	|解析 Local File Header，初始化 Inflater，返回 ZipEntry 对象|
|读数据	|read(byte[])|	从底层 InputStream 读压缩数据 → Inflater 解压 → 返回解压后字节|
|读到-1	|—	|当前条目读完，自动定位到下一个条目的 Local File Header|
和压缩流一样是流式处理——不需要把整个 ZIP 文件加载到内存，读一个条目解压一个条目

> 为什么要设计打印流呢?之前那么多流难道不够输出时用吗? 压缩流与解压缩流为什么要设计?直接使用其他的解压软件就能实现这个功能? 这两个流在整个IO体系中处于什么位置呢?

**我的回答**
1. 打印流默认打印在控制台,可以让我们程序员更好的查看代码的状态,可以输出任意类型的数据,让我们不用手动转换类型
2. 压缩流与解压缩流可以让我们的软件具有这个功能比如Windows自带的压缩与解压缩
3. 两者都是不继承于其他流的高级流

**错漏点**
1. 为什么设计打印流

|  你的回答|	判定|	更深层原因|
|---|---|---|
|方便程序员查看状态|	✅ 对|	调试基础设施：没有打印流，每次调试都要 Integer.toString(i).getBytes() 然后 write()，没人受得了|
|不用手动转换类型	|✅ 对	|降低认知负担：print 关注的是"人类可读"，write 关注的是"机器可读"|
|  —	|❌ 遗漏|	容错设计哲学（fire and forget）：日志/调试输出不应该因为 IO 异常中断主业务逻辑。对比：如果用普通 FileOutputStream 写日志，磁盘满了直接抛异常崩程序——打印流不会|

2. 为什么设置压缩流与解压缩流

| 你的回答 | 	判定   | 更深层原因|
|---|-------|------|
| 让软件自带压缩功能 | 	✅部分对 | 不只如此——JAR 文件就是 ZIP 格式。Java 的类加载器、打包发布、Maven 依赖，全部建立在 ZIP 之上。没有压缩流，Java 生态本身就不成立|
| — | ❌ 遗漏  | 	流式处理 vs 全量加载：不用压缩流，你想读一个 ZIP 里某个文件，就得把整个 ZIP 读到内存。压缩流让你边读边解压，100MB 的 ZIP 只用几 KB 缓冲区 |
| — | ❌ 遗漏	 | 链式组合：new ZipInputStream(new BufferedInputStream(new FileInputStream("a.zip")))——可以和其他 IO 流无缝嵌套，这就是装饰器模式的威力 |
3. 它们都继承自 FilterOutputStream/FilterInputStream，和之前学的 BufferedInputStream、BufferedOutputStream、DataInputStream 是同一家族。装饰器模式是贯穿整个 IO 体系的设计主线，打印流和压缩流不是特例而是遵循同一套规则

## Day 14 (2026-8-4)
### 无
> 正则表达式中中文返回可以用[\u4e00-\u9fff]表示

## Day 15 (2026-8-5)
### IO流练习反思
> 对于字节流FileInputStream()的read()方法知识有所遗忘,跟BufferedReader的readLine()有搞混.
1. read()方法返回的是int类型的,如果read()是空参实现的,在FileInputStream中返回的就是字节对应的ASCII码值.如果是字符流中,那返回的就是对应字符集中的码值.
2. 如果我们手动书写了buffer数组,那么read()返回的int类型的数据就是实际读取到的`字节或者字符数`
3. 这时我们write()时,内部填写的就是(buffer,0,len),代表写入数组中实际的元素
4. 而BufferedReader的readLine()则是将一整行数据转换成String类型返回,通过回车符判断一整行是否结束(不同操作系统都可识别)\n,\r\n,\r.且不返回回车符.

> 对于带权重的算法进行了初步了解.
1. 如果我们想要通过权重来分配每个对象所被选到的概率,可以给每个对象设定权重,然后向加获得总的权重,再通过随机一个这个总权重范围内的数字.再通过遍历相加每个对象的权重,看看数字落到哪个区间.

> 学习了String类中的trim()方法,该方法的作用是去除字符串两端的ASCII码表中的空格等

1. 实际上去除的是ASCII码小于普通空格的所有元素,包含了制表符,换行符等
2. 底层是通过双指针实现去除字符串两边的空格的,而中间的空白不会动

## Day 16 (2026-8-6)
### 多线程第一天(学习了properties配置文件,多线程的基本概念,以及创建线程的前3种方法)

> 什么是properties配置文件,它是干什么的?进程,线程的基本概念?创建线程的前三种方法是哪三种?多线程到底是什么?

**我的回答**
1. properties是一种文件类型,常用来进行程序的一些数据的配置,将这些配置持久化保留.比如游戏设置的内容
2. 进程: 系统分配资源的最小单位,一个正在进行种的程序就是一个进程.每个进程分别独立,不相互影响
3. 线程: CPU的最小调度单元,一个进程里面可以有多个线程,一个进程里的所有线程共享一个堆内存但线程本身是私有的,元空间,栈都是线程私有的
4. 创建线程的前三个方法:
- 1.创建一个类继承Thread类,并重写里面的run()方法. 缺点: 类只能单继承,会占用仅有的继承位.
- 2.创建一个类实现Runnable接口,并重写里面的run()方法,也是最常用的创建线程的方法. 好处: 一个类可以实现多个接口,完全不影响继承.
- 3.FutureTask + Callable.创建一个类实现Callable<T>,再通过包装成FutureTask类型交给Thread. 好处:解决了前面两个方法没有返回值,无法抛出异常的问题
5. 多线程是一种为了提高进程效率的方法,在进程等待时执行其他的代码

**错漏点**
1. properties既是一个Java类(继承了Hashtable),拥有Map集合的全部特征,又是一种文件格式(key=value).你说的只是文件格式这一半
2. (概念错误)元空间（Metaspace）是共享的！ 存放类信息、常量池，所有线程都能访问
3. 记忆口诀: 堆和元空间共享,PC(程序计数器),虚拟机栈,本地方法栈私有
4. 1.Thread调用 start() 启动线程，不是调用 run() 2.Runnable对象还是要包装成 new Thread(runnable).start()，Runnable本身不是线程 3.需补充：futureTask.get() 会阻塞当前线程直到结果返回
5. 这只描述了"阻塞等待时切换"这一个场景。多线程还有另一个核心场景：多核CPU上真正并行执行（4核CPU可以同时跑4个线程）

> 怎么通过properties配置文件进行配置的?具体该怎么使用这些创建的线程?怎么样通过这些线程实现多线程?难道是创建多个Thread对象,调用start()就是多线程了?

**我的回答**
1. 通过每次执行程序时读取properties配置文件的内容实现设置.
2. 第一种通过继承Thread类,创建Thread的子类对象,来使用Thread的方法.第二种通过调用重写的run()方法调用里面的内容.第三种将包装后的FutureTask类型的返回值交给Thread后进行处理.
3. 不清楚,目前猜想是多个Thread类型对象同时调用.

**错漏点**
1. properties过程: load() 读文件 → getProperty() 取值 → setProperty() 改值 → store() 写回文件。本质就是对Map做持久化。
2. 基本正确
3. "猜想是多个Thread类型对象同时调用"——方向对了，但关键细节没抓住。

|调用方式|	发生了什么|	是不是多线程？|
|---|---|---|
|thread.start()|	JVM向OS申请创建新线程，新线程自动调用run()|	✅ 是，有新线程|
|thread.run()|	就是普通的方法调用，在当前线程里执行run()的代码|	❌ 不是，没有新线程|

> 为什么要专门使用properties类型的文件配置?线程以及多线程跟我们之前学习的代码右键run执行之间有什么区别?

**我的回答**
不清楚

**错漏点**
1. 为什么用 Properties 配置文件
-   如果数据库密码写死在代码里，每次换环境都要改代码重新编译.且非程序员可改,运维人员改配置文件就行，不需要懂Java代码
2. 之前你写的所有代码都是单线程的：
-   多线程：main方法 → 启动线程A → 启动线程B → main继续执行
   ↓           ↓            ↓
   线程A干活    线程B干活     main也在干活
   （三件事同时进行）
3. 具体区别

|维度|	单线程（之前）|	多线程（现在）|
|---|---|---|
|执行方式|	一件事做完再做下一件|	多件事交替/同时进行|
|阻塞影响|	一处阻塞全程序卡死|	一个线程阻塞，其他线程继续跑|
|CPU利用|	单核也只能用一部分|	多核CPU可以真正并行|
|复杂度	|简单，不用考虑线程安全|	复杂，要考虑共享数据竞争、死锁|

## Day 17 (2026-8-7)
### 多线程第二天(学习了Thread类中的常用方法,同步代码块,同步方法,Lock锁,死锁)

> 列举出Thread类中的常用方法.说明同步代码块,同步方法是什么,两者之间有什么区别?说明Lock锁,死锁的概念.

**我的回答**
1. Thread类中的常用静态方法:sleep() 特点:抱着锁睡觉,到时间自然醒.常用的非静态方法如setName(),getName(),setProportiey(),getProportiey(),setDeamon()等
2. 同步代码块Java为了解决多线程下的并发修改问题所设计的一种代码块.该代码块格式为synchronized(锁对象){} 组成只有当线程持有锁对象时才能进入{}也就是临界区执行内部代码
,每次只有一个线程持有这个锁的对象.当线程执行完临界区的代码后自动释放所持有的锁.这里有几个需要注意的点: 任何非null的对象都可以作为锁对象,锁的对象必须唯一,一般调用对象静态情况下为该类类名.class,
实例对象时用this.
3. 同步方法则是在整个方法上加锁,锁的位置在修饰符的后面,只有当对象持有该锁时,才能使用该方法.不过这种锁直接锁整个方法,锁粒度比较大,并发性能下要低于同步代码块.
4. Lock锁是Java为了解决synchronized锁的不足而推出的新的锁.因为synchronized的锁是JVM层面的,隐式加锁,我们无法知道什么时候有锁,什么时候释放.
于是Lock在JDK5被推出,Lock本身是个接口,我们一般通过它的实现类Reentrantable()创建Lock对象.它最大的优点就是灵活,我们可以通过lock()和unlock()方法自主选择锁的位置.
以及它的一些特有方法如: tryLock(),尝试获得锁,未获得就自动释放手中的锁. unlock一定要写在finally块中.
5. 死锁就是多个线程互相持有对方的锁,但本身又不会自主释放自身的锁的情况.它的形成有4个原因.

**错漏点**
1. 拼写错误: set/getProportiey()应为set/getPriority().Reentrantable()应为ReentrantLock(),setDeamon()应为setDaemon().
2. Thread方法补充: currentThread()（获取当前线程对象）、join()（等待该线程执行完毕）、yield()（让出CPU时间片）、interrupt()（中断线程）
3. 补充一个关键细节：你说"执行完临界区代码后自动释放锁"——正确，但补一句：如果临界区代码抛异常，synchronized也会自动释放锁（JVM层面保证）。这和Lock不一样，Lock抛异常不会自动释放，所以你必须写finally。
4. 对于tryLock()的描述有误: tryLock()是尝试获取锁，成功返回true，失败返回false。它不会"自动释放你已有的锁"——你手里的锁你得自己unlock().
对比 lock()：拿不到就一直等（阻塞），而 tryLock() 拿不到就立刻走（不阻塞）。这就是它最大的价值。
5. 应将死锁的4个条件以及解决方法背下来.

> synchronized锁和lock锁的底层是怎么让不持有该锁的对象无法执行临界区的?两者一个是关键字,一个是接口,有什么区别吗?

**我的回答**
1. 底层通过判断线程是否持有锁,来决定是否让对象执行临界区的代码.
2. synchronized关键字太过固定,接口通过实现类创建它的对象,我们可以灵活地使用它的方法

**错漏点**
1. 循环论证,没有答到点上.
- 怎么实现,两个层面
- ① 字节码层面——synchronized代码块编译后会生成两条指令：

synchronized(obj) {
// 临界区代码
}
↓ 编译后
monitorenter       // 进入：尝试获取obj的monitor
// 临界区代码
monitorexit        // 退出：释放obj的monitor
- 每一个Java对象都有一个Monitor(管程/监视器).monitorenter就是去获取这个monitor
- 如果 Monitor 没被占用 → 获取成功，进入临界区
- 如果 Monitor 已被其他线程占用 → 阻塞等待
- ② 对象头层面——锁信息存在对象头的 Mark Word 中：

对象头
├── Mark Word（存锁状态、hashcode、GC年龄）
└── 类型指针（指向类元数据）

Mark Word 中的锁状态：
无锁 → 偏向锁 → 轻量级锁（自旋） → 重量级锁（monitor）

- 这就是锁升级-------synchronized不是一开始就上重量级锁,而是通过竞争的激烈程度逐渐上升
2. Lock 底层怎么实现？
   ReentrantLock 内部基于 AQS（AbstractQueuedSynchronizer）：

ReentrantLock
└── 内部类 Sync extends AQS
├── state（int变量，0=无人加锁，>0=已加锁，CAS修改）
├── exclusiveOwnerThread（记录持锁线程）
└── CLH队列（双向链表，存等待锁的线程）
- 加锁流程：

lock() → CAS尝试把state从0改成1
成功 → 记录当前线程为持锁线程
失败 → 包装成Node加入CLH队列尾部，阻塞等待
前一个线程unlock() → 唤醒队列头部的下一个线程




> 为什么要设计锁呢?有没有其他的方法可以解决多线程的问题?

**我的回答**
1. 为了解决多线程情况下并发修改可能会出现问题的情况
2. 不知

**错漏点**
为什么要设计锁？
1. 你说"为了解决并发修改问题"——方向对，但要更精确：
线程安全三要素，锁是为了同时保证这三点：

| 特性   | 	含义   | 	不加锁会出什么问题   |
|:-----|:---|:---|
| 原子性  | 操作不可分割，要么全做要么不做  | 	i++在多线程下不是原子操作，两个线程同时读到i=0，各加1，写回还是1而不是2 |
| 可见性  | 	一个线程修改了共享变量，其他线程立刻能看到 | 	线程A把i从0改成1，线程B的CPU缓存里i还是0，继续用旧值算 |
 | 有序性  | 	编译器/CPU不会把代码重排到导致结果错误	| 初始化对象的步骤被重排，别的线程拿到一个半初始化的对象  |

2. 有没有不加锁解决多线程问题?有,而且面试官很爱问这个

|方案|	原理|	适用场景|	解决了什么|
|---|---|---|---|
|volatile|	禁止CPU缓存+禁止指令重排|	状态标志位|	只保证可见性和有序性，不保证原子性（i++仍然不安全）|
|Atomic类|（AtomicInteger等）|	CAS（Compare-And-Swap）无锁原子操作|	计数器、累加器|	保证原子性，不加锁，性能更好|
|ThreadLocal|	每个线程各有一份副本，不共享|	数据库连接、SimpleDateFormat|	根本不共享→不需要锁|
|并发集合|	CopyOnWrite写时复制 / ConcurrentHashMap分段锁|	高并发读写集合|	集合层面自带的线程安全|
|不可变对象|	对象创建后不可修改（String、final字段）|	配置对象、常量|	不可变→没有修改→没有竞争|

- 一句话总结：锁是"大家排队用"的方案，这些替代方案是"根本不用排队"的方案——要么不共享（ThreadLocal），要么不改（不可变），要么用硬件指令保证原子（CAS）。

## Day 18 (2026-8-8)
### 多线程第三天(学习了等待唤醒机制以及通过阻塞队列实现等待唤醒机制)

> 什么是等待唤醒机制?什么是阻塞队列?

**我的回答**
1. 等待唤醒机制是基于synchronized为了实现两个线程之间交流的一种机制.它常见的Object方法为wait(),notify(),notifyAll().
最经典的场景是生产者与消费者,两者相互竞争同一把锁,不满足条件就wait()等待,通过另一条线程来notify()唤醒.
这里值得注意的点是wait()等待时是释放锁的,与sleep不同,且sleep()不需要锁对象.还有一点就是wait()应写在while循环中,而不是单纯的if判断,if判断的话可能会出现假唤醒等问题.
2. 阻塞队列waitQueue继承于Queue,它的常用的实现类有ArrayBlockedQueue,LinkBlockedQueue.ArrayBlockedQueue必须初始化大小,消费者可以通过get()方法获得队列中内容

**错漏点**
1. 两者相互竞争同一把锁不准确: 不是"竞争"，是"协作"——生产者放东西后通知消费者来取，消费者取完通知生产者继续放
2. 遗漏点：wait()/notify()必须在synchronized块内调用，否则抛 IllegalMonitorStateException。原因是：wait()要释放monitor，如果你没持有monitor，释放什么？
3. waitQueue拼写错误,应为BlockingQueue,继承自Queue接口,两个队列也应该为ArrayBlockingQueue,LinkBlockingQueue
4. 补充：LinkedBlockingQueue可以不指定大小（默认Integer.MAX_VALUE）
5. BlockingQueue的核心方法是 put() 和 take()，不是get()。get()是普通Queue的方法
6. 阻塞队列的核心特性你没说：put()和take()都是阻塞等待. 阻塞队列的价值就是：put和take自带阻塞，你不用自己写wait/notify了
7. 

> 等待唤醒机制是怎么实现唤醒wait()的线程的呢?如果是lock.notify(),此时线程在等待时已经释放锁了,怎么具体知道它在等待呢?
> 阻塞队列又是怎么让我们不用手动加锁就能实现等待唤醒机制的呢?

**我的回答**
1. 线程wait()后进入等待队列,notify()可以唤醒对应的线程,具体什么实现不了解
2. 阻塞队列内部封装了锁,它会给队列中的内容加锁

**错漏点**
1. 你问的问题很好——"线程等待时已经释放锁了，怎么知道它在等待？
- 答案：每个Java对象内部维护两个集合：

对象（锁对象）
├── Entry Set（入口集）—— 等待获取锁的线程队列
└── Wait Set（等待集）—— 调用了wait()的线程队列

|操作|	线程去了哪|
|---|---|
|线程要进synchronized但锁被占|	进入该对象的 Entry Set，阻塞|
|线程在synchronized内调用wait()|	释放锁，进入该对象的 Wait Set，阻塞|
|调用notify()|	从Wait Set中随机取一个线程，移到 Entry Set，让它重新竞争锁|
|调用notifyAll()|	把Wait Set中所有线程移到 Entry Set，全部重新竞争锁|
所以notify()不是"直接唤醒线程让它继续执行"，而是"把线程从Wait Set移到Entry Set，线程还要重新抢锁，抢到才能继续执行".

- Lock体系的等待/唤醒方法: Condition.await() / signal() / signalAll()
- Lock体系的优势：一个Lock可以创建多个Condition，比如阻塞队列里用两个——notEmpty和notFull，分别管理"队列空了等数据"和"队列满了等空位"两组等待线程，互不干扰。synchronized只有一个Wait Set，notifyAll会唤醒所有等待的线程，包括不该唤醒的。
- 你用的put()/take()内部就是 ReentrantLock + 两个Condition 的等待唤醒。阻塞队列把这套逻辑封装好了，你直接调put/take就行。

- 知识链梳理：AQS / CLH / Lock / Condition / BlockingQueue 的关系
  你说"AQS, CLU的，需要梳理"——应该是 CLH（不是CLU）。它们的关系是：

AQS（AbstractQueuedSynchronizer）
—— Doug Lea写的框架，核心是 state变量 + CLH变体队列（双向链表存等待线程）
│
├── ReentrantLock（AQS的子类实现）
│     └── lock.newCondition() → ConditionObject（AQS内部类）
│           —— 每个Condition有自己的条件队列（单向链表）
│
├── ReentrantReadWriteLock（读写锁，也基于AQS）
├── CountDownLatch（也基于AQS）
├── Semaphore（也基于AQS）
│
└── ArrayBlockingQueue
└── 内部持有 ReentrantLock + 两个Condition
—— 所以阻塞队列底层 = ReentrantLock + Condition = AQS
- 一句话：AQS是地基，ReentrantLock是房子，Condition是房子里的房间，BlockingQueue是精装修——你住进去不用自己建墙（不用自己加锁）。


> 等待唤醒机制具体是怎么设计的呢?锁好像已经能够解决多线程并发修改的问题了.
> 阻塞队列底层是怎么实现的呢?之前没学多线程时根本不了解这些队列,什么AQS,CLU的,需要梳理这些知识.

**我的回答**
1. 无

**错漏点**
1. 锁解决的是"互斥"问题，等待唤醒解决的是"协作"问题
- 等待唤醒是线程之间按条件协调执行顺序,如果没有这个机制,会出现忙等待,需要while(true)一直检查,浪费CPU性能
2. 阻塞队列为什么要设计？

|原因|	说明|
|---|---|
|封装等待唤醒|	生产者-消费者模式太常用了，每次手写wait/notify容易写错（忘记while、忘记notify）。BlockingQueue帮你封装好，put/take直接用|
|解耦|	生产者不需要知道消费者是谁，只管往队列里put；消费者不需要知道生产者是谁，只管从队列take。两者通过队列通信，互不依赖|
|线程安全内置|	不用你自己加锁，队列内部已经用ReentrantLock保证线程安全|

## Day 19 (2026-8-9)
### 多线程第四天(学习了线程池的基本概念以及如何创建线程池)

> 线程池是什么?它有什么要注意的点?它与之前我们创建多线程有什么关系?

**我的回答**
1. 线程池是Java为了解决手动创建线程繁琐而创建的一个容器,它可以帮我们对线程进行统一管理.
2. 它的一些基本知识保括如何创建线程池,我们可以通过new它的实现类ThreadPoolExecutor.几个关键的参数分别为核心线程数,总线程数,临时线程空闲多长时间释放,释放时长单位,阻塞队列,线程工厂,拒绝机制.
也可以通过Executors工具类创建,但存在内存泄露的风险,不符合阿里巴巴编码规范,线上禁止使用.
3. 它的一些核心方法包括submit(),executor(),shutdown(),以及shutdownNow().阻塞队列有ArrayBlockingQueue,LinkBlockingQueue,SynChronousQueue,DelayQueue.
4. 值得注意的是线程池的机制是核心线程满后,线程进入阻塞队列,阻塞队列满后才创建临时线程.都满后就会触发拒绝访问机制.
5. 它就是为了让我们更好实现多线程的.

**错漏点**
1. Executors工具类不是"内存泄露"，是OOM（内存溢出）。FixedThreadPool用无界队列 LinkedBlockingQueue→任务堆积撑爆内存；CachedThreadPool最大线程数Integer.MAX_VALUE→线程无限创建撑爆内存
2. executor()拼写错误,应为execute()submit()有返回值(Future)，execute()没有返回值.
3. 应为 LinkedBlockingQueue（少了d）、SynchronousQueue（S小写c，不是大写C）
4. 机制：核心线程满→阻塞队列→临时线程→拒绝机制	⚠️ 方向对	不是"线程进入阻塞队列"，是任务进入阻塞队列。线程是执行者，任务是Runnable
5. 4种拒绝策略没有题,需要背下来.
6. submit() vs execute() 的区别（你没提）：

|方法|	返回值|	能否捕获异常|	参数|
|---|---|---|---|
|execute(Runnable)|	void|	不能，异常直接抛出|	只接受Runnable|
|submit(Callable/Runnable)|	Future|	能，通过Future.get()获取|	接受Runnable和Callable|



> 线程池底层是如何实现装载并管理这些线程的呢?它有什么精妙之处?

**我的回答**
1. 底层通过创建HashSet来存储这些线程.其他不了解

**错漏点**
1. 线程池内部结构

ThreadPoolExecutor
   ├── HashSet<Worker> workers     ← 你说的HashSet，存的是Worker不是Thread
   ├── BlockingQueue<Runnable> workQueue  ← 存等待执行的任务
   ├── AtomicInteger ctl           ← 一个变量同时记录线程池状态和线程数量
   │     ├── 高3位：线程池状态（RUNNING/SHUTDOWN/STOP/TIDYING/TERMINATED）
   │     └── 低29位：线程数量
   └── Worker（内部类）
   ├── Thread thread         ← 真正的线程
   ├── Runnable firstTask    ← 第一个任务
   └── extends AQS           ← Worker继承了AQS！（你自己实现了不可重入锁）

execute() 的完整流程（这是线程池最核心的源码逻辑）
提交任务 task
↓
① 当前线程数 < corePoolSize？
→ 是：创建核心线程，执行task，结束
→ 否：继续
↓
② 阻塞队列没满？
→ 是：task放入阻塞队列，结束
→ 否：继续
↓
③ 当前线程数 < maximumPoolSize？
→ 是：创建非核心线程，执行task，结束
→ 否：继续
↓
④ 触发拒绝策略

- 你说的机制顺序是对的，但注意：核心线程和临时（非核心）线程在`代码层面没有区别`，都是Worker对象。区别只在于——超过corePoolSize后创建的线程，在空闲keepAliveTime后会被回收；核心线程默认不回收（除非设置allowCoreThreadTimeOut(true)）。
- Worker 怎么不断取任务执行？
- 核心线程用 workQueue.take()（阻塞，一直等），非核心线程用 workQueue.poll(keepAliveTime)（等一段时间没任务就退出，被回收）。这就是为什么非核心线程空闲超过keepAliveTime会自动销毁。


> 为什么要这样设计线程池?为什么是这些参数呢?实际开发中还需要我们手动创建线程吗?

**我的回答**
1. 不了解

**错漏点**
- 为什么要设计线程池？手动创建线程有什么问题？

|手动创建线程的问题|	线程池怎么解决|
|---|---|
|每次new Thread()都要创建和销毁线程，OS层面有开销（用户态→内核态切换）|	线程复用，创建后不销毁，反复执行不同任务|
|无限制创建线程会导致OOM（每个线程默认占1MB栈空间）|	线程池有maximumPoolSize上限，可控|
|线程无法统一管理（无法监控数量、无法优雅关闭）|	线程池提供shutdown()、shutdownNow()、监控方法|
|任务多时响应慢（每个任务都要等线程创建）|	核心线程常驻，任务来了直接执行，不用等创建|

- 为什么是这7个参数？每个参数解决什么问题？

|参数|	解决的问题|	如果没有会怎样|
|---|---|---|
|corePoolSize|	日常并发量下需要多少线程常驻|	每次任务来了都要创建线程，慢|
|maximumPoolSize|	峰值时最多允许多少线程|	无限创建→OOM|
|keepAliveTime|	非核心线程空闲多久后回收|	空闲线程一直占内存，浪费|
|unit|	keepAliveTime的时间单位|	—|
|workQueue|	任务排队的地方|	任务多时没地方放，直接拒绝|
|threadFactory|	统一创建线程的方式（命名、优先级等）|	线程没有统一命名，出问题无法排查|
|handler|	所有资源都满了怎么办	|没有兜底策略，任务直接丢失或异常|

- 实际开发中还需要手动创建线程吗？
  几乎不需要。 实际开发中创建线程的方式：

|场景|	方式|
|---|---|
|Spring项目|	@Async注解 或 注入 ThreadPoolTaskExecutor|
|需要自定义线程池|	new ThreadPoolExecutor(7个参数)|
|简单异步任务	|CompletableFuture.runAsync()（内部用ForkJoinPool）|
|定时任务	|ScheduledThreadPoolExecutor|

## Day 20 (2026-8-10)
### 网络编程第一天(多线程收尾,学习了网络编程的几个基本概念,ip地址,端口,UDP协议,TCP协议目前没了解)

> 创建线程池多大合适?网络编程是什么,它有什么基本概念?UDP协议的基本知识点?

**我的回答**
1. 创建线程池,对于CPU集中型: 核心线程数 = CPU线程数 + 1;这里+1是防止有线程阻塞而影响CPU利用率.
对于IO集中型: 核心线程数 = CPU线程数 * ( 1 + 平均IO等待时长/计算耗时);
2. 网络编程是不同计算机之间,通过网络实现计算机之间通信的Java程序.
3. 网络编程的三大基本概念分别为ip,端口,协议.
ip地址分为ipv4地址和ipv6地址,每一台设备都会分配一个ip地址,在Java中通过InetAddress实现,不过该类未提供构造方法,只能通过静态方法getByName()获得地址对象.
如果要定位具体是哪一个程序或网站,就需要使用到端口,范围为0~65535,前1023不可使用.
其中协议就包括了UDP协议,该协议是传输是不可靠的,且存在容量限制Java中有关的工具类:DatagremDocker()以及DatagremPackeger().通过前者创建连接通道,后者打包或者接收打包
有关主要方法有send(),对于解包有getBytes(),getLength(),getAddress(),getPort(),执行完毕后需调用close()释放资源.

**错漏点**
1. 应将集中该为`密集`

补充一个实操细节：实际开发中很少用公式精确计算，更常用的做法是先设一个经验值（CPU密集型=N+1，IO密集型=2N），然后通过压测调整。面试时能说出公式+经验值就足够了。
- ip地址补充：还有getLocalHost()获取本机IP；InetAddress没有公开构造方法，是静态工厂方法模式

- 0~1023是知名端口（Well-known Ports），保留给系统服务（HTTP=80, HTTPS=443, MySQL=3306），不是"不可使用"而是"不建议自定义程序使用"。自定义程序一般用1024以上的端口
- 应为 DatagramSocket（是Socket不是Docker）. 应为 DatagramPacket（是Packet不是Packeger）
- 遗漏的UDP关键特性: 不需要先建立连接，直接发送（对比TCP需要三次握手)

> 网络编程是如何让我们编写几行代码就实现的呢?创建这些对象时它们源码是怎么实现的?

**我的回答**
1. 不了解,源码看不懂

**错漏点**
1. 网络编程底层是怎么工作的？
   你写几行代码就能通信，不是因为Java有什么魔法，而是因为操作系统提供了Socket API，Java只是封装了它：
   
   你的Java代码
   ↓ Java封装
   DatagramSocket.send(packet)
   ↓ 调用
   操作系统Socket API (C语言层面的socket/sendto/recvfrom)
   ↓ 系统调用
   网卡驱动 → 物理网络 → 对方网卡 → 对方操作系统 → 对方的Java程序
-  关键认知：Socket不是Java发明的，它是操作系统的网络通信接口。C、Python、Go都有Socket API，Java的DatagramSocket/Socket只是对这个操作系统接口的Java封装。


> 为什么要专门设计工具类来实现网络编程?它跟多线程一样,学习起来都给人一种不知道如何运用的感觉.

**我的回答**
1. 帮助我们在服务器与客户端之间传输数据.

**错漏点**
- 为什么要专门设计网络编程工具类？

 |原因|	说明|
|---|---|
 |跨平台抽象|	Windows/Linux/Mac的Socket API细节不同，Java封装后你在任何系统写同样的代码都能通信|
 |面向对象封装|	操作系统的Socket是C语言接口（int fd + 一堆函数），Java把它封装成DatagramSocket对象，有方法有属性，更好用|
 |与IO体系统一|	Socket的输入输出流可以和Java IO体系无缝衔接——socket.getInputStream()返回InputStream，你之前学的BufferedReader/PrintStream全都能用|

- UDP为什么要设计成"不可靠"？
-  这听起来像缺陷，实际是刻意的选择：
- 为什么直播用UDP不用TCP？ 因为直播要求实时性，晚到的数据包没用了（你已经看到下一帧了）。TCP会重传丢失的包，导致画面卡顿——可靠性反而成了负担。UDP丢了就丢了，继续发新的，画面流畅。

- 关键认知：你学网络编程不是为了以后手写Socket通信，而是为了理解上层框架的底层原理。面试官问"Spring Boot怎么接收HTTP请求"，你回答时能说到"Tomcat底层用Socket监听端口，收到TCP连接后解析HTTP报文"，这就比只说"用@RequestMapping"的人高了一个档次。

## Day 21 (2026-8-11)
### 网络编程第二天(学习了TCP协议以及部分练习)

> TCP协议是什么协议?说出它的一些基本概念以及跟UDP协议的区别.

**我的回答**
1. TCP协议是面向连接协议,相比于UDP协议数据报,它更加安全可靠,通过流来进行数据传输.在Java中对应的工具类为Socket,ServerSocket.
2. 常用的方法有getInputStream(),getOutputStream(),accept(),close()等.
3. 客户端与服务器创建连接需要经过三次握手,第一次客户端生成随机初始序列号,向服务器发送SYN,进入SYN_SENT状态.第二次服务器向客户端发送SYN+ACK发送的初始序列号+1,表示连接可以建立,第三次客户端向服务器发送ACK两者同时进入ESTABLISHED状态.
为什么是三次而不是两次?为了防止服务器与过期的SYN产生连接,浪费服务器资源.服务器端会判断SYN是否过期来决定是否连接
4. 客户端与服务器断开连接则需要4次挥手,因为TCP是双向连接的,两边都需要断开,以客户端向服务器端断开连接来举例,第一次客户端先向服务器发送FIN,第二次服务器端向客户端发送ACK,第三次数据发送完后服务器向客户端发送FIN,第四次客户端向服务器发送ACK,客户端进入TIME_WAIT状态
为什么要有TIME_WAIT? 客户端在发送完ACK后等待2倍的MSL时间,方式服务器端没有收到ACK向客户端重发FIN,此时客户端可以重发ACK,确保完全断开连接.
5. 与UDP协议差别主要还是体现在可靠性上,TCP协议通常用在文件传输,打字输入等需要数据完全传输的场景.UDP协议通常在直播,视频播放时使用,数据丢包直接传输新的,方式重传卡顿.

**错漏点**
1. 补充一个你没提到的关键点——为什么挥手要4次而握手只要3次？
- 服务器收到FIN后，自己可能还有数据没发完，所以先回ACK（"我知道你要走了"），等数据发完再发FIN（"我也走了"）

- 遗漏点

|遗漏|	说明|
|---|---|
|TCP可靠传输的实现机制|	你说了TCP"可靠"，但没说怎么做到可靠的——序号、确认、超时重传、滑动窗口、拥塞控制。这是第二层要回答的|
|Socket和ServerSocket的关系|	ServerSocket负责"等连接"（accept），Socket负责"读写数据"。一个ServerSocket.accept()返回一个Socket，之后双方通过这个Socket的InputStream/OutputStream通信|



> Java也是通过封装操作系统提供的Socket API实现TCP协议的吗?还有什么其他要注意的点呢?

**我的回答**
1. Java就是通过封装Socket API来进行实现的.

**错漏点**
1. Socket 到底封装了什么？

   你的Java代码
   ↓
   Socket类 / ServerSocket类（Java API）
   ↓ native方法
   操作系统的 Socket API（C语言的 socket()、bind()、listen()、accept()、connect()、send()、recv()）
   ↓
   网卡驱动 → 网线 → 互联网

- Socket 的本质：操作系统提供的一个"网络通信端点"抽象。
- TCP 可靠传输怎么实现的？（你没回答的核心问题）
  你说TCP"安全可靠"，但可靠不是天生的，靠的是5个机制：

|机制|	作用|	通俗理解|
|---|---|---|
|序号（Sequence Number）|	每个字节都有编号，接收方知道哪些收到了、哪些没收到|	快递单号，每个包裹都有唯一编号|
|确认（ACK）|	接收方收到数据后回确认，告诉发送方"我收到了到第N个"|	签收快递|
|超时重传|	发送方等一段时间没收到ACK，就重发	|快递超时没签收，重新派送|
|滑动窗口|	接收方告诉发送方"我还能收多少"，发送方按窗口大小发|	快递柜容量有限，别一次塞太多|
|拥塞控制|	发送方根据网络拥堵情况动态调整发送速度，避免网络瘫痪|	路上堵车就开慢点|

关键：ServerSocket.accept() 不是"收到第一次握手就返回"，而是"等三次握手全部完成后才返回一个新的Socket对象"。这个新Socket代表了这条已建立的连接。


> 现阶段是只需要了解网络编程的知识点,这些内容在后面学习到web的时候才会用到吗?以后还需要手动new Socket吗?

**我的回答**
1. 不了解

**错漏点**

① 以后几乎不手动new Socket

|阶段|	你做什么|	底层谁在管Socket|
|---|---|---|
|现在（JavaSE）|	手写Socket/ServerSocket练手|	你自己|
|Spring Boot阶段|	写Controller处理HTTP请求|	Tomcat（内嵌服务器）|
|实际工作|	写业务逻辑，接收请求返回响应|	Tomcat + Spring MVC|

Tomcat的底层就是ServerSocket + 线程池：

你在Spring Boot写的代码
↓
Spring MVC（DispatcherServlet分发请求）
↓
Tomcat（HTTP服务器）
↓ 底层
ServerSocket.accept() 等连接 → 每来一个连接，用线程池分配一个线程处理
↓
Socket.getInputStream() 读HTTP请求
Socket.getOutputStream() 写HTTP响应

所以你这几天学的东西不是白学的——你手写的Socket通信，就是Tomcat内部在做的事。只不过Tomcat帮你封装了几百层，你只需要写 @GetMapping("/hello") 就能处理网络请求了。

## Day 22 (2026-8-12)
### 反射第一天(进行了网络编程收尾,学习了反射的基本概念)

> 反射是什么?反射有那些基本的概念?它是用来做什么的?它有那些弊端?

**我的回答**
1. 反射是Java提供的一种动态处理方式,它允许我们在运行阶段动态无视修饰符获取任何一种类的成员方法,构造方法,成员变量.
2. 获得Class对象的三种方式:
-   Class.forName("全类名"),全类名: 包名+类名. 这种方式使用次数最多,会触发静态代码块,当我们需要对获取的Class对象进行处理时适用.
-   类名.class  这种方式最安全,编译期时就能确定.不过这种方式获得的Class并不会触发静态代码块,一般用于当参数时,如作为锁对象.
-   对象.getClass()  通过该类的对象获得该类的Class对象,主要在要对对象属性进行处理时使用

- Java反射的三种API
- 1. Constructor获得构造方法,常用方法为getConstructors()获得所有public构造器,并放入数组返回,getDeclaredConstructors()获得所有构造器,包括private构造.
以及通过参数如(String.class)获得指定构造.可以通过newInstance()创建该构造器对象.如果是Declared获得的需要setAccessible(true)暂时绕过权限修饰符检查.
- 2. Field获得成员变量,常用方法与第一个类似,getFields()获得所有的public的成员变量.getDeclaredFields()获得所有包括private的成员变量.
也可以同过参数指定要获取的成员变量,如("name").可以通过获得的该name.get(对象)获取指定对象的该name值,也可以通过name.set(对象,Name)来设置指定对象该值.
如果要对private修饰的成员变量进行操作,必须要name.setAccessible()暴力反射绕过权限检查.
- 3. Method获得成员方法,与上面两个一致.

- 反射拥有极高的灵活性,可以让我们随时随地的查看和修改对象中的内容.
- 它的主要缺点体现在是运行期动态实现,效率比普通调用低非常多.而且安全性维护性不高,问题全在运行时出现.破坏封装,违背了Java面向对象的理念.

**错漏点**
1. 补充一个更完整的定义：反射是在运行期间动态获取类的信息（字段、方法、构造器、注解等）并操作它们的能力。关键词是运行期间——普通代码在编译期就确定了调用关系，反射在运行期才知道操作的是什么。
2. 补充 Class.forName("全类名")：不仅触发静态代码块，而是触发**完整的类加载过程**（加载→链接→初始化）。这是forName和.class的关键区别
3. 补充 类名.class：不触发初始化但会触发**加载和链接**（只是不执行静态代码块）。你说的"做锁对象"是个好例子
4. 补充：getClass()定义在Object类中，所有对象都有这个方法
5. 补充：newInstance()可以传参数 constructor.newInstance("张三", 20)；JDK9+推荐用 clazz.getDeclaredConstructor().newInstance() 代替过时的 clazz.newInstance()

6. Method的核心方法你没写：invoke(对象, 参数...) 调用方法。这是反射最常用的操作
- Method method = clazz.getDeclaredMethod("setName", String.class);
  method.setAccessible(true);
  method.invoke(obj, "张三");  // 等同于 obj.setName("张三")

- 反射速度补充：JDK有优化（反射方法调用15次后会生成字节码，接近普通调用速度）


> 反射底层是怎么实现的呢?它与普通的调用有什么不同?运行期动态实现,怎么个实现法?它的源码有什么神奇之处?

**我的回答**
1. 底层通过Class文件获得里面封装的内容.
2. 普通调用就直接编译期确定,而反射是通过编译好的Class文件调用.
3. 后面不知

**错漏点**
1. 反射能工作的前提是JVM已经把类加载到内存中了。整个链条：

编译期：
Student.java → 编译 → Student.class（字节码文件）

运行期：
JVM启动 → 类加载器把 Student.class 加载到内存
→ 方法区（元空间）中生成一个 Class<Student> 对象
→ 这个Class对象包含了Student类的所有信息：
├── 字段信息（名字、类型、修饰符）
├── 方法信息（名字、参数、返回值、修饰符）
├── 构造器信息
├── 父类信息
└── 注解信息
→ 反射就是通过这个Class对象，获取并操作这些信息

所以反射**不是**"读Class文件"——Class文件在类加载后已经被解析成内存中的Class对象了。反射操作的是这个内存中的Class对象，不需要再去读文件。

普通调用 vs 反射调用的区别

|维度|	普通调用|	反射调用|
|---|---|---|
|何时确定调用目标|	编译期|	运行期|
|类型检查|	编译期检查（s是Student类型，编译器知道有setName方法）|	运行期检查（method.invoke时才知道方法存不存在）|
|性能|	直接调用，快|	需要安全检查、参数类型匹配、方法查找，慢|
|灵活性|	只能调用已知的类和方法|	可以调用运行时才知道的类和方法|

反射为什么慢？性能开销在哪？

|开销来源|	说明|
|---|---|
|方法查找|	运行时遍历方法表找匹配的Method对象|
|安全检查|	每次invoke都要检查访问权限（除非setAccessible(true)）|
|参数装箱|	基本类型参数要自动装箱成Object|
|JIT优化受限|	反射调用难以被JIT内联优化|
但JDK有优化：Method.invoke()在调用超过15次后，JVM会生成一个"MethodAccessor"字节码，之后的调用直接走生成的字节码，性能接近普通调用。所以"反射非常慢"这个说法在现代JDK中已经被大幅改善。

setAccessible(true) 底层怎么绕过权限检查的？

正常调用：
Method.invoke() → 检查AccessibleObject.override标志
→ override=false → 检查调用者是否有权限访问private方法
→ 没权限 → 抛IllegalAccessException

setAccessible(true)：
→ 把override标志设为true
→ Method.invoke()看到override=true → 跳过权限检查 → 直接调用

注意：setAccessible(true)绕过的是Java语言层面的访问控制，不是JVM安全机制。如果有SecurityManager，setAccessible本身也可能被拒绝（不过JDK17之后SecurityManager已废弃）。


> 为什么要设计反射?如果是框架要使用,那是不是可以通过另外的内部权限之类的方法实现获得封装的内容,而不是选择反射.

**我的回答**
1.后面学习的很多内容都需要反射,如Spring ioc,现在的idea生成提示都是通过反射实现的.

**错漏点**
- 反射解决的核心问题是什么？
- 反射解决的是"**编译期不知道要操作什么类，运行期才知道**"的问题。

没有反射的世界：

java
// 没有反射：你要创建什么对象，编译时就得写死
Student s = new Student();
Teacher t = new Teacher();
// 如果类名是从配置文件读出来的，你怎么办？
// String className = config.getProperty("className");
// new ???  ← 编译器不允许你写变量类型来new

有反射：

java
// 有反射：类名从配置文件读取，运行时动态创建
String className = config.getProperty("className");  // "com.example.Student"
Class<?> clazz = Class.forName(className);
Object obj = clazz.getDeclaredConstructor().newInstance();
// 配置文件改成"com.example.Teacher"，代码一行不用改！

没有反射，Spring根本无法存在。 配置文件写的是字符串类名，Spring需要根据字符串动态创建对象——这件事不用反射做不到。

- 能不能用"内部权限"替代反射？答案: 不能.

|原因|	说明|
|---|---|
|Java没有"内部权限"这种东西|	Java的访问控制是public/protected/默认/private，没有"框架可以访问但用户不能访问"这种级别。Spring是第三方库，和你的代码不在同一个包，它只能看到public的东西|
|类名是运行期才知道的|	就算Spring能访问private，它也不知道要访问哪个类。类名是从配置文件/注解中读出来的字符串，必须用Class.forName()加载，这就是反射|
|编译期无法确定|	Spring在编译时不知道你要把哪些类注册为Bean，它只能在运行时扫描。不用反射，Java没有其他机制能在运行时动态操作任意类|

反射在设计层面的意义

|维度|	说明|
|---|---|
|配置驱动|	代码写逻辑，配置写数据，反射把两者连起来。Spring用XML/注解配置Bean，运行时反射创建|
|解耦|	框架代码不需要知道你的业务类，只通过反射操作。Spring不知道你有UserServiceImpl，但通过反射能创建和注入它|
|注解的基石|	注解本身不产生任何行为，必须通过反射读取注解才有意义。@Autowired、@GetMapping全是靠反射扫描和执行的|
|动态代理的基石|	JDK动态代理（Proxy.newProxyInstance）底层用的就是反射，这是Spring AOP的实现方式|
**一句话总结**：反射是框架的灵魂。没有反射，Java就没有Spring、MyBatis、Hibernate等任何框架。你之前学的IO流是基础设施，多线程是并发能力，网络编程是通信能力，而反射是让框架成为可能的能力。

## Day 23 (2026-8-13)
### 今天学习了动态代理的知识

> 动态代理是什么?它有什么知识点需要我们掌握?

**我的回答**
1. 动态代理是Java提供的一种无入侵性的一种实现类的方案.它允许我们在不修改源代码的情况下对类中的方法进行实现,并对其进行一些增强的操作.
2. 常用的代理方案有两种,第一中是JDK原生提供的代理方案,该方案要创建代理类原类必须实现接口.不然无法创建.第二种CGLIB第三方方案,不需要实现接口,这种方案之后会学,现在暂时了解存在.
3. 第一种动态代理方案具体怎么实现的?
- 前提条件: 存在对应的类,该类必须实现接口.
- 通过Proxy类中的静态方法newProxyInstance()来创建代理类的对象,其中一共有三个参数:
- 第一个参数表示通过哪一个类来加载.第二参数表示一个Class类的数组,该数组中传入对应实现的接口的class对象
- 第三个参数表示类实现器,一般通过匿名内部类的方式重写里面的invoke方法,该方法的三个参数(Object Proxy, Method method, Sting[] args),分别表示对象本身,一般不使用这个参数;方法的对象,我们可以对这些方法进行增强处理;方法的参数.
- 通过返回方法对象的invoke(原本类对象,args);来返回对应的方法,外层再返回代理类的对象.
- 外界代理类对象调用时会先触发newProxyInstance()中的invoke(),再通过内部invoke()实现原本类中对应的方法.

**错漏点**
1. invoke第三参数是 Object[] args，你写成了 Sting[] args。另外三个参数标准名是：Object proxy（代理对象本身）、Method method（被调方法）、Object[] args（方法实参）
2. 遗漏点：

|遗漏|	说明|
|---|---|
|第三个参数的名字|	你叫它"类实现器"，标准名字是 InvocationHandler（调用处理器） 接口。重写的那个方法叫 invoke()|
|触发机制|	你说"外界代理类对象调用时会先触发newProxyInstance()中的invoke()"——表述不准。准确说法：代理对象调用任意接口方法时，都会自动转发到 InvocationHandler.invoke()。不是"触发newProxyInstance()里的invoke"，newProxyInstance是创建代理的那一步，invoke是之后每次调用的入口|
其他细节区别看自己笔记

> 动态代理内部到底是怎么实现的?这些参数又各起了什么作用?光从自己写的代码很难理解内部做了什么.

**我的回答**
内部应该是通过反射获取创建代理类的.

**错漏点**
1. JDK 动态代理的底层：运行时生成字节码

Proxy.newProxyInstance(loader, interfaces, handler)
↓
运行时动态生成一个代理类（名为 $Proxy0 的字节码，默认存在内存里）
↓
$Proxy0 的结构：
class $Proxy0 extends Proxy implements 目标接口 {
// 继承了Proxy，构造时保存handler
// 实现了目标接口的每个方法，方法体都一样：
public String getName() {
return (String) handler.invoke(this, getName方法对象, 实参);
//     ↑ 方法体只有一行：把调用转发给handler
}
}

- 关键点：**代理类的每个方法都只做一件事——转发**。 
调用方调 proxy.getName() → 实际上执行的是 $Proxy0.getName() → 方法体里调用 handler.invoke() → 你的增强逻辑在这里执行 → 内部再 method.invoke(目标对象, args) 调真实方法。

- 完整调用链（面试必讲）

调用方
↓ 调 proxy.method()
$Proxy0.method()（代理类，字节码生成）
↓ 转发
InvocationHandler.invoke(proxy, method, args)   ← 你的增强逻辑在这里
↓ 内部调
method.invoke(目标对象, args)                    ← 反射调真实方法
↓
目标对象.method()                                ← 真实业务逻辑

为什么必须实现接口？（这是核心设计原因）

**因为Java单继承**.代理类$Proxy0已经继承Proxy类了,就不能再继承目标类了.那怎么让调用方感觉代理对象"就是"目标对象？只能通过实现相同的接口来伪装成同一种类型。

$Proxy0
├── 继承 Proxy（必须，Java规定）
└── 实现 UserService 接口（这样代理对象能被当成UserService用）

小结：JDK动态代理 = 反射（method.invoke）+ 字节码生成（$Proxy0）+ 多态（接口伪装）。


> 为什么要设计动态代理?还有没有非动态代理的低入侵方案?它在具体业务中有那些体现?

**我的回答**
1. 动态代理可以让我们在低入侵的情况下实现更多增强逻辑,它相比于修改源码更加灵活,而且不容易出错.

**错漏点**
1. 你只提到了动态代理的好处,但不是"为什么设计".

**设计的出发点：静态代理的痛点**

|静态代理的痛点|	动态代理怎么解决|
|---|---|
|每个类都要手写一个代理类 → 类爆炸|	一个 InvocationHandler 可以给任意类生成代理|
|接口加一个方法，代理类也要跟着改 → 维护成本高|	接口方法变化，$Proxy0 自动重新生成，代码不用动|
|增强逻辑散落在每个代理类里，重复|	增强逻辑只写在 invoke() 一处|

一句话：静态代理把"增强"写死在类里，动态代理把"增强"抽象成一段逻辑，运行时动态套到任何类上。

实际业务场景（回答你问的"具体业务中有哪些体现"）

|场景|	怎么用动态代理|
|---|---|
|Spring AOP 事务|	你在Service方法上标@Transactional，Spring运行时给Service生成代理，invoke()里"开启事务→调真实方法→提交/回滚事务"|
|Spring AOP 日志/权限|	同样机制，invoke()里先做权限校验、记日志，再调真实方法|
|MyBatis Mapper	|你只定义了接口 UserMapper 没有实现类，MyBatis用动态代理自动生成实现，invoke()里解析方法名拼SQL执行|
|RPC 远程调用|	你调 userService.save() 感觉是本地方法，代理对象把调用序列化成网络请求发到远程服务器（Dubbo、gRPC底层）|
|监控埋点|	给所有方法统一加耗时统计，不改业务代码|

有没有"非动态代理"的低入侵方案？

|方案|	是否低入侵|	问题|
|---|---|---|
|继承重写|	中等|	和目标类紧耦合，增强逻辑写死在子类，每个类写一个子类 → 类爆炸|
|装饰器模式|	中等|	需要修改调用方代码（把 new 目标对象 换成 new 装饰器）→ 还是入侵了调用方|
|动态代理|	✅ 最优|	调用方代码一行不用改，代理在运行时自动套上|

结论：动态代理就是"低入侵增强"的最优解，没有更好的替代。它把"改代码"变成了"套壳"，这是开闭原则（对扩展开放、对修改关闭）的经典体现。





